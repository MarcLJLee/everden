# Godot 4 — 임포트 캐시. 재생성되고, 머신마다 달라진다.
.godot/

# ★ `*.import` 와 `*.uid` 는 여기 넣지 말 것.
#   UID 가 그 안에 들어 있어서, 빼면 클론한 쪽에서 씬의 리소스 참조가 전부 끊긴다.
#   Godot 3 의 `.import/` 폴더와 헷갈리기 쉬운 자리다.

# 빌드 산출물
/build/
/export/
*.exe
*.pck
export_presets.cfg

# 스프라이트 생성기
sprites/__pycache__/
__pycache__/
*.pyc

# macOS
.DS_Store

# 지우기로 한 것 — 히스토리에 남길 이유가 없다
_to_delete/
