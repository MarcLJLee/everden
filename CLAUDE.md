# 사파리 (가제) — 작업 지침

7살 아이를 첫 플레이어로 상정한 동물 수집·육성 게임. **Godot 4**, 1인 개발.

## 문서

| 파일 | 용도 |
|---|---|
| `PROJECT-BRIEF.md` | **단일 기준 문서 (v3.9).** 설계 질문에 답하기 전에 먼저 읽을 것 |
| `HANDOFF.md` | 아트 인계 규약 — `sprites/extracted/` 를 어떻게 물리는가 |
| `RUN.md` | 실행 · 조작 · 검증 |
| `DEMO-SPEC.md` | 지금 만들 것 — Demo 1(필드 슬라이스)의 구현 사양과 완료 조건 |
| `data/tags.json` | 태그 스키마. 열거형 정의, 파생 규칙, 캔버스 버킷 |
| `data/animals.json` | 종(class) 정의 8개. 개체가 아니다 |
| `sprites/extracted/` | 게임이 읽는 아트. `sprites/*.py` 가 생성한다 |
| `fonts/` | 갈무리 11 (OFL 1.1). **11px 로 찍고 정수배로만 키운다** |
| `project-instructions.md` | 대화형 프로젝트 지침 (Claude 프로젝트 custom instructions용) |

## 문서를 낡게 두지 않는다

설계에서 정한 것도, 구현에서 발명한 것도 **그 자리에서 `PROJECT-BRIEF.md` 의 해당 절에 반영한다.**
따로 모아두면 브리프가 폴더에서 가장 못 믿을 파일이 된다 — v3 를 만들 때 실제로 다섯 건이 밀려 있었다.
**파일명에 버전을 넣지 않는다.** 버전은 문서 첫머리에만 있다.

아트는 `sprites/extracted/` 에 있고 `sprites/*.py` 가 생성한다.
**PNG 를 손으로 고치지 말고 스크립트를 고쳐 다시 내보낼 것** — 아트 규칙이 코드에 박혀 있다.

## 파일 주인 — 두 세션이 같은 폴더를 쓴다

설계 세션과 구현 세션이 같은 폴더에서 동시에 일한다. 같은 파일을 양쪽이 고치면
**나중에 재생성하는 쪽이 조용히 이긴다.** 실제로 `build.py` 의 냄새 아이콘이 세 번 사라졌다.
버전 관리 문제가 아니라 **소유권 문제**다 — 소유자를 정하면 없어진다.

| 파일 | 주인 | 다른 쪽은 |
|---|---|---|
| `sprites/*.py` (생성기) | 설계 세션 | 고치지 말고 **변경분을 블록으로 넘긴다** |
| `sprites/extracted/` | 그 파일을 내보낸 스크립트 | 남의 폴더를 지우지 않는다 |
| `data/*.json` | 구현 세션 | 값을 바꾸려면 이유와 함께 요청한다 |
| `fonts/` | 파일은 설계 세션 · **임포트 설정은 구현 세션** | `fonts/README.md` 참조 |
| `scripts/`, `scenes/`, `tuning/` | 구현 세션 | 읽기만 |
| `PROJECT-BRIEF.md`, `HANDOFF.md`, `DEMO-SPEC.md` | 설계 세션 | 낡은 곳을 지적한다 |
| `RUN.md` | 구현 세션 | |
| `CLAUDE.md` | 양쪽 — **추가만, 삭제는 상의** | |

**생성기는 자기가 쓰는 자리만 지운다.** `extracted/` 를 통째로 `rmtree` 하면
그 스크립트가 만들지 않는 것까지 날아간다. `export_godot.py` 의 `OWNED` 목록이 그 장치다.

이 규칙은 아트 방향을 설계 세션이 독점한다는 뜻이 아니다. 구현 세션이 도트를 고치는 편이
빠른 경우도 있고 실제로 있었다(고양이 주둥이·앵커). 그때는 **고쳐서 넘기고 주인이 반영한다.**

## 절대 어기면 안 되는 것

구현 중 판단이 필요하면 이 목록이 우선한다. 충돌하면 BRIEF의 해당 절을 인용해 알릴 것.

1. **동물은 죽지 않는다.** 방치의 대가는 손실이 아니라 진행 정체다.
2. **되돌릴 수 없는 실패를 만들지 않는다.** 파산도, 중간에 끊기는 게이지도 포함된다.
   게이지는 시작하면 반드시 완료된다 — 방해 요소나 실패 조건을 넣지 말 것.
3. **규칙은 눈에 보여야 한다.** 확률을 노출하지 말고 진행은 누적으로 보이게.
4. **종 이름으로 분기하지 않는다.** 상성·행동·단서·유도 전부 태그 조합으로.
   `if species == "dog"` 같은 코드가 나오면 설계가 틀린 것이다.
5. **보이지 않는 데이터 필드를 만들지 않는다.** 새 필드를 제안할 때는
   화면에서 어떻게 보이는지 같이 말할 것.
6. **수집이 벌이 되면 안 된다.** 동물이 늘수록 유지비·의무가 커지는 구조는 금지.

## 구현할 때 자주 틀리는 지점

- **걷기 바운스는 스프라이트가 아니라 노드 Y로 처리한다.** 얼굴 파츠가 몸통의 자식이라
  부모를 따라가야 한다. 프레임별 앵커를 만들기 시작하면 잘못 든 길이다. (BRIEF §4.6)
- **노드 Y는 정수 픽셀로 스냅.** 안 하면 얼굴이 떨린다.
- **북향에서는 눈·입 레이어를 숨긴다.** 뒷모습엔 얼굴이 없다.
- **Y-sort 대상은 캐릭터 루트 노드 하나.** 파츠가 따로 정렬되면 안 된다. (BRIEF §6.2)
- **동물은 측면 1방향이다. 방향을 늘리지 말고 모션을 늘린다.** ★ v3.16 —
  방향은 곱셈이고 모션은 덧셈이다. 3방향이면 새 모션 하나가 종당 6장, 측면이면 2장이다.
  플레이어만 4방향. (BRIEF §4.5)
- **유도의 방향은 몸이 아니라 동료의 이동과 머리 위 표시가 말한다.** 몸을 4방향으로
  그려서 방향을 말하려 들면 종당 6장이 든다 — 공용 아이콘 4장이면 된다. (BRIEF §4.5)
- **측면 스프라이트가 수직으로 움직이면 미끄러져 보인다 — AI 로 푼다.**
  배회 목표에 수평 성분이 항상 있게. 도트 0장. (BRIEF §4.5)
- **튜닝 상수를 코드에 박지 않는다.** `tuning/*.tres`로 빼서 리모트 인스펙터로 만진다.
- **텍스처 필터는 Nearest, 밉맵 끄기.** 이펙트 레이어만 Linear + 자유 좌표. (BRIEF §6.2)
- **마우스에서만 얻는 정보를 만들지 않는다.** 기본 조작은 WASD/패드다. 툴팁은
  호버하면 즉시, 아무 입력이 없으면 몇 초 뒤에 — 마우스는 더 빠를 뿐이다. (BRIEF §6.7)
- **갈무리는 11px 로 찍고 정수배로만 키운다.** 22px 로 바로 찍으면 획이 흔들린다.
- **디더 마스크(구름 그림자·안개)는 정수 픽셀로만 스크롤한다.** 반픽셀이면
  디더 무늬가 자글거려서 도트가 아니라 노이즈로 보인다. (BRIEF §6.8)
- **"잡다"를 쓰지 않는다.** 이건 포획이 아니라 **초대**다 — 아이가 데려가는 게 아니라
  얘가 따라오기로 한 것이다. 만나다 · 친해지다 · 초대하다 · 찾다까지가 한계다. (BRIEF §3.12)
- **필드 위 표시에 판을 두르지 않는다.** 검은 판을 깔면 세계 밖 UI 위젯이 된다 —
  1배 아이콘 + 1px 그림자 + 꼬리 3px 면 충분하다. (BRIEF §3.3 · screens.json field)
- **방향을 발자국으로 가리키지 않는다.** 지면 발자국은 "여기 지나갔다"(시야 단서)로 이미 쓰인다.
  감각 아이콘(무엇으로) + 화살표(어느 쪽)로 나눈다 — 2×4=8 이 아니라 2+4=6. (BRIEF §4.5)
- **상성을 UI 로 알려주지 않는다.** 아이는 전략을 세우지 않는다 — 애착으로 고르고
  경험으로 느끼고 말로 공유한다. 게임은 인과의 순간(킁킁 → 발자국 → 발견)만 보여준다. (BRIEF §3.3)
- **못 느낄 차이를 만들지 않는다.** 1.2배는 아무도 못 느낀다. 감각 차이는 배율이 아니라
  범주로 준다 — 곱셈 다섯 개의 결과에는 이름이 안 붙고, 이름이 없으면 공유가 안 된다. (BRIEF §3.3)
- **암수를 몸통 시트로 나누지 않는다.** 이형은 눈·입과 같은 파츠 레이어다 —
  정면·측면 2장이고 북향에서는 숨긴다. 나누면 종당 16장이 32장이 된다. (BRIEF §4.9)
- **이형이 없는 종에 리본·분홍/파랑을 붙이지 않는다.** 11종 중 이형이 있는 건 고라니 하나뿐이고,
  나머지의 성별은 이름표 뱃지로만 보인다. 색은 §4.3 유전이 이미 쓰고 있다.
- **날씨는 팔레트를 건드리지 않는다.** 시간대는 팔레트, 날씨는 틴트+오버레이 —
  그래야 시간대 × 날씨가 곱셈이 아니라 덧셈이 된다. (BRIEF §6.8)

## 툴 정책

전용 툴은 미리 만들지 않는다. Godot의 리모트 씬 트리·인스펙터, F6 단독 씬 실행,
`SpriteFrames` 미리보기가 대부분의 필요를 흡수한다.

지금 만드는 것은 셋뿐이다 — **임포터 검증 스크립트, 소환·치트 화면, 측정 라벨 하나.**
전부 게임 씬으로 만들고 릴리스 빌드에서 제외한다.

> 새 툴이 떠오르면 먼저 환산할 것: **툴 하루 = 스프라이트 2~3장, 일주일 = 동물 한 종.**

## 아트

플레이스홀더로 시작한다. Kenney(CC0)나 단색 사각형이면 충분하고,
레이어 구조는 색 사각형으로도 완전히 검증된다.
**재미가 확인되기 전에 그리지 않는다.**

종당 몸통 16장(성체 10 + 아기 6, E/W 는 미러). 눈·입·아이콘·이펙트는 공용이라 종 수와 무관하다.
