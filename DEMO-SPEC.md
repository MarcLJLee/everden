# 데모 런타임 사양 — Demo 1: 필드 슬라이스

> 대상: Claude Code 세션에서 구현할 첫 실행 가능한 데모.
> 근거 문서: `PROJECT-BRIEF.md` (절 번호는 전부 이 문서를 가리킨다)
> 최종 수정: 2026-08-25

---

## 0. 왜 필드부터인가

전체 루프(§8)에서 **가장 모르는 부분**이 필드다.

사파리 층은 기술적으로 뻔하다 — 자율 이동 AI와 상태값이고, 재미 여부도 예측 가능하다.
반면 **필드 탐색 + 동료 유도 + 점유 게이지**는 이 게임의 발명품이고,
"30초 안에 첫 유도"라는 밸런싱 지표(§8)도 여기 걸려 있다.
재미없으면 설계를 다시 해야 하는 부분이므로 여기부터 만든다.

**Demo 1은 게임이 아니라 질문에 답하는 장치다.**
답해야 할 질문: *필드를 돌아다니다 동료가 이끄는 대로 가서 동물을 만나는 것이 재미있는가?*

---

## 1. 범위

### 만든다

| | 내용 | 근거 |
|---|---|---|
| 1 | 작은 필드 맵 1개 (플레이스홀더 타일) | §6.4 |
| 2 | 플레이어 4방향 이동 | §4.5, §6.3 |
| 3 | 동료 2마리(개·고양이)가 따라오고, 각자의 감각으로 유도 | §3.3 |
| 4 | 야생동물 3종이 화면 밖에서도 돌아다님 | §3.2, §6.4 |
| 5 | 접근 → 점유 시간 게이지 → 초대 성공 | §3.4 |
| 6 | 스프라이트 레이어 조합 (몸통/눈/입/아이콘) | §4.6 |
| 7 | 데이터 로드 + 태그 검증 | §5.2, §5.5 |
| 8 | 디버그 오버레이 — 첫 유도까지 걸린 시간 | §8 |

### 만들지 않는다

사파리 층, 쉼터, 경제, 번식, 성장 진행, 치료·도움, 세계 지도, 팀 편성 UI,
콘텐츠 팩, 저장/불러오기, 밤낮·조명, 소리.

> 전부 §8의 프로토타입 축소 표를 따른 것이다. **늘리지 말 것.**
> 데모가 질문에 답하고 나면 그때 다음 슬라이스를 정한다.

---

## 2. 데이터

`data/tags.json`, `data/animals.json`이 이미 있다. 그대로 쓴다.

- 종 11개 중 `prototype: true`인 **6종**(개·고양이·청설모·너구리·수달·고라니)만 로드한다.
- Demo 1에 실제로 등장하는 것은 **동료 2종(개·고양이) + 대상 3종(청설모·너구리·수달)**이다.
  고라니는 데이터에만 있어도 된다(로더가 안 깨지는지, `대` 캔버스 버킷이 도는지 확인용).
- 프로토타입에서 켜는 상성 축은 **먹이 유형 + 활동 시간 2개뿐**이다(§8).
  나머지 축은 데이터에 있어도 판정에 쓰지 않는다.
- 감각은 **후각·청각·시야 셋 다** 구현한다. 여기는 깎지 않는다 — §3.4 참조.

### 검증 규칙 (§5.2)

로드 시점에 반드시 검사한다:

1. `required_tags`에 없는 값이 오면 → **로드 거부, 어느 종의 어느 필드인지 출력**
2. `optional_tags`에 없는 값이 오면 → **기본값으로 대체하고 경고만**
3. `requires_behavior_schema` > 본체 지원 버전 → **로드 거부, "게임을 업데이트해주세요"**
4. `size_class`가 `derivations` 규칙과 다르면 → **경고**(수동 오버라이드가 허용되므로 거부는 아님)

이 검증기는 §5.5가 인정한 "미리 만들어도 되는" 유일한 툴이다. CLI로 단독 실행도 되게 한다.

---

## 3. 구현 사양

### 3.1 이동과 방향

- WASD / 방향키 / 패드 왼쪽 스틱 → 추상 입력 `move_vector` (§6.3)
- 스프라이트 방향은 **4방향**: 이동 벡터의 지배적 축으로 결정
- 대기 상태는 **마지막 수평 방향**의 idle을 쓴다 (idle은 좌우만 존재, §4.5)
- 북향일 때 **눈·입 레이어를 숨긴다** (§4.6)

### 3.2 ★ 걷기 바운스는 노드 Y로

**이 데모에서 반드시 검증해야 할 구조다.**

```
Actor (Node2D)                ← Y-sort 대상은 이 노드 하나
 ├─ Shadow (Sprite2D)         ← 바운스에 따라가지 않는다. 고정
 └─ Body (Node2D)             ← ★ 여기 Y를 프레임마다 0/-1px 로 움직인다
     ├─ BodySprite (AnimatedSprite2D)
     ├─ Eye  (Sprite2D)       ← eye_anchor 위치
     ├─ Mouth(Sprite2D)       ← mouth_anchor 위치
     └─ Emote(Sprite2D)       ← head_anchor 위치, 머리 위
```

- 걷기 2프레임은 **다리 모양만** 바꾼다. 몸 전체의 오르내림은 `Body`의 Y가 담당한다.
- `Body.position.y`는 **정수 픽셀로 스냅**한다. 안 하면 얼굴이 미세하게 떨린다.
- 그림자는 바운스를 따라가지 않는다 (발이 땅에서 떨어져 보이는 효과).
- 눈·입 앵커는 `animals.json`의 값을 그대로 읽어 `position`에 넣는다.
  **프레임별 앵커는 만들지 않는다.**

### 3.3 필드 시뮬레이션 (§6.4)

```
전체 동물 목록
 ├─ 화면 밖 (얕은 시뮬)  : position += velocity * dt.  애니메이션·AI 없음
 └─ 근접 반경 안 (풀 AI) : 행동 태그 반영, 애니메이션 재생, 유도 판정 대상
```

- 승격/강등 경계는 `activation_radius` 상수 하나로 제어한다.
- 동물은 필드 경계 안에서 랜덤 워크한다. 정교한 AI는 필요 없다.
- 개체 수는 `animal_count` 상수로 제어한다.

### 3.4 동료의 유도 (§3.3)

**종 이름으로 분기하지 않는다.** `tags.json`의 `trait_to_sense` 테이블이 전부를 만들어낸다.

```
for 대상 in 활성 동물:
    for trait in 대상.traits:
        sense = trait_to_sense[trait].sense
        if sense in 동료.senses and 거리 <= guide_radius:
            → 동료가 그 방향을 보고
              머리 위에 sense 아이콘(코/귀/눈)을 띄우고
              화면 가장자리에 방향 화살표를 표시한다
```

- 유도 대상이 여럿이면 **가장 가까운 하나**만 가리킨다.
- `guide_radius`는 동료의 `stats_range.sense_range`에서 뽑힌 개체값을 곱한다.
  (능력치가 무언가를 결정해야 한다 — §2.5)
- 대상이 `guide_radius`를 벗어나면 아이콘을 끈다.

#### ★ 이 데모가 답해야 할 대비

데이터에서 자동으로 나오는 결과다. 코드에 종 이름이 하나도 없어야 이렇게 된다:

```
개(후각)         → 너구리 ✅  수달 ✅   청설모 ❌  고라니 ❌
고양이(청각·시야) → 너구리 ✅  수달 ✅   청설모 ✅  고라니 ✅
```

**"개로는 못 찾는 애가 있다"**가 이 데모의 핵심 장면이다.
아이가 그걸 겪고 고양이를 데려가서 푸는 것 — 그게 §3.3이 노린 것이고,
동료가 하나뿐이면 검증 자체가 불가능하다.

청설모는 `traits`가 `나무흔적`뿐이라 후각에 안 걸린다. 버그가 아니라 의도다.

### 3.5 교감 게이지 (§3.4)

```
대상에게 interact_radius 안으로 들어가서 상호작용 키를 누르면
    → 플레이어가 뭉뚱그린 모션 재생
    → 대상 위에 이펙트(반짝임) 표시
    → 게이지가 찬다
    → 다 차면 "초대됨" — 대상이 사라지고 카운터 +1
```

**절대 규칙: 게이지는 중간에 끊기지 않는다.**
방해 요소·실패 조건을 넣지 않는다. 취소는 플레이어가 명시적으로 누를 때만 (§3.4).

게이지 시간 = `base_gauge_time` × 상성 계수:

| 조건 | 계수 |
|---|---|
| 기본 | 1.0 |
| 대상이 `육식`이고 동료가 `초식` (또는 반대) | 1.5 |
| 대상 `activity`와 현재 시간대가 안 맞음 | 1.5 |
| 동료의 `charm` 개체값 | ÷ charm |

> 시간대는 Demo 1에 밤낮이 없으므로 **디버그 오버레이에서 강제 전환**할 수 있게 한다.
> 게이지 길이가 눈에 띄게 달라지는 것을 확인하는 것이 이 항목의 목적이다 —
> **게이지가 상성을 보여주는 UI**라는 설계(§3.4)의 검증이다.

⚠️ 대상 동물의 상호작용 모션은 **만들지 않는다.** 기존 대기 스프라이트 + 이펙트다(§3.4).

### 3.6 튜닝 상수 (§6.4)

`tuning/field_tuning.tres` 하나에 모아 **코드에 박지 않는다.**
리모트 인스펙터로 실행 중에 만질 수 있어야 한다(§5.5).

| 상수 | 초기값(제안) |
|---|---|
| `map_size` | 40 × 30 타일 |
| `animal_count` | 3 |
| `move_speed` | 3.5 타일/초 |
| `guide_radius` | 8 타일 |
| `activation_radius` | 12 타일 |
| `interact_radius` | 1.5 타일 |
| `base_gauge_time` | 3.0 초 |

초기값은 §6.4의 역산(이동 3~4타일/초, 첫 유도 30초 → 100타일 남짓)에서 나온 것이다.
**맞을 거라고 가정하지 말고 측정해서 맞춘다.**

### 3.7 디버그 오버레이 (§5.5)

`Label` 몇 개면 된다. 슬라이더 UI를 만들지 말 것 — 값은 리모트 인스펙터로 만진다.

표시할 것:

- **첫 유도까지 걸린 시간** ★ 밸런싱 지표(§8). 필드 입장 시각부터 첫 코 아이콘까지
- 현재 게이지 시간과 적용된 상성 계수
- 활성(풀 AI) / 비활성(얕은 시뮬) 동물 수
- 초대 성공 카운터
- 시간대 강제 전환 토글

### 3.8 플레이스홀더 아트

**손으로 그린 스프라이트를 기다리지 않는다.** Kenney(kenney.nl, CC0) 애셋이나 단색 사각형으로 시작한다.

레이어 구조(§4.6)는 색 사각형으로도 완전히 검증된다:

- 몸통 = 종별 색 사각형 (`size_class`에 따라 24/32/48px)
- 눈 = 작은 어두운 사각형 (앵커 위치)
- 입 = 더 작은 사각형 (앵커 위치)
- 아이콘 = 머리 위 밝은 사각형

이걸로 **북향 숨김, 앵커 정렬, 바운스 동기화**가 다 확인된다.
재미가 확인된 뒤에 그린다(§8).

---

## 4. 완료 조건

이 목록이 전부 참이면 Demo 1은 끝이다.

- [ ] 플레이어가 4방향으로 움직이고, 북향에서 눈·입이 사라진다
- [ ] 걷기 중 얼굴이 몸에서 떨어지지 않고, 정지 시 떨리지 않는다 (정수 스냅)
- [ ] 동료가 따라오고, 감지 가능한 대상이 반경에 들어오면 감각 아이콘과 방향 화살표가 뜬다
- [ ] **개만 데려가면 청설모·고라니를 못 찾고, 고양이를 데려가면 찾는다** (의도된 대비)
- [ ] 코드 어디에도 종 id로 분기하는 곳이 없다 (`trait_to_sense`만 쓴다)
- [ ] 대상에 다가가 상호작용하면 게이지가 차고, 중간에 끊기지 않는다
- [ ] 상성이 안 맞으면 게이지가 눈에 띄게 길어진다
- [ ] 화면 밖 동물이 움직이고 있고, 다가가면 풀 AI로 승격된다
- [ ] `animals.json`의 태그를 일부러 오타 내면 **로드가 거부되고 어느 필드인지 출력된다**
- [ ] 선택 태그를 오타 내면 경고만 뜨고 게임이 돈다
- [ ] 튜닝 상수를 코드 수정 없이 리모트 인스펙터로 바꿀 수 있다
- [ ] **첫 유도까지 걸린 시간이 화면에 표시되고, 상수만 조정해 30초 이내로 맞출 수 있다**

---

## 5. 권장 구조

```
res://
  data/
    tags.json
    animals.json
  scenes/
    field/Field.tscn
    actors/Actor.tscn
    ui/DebugOverlay.tscn
  scripts/
    data/DataLoader.gd        # 로드 + 검증
    data/TagSchema.gd         # 열거형 대조
    actor/Actor.gd            # 이동, 방향, 바운스
    actor/FaceLayer.gd        # 눈·입 앵커, 북향 숨김
    actor/EmoteLayer.gd       # 머리 위 아이콘
    field/FieldSim.gd         # 얕은/깊은 승격
    field/GuideSystem.gd      # 유도 판정 (trait_to_sense 기반)
    interaction/Gauge.gd      # 점유 게이지
    debug/Metrics.gd          # 첫 유도까지 시간
  tuning/
    field_tuning.tres
  tools/
    validate_data.gd          # CLI 검증기
```

**툴은 전부 게임 씬으로 만들고 릴리스 빌드에서 제외한다**(export preset의 exclude filter, §5.5).

---

## 6. 이후 슬라이스 (지금 만들지 말 것)

| | 내용 | 검증할 질문 |
|---|---|---|
| Demo 2 | 사파리 슬라이스 — 자율 행동, 급식, 표정, seat/쉼터 | 동물이 사는 걸 보는 게 재미있나 |
| Demo 3 | 두 층 연결 — 데려오기, 여행 행복도, 집 보는 당번 | 순환이 성립하나 |
| Demo 4 | 성장·번식·색 유전 | 개체에 애착이 생기나 |

Demo 1의 답이 나오기 전에는 착수하지 않는다.
