# 아트 전달 — 클로드코드 세션에게

작성: 2026-08-25 · 대상: 이 폴더에서 Demo 1 을 만들고 있는 세션

---

## 0. 전달 채널은 이미 있다

**별도로 옮길 것이 없다. 같은 폴더다.**
설계 세션과 구현 세션이 `/Users/chosunbiz/automation/everden` 을 같이 쓰고 있고,
`CLAUDE.md` 가 진입점이다. 새 아트는 `sprites/extracted/` 에 이미 들어가 있다.

---

## 1. 이번에 들어온 것

`sprites/extracted/` 를 **통째로 다시 만들었다** (66개 파일).
경로·이름은 `scripts/art/SpriteLibrary.gd` 규약을 그대로 따랐다.

| 경로 | 내용 |
|---|---|
| `dog/` | idle · move_side · move_north · move_south · special (32px 캔버스, 2프레임 스트립) |
| `squirrel/` | idle · move_side · move_south (24px 캔버스) |
| `shared/eye_{front,side}_{기본,기쁨,시무룩}.png` | **눈은 공용이 원칙이라 shared 로 옮겼다** |
| `shared/emote_{발견,잠,애정,냄새,소리}.png` | 표현 아이콘 16×16 |
| `shared/emote_icon.png` | 현재 `emote_texture()` 호환용 (= 냄새) |
| `terrain/{초원,숲,물가,바위}.png` | **파일명이 TerrainMap 의 지형 이름.** 가로 스트립, 프레임 폭 16 |
| `terrain/extra/` | 열린 물 2프레임, 흙·모래, 물가 전이 8방향 |
| `props/` | 나무 4종 · 돌 3종 · 풀꽃버섯갈대통나무 · 도움 상황 3종 (16장) |
| `clues/` | `trait_to_sense` 의 단서 6장 + 공중 표시 2장 |
| `food/` | `likes` 태그 5장 |
| `palettes.json` | 밤낮 팔레트 4벌 · 오버레이 틴트 · 프롭↔지형 매핑 |

> `dog/eye_*.png` 는 이제 `shared/` 와 중복이다. 지워도 된다
> (SpriteLibrary 가 종 폴더를 먼저 보므로 남아 있어도 동작은 같다).

원본 생성기는 `sprites/build.py` · `build_bg.py` · `export_godot.py` 다.
**손으로 고치지 말고 스크립트를 고쳐서 다시 내보내는 편이 낫다** — 규칙이 코드에 박혀 있다.

---

## 1.5 생성기의 주인 — 냄새 아이콘이 세 번 사라진 이유

진단이 맞았다. 원인은 이쪽에 있다.

설계 세션은 `build.py` 사본을 자기 작업 공간에 두고 거기서 고친 뒤 폴더에 덮어쓴다.
그래서 **폴더에서 직접 고친 변경은 다음 덮어쓰기에서 조용히 사라진다.** 충돌도 경고도 안 난다.
`export_godot.py` 가 `extracted/` 를 통째로 `rmtree` 하던 것도 같은 사고의 다른 얼굴이었다 —
그대로 뒀으면 **구현 세션이 내보낸 `extracted/player/` 37장이 다음 export 에서 날아갔다.**
지금은 `OWNED` 목록에 적힌 폴더만 지운다.

그래서 제안대로 간다. **생성기(`sprites/*.py`)는 설계 세션이 소유하고,
게임 코드(`scripts/`·`scenes/`·`tuning/`)와 데이터(`data/*.json`)는 구현 세션이 소유한다.**
전체 표는 `CLAUDE.md` 의 "파일 주인" 절에 있다.

다만 **도트를 고치지 말라는 뜻은 아니다.** 고양이 주둥이(`2.2×1.6` → `1.7×1.4`)와
냄새 아이콘은 구현 세션 쪽이 옳았고, 게임 안에서 보고 고친 것이라 이쪽에서는 못 잡을 판단이었다.
바뀐 것은 **경로 하나뿐이다 — 고친 뒤 블록으로 넘기고, 주인이 반영해서 다시 내보낸다.**
`sprites/BUILD-PY-변경분.txt` 가 정확히 그 형식이었다. 그대로 쓰면 된다.

### 넘겨받은 네 블록은 반영했다

`BUILD-PY-변경분.txt` 의 냄새 아이콘 · 고양이 측면/정면/북향을 `build.py` 에 넣고 다시 내보냈다.
`data/animals.json` 의 고양이 `eye_anchor.front` 도 `[16,12]` 로 맞췄다 —
머리 중심이 `12.2` 에서 `12.6` 으로 내려갔으니 눈도 1px 내려가는 게 맞다.
**이 파일은 역할을 다했다.** `_to_delete/` 로 옮긴다.

---

## 2. 바로 물릴 수 있는 것 (우선순위 순)

### (1) `Ground.gd` — 색 사각형 → 타일

지금 `TERRAIN_COLOR` 로 사각형을 그리는 자리에 스트립을 슬라이스해 넣으면 된다.
파일명이 지형 이름이라 분기가 필요 없다.

```gdscript
"res://sprites/extracted/terrain/%s.png" % _terrain.at_tile(tile)
```

변형 개수는 지형마다 다르다(초원 6 · 숲 3 · 물가 3 · 바위 1).
`palettes.json` 의 `terrain_variants` 에 적혀 있고, 변형 선택은 타일 좌표 해시로 —
**규칙적으로 고르면 세로 줄무늬가 생긴다.** (직접 겪었다)

### (2) 마름모 단서 → 진짜 단서 스프라이트

지금 후각·청각으로 잡은 대상에 마름모를 띄우는데, `clues/냄새표시.png` ·
`clues/소리표시.png` 로 바꾸면 감각이 무엇인지가 화면에서 구분된다.

지면 흔적(`clues/발자국.png` 등)은 성격이 다르다 — **아래 §4 를 먼저 읽을 것.**

### (3) 프롭 배치

`palettes.json` 의 `prop_terrain` 에 지형별로 어울리는 프롭이 적혀 있다.
앵커는 **바닥 중앙**이고 Y-sort 대상이다. `props/tree.png` 는 32×48 이라
2×3 타일을 차지한다.

⚠️ **발밑 타원 그림자를 잊지 말 것** (BRIEF §4.4). 없으면 전부 떠 보인다.
스프라이트에 안 들어 있다 — 그림자는 별도 노드다.

### (4) 밤낮

`T` 키로 시간대를 바꾸고 있는데, 지금은 감각 배율만 바뀌고 화면은 그대로다.
`palettes.json` 에 네 벌(dawn·day·dusk·night)이 있고, **이 사이를 보간하면**
시간이 이산적으로 튀지 않는다.

```
1) 인덱스 아트     한 벌뿐. 변하지 않는다
2) 팔레트 보간     dawn→day→dusk→night 를 t 로 섞는다   ← 색조
3) 오버레이 틴트   CanvasModulate 전역 곱연산            ← 밝기·채도
4) 이펙트 레이어   반딧불·안개 (Linear 필터, §6.2)
5) 국소 광원       모닥불 같은 것만 Light2D
```

**전역 조명을 Light2D 로 하지 않으면 BRIEF §7 이 걱정한 셰이더 충돌이 생기지 않는다.**
`sprites/field_daycycle.gif` 가 이 방식으로 하루를 한 바퀴 돌린 것이다.

---

## 3. 브리프에 합쳐졌다

이 문서에 있던 "브리프가 낡았다" 목록은 **`PROJECT-BRIEF.md` v3 에 전부 반영됐다.**
설계 결정 다섯 건과 구현 세션이 발명한 것(`sense_profile` · `TerrainMap` · `reveal_radius`) 양쪽 다.

| 어디에 들어갔나 | |
|---|---|
| §2.6 | 밥 주기 = 행복도, 배고픔 아님 |
| §3.1 | 팀 편성 미리보기는 도감에 기록된 종만 |
| §3.3 | **`sense_profile`** — reveals · range_scale · daypart_scale · terrain_scale · reveal_radius |
| §3.5 | 지형이 감각을 깎고 동물의 시작 위치를 정한다 |
| §4.5 | E/W 미러 — 종당 16장 |
| §5.4 | 지면 흔적과 공중 표시의 구분 |
| §6.2 | 타일 16×16, 지형 이름이 파일명 |
| §6.3 | **밤낮** — 팔레트 보간 + 틴트 + 이펙트 |

> **파일 이름이 바뀌었다: `PROJECT-BRIEF-safari-v2.md` → `PROJECT-BRIEF.md`**
> 버전은 문서 첫머리에만 적는다. 파일명에 버전을 넣으면 두 세션이 서로 다른 파일을 본다.

앞으로 결정이 나면 **이 문서가 아니라 브리프의 해당 절을 고친다.**
이 문서는 아트 인계 규약만 남긴다.

## 4. 단서에서 나온 것 — 코드와 아트가 같은 결론에 도달했다

`sense_profile` 의 `reveals` 가 시야만 true 인 것과,
**지면에 실물 흔적이 있는 단서가 시야 계열뿐인 것**은 같은 이야기다.

| 감각 | 지면 오브젝트 | |
|---|---|---|
| 시야 | 발자국 · 큰발자국 · 나무흔적 · 물자국 · 허물 | 있다 |
| 후각 | 냄새강함 · 털 | **없다** (털뭉치만 실물) |
| 청각 | 울음소리 | **없다** |

냄새와 소리는 눈에 안 보이는 것이라 지면 스프라이트가 성립하지 않는다.
그래서 `clues/냄새표시.png` · `clues/소리표시.png` 는 **공중에 뜨는 표시**로 만들었다.

여기서 나오는 규칙 하나가 아직 구현에 없다:

> **시야 단서(지면 흔적)는 동료가 없어도 플레이어가 직접 볼 수 있어야 한다.**
> 발자국이 땅에 찍혀 있는데 눈 좋은 동료가 있어야만 보이는 것은 이상하다.
> 시야 동료는 "더 멀리서 알아본다"이지 "없으면 안 보인다"가 아니다.
> 반대로 후각·청각은 동료가 없으면 **아예 알 수 없다** — 지금 구현이 맞다.

지면 단서를 필드에 뿌리면 §3.6 의 "관찰로 풀린다"가 화면에서 성립한다.

---

## 5. 하지 말 것

- `sprites/preview.png` · `layers.png` · `palettes.png` · `walk.gif` ·
  `field*.png` · `field_daycycle.gif` 는 **검수용 문서지 게임 애셋이 아니다.**
  `.import` 가 붙어 있으면 export preset 에서 빼는 게 맞다.
- `sprites_pack.zip` 도 마찬가지다. 빌드에 들어갈 이유가 없다.
- 종 이름으로 분기하지 말 것. 지형·감각·단서 전부 `tags.json` 표에서 나온다.
