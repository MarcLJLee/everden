# 사파리 (가제) — 프로젝트 기획서

> 이 문서는 프로젝트의 단일 기준 문서다. 대화 맥락 없이 이 문서만 읽어도
> 프로젝트를 이해할 수 있도록 작성되었다.
>
> **버전 v3.20** · 최종 수정: 2026-08-27 · 상태: Demo 1(필드 슬라이스) 구현 중
> 변경 이력은 **부록 B** 참조.
>
> ⚠️ **파일 이름에 버전을 넣지 않는다.** 버전은 이 줄에만 있다.
> 두 세션(설계·구현)이 같은 경로를 참조해야 문서가 갈라지지 않는다.

---

## 0. 이 프로젝트가 무엇인가

**7살 아이를 첫 플레이어로 상정한, 동물 수집·육성 게임을 1인 개발로 만든다.**

넓은 사파리를 소유하고, 그곳에서 동물들이 서로 어울려 살아간다.
플레이어는 먹이를 주고 돌보며, 동료들과 함께 세계 각지로 여행을 떠나
그곳에서 만난 동물들과 교감하고 우리 사파리로 **초대**한다.
사냥이나 포획이 아니라, 친구가 되어 초대하는 개념이다.

**만드는 사람**: 15년 경력 소프트웨어 엔지니어. 게임 개발과 도트 그래픽은 처음.
코드/설계 역량은 충분하나, 미술과 게임 특유의 감각은 경험이 없는 영역.

**진짜 목적**: 아이와 함께 만드는 것. 아이가 동물을 그리고 그게 게임에서 살아 움직이는
사이클이 이 프로젝트의 핵심 동력이다. 상업적 성공이 목표가 아니다.

---

## 1. 게임 구조

### 1.1 두 개의 층

```
┌─ 사파리 (집) ─────────────────────────┐
│  · 동물들이 서로 어울려 살아감          │
│  · 먹이 주기, 돌보기                    │
│  · 가족 형성 → 아기 탄생                │
│  · seat(자리) 확장이 진행도             │
│  · 정원을 넘은 동물은 쉼터에서 대기      │
└───────────────────────────────────────┘
        ↑ 새 친구를 초대해 옴   ↓ 동료를 데리고 떠남
┌─ 원정 (세계 지도 → 지역 필드) ────────┐
│  · 지역을 골라 동료와 함께 여행         │
│  · 필드를 돌아다니며 활동               │
│  · 동료의 감각이 동물의 위치를 알려줌    │
│  · 교감·치료·도움 → 초대 / 재화 획득    │
└───────────────────────────────────────┘
```

**두 층이 서로를 먹여준다.**
사파리에 동물이 늘수록 원정에 데려갈 동료가 늘고, 원정이 성공할수록 사파리가 풍성해진다.
사파리에 이미 있는 동물이 필드에서 힌트를 주는 구조가 이 순환을 강화한다.

### 1.2 핵심 정서

**"무슨 동물이건 매력이 중요하다."**
이 게임의 재미는 결국 "저 동물을 우리 사파리에 데려오고 싶다"에서 나온다.
모든 설계 판단은 이 감정을 해치지 않는지를 기준으로 한다.

> **이 정서를 가장 자주 위협하는 것은 "수집이 벌이 되는 구조"다.**
> 동물이 늘어날수록 유지비·관리 부담·의무가 커지면 데려오고 싶은 마음이 식는다.
> 경제(§2.6)와 여행 로테이션(§3.7)을 설계할 때 이 함정을 반복해서 확인할 것.

---

## 2. 사파리 층

### 2.1 동물의 삶

동물은 메타데이터에 따라 자율적으로 행동한다. 플레이어가 지시하지 않는다.
물가 동물은 물가로 가고, 야행성은 낮에 자고, 무리 성향은 같은 종 옆에 붙는다.

> **설계 원칙: 데이터는 화면에 드러나야 한다.**
> 메타데이터 필드를 만들 때마다 "이게 화면에서 어떻게 보이는가"를 같이 정한다.
> 보이지 않는 필드는 만들지 않는다. 스프레드시트만 화려하고 게임이 밋밋해지는 것을 막는다.

### 2.2 돌봄과 상태

- **행복도/건강 상태가 있다.** 아프거나 심심하면 표정이 시무룩해지고, 움직임이 느려지고,
  친구와 안 놀고, 아기를 낳지 않는다.
- **돌보면 회복된다.**
- ❌ **죽지 않는다.** 수명으로도, 방치로도 죽지 않는다.

> **왜 죽음을 뺐는가**: 다마고치식 사망은 7살에게 지나치게 무겁다. 게다가 이 게임은
> 동물이 수십 마리라 "죄책감 관리 게임"이 되기 쉽고, 그러면 아이가 게임을 안 켜게 된다.
> **방치의 대가는 손실이 아니라 진행 정체**로 표현한다.

#### 시간이 흐르는 조건 (v2 확정)

- **게임을 켠 동안에만 시간이 흐른다.** 오프라인 시간은 흐르지 않는다.
  이 게임은 인터넷 없이 완전히 동작하는 것이 전제다.
- 따라서 "방치"란 **세션 안에서 다른 활동(주로 원정)을 하는 동안** 사파리를 못 돌보는 것을 뜻한다.
- ⏳ **원정 중에 사파리 시간이 흐를지는 미정.** 프로토타입에서 만들어보고 결정한다(§7).

#### 행복도가 오르는 경로는 둘이다

| 경로 | 내용 |
|---|---|
| 돌봄 | 먹이·물·치료. 사파리에서 상시 가능 |
| **여행** | 원정에 동료로 데려가면 행복해진다 (§3.7) |

기본 먹이는 무료다(§2.6). 굶어서 나빠지는 일은 없다.

### 2.3 성장 — grow 배열

동물은 **성장 시퀀스**를 따라 자란다. 시퀀스는 데이터로 정의한다.

```
growth: [
  { stage: "baby",  from_day: 0, to_day: 5,    sprite_set: "...",
    size: "5-10cm",  weight: "1kg", behavior_tags: [부모근처, 원정불가] },
  { stage: "adult", from_day: 5, to_day: null, sprite_set: "...",
    size: "20-30cm", weight: "8kg", behavior_tags: [무리, 원정가능, 번식가능] }
]
```

**스키마는 배열로 열어두되, 초기 콘텐츠는 2단계(아기/성체)만 채운다.**
배열이면 나중에 대표 종 몇 개만 3단계로 만들 수 있고,
스프라이트 물량이 코드 문제가 아니라 **데이터 선택 문제**가 된다.
(§5.2의 "본체는 능력을 갖고 팩은 필요한 만큼만 참조한다"와 같은 모양)

**규칙:**

- `stage`는 **열거형으로 고정**하고 **필수 태그**로 분류한다 — 모르는 값이면 로드 거부(§5.2).
  자유 문자열로 두면 청소년/juvenile/teen이 섞인다.
- **단계별 차이는 숫자 스탯이 아니라 행동 태그로 표현한다.**
  숫자는 화면에 안 보이지만 행동은 보인다. 아기는 부모 옆에 붙어 다니고,
  청소년(넣는다면)은 혼자 돌아다니되 무리엔 안 낀다.
- ❌ **아기는 성체를 축소해서 재활용하지 않는다.** 비정수 배율 축소는 nearest 필터에서
  픽셀이 깨지고, 눈(§4.2에서 매력의 90%)이 제일 먼저 망가진다.
  아기는 **더 작은 캔버스에 새로 그린다** (성체 24×24면 아기 16×16).
  실제 아기 동물의 특징도 단순 축소가 아니라 머리가 크고 다리가 짧고 눈이 큰 것이다.
- 아기는 애니메이션을 줄인다 (대기·이동만, 특징 동작 없음).
- **아기는 원정에 못 간다.** 이것이 성장을 기다리는 이유가 된다 — "빨리 커라, 같이 가자".

`from_day` 값은 §7의 "시간 흐름의 단위" 미결에 물려 있다.
기준으로 삼을 것: **한 세션(20~30분) 안에 성장 이벤트를 최소 한 번은 봐야 한다.**
아이가 성장을 못 보면 그 시스템은 없는 것과 같다.

### 2.4 가족·번식·정원

- 동물이 **결혼**해서 가족을 이룬다. 짝짓기 시기 같은 생태적 요소는 넣지 않고 **의인화**한다.
  한 번 맺어지면 가족이 형성되는 형태.
- 행복하지 않은 동물은 아기를 낳지 않는다 (돌봄의 의미를 여기서 준다).
- **암수 한 쌍이어야 아기가 생긴다** ★ v3.11. 성별은 개체마다 있고, 화면에서 보인다(§4.9).

#### 성별이 진행을 막지 않게 하는 장치 ★ v3.11

성별을 번식에 걸면 **막힘이 생길 수 있다** — 어떤 종을 셋이나 모았는데 전부 암컷이면
아기를 영영 못 본다. 그건 원칙 2(되돌릴 수 없는 실패 없음)에 걸린다.
그래서 **거는 순간 아래 셋이 같이 들어간다.** 하나라도 빠지면 성별을 걸지 않는다.

| 장치 | 내용 |
|---|---|
| **혼자예요 표시** | 짝이 없는 개체에 **반쪽 하트**가 뜬다. 아이가 이유를 **먼저** 안다(원칙 3) |
| **짝 핀** | 세계 지도의 그 지역에 **하트 핀**이 뜬다. 짝을 찾는 것이 원정 동기가 된다 |
| **확정 배치** | 그 필드에 들어가면 그 종의 **반대 성별이 반드시 정의된다** — §3.11 1단계의 예외. 개체수 0 이 안 된다 |

> **확률로 풀지 않는다.** "언젠가 나오겠지"는 7살에게 벽이다.
> 짝이 없다는 것이 보이고, 어디 가면 있는지가 보이고, 가면 **있다.**
> 확률을 안 쓰니 노출할 숫자도 없다.

- 시작 동료(개)는 **처음부터 한 쌍**으로 준다. 첫 가족을 성별 때문에 못 만드는 일이 없어야 한다.

#### seat(자리)와 쉼터 — v2 확정

- 사파리에 정해진 수의 **seat**가 있다.
- **seat는 원정으로 데려온 동물에게도 적용된다.** 정원 제한이 있어야 "꾸린다"는 관리 감각이 생긴다.
- 여분의 seat가 있을 때만 아기가 태어난다.
- **정원을 넘은 동물은 쉼터에서 대기한다.**

**쉼터의 규칙:**

| 항목 | 내용 |
|---|---|
| 형태 | 목록 UI가 아니라 **장소**다. 별도 화면에서 동물들이 어슬렁거린다 |
| 왜 | 7살에게 "목록 화면의 동물"은 존재하지 않는 동물이다. 원정에서 데려온 애가 화면에 안 보이면 §1.2가 그 자리에서 깨진다 |
| 시간 | 대기 중인 동물은 **시간이 정지**한다. 배고픔·행복도 변화 없고 번식하지 않는다. 관리 부담이 늘지 않는다 |
| 이동 | 사파리 ↔ 쉼터는 언제든 자유롭게, 되돌릴 수 있게. **방출은 없다** |
| 원정 | **쉼터의 동물도 원정 동료로 쓸 수 있다.** 그래야 벤치가 죽은 공간이 안 된다 |

**이로써 "꾸린다"가 두 층으로 갈라진다** — 사파리에 누구를 둘까(전시·번식),
원정에 누구를 데려갈까(로스터).

#### seat 확장은 도감 진척에 연동한다

seat가 꽉 찬 뒤에도 원정이 보상을 줘야 한다.
새 동물이 다 쉼터로 가면 원정 동기가 약해지므로, **새 종을 만날수록 자리가 늘어나게** 한다.

#### 쉼터 화면 ★ v3.5

**창고나 우리처럼 보이면 안 된다.** 목록이 아니라 **장소**로 그린다 — 그늘막 아래
방석에 누워 자는 표시가 떠 있는 작은 마당이다. 동물이 늘어서 밀려난 곳이 아니라
**잘 지내는 곳**으로 보여야 §"수집이 벌이 되면 안 된다"가 화면에서도 지켜진다.

집 마당 바닥을 그대로 쓰면 같은 곳으로 읽힌다 — 길도 울타리도 없는 **다른 마당**이다.

세 규칙이 전부 화면에 있어야 한다.

| 규칙 | 화면에서 |
|---|---|
| 방출은 없다 | **"내보내기" 버튼이 없다.** 집 ↔ 쉼터 둘뿐 |
| 쉼터의 동물도 원정에 나간다 | 이름표에 **"원정 갈 수 있어요"** 배지. 없으면 아이가 쉼터를 '못 쓰는 곳'으로 배운다 |
| 되돌릴 수 있다 | 자리가 꽉 차면 막지 않고 **"누구랑 바꿀까요?"** 를 묻는다. 바꿔도 되돌린다 |
재화 소비(§2.6)도 확장 경로로 함께 둔다.

### 2.5 개체의 개성

같은 종이어도 우리 사파리의 이 아이는 특별하다 — 이 감각을 만드는 장치가 셋이다.

**(1) 능력치** — 개체마다 바운더리 안에서 랜덤한 값을 갖고, 번식하면 부모 값을 물려받는다.

두 조건을 지킬 것:
1. **능력치가 무언가를 결정해야 한다** — 유도 반경, 교감 게이지 속도 등.
2. **7살이 알아볼 수 있게 표시한다** — 숫자가 아니라 별 개수, 크기 비교, 아이콘.
   "민첩 47"은 7살이 못 읽는다.

**(2) 색** — 인덱스 컬러 + 셰이더 팔레트 교체, 부모 색 유전, 특수색(§4.3).

**(3) 여행 기록** ★ v2 신설 — 개체에 `visited_regions: []`를 둔다.

> 필드 하나로 애착이 생긴다. **"민첩 47"은 7살이 못 읽지만
> "얘랑 호주 갔었지"는 즉시 읽힌다.**
> 화면 자리도 자명하다 — 도감 개체 페이지의 "함께 간 곳", 또는 사파리에서 누르면 나오는 말풍선.

### 2.6 경제 ★ v2 신설

원정에 나갈 이유를 만들고, 돕는다는 행위에 보상을 준다.

#### 원칙: 유지비가 아니라 성장비

> **기본 먹이는 무료다.** 사파리에 풀이 자라고 물이 있다. 굶어서 나빠지는 일은 없다.
> **재화는 "더 좋게 만드는 것"에만 쓴다.**

| 무료 | 유료 |
|---|---|
| 기본 먹이·물 | 특별한 먹이 (행복도, 번식 촉진, 특수색 확률) |
| 기본 돌봄 | seat 확장 |
| | 시설 |
| | 원정 준비물 |

#### ★ 밥 주기는 배고픔을 막는 행위가 아니다 (v3)

사파리에 풀이 자라고 물이 있으므로 **동물은 굶지 않는다.**
그런데도 "밥 주기"가 있는 이유는 **행복도를 올리기 때문**이다.

이 구분을 놓치면 화면이 거짓말을 한다 — 굶지 않는데 머리 위에 배고픔 아이콘이 뜨면
아이는 "안 주면 어떻게 되지?"를 묻고, 답이 "아무 일도 안 일어남"이면 그 아이콘은 무의미해진다.

> 머리 위 아이콘은 **"밥 달라고 조르는" 애정 표현**이지 경고가 아니다.
> §4.6 의 시무룩 트리거에서도 배고픔을 뺀다.

**왜 이렇게 나누는가**: 먹이에 유지비를 붙이면 "돈 없음 → 밥 못 줌 → 시무룩 → 돈 벌러 나감 →
더 시무룩"이라는 하강 나선이 생기고, 7살에게 파산은 **되돌릴 수 없는 실패**다(원칙 2 위반).
더 나쁜 것은 동물이 많을수록 유지비가 커져서 **수집이 벌이 되는 것**이다(§1.2 직격).

이 구조에서는 재화가 0이어도 게임이 멈추지 않고 **진행만 느려진다** —
§2.2의 "방치의 대가는 진행 정체"가 경제에도 그대로 적용된다.

#### 획득: 치료와 도움

원정지에서 **다친 동물을 치료하거나 곤란한 동물을 도와주면** 재화를 얻는다.
돕고 보상받는 구조는 "사냥이 아니라 초대"라는 컨셉과 같은 결이다.
자원 채집(광물 캐기 등)은 넣지 않는다 — 정서가 다르다.

**부상·곤란의 원인은 사고 쪽으로 둔다.** 가시덤불, 폭풍에 다친 날개, 진흙에 빠짐 등.
포식·폭력은 넣지 않는다. 7살은 "누가 다치게 했어?"를 반드시 묻는다.
(육식/초식을 상성 축으로만 쓰고 실제 포식 장면은 없는 §3.5의 태도와 같다)

재화의 이름은 미정(§7).

---

### 2.7 집 — 게임을 켜면 처음 서는 곳 ★ v3.2 신설

타이틀에서 `CONTINUE` / `NEW GAME` 을 고르면 **필드가 아니라 집으로 들어온다.**

나간다 → 만난다 → 데려온다 → **집이 달라진다.** 이게 이 게임의 고리인데,
집이 없으면 "데려온다"가 어디로 가는지가 화면에 없다.
집은 §"진행은 누적으로 보이게"(원칙 3)를 **눈으로 보여주는 화면**이다.
동물이 늘고 사물이 늘어난 마당이 곧 진행도다 — 숫자로 보여줄 필요가 없다.

문서에서는 **사파리 층**(§2)이고 화면에서는 **집**이다. 같은 것이다.

#### 집은 보는 곳이지 해야 하는 곳이 아니다

> **사물은 고장나지 않고 유지비가 없다.** 물을 갈아줄 필요도, 청소할 필요도 없다.
> 동물이 늘고 사물이 늘수록 할 일이 늘어나면 §"수집이 벌이 되면 안 된다"(원칙 6) 위반이다.

같은 이유로 **HUD 는 둘뿐이다 — 재화와 자리.** 배고픔·청결·기분 게이지를 두지 않는다.
그것들이 생기는 순간 집이 할 일 목록이 되고, 아이가 게임을 켜기 싫어진다.
사물의 값어치는 수치가 아니라 **동물이 그걸 갖고 노는 걸 보는 것**이다.

#### 사물 — 사서 놓는다

§2.6 이 "돈은 더 좋게 만드는 것에만 쓴다 — 특별한 먹이, seat 확장, **시설**, 원정 준비물"
이라고 정해뒀다. **그 "시설"이 이 사물이다.** 새 시스템이 아니라 이미 있던 칸을 채운 것이다.

| 사물 | 누가 쓰나 (`for_tags`) | 값 |
|---|---|---|
| 공 | `호기심` · `대담함` | 40 |
| 밥그릇 | `잡식` · `육식` · `초식` | 20 |
| 물그릇 | 누구나 | 20 |
| 긁는기둥 | `나무선호` | 60 |
| 그늘막 | `겁많음` | 80 |
| 웅덩이 | `물가선호` | 50 |
| 방석 | 누구나 | 30 |

**`for_tags` 의 값은 전부 `tags.json` 에 이미 있다.** 사물을 위해 새 열거형을 만들지 않는다.
그 태그를 가진 동물이 그 사물을 쓰고, 비어 있으면 누구나 쓴다 —
**종 이름으로 분기하지 않으므로 종을 추가해도, 사물을 추가해도 코드를 안 고친다.**

#### 사물이 새 동작을 요구하지 않는다 ★ 비용이 갈리는 자리

동물이 사물을 갖고 노는 것이 이 기능의 심장인데, **여기서 스프라이트가 곱셈으로 터질 수 있다.**
종 × 사물로 놀이 동작을 만들면 7종 × 7사물 = 49벌이다. 절대 그렇게 하지 않는다.

> **사물 옆에서 그 동물의 기존 특징 동작(`idle_animation`)이 재생된다.**
> 공 옆에서는 개가 꼬리를 흔들고, 긁는기둥 옆에서는 고양이가 몸을 턴다.
> 사물은 **자리와 태그만 제공하고 동작은 동물이 들고 온다.**

이 규칙이 종당 장수를 사물 수와 **무관하게** 묶는다 (§4.5 종당 16장은 그대로다).
나중에 사물 전용 동작이 정말 필요해지면 그때 **한 종에 한 벌씩만** 추가한다.

#### 화면

640×360. 마당을 울타리가 두르고 **집이 위쪽 담장을 대신한다.**
집 문에서 대문까지 흙길이 곧게 나 있고, **대문은 열려 있다** —
닫힌 문은 "못 나간다"로 읽힌다. 나가는 곳이 어디인지가 화면에서 보여야 한다(원칙 3).

새로 그린 도트는 **12장**이다 — 집 1, 울타리 3(가로·세로·대문), 사물 7.
바닥 타일·나무·풀꽃은 전부 필드 것을 그대로 쓴다. 밤낮도 §6.3 팔레트 보간이 그대로 걸린다.

#### 상점과 배치 ★ v3.3

**사는 화면과 놓는 화면이 갈린다.** 상점에서 고르고, 배치 모드에서 놓는다.

상점에서 값 옆에 붙는 것은 **태그 이름이 아니라 우리 아이의 얼굴**이다.
"이걸 좋아할 아이"에 밤톨(청설모)이 뜨면 아이가 사는 이유를 그림으로 안다 —
`나무선호` 라고 쓰면 7살은 못 읽는다. §"규칙은 눈에 보여야 한다"가 여기 걸린다.

배치는 **16px 격자에 스냅한다.** 자유 배치는 7살한테 어렵고 겹침 판정이 지저분해진다.
격자는 **점으로만** 찍는다 — 선으로 그으면 화면을 통째로 잡아먹어서 어디에 놓는지가
아니라 격자가 주인공이 된다(직접 그려봤다). 강조는 한 칸이 아니라 **사물이 차지하는 만큼**
그린다.

> **판 값은 그대로 돌려준다.** 잘못 사도 손해가 없어야 한다 —
> 되돌릴 수 없는 실패를 만들지 않는다(원칙 2)는 게임 안에만 적용되는 원칙이 아니다.

산출물: `sprites/build_home.py` → `sprites/extracted/home/` (`house` · `fence_*` ·
`obj_*` · `home.json`). `home.json` 이 사물의 `for_tags` 와 값을 들고 있다.
상점·배치 화면 시안은 `sprites/build_screens.py`.

---

### 2.8 도감 — 보물 상자지 데이터베이스가 아니다 ★ v3.4 신설

수집의 보상이 모이는 화면이다. 여기가 지루하면 나갈 이유가 줄어든다.

#### 못 만난 종을 어떻게 보여주는가

| | |
|---|---|
| 이름 | **보여준다.** `"???"` 보다 `"너구리"` 가 세게 당긴다 (§6.7 과 같은 선) |
| 모습 | **감춘다.** 도트가 있으면 실루엣까지, 없으면 `?` |
| 칸 | 빈 칸이 아니라 **점선으로 두른 "채워질 자리"** 다 |

빈 칸으로 두면 "너 아직 못 했어"가 되고, 다 보여주면 만날 이유가 없어진다.
**이름은 미끼고 모습이 보상이다.**

#### 만난 종에서 푸는 것

타이틀에서 감췄던 **사는 곳을 도감에서는 보여준다.** 한 번 만난 동물의 서식지는
이미 아이가 겪은 것이라 정답을 주는 게 아니라 기록해 주는 것이다.

- **크기** — `size.adult` 를 **아이 키(120cm)와 나란한 막대**로. 실측이 여기서 처음
  화면에 나온다. §"실측은 스케일 정의지 지표가 아니다"와 어긋나지 않는다 —
  게임 규칙에 쓰이는 게 아니라 아이가 "나보다 작구나"를 아는 데 쓰인다
- **사는 곳** — 지형 타일 그림 + 이름
- **언제** — `activity` 를 말로 ("낮에 나와요" · "밤에 나와요" · "해뜰 때 해질 때")
- **좋아해** — `likes` 를 먹이 스프라이트로
- **잘하는 것** — `senses` 를 코·귀·눈 아이콘으로

전부 `animals.json` 에 이미 있는 필드다. **도감을 위해 새 필드를 만들지 않았다.**

#### 진행도와 자리

머리에 `3 / 11`. 그리고 발치에 한 줄 — **"새로운 아이를 만나면 자리가 하나 늘어요."**
§2.4 의 "새 종을 만날수록 자리가 늘어난다"를 화면에서 말하는 자리다.

#### 채운 것 ★ v3.6

- **먹이 아홉 장 전부** — 나뭇잎·뿌리·곤충·고기를 더했다. `likes` 의 모든 값에 그림이 있다
- **11종 전부 측면 대기 도트** — 못 만난 종도 이제 `?` 가 아니라 **실루엣**으로 나온다.
  도감이 "채워질 자리"로 보이려면 이게 있어야 했다

> ⚠️ 여덟 종은 **측면 대기 + 측면 걷기 둘뿐이다** (v3.9). §4.5 가 잡아둔 종당 16장 중 2/16 이다.
> ★ v3.16 — 남은 것의 **내용이 바뀌었다.** 남향·북향이 아니라
> **특징 동작 · 먹기 · 자기 · 놀기 · 기뻐하기 + 아기 이동**이다.
> 남은 빚은 8 × 14 = **112장**으로 같지만, 이제 그 112장이 방향이 아니라 **행동**을 산다.
> 걷기는 §4.6 대로 **다리만 바뀐다** — 몸을 위아래로 흔들지 않는다(바운스는 노드 Y).

### 2.9 첫 만남 — **튜토리얼이 아니라 첫 번째 초대다** ★ v3.17 신설

> 구현 세션이 먼저 만들었다. **브리프에 이 화면의 설계가 없었던 것이 드리프트의 원인이다** —
> 진입 흐름(§6.7)을 타이틀까지만 닫고 그 다음을 안 그렸다.

처음부터 개와 고양이를 들려주면 *"친구가 생긴다"* 는 이 게임의 첫 사건이 사라진다.
첫 강아지는 **초대해서 얻는 것**이고, 그 자리가 곧 초대를 배우는 자리다. 여기까지는 옳다.

#### 절대 규칙 — **본편과 같은 조작, 같은 게이지, 같은 카드**

여기서 배운 것이 필드에서 안 통하면 배운 게 아니라 헛것을 가르친 것이다.

| | 본편 (§3.4 · `Gauge.gd`) | 첫 만남도 **똑같이** |
|---|---|---|
| 시작 | `interact` **한 번** 누른다 | 한 번 |
| 진행 | 가까이 **머물면 저절로** 찬다 | 머물면 찬다 |
| 멀어지면 | 멈춘다. 진행은 개체에 남는다 | 같다 |
| 취소 | `interact_cancel` 을 명시적으로 | 같다 |
| 끝 | **초대 카드**(§3.12) | 같다 — **여기가 카드를 배우는 자리다** |

> ⚠️ **누르고 있게 만들지 않는다.** 초대는 *누르는* 것이 아니라 **곁에 있어 주는** 것이다.
> 홀드로 만들면 아이가 손가락에 힘을 주고 화면을 안 본다.
> 첫 만남이 별도 코드로 게이지를 다시 짜면 이 규칙이 반드시 갈라진다 — **`Gauge` 를 그대로 쓴다.**

#### 글로 가르치지 않는다 — **한 줄 + 키캡**

7살은 글을 다 안 읽는다. 다섯 줄짜리 안내는 넘기는 대상이지 읽는 대상이 아니다.

| 칸 | 말 | 키캡 |
|---|---|---|
| ① | "걸어 볼까요?" | **WASD** |
| ② | "어? 누가 왔어요." | 없음 — 강아지가 먼저 다가온다 |
| ③ | "가까이 가서 인사해요." | **SPACE** |
| ④ | "곁에 있어 주면 돼요." | **없다** ★ |

★ ④ 에 키캡이 없는 것이 이 화면의 핵심이다. 누를 것이 없다는 걸 **키캡의 부재**로 말한다.

- **말로 시키지 말고 해 보면 넘어간다.** ① 은 실제로 걸으면 넘어간다(구현 세션의 판단, 옳다)
- **마당은 비어 있다.** 산 사물도, 사는 동물도 없다. 아무것도 없어야 첫 친구가 사건이 된다
- **게이지는 동물 머리 위**에 뜬다. 화면 아래 바가 아니다 — 본편과 같은 자리여야 한다

산출물: `sprites/build_screens.py` → `first_meeting()`, `build_home.py` → `home_field(bare=True)`

---

### 2.10 조작을 글이 아니라 그림으로 — 키캡 ★ v3.17 신설

*"◀ ▶ 로 고르고 [스페이스] 로 정해요"* 는 7살에게 **읽기 과제**다.
키 모양을 그려주면 읽지 않고 알아본다.

`ui/key_{wasd,space,start,esc,arrows}.png` — **5장, 화면 수·종 수와 무관.**
글로 적힌 조작 안내를 발견하면 그 자리에 키캡을 넣는다.

> ⚠️ 키 이름을 문장 안에 대괄호로 넣지 않는다(`[스페이스]`). 문장은 **무엇을 하는지**만 말하고,
> **어떻게 누르는지는 그림**이 말한다.

#### 패드를 들면 패드가 보인다 ★ v3.18

첫 플레이어는 **엑스박스 패드**로 할 가능성이 높다. 장치에 따라 **그림만** 갈아 끼운다.

| 행동 | 키보드 | 패드 |
|---|---|---|
| 이동 | WASD | **왼쪽 스틱** |
| 고르기 | ◀ ▶ | **십자키** |
| 인사 · 정하기 | SPACE | **Ⓐ** |
| 그만두기 | ESC | **Ⓑ** |
| 메뉴 | START | **☰** |

★ **위 규칙이 여기서 값을 한다.** 문장에 `[스페이스]` 를 안 넣었기 때문에
**바뀌는 것이 그림 하나뿐**이다. 넣었다면 장치마다 문장을 다시 써야 했다.

★ **자리와 크기를 맞춰 그렸다.** 글리프를 갈아도 배치가 안 흔들린다 —
조작 안내 줄은 높이 24px 칸이고 글리프는 그 안에서 세로 가운데다.

**전환은 마지막으로 쓴 입력 장치를 따라간다.** 패드를 만지면 패드가, 키보드를 만지면
키보드가 보인다. 설정에서 고르게 하지 않는다 — 7살에게 설정 항목을 하나 더 만들 이유가 없고,
**패드를 집어 드는 것 자체가 이미 선택**이다.

- 7살에게는 **A·B 글자보다 색과 자리가 먼저 읽힌다.** 그래서 초록 Ⓐ · 빨강 Ⓑ 로 그렸다
- 스틱은 **기울이는** 물건이라 화살표를 원 **밖에** 둘렀다. 안에 넣었더니 그냥 도넛으로 보였다
- 메뉴는 **☰ 세 줄**이다. 엑스박스 로고를 그리지 않는다
- ⚠️ **엑스박스 배치 기준이다.** 닌텐도 패드는 A/B 자리가 반대라 글리프를 갈아야 한다

`ui/pad_{stick,dpad,a,b,menu}.png` — **5장 더.** 대응표는 `ui/screens.json` 의 `glyphs.map`.

> ⚠️ `project.godot` 의 입력 맵에 **패드 바인딩이 없다**(지금은 `interact`·`interact_cancel`
> 키보드뿐). 메뉴 액션도 없다 — §3.13 이 그걸 필요로 한다.
### 2.11 커플 카드 — 집에서 가끔 일어나는 일 ★ v3.20 신설

§2.4 가 정한 결혼이 **화면에서 일어나는 순간**이다. 로직(언제·누구끼리)은 구현 세션이 정하고,
여기서는 **그 순간을 어떻게 보여줄 것인가**만 정한다.

#### ★ 거절이 없다

가장 중요한 결정이다. "허락하시겠습니까? 예 / 아니오" 를 만들지 않는다.

- 7살에게 **두 동물이 친해지는 걸 거절하게 하는 것**이 이상하다
- 거절이 있으면 **"잘못 눌렀다"** 가 생긴다 — 되돌릴 수 없는 실패를 만들지 않는다(원칙 2)

> 아이가 하는 것은 **축하 한 번**이다. 버튼은 **「잘됐다!」 하나뿐**이다.

#### 두 단계 — 화면을 막지 않는다

| | |
|---|---|
| **① 마당에서** | 두 동물이 마주 서고 **사이 위에 하트**가 떠오른다. 판 없이 1배 + 1px 그림자 + 꼬리 3px (§3.3 과 같은 규약) |
| **② 다가가면** | 카드가 열린다 |

**아이가 안 가도 진행은 된다.** 놓쳐서 손해 보는 것을 만들지 않는다 —
다만 §2.3 이 말한 대로 **못 보면 없는 것과 같으므로**, 하트는 그 자리에 머문다.

#### 카드에 들어가는 것 — 초대 카드(§3.12)와 **같은 모양**

아이가 이미 배운 화면이라 새로 가르칠 것이 없다.

- **두 마리가 마주 본다** — 오른쪽 한 마리를 `flip_h` 하면 된다. **도트 0장**
- 두 얼굴 **사이**에 하트 (`ui/pair_ok.png` 2배). 위로 띄우면 제목에 붙어 버린다
- 이름 + **성별 뱃지**(§4.9). 뱃지가 이름 **왼쪽**에 붙어 한 덩어리로 읽힌다

#### ★ 다음에 무슨 일이 생기는지 **여기서 말한다**

§2.4 는 **여분의 seat 가 있을 때만 아기가 태어난다**고 정했다.
그 규칙을 이 카드에서 안 보여주면, 아이는 **"왜 아기가 안 나와?"** 를 영영 모른다(원칙 3).

| 자리 | |
|---|---|
| 있음 | "이제 아기를 기다려요" — 조용히 |
| 꽉 참 | **"자리가 하나 생기면 아기가 와요"** + 자리 표시를 **강조색**으로 |

- **자리 표시는 꽉 찼을 때만 눈에 띈다.** 여유가 있을 때도 강조하면 잔소리가 된다
- **막다른 길을 만들지 않는다** — 꽉 찼을 때는 빠져나갈 길을 같이 적는다:
  *"쉼터로 보내면 자리가 나요"* (§2.4 쉼터는 되돌릴 수 있고 방출이 아니다)

#### 새로 그리는 도트 **0장**

몸통(뒤집어 쓴다) · 하트 · 성별 뱃지 · 발자국이 전부 이미 있다.

산출물: `sprites/build_screens.py` → `home_couple()` · `couple_card()` ·
`ui/screens.json` 의 `couple`

> **아직 안 정한 것** — 커플이 된 뒤 둘의 관계가 화면에 계속 보이는가.
> 이름표에 작은 하트를 붙일지, 아니면 카드 한 번으로 끝낼지. §7 에 올려둔다.
---

## 3. 원정 층

### 3.1 원정은 목표 지향이 아니다 (v2 핵심 변경)

**특정 동물을 노리고 나가는 것이 아니다.**
동료들과 어떤 지역으로 떠나서, 그곳에서 활동하다가 이런저런 동물을 만나고 교감한다.

```
세계 지도에서 지역 선택 (때때로 "저기서 누가 도와달래" 호출)
    ↓
동료 팀 편성 (3마리 내외) — 감각 × 적응의 절충
    ↓
필드 도착 — 돌아다니며 활동
    ↓
동료가 무언가를 감지 → 유도 → 그 위치로 이동 → 짜잔
    ↓
교감 / 치료 / 도움  (점유 시간 게이지)
    ↓
초대 성공 → 사파리 또는 쉼터로 / 재화 획득
```

**팀 편성의 의미가 바뀐다.** "누구를 만날까"가 아니라
**"오늘은 어떤 종류의 발견을 가능하게 할까"**를 고르는 것이다.

#### ★ 팀 편성 화면이 스포일러가 되면 안 된다 (v3)

팀 편성 화면에서 "이 팀으로 만날 수 있는 동물"을 미리 보여주면,
**원정이 다시 목표 지향으로 돌아간다** — 화면이 이 절과 정반대로 말하게 된다.

> **미리보기는 도감에 이미 기록된 종만 보여준다.**
> 처음 가는 지역은 전부 물음표다.

그러면 미리보기가 스포일러가 아니라 **§3.6 "알아낸 건 도감에 자동 기록된다"의 화면 표현**이 된다.
아이는 "가봤더니 얘가 있더라 → 다음엔 개를 데려가면 되겠네"를 배우고,
처음 가는 곳은 여전히 뭐가 나올지 모른다.

#### 왜 지금 나가는가

목표가 없으면 출발 이유가 빈다. 세 가지가 함께 작동한다:

1. **재화** — 치료·도움 보상 (§2.6)
2. **도감** — 지역마다 나오는 동물군이 다르다
3. **여행** — 동료가 즐거워한다 (§3.7)

7살에게 가장 확실한 것은 **"오늘 저기서 누가 도와달래"** 같은 가벼운 호출이다.
목적지가 생기고, 가면 반드시 뭔가 있고, 가는 길에 다른 걸 만난다.
퀘스트 시스템으로 만들 필요 없이 **데이터 한 줄**(지역 + 요청 태그 + 보상)이면 된다.

### 3.2 필드는 실제 공간이다 ★ v2 핵심 변경

**동물은 화면에 안 보여도 필드를 실제로 돌아다니고 있다.**
"발견 확률"은 통계 수치가 아니라 시뮬레이션 위에서 일어나는 일이다.

이 결정으로 **v1 §3.4의 "미결: 플레이어의 조작"이 해소된다.**
플레이어는 필드를 직접 이동한다 — 이동 조작이 이 게임의 손맛이다.
(v1의 세 후보 중 "탐색 구간을 직접 돌아다니기"로 착지)

### 3.3 동료의 감각과 유도 ★ v2 신설

**동료가 무언가를 감지하고 플레이어를 그쪽으로 이끈다.**
개가 너구리 냄새를 맡고 킁킁대며 방향을 알려주면, 다가가서 만난다.

> **왜 이게 좋은가**: 규칙이 **대사가 아니라 행동으로** 보인다.
> 아이는 설명을 읽지 않아도 "쟤가 저기 보고 있네"를 안다. 원칙 3이 UI 없이 충족된다.

태그 조합으로 완전히 자동화된다. 종별 스크립트가 하나도 필요 없다:

```
동료 senses: [후각]  ×  대상 traits: [냄새강함]   → 유도 발생
동료 senses: [청각]  ×  대상 noise: 시끄러움      → 유도 발생
동료 senses: [시야]  ×  대상 habitat: 초원        → 넓은 반경 유도
```

**표현 비용이 거의 없다**: 동료가 그 방향을 보고 코를 킁킁대는 2프레임
(= §4.2의 "종마다 대표 동작 하나"가 장식이 아니라 기능이 되는 지점)
\+ 화면 가장자리 방향 화살표.

**동료마다 감각이 다르므로 데려가는 애에 따라 원정이 달라진다.**
코가 좋은 애를 데려가면 숨은 애들이 나오고, 눈이 좋은 애를 데려가면 멀리 있는 게 보인다.

#### ★ 아이는 전략을 세우지 않는다 — v3.13 신설

이걸 "누굴 데려갈까가 진짜 **선택**이 된다"고 적어뒀는데, 프레이밍이 틀렸다.

> **아이는 탐색과 수집에 전략적으로 행동하지 않는다.**
> 후각이 뛰어난 개를 데려갔을 때 어떤 동물을 찾기 쉬워진다는 것은
> **경험적으로 느끼고, 서로 공유되는 것**이다.

아이는 오늘 데려갈 애를 **애착으로 고른다** — "오늘은 나비랑 갈래". 최적해를 계산하지 않는다.
그러다 여러 번 나갔다 온 뒤에 **몸으로** 안다. 그리고 그건 **말로 옮겨져 남에게 전해진다** —
아빠에게, 친구에게, 나중에는 누군가 적어둔 글에.

**이 한 문장이 설계에 조건 셋을 건다.**

| 조건 | 왜 | 어기면 |
|---|---|---|
| **UI 가 상성을 알려주지 않는다** | 알려주는 순간 발견이 아니라 지시가 된다 | 아이가 느낄 기회를 게임이 먼저 뺏는다 |
| **효과가 커야 한다** | 못 느끼는 차이는 없는 것과 같다 | 1.2배는 아무도 못 느낀다 |
| **한 마디로 옮겨져야 한다** | 공유되려면 말이 되어야 한다 | 곱셈 다섯 개의 결과는 이름이 안 붙는다 |

> **위키에 적힐 만한 것을 만들되, 게임 안에는 적지 않는다.**

★ **반복 가능해야 한다.** 아이가 친구에게 "개 데려가면 두꺼비 잘 찾아져"라고 말했는데
그 친구가 해봐서 안 되면, 그건 배신이다. 상성은 흔들리는 확률이 아니라
**거의 항상 그런 것**이어야 한다.

★ 게임이 보여주는 것은 **상성이 아니라 인과의 순간**이다 —
개가 킁킁대고 → 발자국이 뜨고 → 두꺼비가 나온다. 아이는 그 장면을 여러 번 보고
스스로 잇는다. 그래서 §3.3 의 유도 **표현**이 중요한 것이지 툴팁이 중요한 게 아니다.

#### ★ 감각마다 성격이 다르다 — `sense_profile` (v3)

감각이 이름만 다르고 하는 일이 같으면 선택이 성립하지 않는다.
`tags.json` 의 `sense_profile` 이 감각마다 네 가지를 다르게 준다.

| | 남기는 것 | 말하는 것 | `range_scale` | 무력해지는 곳 | 한 마디로 |
|---|---|---|---|---|---|
| **후각** | 공중 표시 | **방향** | 2.0 | 비 (냄새가 씻긴다) | "코는 어느 쪽인지 알아" |
| **청각** | 공중 표시 (시끄러운 대상) | **방향** | 1.3 | 폭우·바람 | "귀는 시끄러운 애를 찾아" |
| **시야** | **지면 흔적** | **자리** | 1.0 | 밤 · 숲 · 안개 | "눈은 어디였는지 알아" |

> **"코로는 어느 쪽인지 알고, 눈으로는 어디였는지 안다."**

```
반경 = guide_radius × 개체 sense_range × range_scale × daypart_scale × terrain_scale
                       (개체차 §2.5)     (감각)        (시간대)        (대상이 선 지형)
```

> ⚠️ **곱셈 다섯 개는 공유되지 않는다** ★ v3.13. 1.5 × 1.1 × 0.7 × 0.6 × 0.35 의 결과에는
> 이름을 못 붙인다 — 위의 "한 마디로 옮겨져야 한다"를 이 식이 어긴다.
> **감각의 차이는 배율이 아니라 범주로 준다.**
>
> | | 지금 | 바꿀 방향 |
> |---|---|---|
> | `reveals` | true/false | **없앤다** (v3.14). 아래 참조 — 원격으로 그려주지 않는다 |
> | `range_scale` | 1.5 / 1.1 / 0.7 | **차이를 벌린다** (2.0 / 1.3 / 1.0). 못 느낄 바엔 확실하게 |
> | `daypart_scale` · `terrain_scale` | 0.35 ~ 0.8 눈금 | 감각마다 **무력화 하나만** — 거의 0 이거나 그대로. "밤엔 눈 좋은 애 소용없어"가 말이 되게 |
>
> 값은 `tags.json` 소유(구현 세션)라 이유와 함께 요청한다.

#### ★ `reveals` 를 없앤다 — **보이는 것은 언제나 내 시야 안에 있는 것** ★ v3.14 개정

v3 에서 "시야로 잡으면 **그 자리에서 몸이 보인다**"고 정했는데, 그건 **원격 표시**다.
고양이가 감지했다고 저 멀리 있는 동물이 화면에 그려지는 건, 세계에서 일어난 일이 아니라
**게임이 알려주는 것**이다.

> **고양이한테 보인다고 플레이어한테 바로 보여주지 않는다.**
> **플레이어 시야에 들어올 때 보인다.** 그리고 그 시야는 **좁게 유지한다.**

세 가지가 한꺼번에 좋아진다.

- **동료에게 의지하는 게 직관적이 된다.** 시야가 좁으니 넓은 필드(96×54)에서
  혼자서는 어디로 갈지 모른다. 동료가 방향을 준다 — 역할이 분명해진다
- **발견의 순간이 아이에게 남는다.** 동료가 다 해주면 아이는 구경꾼이 된다.
  동료는 **데려다주고**, 보는 것은 아이가 한다
- **화면에 그려지는 것은 전부 실제로 거기 있는 것**이 된다. 원격 정보는 방향 하나뿐이고,
  그 방향의 본체도 UI 가 아니라 **동료가 그쪽을 보고 킁킁대는 몸짓**이다(§4.2)

#### ★ 시야도 "거기에 뭔가 있다" 까지만 말한다 — v3.19 재개정

v3.14 는 시야를 **"보이는 거리를 늘린다"** 로 뒀는데, 한 걸음이 더 남아 있었다.

> **시야로 보이는 것도 플레이어가 직접 보는 게 아니다.**
> 동료 덕에 시야가 늘어나는 것도 결국 **게임이 거들어주는 것**이다.
> 후각처럼 **"거기에 뭔가 있다"** 까지만 말하게 한다.

v3.14 의 핵심("보이는 것은 언제나 내 시야 안")이 오히려 **더 강해진다** —
이제 예외가 하나도 없다. 셋 다 단서를 남기고, 몸은 항상 내 눈으로 확인한다.

**그런데도 범주는 안 무너진다 — 이미 갈려 있었다.**
`trait_to_sense` 를 보면 **코와 귀는 공중 표시라 방향을 말하고, 눈은 땅에 남은 자국이라
자리를 말한다.** 새 표를 만들 필요가 없다 (구현 세션이 찾아냈다).

| | |
|---|---|
| **후각 · 청각** | 공중 표시 → **어느 쪽인가.** 무엇인지는 모른다 |
| **시야** | 지면 흔적 → **어디였는가.** 지나간 자리는 알아도 지금 어디 있는지는 모른다 |

> **딸려 나온 것 — 승격 반경 26 → 8 타일.** 감각 반경 전체를 덮어야 했던 이유가
> 저 멀리서 감지된 **몸을 그리는 것**이었다. 단서는 자리에서 나오므로 노드가 필요 없다.
> 13마리 중 노드는 가장 많을 때 1개다 (구현 세션 측정).

> **구현이 같이 쉬워진다.** 원격 표시를 없애면 `activation_radius`(§6.4)가
> 감각 반경 전체를 덮을 필요가 없다 — **보이는 반경만** 덮으면 된다.
> 방향 유도는 몸이 안 그려지므로 시뮬이 덜 깨어 있어도 된다.

**지형·날씨가 감각을 깎는다.** 숲에 있는 동물은 눈으로 못 찾고 코로 찾아야 하고,
비가 오면 냄새가 씻겨 코가 안 듣는다. 이것이 §3.7 의 "감각 × 적응" 절충에
세 번째 축을 준다 — **어디를 뒤질 것인가.**

#### 가까이 가면 그냥 보인다 — `reveal_radius`

감각과 무관하게, 일정 거리 안에 들어오면 동물의 몸이 보인다.
동료가 하나도 없어도 **돌아다니는 것만으로 만날 수 있어야** 하기 때문이다(§3.6 안전장치).

★ v3.14 — **이제 이 반경이 "보인다"의 전부다.** 원격 표시가 없으니 화면에 그려지는
동물은 언제나 이 원 안에 있다.

★ v3.19 — **시야 동료도 이 원을 키우지 않는다.** 동료가 늘려주는 것은 **단서가 보이는 거리**지
몸이 보이는 거리가 아니다. 이 원은 **오직 플레이어의 것**이고 아무도 못 건드린다.

> ⚠️ **기본값을 넉넉하게 잡고 싶어지는데, 참아야 한다.** 이 원이 넓으면
> 동료가 필요 없어지고 §3.3 이 통째로 죽는다. **좁게 유지한다** —
> 혼자서도 만날 수 있되, 넓은 필드를 혼자 훑는 건 답답해야 한다.
> 실제 값은 §8 지표(7살이 30초~1분 안에 뭔가를 만난다)로 잡는다.

> ⚠️ `activation_radius`(풀 AI 승격, §6.4)는 **보이는 반경**을 덮으면 된다.
> 방향 유도는 몸을 안 그리므로 그보다 멀어도 된다 — 원격 표시를 없앤 덕이다.
> 본체가 실제 값을 계산해 모자라면 올려 쓰고 경고를 남긴다.

### 3.4 교감·치료·도움 — 점유 시간 게이지 ★ v2 신설

동물을 만난 뒤의 상호작용은 **뭉뚱그린 모션 + 점유 시간 게이지** 하나로 통일한다.
교감(초대), 치료, 도움이 전부 같은 구조를 쓴다.

#### 게이지가 상성을 보여주는 UI가 된다

게이지 길이를 태그에서 뽑는다 — 겁 많은 동물은 길고, 맞는 동료가 있으면 짧아진다.
그러면 **별도 UI 없이 상성이 읽힌다.** "어? 이번엔 금방 됐네"
7살에게 진행 바는 완전히 읽히는 몇 안 되는 UI라 특히 값어치가 있다.

원정 중 사파리 시간이 흐르게 한다면(§7), **점유 시간이 원정의 유일한 자원**이 된다.

#### 모션은 플레이어·동료 쪽에만 둔다 (물량 결정적)

> 종별 치료 모션을 만들면 20종 × 2프레임 × 4방향 = **160장 추가**다.
> **대상 동물은 기존 대기 스프라이트를 그대로 쓰고, 그 위를 이펙트로 덮는다** —
> 반짝임, 하트, 먼지 구름. 종별 추가 **0장**.

이펙트는 §6.2에서 이미 별도 레이어(Linear 필터, 자유 좌표)로 분리해뒀으니 도트 규칙에 안 걸리고,
파티클이라 손으로 그릴 것도 거의 없다.
**이펙트 하나로 교감·치료·도움·유인을 다 커버**한다. 색만 바꿔도 다르게 읽힌다.

#### 게이지는 중간에 끊기지 않는다

방해 요소나 실패 조건을 넣지 않는다.
"거의 다 찼는데 놓침"은 7살에게 되돌릴 수 없는 실패로 느껴진다(원칙 2).
**시작하면 반드시 완료.** 취소는 플레이어가 명시적으로 누를 때만.

### 3.5 상성 시스템

**축을 여러 개 두되, 각 축은 단순하게.**
"육식↔초식" 하나만 있으면 금세 단순해지고, 복잡한 상성표는 7살이 감당 못 한다.

| 축 | 규칙 |
|---|---|
| 먹이 유형 | 초식/육식 — 육식동물이 가면 초식동물이 무서워함 |
| 크기 | 큰 동물이 오면 작은 동물이 위축됨 (실측 임계값에서 유도, §5.3) |
| 소리 | 시끄러운 동물이 있으면 겁 많은 동물이 안 나옴 |
| 활동 시간 | 야행성 동물은 야행성 동료가 있어야 찾을 수 있음 |
| 서식지 | 물가 동물은 수영 가능한 동료가 있어야 접근 |

각 축은 규칙 하나뿐이지만 조합하면 경우의 수가 충분하다.
그리고 **아이가 하나씩 발견하는 재미**가 생긴다 — "아, 얘는 밤에만 나오는구나"가 핵심 쾌감.

상성의 결과는 **게이지 길이와 유도 반경**으로 나타난다(§3.3, §3.4).

#### 지형 (v3)

필드는 **초원 바탕에 숲·물가·바위 덩어리가 뿌려진** 형태다.
지형은 두 가지 일을 한다:

1. **감각 반경을 깎는다** (§3.3 `terrain_scale`) — 숲에 있으면 눈에 안 띈다
2. **야생동물이 자기 `habitat` 위에서 시작한다** — 수달은 물가에, 청설모는 숲에

그래서 "어디를 뒤질까"와 "누굴 데려갈까"가 맞물린다.
숲을 뒤질 거면 코가 필요하고, 초원이면 눈이 낫다.

**부가가치**: 아이가 게임하면서 실제 동물 지식을 배운다. 초식/육식, 무리 생활, 야행성 등.

### 3.6 안전장치

몰라서 못 하면 7살은 답답해서 그만둔다. 반드시 다음을 갖춘다:

- **관찰하면 알 수 있게** — 동료의 행동, 필드의 흔적이 곧 힌트다. 추론이 아니라 관찰로 풀리게
- **도감에 자동 기록** — 한 번 알아낸 건 적히고, 다음엔 안 헤맨다
- **실패해도 손해 없게** — 상성이 안 맞으면 게이지가 길어지거나 유도가 덜 될 뿐이다.
  **실패가 아니라 느려짐으로 표현한다**
- **동료가 힌트를 준다** — 사파리가 풍성해질수록 원정이 쉬워지는 순환

> ⚠️ 확률을 7살에게 그대로 노출하지 말 것.
> "제대로 했는데 안 나왔다"가 반복되면 규칙을 못 읽고 그만둔다.
> 진행은 항상 **누적으로 보이게** 한다(게이지, 동료의 반응).

### 3.7 여행이라는 프레이밍 ★ v2 신설

**동료를 데리고 어디에 간다는 것은, 동료에게도 즐거운 놀이이자 여행이다.**
일하러 끌고 가는 것이 아니다.

이 프레이밍이 여러 문제를 동시에 푼다:

- **행복도의 두 번째 경로**가 생긴다(§2.2). 지금까지는 먹이뿐이었다.
- **원정 중 사파리 방치가 벌이 아니게 된다.** 데려간 애는 행복해지고 남은 애는 심심해진다.
  손실이 아니라 **순번의 문제** = "나도 데려가줘". 원칙 1과 정확히 맞는다.
- **팀 인원 제한에 세계관 안의 이유가 생긴다.** "차에 세 자리뿐"이 아니라
  **누군가는 사파리를 지켜야 한다.** 남은 애는 방치된 애가 아니라 **집 보는 당번**이 된다.
  당번에게 작은 역할(그동안 아기를 돌봤다, 뭔가를 발견했다)을 주면 남는 것도 선택이 된다.

#### 지역 적응 — 팀 편성 축이 하나 늘고 비용은 0이다

`habitat` 태그를 재사용한다. 물가 동물을 사막에 데려가면 시무룩하고, 숲 동물은 숲에서 신난다.

```
감각(무엇을 발견할 수 있나)  ×  적응(이 지역에서 즐거운가)
```

최적해가 지역마다 달라지므로 편성이 매번 같은 답이 안 된다.
그리고 아이에게는 진짜 지식이다 — "펭귄은 사막에 못 가".

⚠️ **시무룩한 동료가 실패를 만들면 안 된다.** 유도를 덜 해서 발견이 조금 느려지는 정도까지만.

#### ⚠️ 로테이션을 의무로 만들지 말 것

심심함에 페널티를 붙이면 "모든 애를 골고루 데려가야 하는 관리 업무"가 된다.
seat가 20~30으로 늘면 물리적으로 불가능해지고, 그 순간 **수집이 다시 벌이 된다**(§1.2).

> 심심함은 **표정과 말풍선으로만** 표현한다.
> 능력치·번식·발견율에 영향을 주지 않는다.
> 아이가 "쟤 삐졌네, 다음엔 데려가야지" 하고 느끼면 그게 동기고, 안 데려가도 잃는 건 없다.
> **채찍이 아니라 당근으로만.**

#### 미루는 것: 동료 간 관계

"같이 여행한 동물끼리 친해진다"는 관계 그래프가 N² 데이터에 화면 표현이 애매해서
부록 A의 "보이지 않는 필드" 함정에 정확히 걸린다.
굳이 넣는다면 가장 싼 형태 하나만 — **같이 다녀온 쌍은 사파리에서 서로 옆에 잘 붙어 있는다**
(기존 무리 행동 로직 재사용, 새 시스템 0). 그 이상은 지금 넣지 않는다.

### 3.8 시작 지역과 첫 동물 ★ v2 확정

**시작 지역은 "집 근처의 뒷산과 냇가"다.** 실제 지명을 박지 않고 **생태만 가져온다.**

만드는 사람이 서울에 살고 첫 플레이어가 그 집 아이이므로, 첫 지역은 한국 중부의
산·하천 생태(청설모, 너구리, 수달, 고라니, 삵, 까치)를 기준으로 한다.

**왜 지명을 박지 않는가**: §7의 "실존 동물 vs 가상 동물"이 아직 미결이다.
`북한산`·`한강` 같은 고유명사를 넣으면 그 미결을 실존 쪽으로 강제 확정하게 된다.
**생태는 실존에서 가져오되 이름은 무명("뒷산", "냇가")으로 두면 양쪽이 다 열려 있다.**

**왜 집 근처인가:**

- **"우리 동네에 진짜 수달이 산대"는 7살에게 강력하다.** §3.5의 부가가치(실제 동물 지식)가
  가장 잘 작동하는 지점이 자기가 아는 장소다.
- **사파리(집)와 첫 필드의 생태가 같아서 배경·타일·식생 애셋이 겹친다.**
  §6.5에서 지적한 "지역 팩에 맵이 딸린다"는 부담을 첫 지역에서는 거의 안 진다.
- **세계로 뻗어나가는 구조가 성립한다.** 시작이 한국이어야
  "호주의 친구들이 도착했어요!"(§6.5)라는 확장이 말이 된다.

#### 프로토타입 6종

| 종 | 역할 | diet / activity | size | 실루엣 특징 |
|---|---|---|---|---|
| 개 | **동료**(후각) | 잡식 / 주행성 | 중 | 집에서 같이 온 첫 친구 |
| 고양이 | **동료**(청각·시야) | 육식 / 박명성 | 중 | 뾰족 귀 · **짧은 꼬리** · 보송한 윤곽 |
| 청설모 | 대상 | 초식 / 주행성 | 소 | 큰 꼬리 |
| 너구리 | 대상 | 잡식 / 야행성 | 중 | 눈가 무늬, 둥근 몸 |
| 수달 | 대상 | 육식 / 박명성 | 중 | 긴 몸, 물가 |
| 고라니 | 대상 | 초식 / 박명성 | 대 | 긴 다리 |

**고양이가 프로토타입에 들어가는 이유는 장식이 아니라 기능이다.**
개(후각)와 감각이 달라서 **§3.3의 "누굴 데려갈까가 진짜 선택인가"를 데모에서 실제로 답할 수 있다.**
동료가 하나뿐이면 그 질문 자체가 성립하지 않는다.
`trait_to_sense` 매핑(§5.4)을 통과시키면 대비가 이렇게 나온다:

```
개(후각)        → 너구리, 수달        ✅   청설모·고라니는 못 찾는다
고양이(청각·시야) → 청설모, 너구리, 수달, 고라니
```

즉 **"개로는 못 찾는 애가 있다"**가 데이터에서 자동으로 나오고,
아이가 고양이를 데려가면 풀린다. 이 대비가 데모의 핵심 질문이다.
그래서 §8의 "감각은 후각 하나만"은 **후각 + 청각·시야**로 확장한다.

프로토타입에서 켜는 상성 축 2개(먹이 유형 + 활동 시간)를 6종이 모두 커버하고,
크기도 소·중·대가 다 나오며 실루엣이 서로 겹치지 않는다.

> **고라니는 전 세계 개체의 대부분이 한국에 있다.** 아이에게 "우리나라에만 이렇게 많대"는
> 그 자체로 재밌는 사실이고, 부가가치(§3.5)의 좋은 예다.

> **고양이는 개와 같은 32×32 캔버스다.** 실루엣이 갈리는 지점은 이 셋이다 —
> **뾰족 선 귀**(개는 처진 귀), **아주 짧은 꼬리**(한국 길고양이에 흔하다),
> **보송보송한 윤곽**(매끈한 타원이 아니라 털 뭉치가 삐져나온다).
> 꼬리가 짧으므로 구분을 귀와 털이 떠맡는다.
>
> 귀엽게 만드는 것은 비례다 — **머리를 크게, 몸을 둥글게, 다리를 짧게.**
> 날씬하게 그리면 고양이가 아니라 사슴이 된다. (실제로 한 번 그렇게 됐다)

#### 지역의 나머지 5종 (데이터에만)

| 종 | 넣는 이유 | 주의 |
|---|---|---|
| 까치 | 소리 축 + 흑백으로 색 다양성 | 이동은 나는 게 아니라 **통통 뛰기**(`biped_hop`)라 장수는 네발동물과 같다 |
| 참새 | 가장 흔한 동물. seat 정원 감각 시험용 | **태그가 까치와 거의 겹친다** — 규칙상 구분이 안 되고 차이는 크기·실루엣·색에서만 온다 |
| 뱀 | 실루엣이 압도적으로 명확. 후각 동료로 유용 | `serpentine` 리그. 장수는 같지만 좌우 flip이 안 먹어 **난이도가 높다**. 무섭지 않게 — 눈을 크고 둥글게 |
| 삵 | 육식 축 확장 | ⚠️ 고양이와 겹칠 뻔했으나, **고양이 꼬리를 짧게 하면서 갈렸다** — 삵은 길고 굵은 줄무늬 꼬리로 그린다 |
| 두꺼비 | `senses`가 빈 종 — 동료가 될 수 없는 경우의 검증 | |

> **뱀이 스키마의 한계를 하나 드러냈다.** 실측 85cm라 `size_class` 파생 계산은 `대`가 나오는데,
> 부피는 작아서 "큰 동물이 오면 작은 동물이 위축"(§3.5) 축에 걸리면 안 된다.
> **길이만으로는 부피를 못 잡는다** — `manual_override_allowed`(§5.3)의 첫 실제 사용 사례이고,
> 오버라이드한 종은 임포터가 경고를 남기되 로드는 통과시킨다.

⚠️ **약점: 포유류 다섯이 전부 갈색 계열이다.** 화면이 단조로울 수 있다.
까치(흑백)와 참새가 색을 살려주고, 개체별 팔레트 교체(§4.3)가 나머지를 보완한다.

#### 물량

11종 × 22장 = **242장**. 그중 프로토타입 6종은 **132장**이다.
하루 2~3장이면 프로토타입 분량이 약 9주.

> ⚠️ **여기서 종을 더 늘리면 부록 A의 "데이터를 먼저 다 채운다" 구간에 들어간다.**
> 11종은 태그 스키마를 검증하기에 충분하고 그 이상은 아직 필요 없다.

---

### 3.9 세계 지도 ★ v3.5 신설

원정은 목표 사냥이 아니라 **여행**이다(§3.7). 지도가 그 프레이밍을 짊어진다.

지도 배경은 **새로 그리지 않는다.** 지형 타일을 넓게 깔아 지역 덩어리를 만들고
길은 **발자국으로 잇는다** — 로고·메뉴 커서와 같은 물건이다.
새로 그린 도트는 **핀 하나와 집 아이콘 하나**뿐이다.

> **축척이 다르면 표현도 달라진다.** 필드에서 "물가"는 젖은 흙 타일이지만,
> 지도에서 그것만 깔면 그냥 갈색 땅이다. 가운데를 **물**로 채워야 물가로 읽힌다.

목적지 판에 들어가는 것:

- **지형** — 타일 그림 + 이름
- **동료가 좋아할지** — 동료의 `habitat` 과 목적지 지형이 겹치면
  "탄이 · 나비가 좋아할 거예요". 여행이 동료에게도 놀이라는 §3.7 이 여기서 보인다.
  **확률이 아니라 안내다**
- **만난 적 있는 아이** — 도감 연동(§3.1). 여기 없는 종도 나온다는 것을 함께 적는다

> ⚠️ **날씨는 여기 안 들어온다** ★ v3.12. 예보도 지역별 날씨 표시도 없다 —
> 날씨는 나가서 겪는 것이지 고르는 재료가 아니다(§3.11). 지도가 고르게 하는 것은
> **어디로**와 **누구랑** 둘뿐이다.
>
> ★ v3.17 — **"누구랑"은 이 화면에서 고르지 않는다.** 지도는 **어디로**만 정하고,
> 동료는 **팀 편성 화면**에서 얼굴을 보고 고른다(별도 화면, `team_screen()`).
> 한 화면에 결정 하나다 — 지도에 동료 줄을 붙이면 커서가 2차원이 되고, 7살에게 그건 무겁다.
> **지도 → 팀 편성 → 출발.**
>
> ★ 그렇다고 이 둘을 **손잡이**로 설계하지 않는다 — 아이는 최적해를 고르지 않는다(§3.3).
> "누구랑"은 애착으로 고르는 자리고, 상성은 여러 번 갔다 온 뒤에 **몸으로 알게 된다.**
> 그러니 지도가 "코가 좋은 애를 데려가면 유리해요" 같은 말을 하면 안 된다.

아직 안 가 본 곳은 `???` 이고 길이 흐리게 그려진다. 여는 조건을 확률이나 수치로 쓰지
않는다 — **"냇가에서 더 놀다 보면 길이 보일 거예요."** 조건이 아니라 약속이다(원칙 3).

---

### 3.10 앰비언트 생물 — 잡을 수 없는 것들 ★ v3.8 신설

> **잡을 수 없는 생명이 있어야 세계가 세계다.**
> 보이는 것이 전부 수집 대상이면 숲이 아니라 **쇼핑 목록**이다.

"생태가 얇다"는 느낌은 수집종이 적어서가 아니라 **잡을 수 없는 생명이 없어서** 온다.
숲이 살아 있어 보이는 것은 나비가 지나가고 물고기가 튀고 개미가 줄지어 가기 때문이다.

앰비언트 생물은 **도감에 없고, 자리를 안 먹고, 데이터도 없고, 상호작용도 없다.**
지형 태그 하나만 들고 있다. 종당 도트 한두 장이라
**수집종(§4.5 종당 16장) 대비 10분의 1 값으로 생태를 열 배로 만든다.**

| | 지형 | 층 |
|---|---|---|
| 나비 · 벌 | 초원 · 숲 | 공중 (Y-sort 밖, 지면보다 10px 위) |
| 잠자리 | 물가 | 공중 |
| 물고기 | 물 | 수면 |
| 새 그림자 | 초원 · 숲 · 바위 | 지면 |
| 개미 | 초원 · 숲 | 지면 |

**새는 그리지 않고 그림자만 그린다** — 하늘은 화면 밖이다. 그림자만으로 위에 무언가
날고 있다는 게 성립하고, 도트가 절반으로 준다.

여섯 종 전부 합쳐 **14장**이다.

산출물: `sprites/build_field.py` → `sprites/extracted/ambient/`

---

### 3.11 희귀 — 정의 · 출현 · 발견은 다른 시점에 일어난다 ★ v3.8 · v3.9 · v3.10 개정

"가끔 발견되는 동물"을 **낮은 확률**로 만들면 두 가지가 깨진다.

- **원칙 3** — 확률을 노출하지 않는다. 그런데 노출을 안 하면 아이는 **왜 못 만나는지 모른다**
- **원칙 6** — 백 번 나가도 못 보면 그게 벌이다

#### 세 단계를 갈라야 한다 ★ v3.10

v3.8 에서 "조건을 채우면 **반드시 나온다**"고 적었고, v3.9 에서 배치와 발견 둘로 나눴다.
둘 다 한 단계씩 빠뜨린 것이었다. **정의 · 출현 · 발견은 서로 다른 시점에 일어난다.**

| 단계 | 언제 | 무엇이 정하나 |
|---|---|---|
| **개체 정의** | **필드에 들어가는 순간, 한 번** | 지형 · 계절 · 그 필드의 생태. 이번 원정에 **어떤 개체가 몇 마리 있는가**가 여기서 끝난다 |
| **출현** | **날씨가 바뀔 때마다** | 이미 놓인 개체들이 지금 날씨에 나와 있는가. 날씨가 바뀌면 **보이는 얼굴이 바뀐다** |
| **발견** | 플레이 내내 | 동료의 감각(§3.3) · 단서(§5.4) · 관찰(§3.6). 나와 있는 것을 찾아내는 것은 **플레이의 몫**이다 |

> 철이 맞아도 산에서 사슴벌레를 보기는 어렵다.
> **거기 있는 것과 지금 나와 있는 것과 내가 찾는 것은 전부 다른 문제다.**

#### 1단계 — 개체 정의는 진입 시 한 번뿐이다

필드에 들어가면 그 필드에서 **찾을 수 있는 모든 개체**가 그 자리에서 확정된다.
종 목록이 아니라 **개체 목록**이다 — 수달 두 마리, 까치 다섯 마리, 고라니 0마리.
**개체가 정해진다는 것은 능력치·색·성별(§4.9)까지 그 자리에서 정해진다는 뜻이다.**
이형이 있는 종은 그래서 **다가가기 전에 멀리서도 암수가 보인다** — 엄니가 있는 고라니는 수컷이다.

**개체수 0 은 정상이다.** 어떤 종은 이번 원정에 아예 없다. 이것이
"조건을 다 맞췄는데 왜 없지"의 정직한 답이고, 같은 조건이 같은 결과를 보장하지 않는
유일한 이유다. 확률 파라미터가 아니라 **개체 수 분포**다 — 화면에서
"오늘은 수달이 많네"로 보이는 값이지 숨은 수치가 아니다.

이 단계는 원정 중에 다시 돌지 않는다. **날씨가 바뀌어도 개체는 늘지도 줄지도 않는다.**

> **예외 하나 — 짝 확정 배치 (§2.4).** 사파리·쉼터에 짝 없이 혼자인 종이 있고
> 지도에 그 종의 **하트 핀**이 떠 있다면, 그 필드에서는 그 종의 **반대 성별이
> 반드시 1개체 이상 정의된다.** 개체수 0 이 될 수 없다.
> 이 예외가 성별 때문에 생기는 영구 벽을 **구조적으로** 불가능하게 만든다.

#### 2단계 — 출현은 날씨가 바뀔 때마다 다시 정해진다 ★ v3.10 개정

v3.9 의 "**한 원정 = 한 날씨**"는 **철회한다.** 날씨는 원정 중에 바뀐다.

놓인 개체마다 **어떤 날씨 컨디션에 나와 있는가**가 있다(§6.8 의 축 값에 붙는다,
종 이름이 아니라 태그와 축이다 — 원칙 4). 날씨가 바뀌는 순간이
**지금 어떤 개체들이 보이는지를 다시 정하는 타이밍**이다.

> 비가 그치자 두꺼비가 들어가고, 안개가 걷히자 고라니가 보인다.
> 개체가 사라진 게 아니라 **지금 안 나와 있을 뿐**이다.

**이것이 원칙 2 를 깨지 않는 이유**: 날씨 변화는 필드에서 동물을 **빼앗지 않는다.**
못 만난 개체는 여전히 그 필드에 있고, **날씨가 돌아오기를 기다리는 것 자체가 플레이**다 —
잃는 선택이 아니라 **기다리는 선택**이다. 그래서 비를 피해 나무 밑에서 기다리는 시간이
허비가 아니라 관찰 시간이 된다(§3.6).

날씨가 바뀌는 리듬 자체는 §6.8 에서 정한다. **급변하지 않는다** — 축 값이
서서히 움직이고, 하늘·소리·구름 그림자가 먼저 알려준다. 아이가 "비 온다" 하고
알아채는 시간이 있어야 그게 규칙으로 읽힌다(원칙 3).

#### 3단계 — 발견

나와 있는 것을 찾는 것은 동료를 누구로 데려갔느냐에 달린다.
넓은 필드(§6.5, 96×54 타일)에 한 마리뿐일 때, 그 한 마리를 코가 찾을지 귀가 찾을지가 갈린다.

아이가 못 찾았을 때의 대답이 분명하다: **"거기 있었어. 다음엔 코가 좋은 애를 데려가자."**

> ★ **이건 게임이 아니라 아빠가 하는 말이다** (v3.13). 게임이 이 말을 하면
> 상성을 UI 로 가르치는 것이 되어 §3.3 을 어긴다. 게임은 **거기 있었다는 것만**
> 보여주면 된다 — 지나간 자리의 발자국이면 충분하다.

#### 확률은 어디에 붙는가

**배치가 아니라 출현에 붙는다.** 그리고 여전히 **수치로 노출하지 않는다**(원칙 3) —
화면에 나오는 것은 언제나 관찰 가능한 사건이다: **"비 오니까 나왔네."**

| 조건 | 나와 있는 것 |
|---|---|
| 비 온 다음 날 + 물가 | 수달 |
| 짙은 안개 + 새벽 + 숲 | 고라니 |
| 맑은 밤 + 바위 | 삵 |
| 비 오는 날 + 물가 | 두꺼비 |

**새 종을 하나도 안 그리고 "가끔 발견되는 동물"이 생긴다 — 도트 0장이다.**

#### ★ 날씨는 **고르는 것이 아니다** — v3.12 정정

v3.10 까지 "아이가 **날씨를 보고 나갈 날을 고른다**"고 적어뒀는데, **틀렸다.**

> **날씨를 보고 나갈 이유는 없다. 나갔는데 날씨에 따라 찾게 되는 동물이 다를 뿐이다.**

이게 맞는 이유가 셋이다.

- **v3.10 과 앞뒤가 안 맞았다.** 날씨는 원정 중에 바뀐다. 나가기 전에 본 날씨는
  들어가자마자 옛날 정보가 된다 — 그걸 보고 고르게 하면 거짓말을 시키는 셈이다
- **7살에게 예보 읽기를 시키게 된다.** "비 올 것 같은 날 물가로 가자"는 계획이고,
  계획에는 **잘못 고름**이 따라온다. 나갈 날을 잘못 골랐다는 감각을 만들 이유가 없다(원칙 2)
- **날씨는 선택지가 아니라 그날의 선물이다.** "오늘은 비가 오네 — 두꺼비가 나왔다!"
  가 되어야지, "두꺼비를 잡으려면 비를 기다려야 한다"가 되면 안 된다

**따라서 지도에 날씨 위젯을 달지 않는다.** 예보도, 지역별 날씨 표시도 없다.
지도에서 고르는 것은 v3.5 그대로 **어디로**(지형)와 **누구랑**(§3.3 감각) 둘뿐이다.

> **예보는 없다. 창밖은 있다.** 집이 게임을 켜면 서는 곳이고(§2.7) 날씨 레이어가
> 거기에도 깔린다 — 아이가 마당에 비가 오는 걸 보고 "나가볼까" 하는 것은 자연스럽다.
> 그건 예보를 읽는 게 아니라 창밖을 보는 것이다. 그 이상을 화면에 만들지 않는다.

도감의 "언제" 칸에 남는 조건도 **할 일 목록이 아니라 추억**이다 —
"비 온 다음 날 물가에서 봤어요". 확률은 어디에도 안 쓴다.

> **출현 조건을 0 으로 막지 않는다.** "이 날씨엔 절대 안 나온다"는 기다림을 강요한다 —
> 어떤 날씨에도 나와 있는 종이 늘 있어야 한다. (구현 세션의 검증기가 0 이하를 거부한다)
> **개체수 0 은 다르다** — 그건 정의 단계의 정상적인 결과고, 오늘 이 필드에 없다는 뜻일 뿐
> 다음 원정에서 다시 굴린다.


### 3.12 초대 카드 — 게이지가 끝나는 순간 ★ v3.14 신설

교감 게이지(§3.4)가 다 차면 **누구를 초대했는지 딱 띄운다.**

> **축하지 평가가 아니다.** 이 구분이 이 화면의 전부다.

#### 무엇이 들어가나

| | | 왜 |
|---|---|---|
| **몸** | 2~4배 확대 (실루엣 높이를 맞춰 정수배로만) | 작은 종이 초라해 보이면 안 된다 |
| **종 이름** | 가장 크게 | 아이가 이름을 배우는 자리 |
| **성별** | ♂♀ 뱃지 (§4.9) | 여기가 뱃지의 **첫 등장**이다 — 한 번 크게 보면 이후 목록의 작은 뱃지가 읽힌다 |
| **나이** | "어른 · 3살". **아기는 "아기"만** | "0살"은 아무 뜻도 없다 |

| **감각** | 코·귀·눈 아이콘 | 종 특성이라 서열이 안 생긴다. §3.3 의 공유 지식과 이어진다 |
| **개략적인 설명** | **두 줄**, 한 줄 15자 안쪽 | 실존 생태(§3.8)라 교육 가치가 붙는다 |
| **도감** | 새 종일 때만 "도감에 새로 들어왔어요" | 수집의 보상이 여기서 보인다 |

#### 나이는 **게임적 허용**이다 ★ v3.15

만나자마자 나이를 아는 근거를 만들지 않는다. 필요 없다 —
**아이가 분명히 궁금해할 것이기 때문에** 답을 주는 것이다.

> **"얘는 엄마 사슴인가? 아기인가?"**

아이가 묻는 것은 **숫자가 아니라 관계**다. 그래서 카드가 답하는 순서가 정해진다.

| | |
|---|---|
| **주인공** | **성별 + 단계** — ♀ + 어른. 아이는 이걸 "엄마 사슴"으로 읽는다 |
| **곁들이** | 숫자 나이. 아이가 자기 나이와 견주는 재미가 붙는다 — "얘가 나보다 어리네" |

★ **게임이 "엄마"라고 부르지는 않는다.** 아직 아기가 없는데 엄마라 부르면 틀린 말이고,
그렇게 부르는 것은 **아이의 몫**이다(§4.8 아이의 참여와 같은 결).
게임은 ♀ 와 "어른"만 내놓고, 이름은 아이가 붙인다.

#### 아기도 혼자 초대할 수 있다 ★ v3.15

**게임적 허용이고, 애초에 걱정이 성립하지 않는다** — 이건 포획이 아니라 **초대**다.

> **개념적으로는 초대하는 것이다.**
> 아이가 데려가는 게 아니라 **얘가 따라오기로 한 것**이다.

떼어온다는 서사가 들어설 자리가 없다. 이 게임에는 **잡는 행위가 없고,
그래서 실패도 도망도 없다**(원칙 2 · §3.4 게이지는 시작하면 반드시 완료된다).

> ⚠️ **말이 새면 개념도 샌다.** 화면에도 문서에도 **"잡다"를 쓰지 않는다** —
> 만나다 · 친해지다 · 초대하다. §3.11 의 "찾는다"까지가 한계다.

다만 `부모근처`(아기 단계의 `behavior_tags`)가 갈 곳을 잃는다. 오류가 아니라
**행동으로 답하는 자리**로 쓴다.

> **부모가 없으면 아무 어른 곁에나 붙는다.**
> 아이가 "얘 엄마는?" 하고 물었을 때, 게임이 글로 답하지 않고
> **화면에서 답한다** — 다른 어른 옆에 붙어 있는 아기.

규칙 한 줄이고 **도트 0장**이다. §3.3 의 "설명하지 않고 보여준다"가 여기에도 걸린다.

#### 넣지 않는 것 — **능력치 숫자**

§2.5 의 개체 능력치는 여기 안 쓴다. 숫자를 띄우는 순간
**"좋은 개체 / 나쁜 개체"** 가 생기고, 7살이 리셋 노가다를 배운다.
그건 수집을 벌로 만드는 가장 빠른 길이다(원칙 6).

> 아이가 이 카드에서 가져가야 할 감정은 **"얘 반갑다"** 하나다.
> "다시 뽑을까"가 들어오면 이 화면은 실패한 것이다.

#### 설명은 **종**의 것이다

개체마다 다르면 못 쓴다. `data/animals.json` 에 종당 두 줄
(값의 주인은 구현 세션 — `sprites/build_screens.py` 의 `BLURB` 에 11종 제안이 들어 있다).

> ⚠️ **길면 벽이 된다.** 글을 이제 배우는 7살이다. 두 줄을 넘기지 않는다.
> 읽어주는 것을 전제해도 마찬가지다 — 화면이 글로 덮이면 그림이 안 보인다.

**새로 그리는 도트 0장.** 몸통·성별 뱃지·감각 아이콘이 전부 이미 있다.

산출물: `sprites/build_screens.py` → `invite_card()`

### 3.13 필드에서 집으로 — **나가는 문이 있어야 한다** ★ v3.17 신설

> ⚠️ **지금 구현에는 필드를 나오는 길이 아예 없다.** 원정을 나가면 못 돌아온다.
> 되돌릴 수 없는 상태를 만들지 않는다(원칙 2)가 여기에도 걸린다 — 가장 먼저 막을 구멍이다.

**START(또는 패드의 그것)로 언제든 연다.** 확인 창 하나.

| | |
|---|---|
| **무엇을 보여주나** | 집 아이콘 + **"집에 갈까요?"** + **같이 가는 아이 얼굴** |
| **왜 얼굴인가** | "동료 2 / 초대 1" 같은 숫자는 안 읽힌다. 데려가는 애가 누구인지는 **얼굴**이다 |
| **선택** | **갈래요 / 더 놀래요** — 두 개뿐 |
| **닫기** | START 를 다시 누르면 닫힌다. 실수로 열어도 손해가 없다 |

- **아무 때나 나갈 수 있다.** 조건도, 확인 절차도, "정말요?" 두 번 묻기도 없다
- **찌꺼기가 없다.** 쏟다 만 게이지는 개체에 남아 있고(§3.4), 다음에 오면 이어서 찬다.
  그래서 "지금 나가면 손해"가 성립하지 않는다 — 나가는 것이 실패가 아니게 하는 장치다
- 원정 성과 정리는 **돌아온 뒤 화면**(§7 미결)에서 한다. 이 창은 문일 뿐이다

산출물: `sprites/build_screens.py` → `field_home()` · `ui/key_start.png`
---

## 4. 아트 방향

### 4.1 기본 방침: 데이터는 치밀하게, 그래픽은 단순하게

**콘텐츠가 스프라이트가 아니라 데이터에 들어간다.** 1인 개발에서 100종을 감당할 수 있는 거의 유일한 길.

| 방향 | 판정 |
|---|---|
| 해상도를 낮춘다 (24×24 등) | ✅ 특징만 남아 캐릭터성이 오히려 강해짐 |
| 애니메이션 프레임을 줄인다 (2~4장) | ✅ 뚝뚝 끊기는 움직임이 귀엽게 읽힘 |
| 형태를 뭉갠다 | ❌ 모든 동물이 비슷한 덩어리가 되어 구분 불가 |

### 4.2 단순한데 매력 있게 만드는 요령

- **실루엣만으로 구분되게** — 색을 다 빼고 검은 덩어리로 만들었을 때 어떤 동물인지 알아볼 수 있어야 한다.
  픽셀 캐릭터 디자인의 제1원칙. 목이 길거나, 귀가 크거나, 몸이 둥글거나.
  이 원칙은 **측면**에서 가장 잘 성립하므로, "알아보는 순간"은 항상 측면으로 고정한다(§4.5)
- **눈이 매력의 90%** — 픽셀 2~3개짜리 눈인데 위치와 크기만으로 인상이 완전히 바뀐다
- **종마다 대표 동작 하나** — 껑충 뛰기, 목 늘이기, 킁킁대기. 프레임 2장이면 충분한데 확 살아난다.
  (§3.3에서 이 동작이 유도 표현으로 쓰인다 — 장식이 아니라 기능이다)
- **"무엇을 하고 있는가"가 매력을 만든다** — 그림이 단순해도 물을 마시고, 옆 동물과 붙어 있고,
  밤엔 자고 있으면 살아있어 보인다

### 4.3 색 바리에이션과 유전 (상세는 추후 논의)

**작업량을 거의 안 늘리면서 콘텐츠를 크게 늘리는 방향.**

- **스프라이트를 인덱스 컬러로 그린다** — 런타임에 셰이더로 팔레트 교체.
  그림 한 장으로 무한한 색 변형이 나온다
- **눈 모양은 별도 레이어** — 몇 종만 따로 그려 얹으면 조합이 곱해진다
- **색 유전** — 부모의 색을 물려받는다. "엄마 색이랑 아빠 색이 섞였네"는 7살에게 즉시 이해되는 인과
- **특수색(희귀 변이)** —
  ⚠️ 확률을 극단적으로 희박하게 두지 말 것. 평생 한 번 볼까 말까 한 확률은 7살에게 존재하지 않는 것과 같다

> **seat 시스템과의 시너지**: 자리가 한정돼 있으니 "어떤 아이를 남길까"가 선택이 되고,
> 색 유전이 있으면 그 선택에 목적이 생긴다.

### 4.4 도트 규칙

- 전체 팔레트 32~48색으로 제한
- 외곽선은 검정 대신 해당 색의 어두운 톤
- 바닥은 어둡고 차분하게 / 동물은 밝고 선명하게
- 발밑 타원 그림자 필수
- **렌더 스케일은 정수/반정수만** (1x, 1.5x, 2x, 3x). 비정수 배율은 nearest에서 픽셀이 깨진다.
  실측 크기(§5.3)는 이 몇 개의 스케일 버킷으로 양자화해서 화면에 반영한다

### 4.5 뷰 각도와 방향 ★ v2 확정 · **v3.16 전면 개정**

> **3/4 뷰 · 동물은 측면 1방향(E/W 미러) · 예산은 방향이 아니라 모션에 쓴다.**
> 플레이어는 4방향 그대로다.

#### v2 는 예산을 방향에 다 썼다 — 그게 틀렸다

v2 는 "이동 4방향"을 확정하고 종당 16장 중 **12장을 방향에** 배정했다.
근거는 "동료의 유도(§3.3)가 방향을 가리키는 연출이라 좌우로만 서는 개는 '저쪽이야'를
표현하지 못한다"였다. 세 가지가 이 근거를 무너뜨린다.

- **방향은 곱셈이고 모션은 덧셈이다.** 3방향(S·N·E)을 유지하면 **새 모션 하나가 종당 6장**이다.
  측면 고정이면 **2장**이다. 방향을 늘리는 순간 이후의 모든 모션에 **3배 세금**이 붙는다
- **§4.2 와 모순이었다.** §4.2 는 *"무엇을 하고 있는가가 매력을 만든다 — 그림이 단순해도
  물을 마시고, 옆 동물과 붙어 있고"* 라고 적어놨는데, §4.5 가 그 예산을 전부 방향에 썼다
- **실제로 보니 이질적이지 않다.** 여덟 종이 지금 측면 대기·걷기 둘로만 화면에 서 있는데
  어색하지 않다. 이건 추론이 아니라 **눈으로 본 것**이다

#### 유도의 방향은 몸이 아니라 다른 둘이 말한다

v2 의 걱정("좌우로만 서는 개는 저쪽을 못 가리킨다")은 **몸이 방향을 말해야 한다**는
전제 위에 있었다. 그 전제를 버리면 더 싸고 더 잘 읽히는 수단이 둘 있다.

| 수단 | 왜 더 나은가 | 비용 |
|---|---|---|
| **동료가 그쪽으로 걸어간다** | 몸의 각도보다 **위치 변화**가 방향을 훨씬 세게 말한다. 이미 있는 걷기로 된다 | **0장** |
| **머리 위 표시가 방향을 향한다** | 냄새·소리 표시(§5.4)를 그쪽으로 기울여 그린다 | **공용 4장** — 종 수와 무관 |

11종 × 6장 = **66장**을 공용 4장으로 바꾼다. 그리고 종이 늘어도 이 비용은 안 늘어난다.

#### 새 장수 — 같은 16장으로 **무엇을 하는지**를 산다

```
성체   이동 2프레임 × 1방향(E)  = 2장   ← W 는 flip_h
       대기 2프레임              = 2장
       특징 동작 2프레임          = 2장
       ─────────────────── 기본 6장
       먹기 2 · 자기 2 · 놀기 2 · 기뻐하기 2   = 8장   ← v2 에서 방향에 쓰던 예산
       ─────────────────────────────── 14장
아기   이동 2프레임              = 2장   ← 대기는 이동 프레임 재활용, 특징 없음
       ─────────────────────────────── 2장
                              종당  16장
```

**총량은 v2 와 같다.** 바뀐 것은 그 16장이 무엇을 사느냐다 —
**동물이 어느 쪽을 보는가**에서 **동물이 무엇을 하고 있는가**로 옮겼다.

새로 사는 넷은 전부 **설계가 이미 약속해놓고 그림이 없던 것**이다.

| 모션 | 어디서 약속했나 |
|---|---|
| **먹기** | 먹이 주기가 돌봄의 핵심 루프(§2.2)인데 먹는 모습이 없었다 |
| **자기** | 잠 아이콘(`emote_잠`)은 있는데 자는 몸이 없었다 |
| **사물과 놀기** | §2.7 "사물을 갖고 노는 동물" 이 그림 없이 적혀 있었다 |
| **기뻐하기** | §3.4 게이지 완료와 §3.12 초대 카드의 순간 |

#### 앞으로가 싸진다 — 이게 진짜 이유다

| | 새 모션 하나 추가 (11종) |
|---|---|
| v2 (3방향) | **66장** |
| v3.16 (측면) | **22장** |

| 규모 | 총 장수 |
|---|---|
| 11종 (시작 지역 전체) | **176장** — v2 와 같다 |
| 20종 | 320장 |

⚠️ **좌우 비대칭 무늬가 있는 종은 미러가 안 된다.** 그 종만 E/W 를 따로 그린다.

**대상 동물의 상호작용 모션은 0장이다**(§3.4 — 이펙트로 처리).

도트 초보 기준 하루 2~3장이면 **종 하나가 약 일주일**이다.
이 환산은 툴 제작·기능 추가를 판단할 때의 기준 단위로 쓴다.

#### 실루엣 원칙과 완전히 합치된다

§4.2 의 "실루엣만으로 종이 구분되어야 한다"는 **측면에서 가장 잘 성립한다.**
고라니의 긴 다리, 수달의 긴 몸, 청설모의 큰 꼬리는 다 옆에서 보인다.
정면·후면 실루엣은 대개 기둥이나 덩어리가 된다.

v2 는 이 충돌을 "알아보는 순간은 측면, 움직이는 순간은 4방향"으로 봉합했다.
**이제 봉합할 것이 없다 — 항상 측면이다.**

> v2 가 스스로 적어둔 경고가 이 개정의 근거이기도 하다:
> *"북향(뒷모습)에는 눈이 없다. §4.2 에서 매력의 90% 라고 한 그것이 사라진다."*
> **매력의 90% 가 사라지는 각도를 종당 4장씩 그릴 이유가 없다.**

#### 함정 — 위아래로 걸으면 옆모습이 미끄러진다

측면 스프라이트가 수직으로 이동하면 "옆으로 서서 위로 미끄러지는" 것처럼 보인다.
**도트로 풀지 말고 AI 로 푼다.**

> **순수 수직 이동을 만들지 않는다.** 배회 목표를 고를 때 **수평 성분이 항상 있게** 한다
> (대각선 위주). 걷는 방향의 수평 성분으로 좌우를 정하면 몸이 늘 맞는 쪽을 본다.
> 규칙 한 줄이고 **도트 0장**이다.

#### 본체는 방향 능력을 그대로 갖는다

방향 구성은 `sprite_set` 데이터에 들어간다. 개·고양이·다람쥐는 **이미 4방향이 그려져 있고**
그건 버리지 않는다 — 데이터가 "이 종은 측면만"이라고 말할 뿐이다.
나중에 대표 종 몇 개만 방향을 늘리고 싶으면 그때 데이터만 고친다.

**본체는 능력을 갖고, 데이터가 얼마나 쓸지 정한다** — 성장 시퀀스(§2.3)와 같은 철학이다.

---

### 4.6 스프라이트 구성 — 레이어 조합 ★ v2 확정

동물 스프라이트는 **한 장이 아니라 레이어의 조합**이다.

```
[그림자]       발밑 타원 — 공용 1장, 크기만 스케일
[몸통]         종별 22장, 4방향, 팔레트 교체 대상 (§4.3)
[눈]           공용 세트, 표정별 × 정면/측면.  북향에서 숨김
[입]           공용 세트, 표정별 × 정면/측면.  북향에서 숨김
[이동 이펙트]  공용 파티클 — 먼지·물방울 (§6.2 이펙트 레이어)
[표현 아이콘]  공용 도트 아이콘 — 머리 위 (감정·상태·감각)
```

> **이 구성의 핵심 이점: 종당 물량이 하나도 늘지 않는다.**
> 눈·입·아이콘·이펙트가 전부 공용이라 종별로 그리는 것은 몸통 22장 그대로다.
> 대신 **종 수와 무관한 고정비**가 한 번 생긴다.

#### 눈·입은 반드시 공용 세트로 만든다

종별로 그리면 즉시 터진다 — 눈 5표정 × 2각도 = 10장, 입도 10장이면 종당 20장 추가로
몸통(14장)보다 커진다.

```
눈  스타일 4종 × 표정 5 × 2각도(정면/측면) = 40장
입  스타일 3종 × 표정 5 × 2각도            = 30장
                                     ─────  70장  ← 20종이든 100종이든 고정
```

종별로 필요한 것은 그림이 아니라 데이터다:

```
sprite_set {
  eye_style: "round",  mouth_style: "small"
  eye_anchor:   { front: [12, 9],  side: [15, 9]  }
  mouth_anchor: { front: [12, 13], side: [16, 13] }
}
```

**북향은 공짜로 절약된다** — 뒷모습엔 눈·입이 없으니 레이어를 숨긴다.
그래서 각도가 4개가 아니라 정면/측면 **2개**면 된다.
§4.5의 "북향엔 눈이 없다"는 제약이 여기서는 이득이 된다.

#### ★ 걷기 바운스는 스프라이트가 아니라 노드로 처리한다

레이어를 나눌 때 **실질적인 난이도는 물량이 아니라 앵커다.**
몸이 위아래로 통통 튀는 걸 스프라이트 안에 그리면 프레임마다 얼굴 위치가 달라지고,
프레임별 앵커를 데이터로 관리하기 시작하면 종당 데이터가 8배가 되고 손으로 맞추다 지친다.

> **걷기의 상하 바운스를 스프라이트 안에 그리지 말고, 노드 Y좌표로 처리한다.**
> 얼굴 파츠를 몸통의 자식 노드로 두면 부모를 따라 자동으로 움직인다.
> 걷기 2프레임은 **다리 모양만** 바꾸고, 몸 전체의 오르내림은 노드가 한다.

그러면 프레임별 앵커가 필요 없고 종당 앵커는 정면·측면 두 쌍이면 끝난다.
⚠️ 픽셀 퍼펙트이므로 **노드 Y는 정수 픽셀로 스냅**해야 한다. 안 하면 얼굴이 미세하게 떨린다.

#### 표정 — 추가할 때 트리거를 같이 정한다

규칙 없는 표정은 화면에 안 나온다 = §2.1의 보이지 않는 필드다.

| 표정 | 트리거 |
|---|---|
| 기본 | 평상시 |
| 기쁨 | 밥 먹은 직후, 여행 중 적응 지역(§3.7), 가족 형성 |
| 조름 | 밥을 오래 안 줬을 때. **경고가 아니라 애정 표현이다**(§2.6) |
| 시무룩 | 아픔·심심함 (§2.2). **배고픔은 넣지 않는다** — 굶지 않으므로(§2.6) |
| 졸림 | 야행성이 낮에, 주행성이 밤에 |
| 놀람 | 발견되는 순간, 유도 성공 |

**새 표정을 추가할 때는 트리거 조건을 반드시 같이 정한다.**

#### 표현 아이콘 계층 — §3.3이 여기로 흡수된다

7살에게 텍스트 없이 상태를 전달하는 최선의 수단이고,
무엇보다 **동료의 유도 표현(§3.3)이 별도 시스템 없이 여기서 해결된다.**
개가 킁킁대는 것은 머리 위의 코 아이콘이고, 감각 종류(후각·청각·시야)는 아이콘 교체로 끝난다.

기본 세트 20~30개면 게임 전체를 커버한다:
`❗발견  💤잠  🍖배고픔  ❤️애정  💧아픔  🎵신남  ❓궁금  👃냄새  👂소리  👀시야`

> ⚠️ **유니코드 이모지를 그대로 쓰지 말 것.** 폰트 이모지는 해상도와 팔레트가 게임과 안 맞아
> 화면이 즉시 튄다 — §4.6의 "아이 그림을 스캔해 그대로 넣으면 스타일이 튄다"와 같은 문제다.
> **16×16 도트로 직접 그린다.**

#### 이동 이펙트 — 파티클로, 종별 애셋 0장

§6.2의 이펙트 레이어를 그대로 쓴다(§3.4의 교감 반짝임과 같은 레이어).
작은 점 텍스처 하나로 먼지가 되므로 실질 애셋은 1~2장이다.

**종별 차이는 태그에서 뽑는다** — `size_class` 대형이면 먼지 크게,
`habitat` 물가면 물방울, 무거운 종은 착지 시 화면 흔들림. 종별 애셋 0장으로 개성이 생긴다.

파티클을 픽셀 그리드에 스냅할지는 취향 문제다. 프로토타입에서 눈으로 보고 정한다.

#### 눈 그리기 (v3 정정 — v2 의 규칙을 철회한다)

⚠️ **눈 둘레에 밝은 외곽을 두르지 않는다.**
v2 는 "검은 몸통에 검은 눈"을 흰 테로 풀려 했는데, 그러면 **눈이 두 배로 커져
개구리가 된다.** 얼굴이 눈에 다 먹힌다.

**눈은 어떤 털색에서도 검다.** 검은 개도 눈은 검다 — 색을 뒤집을 일이 아니다.
어두운 털에서 눈이 읽히게 하는 것은 **동공 안의 반짝임 1픽셀**(catchlight)이다.
테두리가 아니라 눈 안쪽에 밝은 점 하나를 찍는다.

| | |
|---|---|
| 32px 동물 몸통 | 눈 **2~3px** + 반짝임 1px |
| 사람 얼굴 (9px 폭) | 눈 **1px 폭** — §4.7 |

**표정은 같은 눈의 변형이어야 한다.**
기본에 없던 눈썹 같은 것을 표정에서만 새로 그리면 다른 얼굴이 된다.

- 기본 — 점 + 반짝임
- 기쁨 — 눈이 위로 휜다
- 시무룩 — 눈꺼풀이 덮여 납작해지고 바깥이 처진다

눈 색은 종 팔레트의 인덱스 7·8 이므로, 눈 레이어도 몸통과 같은 팔레트 셰이더를 통과시킨다.
그래야 특수색 개체에서 눈만 따로 놀지 않는다.

#### 물량 정리

| 구분 | 장수 | 종 수에 비례? |
|---|---|---|
| 종별 몸통 | 22장/종 | ✅ |
| 눈·입 세트 | 70장 | ❌ 고정 |
| 표현 아이콘 | 20~30장 | ❌ 고정 |
| 그림자·파티클 | 3~4장 | ❌ 고정 |

고정비 약 100장 ≈ 동물 4.5종어치. 한 번 만들면 100종까지 안 늘어나는 투자다.

### 4.7 플레이어 캐릭터 ★ v3 신설

**여자아이. 32×48 — 2타일 폭, 3타일 높이.** 개(32×32)보다 1타일 높다.
사람은 종이 아니므로 `size_class` 버킷(§5.3)에 들어가지 않는다.

#### 실루엣은 양갈래와 배낭이 만든다

- **양갈래인 이유는 좌우 대칭이라 E/W 미러가 깨지지 않기 때문이다**(§4.5).
  포니테일 하나면 미러할 때 머리가 반대쪽으로 넘어간다.
- **북향에는 얼굴이 없으므로**(§4.6) **배낭이 뒷모습의 정체성**을 맡는다.
  뒤에서 보이는 것이 머리와 옷뿐이면 누구인지 알 수 없다.

#### `special` 은 교감 모션이다

§3.4 에서 "상호작용 모션은 플레이어 쪽에만 둔다"고 했으므로,
플레이어의 특징 동작 자리는 **쪼그려 앉아 손을 내미는 동작**이 차지한다.
대상 동물은 대기 스프라이트 그대로 + 이펙트다.

#### ★ 플레이어만 레이어를 더 나눈다

동물은 `몸통 + 눈` 2레이어지만(§4.6), 플레이어는 여섯이다.
근거는 **프레임을 따라가는가**다.

| 레이어 | 장수 | 프레임 따라감? |
|---|---|---|
| `body` 피부·팔·다리·신발 | 10 | **예** |
| `bottom` 하의 | 4 (남 북 측 특징) | 아니오 |
| `top` 상의 | 4 | 아니오 |
| `gear` 배낭 | 4 | 아니오 |
| `hair` 머리 | 4 | 아니오 |
| `eye` 눈 | 6 (표정3 × 각도2) | 아니오 |

걷기에서 실제로 움직이는 것은 다리와 팔뿐이다(몸의 상하 바운스는 노드가 낸다, §4.6).
그래서 **몸통 레이어가 프레임을 전담하고 나머지는 방향당 한 장**이면 된다.

> **새 의상 한 벌 = 상의 4 + 하의 4 = 8장.** 통짜였다면 10장을 다시 그려야 한다.
> 머리 한 종 = 4장. 머리 3 × 상의 3 × 하의 3 = **27 조합**이 팔레트 교체와 곱해진다.

**신발은 나누지 않는다.** 다리 끝에 붙어 있어 걷기 프레임을 따라가므로,
나누면 장수가 안 줄고 정렬만 어려워진다. 색은 팔레트로 바꾼다.

머리카락 레이어에는 **얼굴 자리가 뚫려 있고** 그 구멍으로 몸통의 피부가 비친다.
머리를 교체할 때 구멍 위치가 맞아야 한다 — 이것이 머리 레이어의 유일한 제약이다.

#### ⚠️ 사람 눈에는 밝은 외곽을 쓰지 않는다

§4.6 의 "눈 레이어에 1픽셀 밝은 외곽"은 **어두운 털 위에서** 눈을 살리는 규칙이다.
피부 위에 얹는 사람 눈에 쓰면 눈이 얼굴을 다 덮는다.
얼굴 폭이 9px 이므로 **눈은 1픽셀 폭**이어야 얼굴이 남는다.
표정은 굵기와 곡률로 낸다 — 기본 1×2 점 · 놀람 굵은 세로 · 기쁨 위로 휜 호.

### 4.8 아이의 참여

동물 캐릭터를 **아이가 그리고, 그것을 도트로 옮긴다.**
아이 그림은 특징을 과장하고 디테일을 버린다 — 좋은 도트 캐릭터의 조건과 정확히 일치한다.

- **가져올 것**: 실루엣, 주요 색 2~3개, 특징 하나
- **통일할 것**: 크기, 외곽선 톤, 눈 스타일, 그림자
- **"아이가 디자인하고 아빠가 통일한다"**

### 4.9 암수 이형 — **몸통 시트를 성별로 나누지 않는다** ★ v3.11 신설

어떤 동물은 암수가 다르게 생겼다. 그걸 넣되, **종당 16장이 32장이 되는 길로 가지 않는다.**

#### 파츠 레이어다 — 눈·입과 같은 물건

이형은 **몸통 위에 얹는 파츠**다(§4.6). 각도는 **정면·측면 둘**이고
**북향에서는 숨긴다** — 눈·입에 이미 있는 규칙을 그대로 재사용한다.
새 장치가 하나도 없다.

| | 장수 |
|---|---|
| 이형이 있는 종 하나 | **1장** (측면. E/W 는 미러) ★ v3.16 — 동물이 측면 고정이라 정면이 안 쓰인다 |
| 아기 | **0장** — 아기는 이형이 없다. 실제로도 그렇고, 6장을 아낀다 |
| 이형이 없는 종 | **0장** |

> 정면 파츠(`dimorph_male_front.png`)는 이미 그려서 남겨뒀다. 버리지 않지만 지금은 안 쓴다 —
> 나중에 정면을 쓰는 연출이 생기면 그때 붙인다. `hide_on: "north"` 도 마찬가지로 잠들어 있다.

> 만약 몸통을 나눴다면 고라니 한 종에 16장이 더 붙었다.
> 파츠로 가면 **2장**이다. 여덟 배 차이다.

#### 지금 이형이 있는 종은 **하나뿐이다**

정직하게 세어보면 11종 중 32~48px 실루엣에서 암수가 갈리는 종은 **고라니 하나**다.

| 종 | 이형 | 판정 |
|---|---|---|
| **고라니** | 수컷의 **엄니**(긴 송곳니) | ✅ 채택. 고라니는 뿔이 없고 수컷에게 송곳니가 있다 — 실존 그대로(§3.8) |
| 두꺼비 | 암컷이 더 크다 | ❌ 크기만 다르면 **아기와 헷갈린다**. 24px 에서 두 가지 크기 축은 못 읽는다 |
| 나머지 아홉 종 | 없음 | ❌ 개·고양이·다람쥐·너구리·수달·삵·까치·참새·뱀은 **실제로 암수가 같게 생겼다** |

> ⚠️ **없는 종에 리본이나 분홍/파랑을 붙이지 않는다.** 그건 동물이 아니라 사람 옷이고,
> 색은 이미 §4.3 유전이 쓰고 있다. **이형은 있는 종에만 있다.**

#### 그럼 나머지 아홉 종의 성별은 어떻게 보이나

**이형과 성별 표시는 다른 층이다.**

| 층 | 무엇 | 몇 장 |
|---|---|---|
| **이형** | 도트 파츠. 필드에서 **다가가기 전에** 멀리서도 안다 | 종당 2장, 지금 고라니만 |
| **성별 뱃지** | 이름표 옆 ♂/♀ 표식. **모든 개체**에 있다 | 2장, 종 수와 무관 |
| **짝 표시** | 하트 · 반쪽 하트 · 지도 핀 | 3장, 종 수와 무관 |

★ **규칙을 짊어지는 것은 ♂♀ 가 아니라 하트다**(원칙 3). 7살은 기호를 읽어서
아기가 왜 안 생기는지 아는 게 아니라, **반쪽 하트**를 보고 "얘는 혼자예요"를 안다.
♂♀ 는 그 다음에 붙는 이름표다.

★ **뱃지 두 개는 같은 회색이고 모양만 다르다.** 색으로 성별을 나누지 않는다.

> **검증 항목**: 아이가 반쪽 하트를 보고 "짝이 없구나"를 스스로 말하는가.
> 안 되면 ♂♀ 뱃지를 빼고 하트만 남긴다 — 기호가 아니라 하트가 본체다.

#### 데이터 (원칙 4 — 종 이름으로 분기하지 않는다)

```
palettes.json
  dimorphism.species.<종 id> = { part, shown_on, anchor{front,side}, file{...}, hide_on:"north" }
```

엔진은 **`if species == "water_deer"` 를 쓰지 않는다.** 이 표에 그 종이 있는지만 본다.
없으면 파츠를 안 얹는다. 새 이형 종이 늘어도 코드는 안 고친다.

★ 엄니는 **인덱스 8(눈밝음)** 으로 찍혀 있다 — 털 팔레트를 뭐로 갈아도 상아색으로 남는다.
검은 털 고라니를 만들어도 엄니는 희다.

산출물: `sprites/build.py` → `extracted/water_deer/dimorph_male_{side,front}.png`,
`sprites/build_screens.py` → `extracted/ui/{sex_male,sex_female,pair_ok,pair_alone,map_pin_pair}.png`

#### 새 종을 고를 때의 함의

**이형이 있는 종은 도트 2장으로 개체 다양성이 두 배가 된다.** §7 의 수집종 후보를
고를 때 이 값을 같이 본다 — 예를 들어 **꿩**(장끼·까투리)은 한국 뒷산 생태에 맞고
이형이 극단적이라, 16장 + 2장으로 사실상 두 종처럼 보인다.
---

## 5. 데이터 설계

### 5.1 태그 기반 원칙

**상호작용·유도·교감 판정을 종 이름이 아니라 태그 조합으로 한다.**
종별로 규칙을 쓰기 시작하면 20종에서 이미 관리 불능이 된다(상호작용 조합 400개).
태그 방식이면 동물을 100종 추가해도 규칙 수는 늘지 않는다.

### 5.2 행동 태그와 스키마 버저닝

**행동 양식도 태그로 표현한다.** pck에 스크립트를 넣지 않기로 했으므로(§6.5),
"이 지역 동물만 특별한 행동을 한다" 같은 요구는 **본체에 행동 타입을 추가하고 팩은 태그로 참조**하는 식으로 푼다.

#### 게임 버전이 아니라 스키마 버전에 의존시킨다

```
manifest {
  requires_behavior_schema: 3     // 게임 버전(1.2, 1.7)과 별개
}
```

본체는 자기가 지원하는 스키마 버전을 알고 있고, 팩이 요구하는 값이 더 크면
"게임을 업데이트해주세요"라고 안내한다.

#### 미지의 태그 처리 — 태그를 필수/선택으로 나눈다

| 태그 종류 | 예시 | 모를 때 처리 |
|---|---|---|
| **필수** (게임 규칙에 관여) | `diet`, `habitat`, `activity`, `stage`, `senses` | **로드 거부** |
| **선택** (연출·표현) | `idle_animation: "목_늘이기"` | **기본값으로 대체하고 진행** |

#### 태그 값은 열거형으로 고정하고 검증한다

JSON에 자유 문자열로 두면 오타 하나가 조용히 넘어가서,
나중에 "얘는 왜 물가에 안 가지?" 하는 디버깅을 하게 된다.

> **임포터에서 알려진 값 목록과 대조하는 검증 스크립트를 초기에 만들 것.**
> 이것은 "툴을 미리 만들지 말라" 원칙의 **예외**다(§5.5).

### 5.3 스키마 초안 (v2 갱신)

```
Animal {
  id, name

  // 상성 축
  diet:        초식 | 육식 | 잡식
  activity:    주행성 | 야행성 | 박명성
  habitat:     [초원, 물가, 숲, 바위]        // 지역 적응(§3.7)에도 재사용
  social:      무리 | 단독 | 쌍
  temperament: 겁많음 | 호기심 | 대담함
  noise:       조용함 | 보통 | 시끄러움
  likes:       [먹이태그...]
  fears:       [태그...]

  // 원정 동료로서의 능력 (§3.3)
  senses:      [후각 | 청각 | 시야 ...]
  traits:      [냄새강함, ...]               // 대상으로서 감지되는 특성

  // 크기 — 실측이 유일한 진실
  size:   { birth: "5-10cm",  adult: "20-30cm" }
  weight: { birth: "1kg",     adult: "8kg" }
  size_class:  소 | 중 | 대                  // 세그먼트 메타. 임포터가 실측에서 파생 계산,
                                            // 수동 오버라이드만 허용

  // 성장 시퀀스 (§2.3)
  growth: [ { stage, from_day, to_day, sprite_set, size, weight, behavior_tags }, ... ]

  // 개체 능력치 바운더리 (개체 생성 시 범위 내 랜덤, 번식 시 부모 값 상속)
  stats_range: { ... }
}

AnimalInstance {
  species_id                  // 문자열로 저장 (§6.5 세이브 안전장치)
  location: safari | waiting  // 사파리 / 쉼터 (§2.4)
  stage, stats, palette
  visited_regions: []         // 여행 기록 (§2.5)
}
```

**크기 규칙:**
- **실측(cm/kg)이 스케일의 유일한 진실이다.** 화면에 숫자로 노출하는 것이 목적이 아니라,
  막연한 소/중/대 대신 실제 수치로 종 간 차이를 정의하기 위한 것이다.
- `size_class`는 삭제하지 않고 **세그먼트 메타데이터**로 유지하되,
  **임포터가 실측에서 파생 계산**한다. 시트에 손으로 적으면
  "실측 32cm인데 중형으로 박힌" 행이 반드시 나온다.
- 상성의 크기 축은 실측 임계값에서 유도하고, 임계값은 코드가 아니라 스키마 테이블에 둔다.
- **같은 종 안의 개체 크기 차이는 픽셀로 표현하지 않는다.**
  24px에서 5% 차이는 1픽셀도 안 된다. 개체 차는 도감 숫자로만.

### 5.4 태그 → 단서·유도 자동 생성 ★ v2 신설

필드의 흔적과 동료의 유도는 **종별로 손으로 쓰지 않는다.**
`tags.json`의 `trait_to_sense` 테이블 하나가 전부를 만들어낸다.

| 대상의 trait | 감지하는 감각 | 화면에 보이는 단서 |
|---|---|---|
| 냄새강함 | 후각 | 코를 킁킁대는 자리 |
| 털 | 후각 | 걸려 있는 털뭉치 |
| 울음소리 | 청각 | 어디선가 들리는 소리 |
| 발자국 / 큰발자국 | 시야 | 작은 / 큰 발자국 |
| 나무흔적 | 시야 | 갉아먹은 솔방울 |
| 물자국 | 시야 | 젖은 흙 |
| 허물 | 시야 | 벗어놓은 허물 |

**동료의 `senses`와 대상의 `traits`가 이 표를 통해 만나면 유도가 발생한다.**
새 종을 추가할 때 코드를 건드리지 않고, 새 단서를 추가할 때도 표에 한 줄이 는다.

> `if species == "dog"` 같은 코드가 나오면 설계가 틀린 것이다(§5.1).
> 종별로 단서를 쓰기 시작하면 §5.1 원칙이 원정 층에서 무너진다.

#### ★ 지면 흔적과 공중 표시는 다르다 (v3)

단서를 실제로 그려보니 **지면에 실물이 있는 것은 시야 계열뿐이었다.**

| 감각 | 지면 오브젝트 | |
|---|---|---|
| 시야 | 발자국 · 큰발자국 · 나무흔적 · 물자국 · 허물 | **있다** |
| 후각 | 냄새강함 · 털 | **없다** (털뭉치만 실물) |
| 청각 | 울음소리 | **없다** |

냄새와 소리는 눈에 안 보이는 것이라 지면 스프라이트가 성립하지 않는다.
그래서 후각·청각 단서는 **동료 머리 위에 뜨는 공중 표시**로 그린다.

> ★ v3.14 — **대상 위치에는 안 띄운다.** 그건 몸만 안 그렸을 뿐 위치를 알려주는
> 원격 표시다. 냄새 표시는 **동료 머리 위에 뜨고 방향을 가리킨다** —
> 대상 위에 뜨는 것은 그 대상이 **내 시야 안에 들어온 뒤**다.

여기서 규칙 하나가 따라 나온다 — **§3.3 의 `reveals` 와 같은 이야기다:**

> **지면 흔적은 동료가 없어도 플레이어가 직접 볼 수 있다.**
> 발자국이 땅에 찍혀 있는데 눈 좋은 동료가 있어야만 보이는 것은 이상하다.
> 시야 동료는 "더 멀리서 알아본다"이지 "없으면 안 보인다"가 아니다.
>
> **후각·청각은 동료가 없으면 아예 알 수 없다.** 냄새와 소리에는 실물이 없기 때문이다.

이것이 §3.6 의 "관찰로 풀린다"를 화면에서 성립시킨다 —
아이는 동료가 없어도 땅을 보고 다닐 수 있고, 동료는 그 범위를 넓혀준다.

### 5.5 관리 방식과 툴 정책

| 용도 | 도구 |
|---|---|
| 동물 데이터 정의 | **구글 시트 → JSON 익스포트 → 임포터 스크립트** |
| 태그 정의·스키마 버전 이력 | 같은 시트의 별도 탭 |
| 런타임 데이터 | Godot `Resource` |
| 맵 편집 | **LDtk** |
| 나머지 | 엔진 에디터 |

밸런싱은 숫자 수십 개를 나란히 놓고 비교하는 작업이라 스프레드시트가 가장 낫다.

> ⚠️ **전용 툴은 미리 만들지 말 것.** 같은 데이터를 손으로 20번 넘게 만졌을 때, 그때 만든다.

#### 이미 Godot가 주는 것 — 만들지 않는다

- **스프라이트 확인** — `SpriteFrames` 리소스를 인스펙터에서 열면 프레임 목록과 재생 미리보기가 나온다
- **필드 단독 실행** — F6으로 씬 하나만 실행된다. 필드 씬을 따로 띄우면 그게 테스트 환경이다
- **리모트 씬 트리 + 인스펙터** ★ — 실행 중인 게임의 노드를 에디터에서 실시간으로 열어보고 값을 바꾼다.
  이동 속도·유도 반경·게이지 시간을 게임 돌아가는 중에 만질 수 있다.
  **"관리 툴이 필요하다"는 욕구의 대부분을 이것이 흡수한다** — 슬라이더 UI를 만들 이유가 없다

#### 만드는 것 — 셋뿐, 전부 반나절~하루

| | 무엇 | 왜 지금 |
|---|---|---|
| 1 | **임포터 검증 스크립트** (CLI, GUI 금지) | §5.2가 인정한 예외. 태그 열거형 대조, 필수 필드 누락, 스프라이트 파일 존재, 실측↔`size_class` 불일치 |
| 2 | **소환·치트 화면** | 성장 5일짜리를 실제로 5일 기다릴 수 없다. 툴이 아니라 **테스트 가능성**의 문제. 종·색·성장 단계를 골라 소환하므로 **팔레트 교체 결과 확인 화면을 겸한다**(§4.3) |
| 3 | **측정 라벨 하나** | "입장 후 첫 유도까지 걸린 시간"을 화면에 찍는 `Label`. 밸런싱 지표(§8)를 측정할 수단 |

**모두 게임 씬으로 만들고 릴리스 빌드에서 제외한다**(export preset의 exclude filter).
외부 앱이나 별도 프로젝트로 만들면 데이터 구조가 이중화돼서 게임과 어긋나기 시작한다.
게임 씬으로 만들면 반대로 **툴이 게임의 첫 테스트가 된다.**

#### 지금 만들지 않는 것

- 동물 데이터 편집 GUI → 구글 시트가 그 역할. GUI를 만들면 "수십 개를 나란히 비교"라는 장점을 잃는다
- 맵 에디터 → LDtk
- 도감·밸런스 대시보드 → 20종 넘어 시트로 감당 안 될 때
- 스프라이트 아틀라스 자동화 → Godot가 임포트 시 처리

> **판단 기준**: 툴에 쓰는 하루는 스프라이트 2~3장이고, 일주일은 **동물 한 종**이다(§4.5).
> 새 툴이 떠오르면 이 환산을 먼저 해볼 것.

---

## 6. 기술 스택

### 6.1 엔진: Godot 4 (확정)

| | Godot 4 | Unity |
|---|---|---|
| 2D 길찾기 | `AStarGrid2D` 내장 | NavMesh가 3D 전제 |
| 픽셀 퍼펙트 | 프로젝트 설정으로 해결 | Pixel Perfect Camera 패키지 |
| 에디터 무게 | 가벼움, 반복 테스트 빠름 | 무거움 |
| 툴링 성숙도 | 보통 | 우수 |
| 자료 양 | 보통 | 압도적 |
| 라이선스 | MIT | 로열티 조건 확인 필요 |
| **exe 배포** | **단일 exe, 50~80MB** | exe + Data 폴더, 200~400MB |
| **DLC 지원** | **`.pck` 런타임 로드 내장** | AssetBundle/Addressables (설정 복잡) |

**배포 요구사항(윈도우 exe + 지역 팩 확장)까지 넣으면 Godot가 명확히 앞선다.**

### 6.2 렌더링

- **네이티브 해상도 렌더링 + 스프라이트 확대 배치**
- 텍스처 필터 **Nearest**, 밉맵 **끄기**
- 줌 지원 시 배율은 기본 대비 **0.7 ~ 1.5배**로 제한
- 카메라는 부드럽게 (서브픽셀 이동 허용)

```
[월드 레이어]    스프라이트, 타일, 동물 — Nearest 필터
[이펙트 레이어]  파티클, 글로우 — Linear 필터, 자유 좌표   ← §3.4의 상호작용 이펙트가 여기
[UI 레이어]      네이티브 해상도                          ← 게이지가 여기
```

**타일 16×16** (v3 확정). 32px 캐릭터가 정확히 2타일 높이, 소형 동물(24px)은 1.5타일이다.
지형 타일은 `terrain/<지형 이름>.png` 로 두어 코드에서 지형별로 분기하지 않는다.
변형 선택은 **타일 좌표 해시**로 한다 — 규칙적으로 고르면 세로 줄무늬가 생긴다.

**Y-sort** — 4방향 탑다운이므로 아래쪽 동물이 위쪽 동물을 가려야 한다. Godot의 `YSort`로 해결된다.
⚠️ 레이어로 나뉜 캐릭터(§4.6)는 **몸통·눈·입·아이콘이 하나의 노드로 묶여** 통째로 정렬되어야 한다.
구조를 잡을 때 미리 알고 있어야 나중에 안 뜯는다.

### 6.3 밤낮 — 팔레트 보간 + 오버레이 틴트 ★ v3 신설

시간대는 **다섯 겹**으로 만든다. 이 순서가 §7 이 걱정하던 셰이더 충돌을 없앤다.

```
1) 인덱스 아트     한 벌뿐이다. 시간과 무관하게 변하지 않는다
2) 팔레트 보간     새벽 → 낮 → 저녁 → 밤 팔레트를 t 로 섞는다      ← 색조
3) 오버레이 틴트   CanvasModulate 전역 곱연산                      ← 밝기·채도
4) 이펙트 레이어   반딧불·안개·노을 (Linear 필터, 자유 좌표)
5) 국소 광원       모닥불 같은 것만 Light2D
```

**팔레트를 이산적으로 두지 않고 보간하므로 시간이 튀지 않는다.**
팔레트는 16×1 텍스처라 섞는 비용이 사실상 없다.

> ⚠️ **전역 조명에 Light2D 를 쓰지 않는다.**
> 인덱스 컬러 팔레트 교체 셰이더 위에 라이팅을 얹으면 색이 의도와 다르게 나오는데,
> 전역을 팔레트+틴트로 처리하면 그 문제가 아예 생기지 않는다.
> Light2D 는 모닥불 같은 **국소 광원 전용**으로 좁힌다.

부수 효과가 하나 있다 — 갈색 동물이 파란 밤 틴트 아래서도 대비가 남아,
§4.1 의 "바닥은 어둡고 차분하게 / 동물은 밝고 선명하게"가 자동으로 성립한다.

시간대는 §3.3 의 `daypart_scale` 을 통해 **감각 반경에도 영향을 준다.**
밤에는 눈이 거의 안 보이고 코는 그대로다.

### 6.4 조작

원정 필드에서 플레이어가 직접 이동하므로(§3.2) 입력 레이어가 필요하다.

- **기본**: WASD / 패드 왼쪽 스틱 (동일 입력으로 처리)
- **입력 추상화 레이어**:
  ```
  원시입력 → 추상입력(moveVector, interact, ...) → 게임로직
  ```
  게임 로직은 입력 장치를 몰라야 한다
- 사파리 층은 이동 + 커서 수준

### 6.5 필드 시뮬레이션 ★ v2 신설

**동물은 화면 밖에서도 필드를 돌아다닌다**(§3.2). 구현 비용은 낮지만 경계를 처음부터 나눠둔다.

```
화면 밖  →  얕은 시뮬: 위치·이동벡터만 갱신
근접 반경 →  풀 AI로 승격 (행동 태그, 애니메이션, 유도 판정)
```

수십 마리 수준은 Godot에서 성능 문제가 아니지만, 이 경계를 나중에 넣으려면 다 뜯어야 한다.
몇십 줄이니 처음부터 둔다.

#### 튜닝 상수는 리소스로 뺀다

필드 규모는 만들어보며 조정한다(§7). 그러려면 다음이 코드에 박히면 안 된다:

```
맵 크기 · 동물 밀도 · 이동 속도 · 유도 반경 · 게이지 시간
```

리소스로 빼두면 리모트 인스펙터(§5.5)로 실행 중에 만질 수 있다.
툴을 만드는 것이 아니라 상수를 빼두는 것이므로 §5.5 원칙에 걸리지 않는다.

#### 맵은 생각보다 작아도 된다

밸런싱 지표(§8)를 역산해보면 — 이동 속도 초당 3~4타일, 첫 유도까지 30초면 100타일 남짓이다.
유도 반경까지 감안하면 **화면 몇 개 분량이면 충분**하다.
"넓은 지역"을 상상하며 큰 맵을 그릴 필요가 없고, 이는 지역 팩의 미술 부담에도 좋은 소식이다.

### 6.6 배포와 콘텐츠 팩

**게임은 스탠드얼론으로 완전히 동작한다.** 인터넷 연결은 선택이며, 연결되면 추가 콘텐츠를 받는다.

#### 팩 단위는 "지역"

세계 지도가 이미 있으므로 지역 단위 확장이 자연스럽다.
태그 기반 데이터 설계 덕분에 **새 동물 20종을 추가해도 게임 로직은 손댈 필요가 없다.**

> ⚠️ v2에서 필드가 실제 공간이 되면서(§3.2) 지역 팩에 **맵이 딸린다.**
> 팩 제작 비용의 성격이 "데이터 입력"에서 "미술 작업"으로 바뀌는 지점이다.
> §6.5의 "맵은 작아도 된다"가 이 부담을 상당히 덜어준다.

#### 팩은 패키지(.pck) 단일 파일로

**사용자가 폴더를 만지지 않는다.** 실제 사용자가 7살이므로, 게임 안에서 버튼 하나로 완결되어야 한다.

```gdscript
ProjectSettings.load_resource_pack("user://packs/oceania.pck")
```

- 팩 하나 = 파일 하나
- 설치 위치는 **`user://packs/`** (%APPDATA% 아래). Program Files 아래는 권한 문제가 생긴다
- 개발 중에는 팩 폴더를 프로젝트 안에 두고 쓰다가, 릴리스할 때만 pck로 분리한다

| 항목 | 내용 |
|---|---|
| **경로 충돌** | pck 로드는 같은 경로의 파일을 덮어쓴다. 팩별로 `res://packs/<id>/...` 접두사를 분리할 것 |
| **스크립트 금지** | pck에는 데이터(JSON)와 이미지·맵만. 로직은 전부 본체에 |
| **무결성 검증** | 인덱스 JSON에 해시를 적어두고 대조. 받다 끊긴 pck를 로드하면 크래시 |

#### 매니페스트

```
pack_manifest {
  id: "oceania"
  region_name: "호주"
  requires_behavior_schema: 3
  welcome: {
    title: "호주의 친구들이 도착했어요!"
    body:  "지금 만나러 가볼까요?"
    teaser_sprites: [...]            // 실루엣으로만 보여준다
  }
}
```

#### 7살을 위한 UX

- ❌ "다운로드 하시겠습니까?" 같은 시스템 대화상자를 그대로 쓰지 않는다
- ✅ **부모가 설정에서 켜두면 조용히 받고**, 아이에게는 게임 내 사건으로 알린다
- ✅ 대표 동물은 **실루엣으로만** 보여준다 (§4.2의 실루엣 원칙과 맞물린다)
- ✅ **"지금 만나러 가볼까요?"를 누르면 바로 세계 지도의 그 지역으로 이동한다**

#### 세이브 안전장치 ★

- 동물 인스턴스에 **원본 종 ID를 문자열로 저장**
- 종 정의를 못 찾으면 "잠든 상태"로 표시하고, 팩을 다시 넣으면 살아난다
- 7살이 실수로 뭘 지웠는데 사파리가 통째로 날아가면 안 된다

#### 호스팅 / exe 빌드 / 자동 업데이트

- GitHub Releases나 정적 파일 서버에 pck와 인덱스 JSON 하나. **서버 코드가 필요 없다**
- 첫 export 전에 **Editor → Manage Export Templates**에서 템플릿을 받아야 한다
- exe 아이콘·제품명·버전은 **rcedit** 경로를 지정해야 붙는다
- 서명 없는 exe는 SmartScreen이 막는다. 본인·아이 PC용이면 무시
- 자동 업데이트는 직접 구현 (100줄 남짓):
  `인덱스 조회 → 새 팩 발견 → 다운로드 → 해시 검증 → user://packs/ 저장 → 로드`

---

### 6.7 진입 화면 — 부팅 · 타이틀 · 메뉴 ★ v3.1 신설

게임이 켜지고 필드에 들어가기 전까지. **해상도는 640×360 으로 고정**이고
(`project.godot` 의 뷰포트와 같다) 확대는 정수배만 쓴다.

```
제작사 로고 (RUN II, 2.5초 · 아무 키나 누르면 건너뛴다)
   ↓
타이틀 — 살아있는 필드 배경 + EVERDEN + 메뉴 4줄
   ↓
CONTINUE → 필드 / NEW GAME → 확인 창 → 필드 / SETTING / EXIT → 확인 창
```

**제작사 이름은 `RUN II`.** 뜻은 "2회차" — 부모가 된 두 번째 삶.
회차 = playthrough/run 이라 직역에 가장 가깝다. 로고 연출은 큰 발자국이 먼저
찍히다가 어느 지점부터 작은 발자국이 나란히 붙는 것이고, 새로 그린 도트는
글자와 발자국 둘뿐이다.

**배경은 켤 때마다 지형이 바뀌고, 거기 사는 동물이 선다.** 정지 일러스트를 그리지 않는다.

이게 이 화면의 핵심이다. **타이틀 화면 자체가 단서다.** 글로 "얘는 숲에 살아요" 라고
알려주는 대신 숲을 깔고 그 위에 청설모를 세운다. 아이는 읽는 게 아니라 **본다** —
§3.6 "관찰로 풀린다" 를 게임에 들어가기 전에 한 번 하는 셈이고, 글을 못 읽어도 통한다.

| 무엇이 | 어디서 나오나 |
|---|---|
| 지형 | 그 종의 `habitat[]` 중 하나 |
| 시간대 | 그 종의 `activity` — 주행성 낮 · 야행성 밤 · 박명성 저녁 |
| 타일·프롭 | `terrain/<지형>.png` 와 `palettes.json` 의 `prop_terrain` |

⚠️ **짝이 어긋나면 틀린 것을 가르친다.** 수달이 초원에 서 있으면 안 된다.
반드시 `habitat` 에서 뽑을 것 — 종 이름으로 분기하면 언젠가 어긋난다.

§6.3 의 팔레트 보간이 그대로 걸리므로 시간대는 색으로 표현된다.
필드는 무늬가 많아 아래로 갈수록 어두워지는 스크림을 한 겹 깐다 — 패널을 깔면
확실하지만 배경을 가려버린다. 설정 같은 하위 화면은 전체를 한 겹 더 누른다.

#### 메뉴

| | |
|---|---|
| 항목 | `CONTINUE` · `NEW GAME` · `SETTING` · `EXIT` |
| 세이브 | **하나뿐이고 자동 저장.** 세이브 선택 화면이 없다 |
| 첫 실행 | `CONTINUE` 가 **아예 없다.** 회색으로 두지 않는다 — 고를 수 없는 것을 보여줄 이유가 없다 |
| 커서 | **발자국.** 삼각형이 아니다. 제작사 로고와 같은 물건이고, 이 게임이 흔적을 따라가는 게임이라는 걸 메뉴에서부터 말한다 |
| 선택 표시 | 밝기 + 오른쪽으로 6px. 둘 다 걸어야 작은 화면에서 읽힌다 |

#### 문구는 영문, 확인 창은 한글

메뉴는 도트 게임 관례대로 영문이다. 대신 **되돌릴 수 없는 동작에는 한글 확인 창**이
붙는다. 첫 플레이어가 `EXIT` 도 `NEW GAME` 도 못 읽기 때문이다.

- 확인 창은 **되돌릴 수 없는 것에만** 뜬다. 그 외에는 묻지 않는다
- **기본 선택은 언제나 안전한 쪽**(아니오)이다
- 새 게임: "지금까지 만든 사파리가 사라져요. / 그래도 새로 시작할까요?"

세이브를 조용히 덮어쓰는 것은 §"절대 어기면 안 되는 것" 2번(되돌릴 수 없는 실패)
위반이다. 게임 안에만 적용되는 원칙이 아니다.

#### 설정은 셋

`SOUND` · `MUSIC` · `FULLSCREEN`. 값은 **숫자가 아니라 칸 수**로 보인다 —
칸이 몇 개 찼는지가 곧 값이다(§"규칙은 눈에 보여야 한다").

#### 글자

| | |
|---|---|
| 로고 (`RUN II` · `EVERDEN`) | **손으로 찍은 도트.** 14×18 격자, 획 3px, 정수배 확대만 |
| 메뉴 · 라벨 | 자체 5×7 비트맵 폰트 (`build_title.py` 의 `FONT5`) |
| 한글 | **갈무리 11 (SIL OFL 1.1).** `fonts/` 에 들어왔다 |

로고를 폰트로 찍으면 다른 게임과 같은 얼굴이 된다. 반대로 본문까지 손으로 찍는 것은
낭비다 — 경계는 **로고냐 아니냐**다.

그리고 로고 안에서도 한 번 더 갈린다. **제작사 로고는 절제하고 게임 타이틀은 정반대다.**
처음에 `EVERDEN` 을 `RUN II` 와 같은 14×18 격자로 찍었더니 타이틀이 제작사 로고처럼
보여서 심심했다. 타이틀은 16×20·획 4px 에 세로 그러데이션·윗변 하이라이트·오른쪽 아래
돌출·완만한 아치를 얹는다. 흔들림은 무작위가 아니라 **아치**여야 한다 — 무작위로 흔들면
실수처럼 보인다.

**갈무리는 11px 로 찍어 정수배로만 키운다.** 22px 로 바로 찍으면 힌팅이 개입해 획이
흔들린다. Godot 임포트에서 안티에일리어싱·힌팅·서브픽셀·밉맵을 전부 꺼야 한다 —
자세한 것은 `fonts/README.md`.

#### 타이틀 옆의 동무 — 켤 때마다 바뀐다

글자 발밑에 풀·꽃을 겹치고 마지막 글자 옆에 동물 하나를 앉힌다. 워드마크만 얹으면
공중에 뜬 스티커로 보인다.

**풀은 `data/animals.json` 의 모든 종 × 모든 `growth` 단계다.** 종을 코드에 적지 않는다 —
`growth[]` 가 이미 `baby`/`adult` 와 `sprite_set` 이름을 들고 있으므로 데이터에서 뽑는다.
종을 하나 추가하면 타이틀 풀이 저절로 늘어난다(§"종 이름으로 분기하지 않는다").
첫 실행·스프라이트 없음은 **개 성체**로 떨어진다.

> 지금 풀은 22칸(11종 × 2단계)인데 실제로 그려진 것은 **개·고양이·청설모의 성체 셋뿐**이다.
> 아기 스프라이트는 아직 한 장도 없다 — §4.5 가 종당 아기 6장을 잡아뒀고, 그게 채워지는
> 만큼 타이틀 풀도 채워진다. `title.json` 의 `candidates[].available` 이 그 상태를 들고 있다.

**못 만난 종도 앉는다.** 도감으로 막지 않는다 — 모르는 동물이 타이틀에 앉아 있는 것은
스포일러가 아니라 미끼다.

#### 이름표 — 이름만 적는다

동무에 마우스를 올리거나 아무것도 안 하고 2초가 지나면 옆에 작은 이름표가 뜬다.
**이름만 적는다.** 도감에 없어도 이름은 보여준다 — `"???"` 보다 `"너구리"` 가 훨씬 세게
당긴다. 아이가 이름을 부를 수 있으니까.

사는 곳은 **쓰지 않는다.** 배경이 이미 말하고 있다. 숲을 깔고 청설모를 세워놓고 그 위에
"숲에 살아요" 를 또 쓰면, 그림으로 읽는 법을 배울 기회를 뺏는 것이다.

한 번 만난 동물이 **한 군데서만 사는 것도 아니다.** 배경은 매번 `habitat` 중 하나를
고르므로, 여러 번 켜다 보면 고양이가 초원에도 숲에도 바위에도 있는 것을 아이가 스스로
알게 된다. 목록을 보여주는 것보다 이쪽이 이 게임의 문법이다.

> **마우스에서만 얻을 수 있는 정보를 만들지 않는다.** 기본 조작은 WASD/패드다(§6.4).
> 이름표는 마우스를 올리면 즉시, 아무 입력이 없으면 2초 뒤에 뜬다 — **마우스는 더 빠를 뿐이다.**
> 이 규칙은 타이틀만의 것이 아니라 앞으로 만들 모든 화면에 걸린다. 툴팁을 마우스에만
> 달기 시작하면 패드로 하는 아이가 게임의 절반을 못 본다.

#### 조각으로 내보낸다 — 합성 스틸이 아니다

로고도 타이틀도 **완성된 한 장이 아니라 조각 + 좌표**로 나간다. 합성본을 주면 엔진이
발자국을 하나씩 찍을 수도, 동무를 갈아끼울 수도 없다. 32프레임을 통째로 굽는 것은
640×360 × 32장이라 낭비다. 지형·프롭·단서를 `palettes.json` 으로 넘긴 것과 같은 방식이다.

- `ui/logo.json` — 워드마크 위치, 발자국 좌표 배열, 프레임 타이밍
- `ui/title.json` — 워드마크·풀밭 위치, 동무 앵커와 후보 목록, 메뉴 좌표, 확인 창 문구

**좌표와 타이밍이 데이터로 나오면 아트를 다시 그려도 엔진 코드를 안 고친다.**

#### 산출물

`sprites/build_logo.py` · `build_title.py` → `sprites/extracted/ui/`

| | |
|---|---|
| 로고 | `logo_wordmark` · `logo_paw_big` · `logo_paw_small` · `logo.json` (+ 검수용 `logo_screen`) |
| 타이틀 | `title_mark` · `title_grass` · `cursor_paw` · `title.json` (+ 검수용 `title_screen`) |
| 폰트 | `fonts/Galmuri11.ttf` 외 3종 + `LICENSE-Galmuri.txt` |

---

### 6.8 날씨 레이어 ★ v3.6 신설 · v3.7 축 모델로 개정

§6.3 의 다섯 겹 위에 날씨를 한 겹 더 얹는다. **화면 변화 대비 비용이 가장 싼 레이어다** —
새로 그리는 것이 이어 붙는 타일 일곱 장이고, 나머지는 이미 있는 장치를 다시 쓴다.

#### 날씨는 이름이 아니라 축이다 ★ v3.7

처음엔 맑음·흐림·비·폭우·안개·눈 여섯 **이름**으로 짰다. 그러면 "조금 오는 비"를
표현할 자리가 없다. 이름을 버리고 **축 다섯 개**로 간다.

| 축 | | |
|---|---|---|
| `cloud` | 0~1 | 구름 그림자의 짙기. **맑음 ↔ 흐림이 이 축이다** |
| `fog` | 0~1 | 안개 |
| `rain` | 0~1 | 비. **폭우는 별개 날씨가 아니라 `rain` 이 센 것이다** |
| `snow` | 0~1 | 눈 |
| `wind` | 0~1 | ★ **하나로 구름 방향 · 빗줄기 기울기 · 눈의 흐름을 동시에 정한다** |

이름(맑음·폭우·진눈깨비…)은 이 공간의 **한 점에 붙인 별명**일 뿐이다.
데이터는 축을 들고 있고 화면은 축을 읽는다 — **새 날씨를 추가할 때 코드를 안 고친다.**
`wind` 를 하나로 묶은 이유도 같다: 구름은 왼쪽으로 흐르는데 비는 오른쪽으로 기울면
화면이 어긋나 보인다.

**틴트도 표로 적지 않고 축에서 뽑는다.**
`cloud*0.9 + rain*0.55 + snow*0.18 + fog*0.2` 만큼 `(0.66, 0.73, 0.88)` 쪽으로 당긴다.
그래야 **강도가 저절로 따라온다** — 가랑비와 폭우가 같은 색일 수 없다.

> ⚠️ **강도에 상한을 둔다** (`fog ≤ 0.52` · `cloud ≤ 0.46`).
> 짙은 안개에서 화면이 안 보이면 7살은 그냥 못 논다. 아무리 세도 **길과 캐릭터
> 실루엣은 읽혀야 한다** — 날씨는 연출이지 장애물이 아니다.

#### 팔레트를 건드리지 않는다 — 여기가 갈리는 지점

시간대가 넷인데 날씨별 팔레트를 또 만들면 폭발한다. 그렇게 가지 않는다.

```
1) 인덱스 아트        한 벌
2) 팔레트 보간        시간대만              ← 날씨는 여기 안 들어온다
3) 오버레이 틴트      시간대 틴트 × 날씨 틴트  ← 여기서 만난다
3.5) 구름 그림자      곱연산으로 흐른다       ← 맑은 날에도 항상
4) 이펙트 레이어      빗줄기 · 눈 · 안개 판
5) 국소 광원          Light2D
```

**시간대 × 날씨가 곱셈이 아니라 덧셈이 된다.**

#### 구름 그림자 — 맑은 날에도 흐른다 ★

이 레이어의 핵심이다. 하늘의 구름이 땅에 떨어뜨리는 그림자가 화면을 가로질러 흐른다.

- **날씨가 "없는" 상태를 만들지 않는 장치다.** 맑은 날 화면이 정지 화면처럼 보이는 것을 막는다
- **캐릭터 위에도 떨어진다.** 땅에만 깔면 필터처럼 보인다
- 가장자리는 **디더로 풀린다.** 단단한 테두리면 그림자가 아니라 얼룩이다
- 비용은 **타일 한 장**이다

#### 빛줄기와 햇살 얼룩 — 날씨의 반대편 ★ v3.9

날씨 겹이 어둡게만 하면 화면이 우중충해지기만 한다. **밝아지는 겹**이 짝으로 있어야 한다.

| 겹 | 언제 | |
|---|---|---|
| **빛줄기** (박명광선) | **구름이 중간일 때 가장 세다 · 낮에만** | 구름이 아주 없으면 새어 나올 틈이 없고, 꽉 차면 빛이 못 뚫는다 |
| **햇살 얼룩** | 맑을수록 · 낮에만 | **구름 그림자와 같은 타일**을 위상만 어긋나게 해 밝게 더한다 |

**햇살 얼룩은 구현 세션이 발명했다.** 구름 사이로 새는 빛이니 그림자와 짝이라는 것이
핵심이고, 그래서 **새로 그리는 도트가 0장**이다. 같은 타일을 두 번 쓴다.

빛줄기는 도트가 있다(`weather/light_shaft.png`, 128² · 주기 64 · 기울기 2).
**띠가 촘촘하면 박명광선이 아니라 창살이다** — 주기를 크게 잡고 심지를 좁혀
넓은 어둠 사이로 몇 줄기만 지나가게 한다. 심지는 희고 가장자리는 노랗다.

> **더하는 빛이다.** 알파로 세기를 주고 곱연산으로 깔지 않는다.

#### 구현에서 나온 함정 세 개 ★ v3.9

구현 세션이 실제로 밟은 것들이다. 다시 밟지 않도록 여기 적는다.

- ⚠️ **타일은 디더 마스크지 반투명 판이 아니다.** 구름 그림자는 53%가 거의 검정이라
  곱연산으로 통째로 깔면 **맑은 날에도 화면이 까매진다.** 세기는 알파로 준다
- ⚠️ **스크롤은 region 에서 빼야 한다.** 더하면 텍스처 아래쪽을 화면 위에서 샘플링해서
  **비가 아래에서 위로 올라간다**
- ⚠️ **날씨 이름이 바뀔 때만 배치를 다시 굴린다.** 축이 계속 흐르므로 매 프레임 굴리면
  동물이 깜빡인다

그리고 회귀 하나가 지킨다 — **가장 사나운 날씨에도 화면이 60% 넘게 덮이지 않는다.**
날씨는 연출이지 장애물이 아니다(§강도 상한).

**집과 타이틀에도 걸리되 정도가 다르다.** 집은 **맑고 구름이 흐르는 정도**만 한다 —
마당은 보는 곳이지 견디는 곳이 아니다(§2.7). 타이틀은 그 배경 지형의 날씨를 한 점 골라 머문다.

#### 도트 규칙

- 전부 **이어 붙는 타일**로 낸다. 엔진은 스크롤만 한다 — 화면 크기 텍스처를 굽지 않으므로
  해상도가 바뀌어도 그대로 쓴다. 주기 함수의 합으로 만들어 경계가 반드시 이어진다
- 농담은 **4×4 Bayer 디더**로 낸다. 알파 그러데이션은 도트가 아니다
- ⚠️ **디더 마스크는 정수 픽셀로만 스크롤한다.** 반픽셀로 움직이면 디더 무늬가
  자글거려서 도트가 아니라 노이즈로 보인다 (§6.2 Nearest 규칙의 연장)

#### 날씨는 감각을 깎는다 — 넣는 진짜 이유

예뻐서가 아니다. **팀 편성에 축이 하나 생긴다.**

§3.3 의 `sense_profile` 에 이미 `daypart_scale` · `terrain_scale` 이 있다.
**`weather_scale` 을 같은 자리에 열 하나로 추가한다** — 새 구조가 아니다.
그리고 **이름이 아니라 축에 건다.** 그래야 "옅은 안개는 시야가 조금만 깎인다"가 된다.

| 축 | 깎이는 감각 | 왜 |
|---|---|---|
| `rain` | **후각** | 냄새가 씻긴다 |
| `fog` | **시야** | 안 보인다 |
| `wind` | **청각** | 소리가 흩어진다 |

**안개 낀 날엔 코가 좋은 아이를 데려간다.** 아이가 스스로 알아낼 수 있는 규칙이고,
§3.6 "관찰로 풀린다"가 한 겹 더 깊어지는 자리다.

#### 출현 패턴과의 규칙

- **날씨는 태그다.** 화면에는 **도감의 "언제" 칸 옆에 "비 올 때 잘 나와요"** 로 나온다
  (§"보이지 않는 데이터 필드를 만들지 않는다")
- **확률을 노출하지 않는다**(원칙 3). "출현률 +30%" 금지
- **날씨가 성공·실패를 바꾸지 않는다**(원칙 2). 바꾸는 것은 **누가 나오는지**뿐이다
- **지도에 예보로 보인다.** 노리고 기다릴 수 있되 기다림을 강요하지 않는다 —
  **아무 날씨에나 만날 수 있는 종이 늘 있어야 한다**
- 타이틀 배경도 따라온다(§6.7). 비 오는 날 배경에 두꺼비가 앉아 있으면 그것만으로 하나를 배운다

#### 산출물

`sprites/build_weather.py` → `sprites/extracted/weather/`
(`cloud_shadow` 320² · `fog` 256² · **`light_shaft` 128²** · `rain` · `rain_heavy` ·
`snow_far` · `snow_near` 96² · `splash` 3프레임 · `weather.json`).
`weather.json` 이 축·상한·프리셋·레이어 순서를 들고 있다.
**햇살 얼룩은 파일이 없다** — `cloud_shadow` 를 위상만 어긋나게 다시 쓴다.

### 6.9 화면 인계 규약 — **화면도 데이터로 넘긴다** ★ v3.17 신설

#### 왜 이 절이 생겼나 — 드리프트의 진짜 원인

세계 지도가 설계와 갈라졌다. 브리프 §3.9 는 *"목적지 판에 **지형 타일 그림** + 이름,
**만난 적 있는 아이** 얼굴"* 이라고 적었는데, 구현은 이렇게 나왔다:

```
"지형: 물가 · 숲"
"만난 적 있는 아이: 개 · 고양이  (처음 보는 아이도 있어요)"
"◀ ▶ 로 고르고 [스페이스] 로 정해요"
```

구현 세션 잘못이 아니다. **설계 세션이 시안을 PNG 로만 넘겼기 때문이다.**
그림을 보고 코드를 짜려면 좌표를 자로 재야 하는데, **`Label.text = "지형: ..."` 은 한 줄이면 된다.**
빠른 쪽으로 가는 것은 당연하다.

> **로고와 타이틀은 안 갈라졌다.** 그 둘은 `logo.json` · `title.json` 으로
> **조각과 좌표를 데이터로** 넘겼기 때문이다. 나머지 화면에 그 방식을 안 썼을 뿐이다.

#### 규약 — `ui/screens.json`

화면마다 **무엇을 어디에 그리는가**를 데이터로 넘긴다. 시안 PNG 는 참고용이지 사양이 아니다.

각 요소는 넷 중 하나로 표시한다. **이 구분이 이 파일의 핵심이다.**

| 종류 | 뜻 |
|---|---|
| `art` | **그림이다.** 글로 바꾸면 안 된다 — 지형 타일, 동물 얼굴, 아이콘, 키캡 |
| `text` | 글이다. 짧게 |
| `count` | 숫자를 써도 되는 자리 (자리 3/5 같은 것) |
| `keycap` | 조작 안내. **문장에 `[스페이스]` 를 쓰지 않는다** (§2.10) |

> ⚠️ **글로 적을 수 있으면 글로 적게 된다.** `art` 로 표시된 자리에 라벨이 들어가 있으면
> 그건 구현 실수가 아니라 **인계가 실패한 것**이다. 검수는 시안 PNG 와 실행 화면을
> 나란히 놓고 본다 — 글의 개수가 늘었으면 갈라진 것이다.

#### 지금 갈라진 것 — 고칠 목록

| 화면 | 지금 | 있어야 할 것 |
|---|---|---|
| 세계 지도 | `"지형: 물가 · 숲"` | **지형 타일 그림** 2장 + 이름 |
| 세계 지도 | `"만난 적 있는 아이: 개 · 고양이"` | **얼굴 스프라이트 줄** |
| 세계 지도 | `"개가 좋아할 거예요"` | 동료 **얼굴 + 기쁨 눈**(`eye_*_기쁨`, 이미 있다) |
| 세계 지도 | 하단에 동료 chip 줄 + 3행 커서 | **팀 편성 화면으로 분리** (§3.9) |
| 세계 지도 · 첫 만남 | `"◀ ▶ 로 고르고 [스페이스] 로"` | **키캡**(§2.10) |
| 첫 만남 | 5줄 안내 + 홀드 게이지 | **한 줄 + 키캡**, 본편 `Gauge` (§2.9) |
| 필드 | 나가는 길 없음 | **집으로 창** (§3.13) |

산출물: `sprites/build_screens.py` → `extracted/ui/screens.json`
---

## 7. 아직 정하지 않은 것

- [ ] **원정 중 사파리 시간이 흐르는가** — 만들어보고 결정(§2.2).
      흐르게 하면 점유 시간이 원정의 자원이 되고, §3.7의 "남은 애는 심심하다"가 자연스러워진다
- [ ] **필드 규모 밸런싱** — 1차 크기를 **96×54 타일(1536×864, 한 화면의 2.4×2.4배)** 로
      잡았다(`build_field.py`). 지형 넷이 한 필드에 들어가야 §3.3 유도가 의미를 갖는다 —
      한 화면짜리 필드에서는 유도할 거리가 없다. 실제 값은 만들어보며 조정(§6.5), 지표는 §8
- [ ] **수집종을 더 늘릴 것인가** — 늘리기 전에 장수를 본다.
      지금 11종인데 **완전한 것은 셋(48장)**이고 나머지 여덟은 측면 대기·걷기 둘씩이라
      **남은 빚이 112장**이다 (v3.16 이후로는 방향이 아니라 **모션** 112장).
      새 수집종 하나 = 16장 ≈ 일주일.
      후보(실루엣이 확실히 갈리는 것): 고슴도치(가시) · 멧토끼(긴 귀) · 왜가리(긴 다리+목).
      셋이면 48장이 더 쌓인다 — **빚을 갚고 나서 결정한다**.
      ★ v3.11 — 후보를 고를 때 **이형 유무를 같이 본다**(§4.9). **꿩**(장끼·까투리)은
      뒷산 생태에 맞고 이형이 극단적이라 16장 + 2장으로 사실상 두 종처럼 보인다
- [ ] **시간 흐름의 단위** — 게임 내 하루가 무엇인가. `growth.from_day`가 여기 물려 있다
- [ ] **재화의 이름과 표현** (§2.6)
- [ ] **치료·도움 상황의 종류** — 사고 유형을 몇 개나 둘 것인가
- [ ] **실존 동물 vs 가상 동물** —
      실존이면 위키 등에서 기초 정보를 반자동으로 채울 수 있고 아이가 실제 지식을 배운다.
      대신 사실과 게임 규칙이 충돌할 때 처리를 정해야 한다.
      가상이면 그런 제약이 없고 아이 그림을 활용하기 좋다.
      §3.8에서 **생태는 실존, 지명은 무명**으로 두어 양쪽을 다 열어놓았다
- [ ] 동물 종 수의 1차 목표
- [ ] **국소 광원** — 모닥불·등불을 언제 어디에 두는가.
      전역 밤낮은 §6.3 으로 닫혔고, 남은 것은 국소 광원의 용도다
- [ ] **UI·화면 레이아웃** — 쉼터, 세계 지도, 팀 편성, 도감, 팩 웰컴.
      글을 이제 배우는 7살이 쓰는 UI라 별도 설계가 필요하다.
      **진입 화면(§6.7)과 집(§2.7)은 닫혔다** — 나머지는 남아 있다
- [ ] **원정에서 돌아온 뒤 화면** — 무엇을 데려왔는지 보여주는 자리.
      자리 배정이 여기서 일어난다. ★ v3.14 — **초대 카드(§3.12)와 역할이 갈렸다.**
      카드는 필드에서 **한 마리씩 그 자리에서**, 돌아온 뒤 화면은 **데려온 전부를 한 번에**.
      같은 정보를 두 번 보여주지 않도록 뒤쪽을 얇게 만든다
- [ ] **동물에게 이름을 지어줄 것인가** ★ v3.14 — 초대 카드가 이름을 지어주기에
      가장 자연스러운 자리다. 다만 글을 이제 배우는 7살에게 **텍스트 입력**은 벽이라,
      후보 몇 개를 보여주고 고르게 하는 편이 맞을 수 있다
- [x] ~~**야생 개체의 나이를 어떻게 정하나**~~ ★ v3.15 닫힘 — **게임적 허용이다.**
      근거를 만들지 않는다. §3.12 참조
- [ ] **날씨 값 정의** — 도트와 축 모델은 §6.8 로 닫혔다. 남은 것은
      **어느 종이 어느 축을 좋아하는가**와 `weather_scale` 의 실제 배율.
      **구현 세션이 출현 패턴 쪽을 손대는 중이라 그쪽과 맞춘다**
- [ ] **날씨가 바뀌는 리듬** ★ v3.10 다시 엶 — v3.9 의 "한 원정 = 한 날씨"는 철회했다(§3.11).
      **원정 중에도 바뀐다.** 남은 것은 **얼마나 자주**와 **얼마나 천천히**다.
      너무 잦으면 개체가 깜빡이는 것처럼 보이고, 너무 느리면 기다림이 대기가 된다.
      전환(비가 그치는 것)은 반드시 **예고**되어야 한다 — 하늘·소리·구름 그림자가 먼저 (원칙 3).
      ★ v3.12 — 이 예고는 **필드 안에서** 하는 것이다. 나가기 전 예보가 아니다(§3.9)
- [ ] **계절** — 눈을 넣으려면 계절이 있어야 한다. 없으면 사철 눈이 온다 (§6.8)
- [ ] **커플이 된 뒤 관계가 계속 보이는가** ★ v3.20 — 이름표에 작은 하트를 붙일지,
      카드 한 번으로 끝낼지 (§2.11). 붙이면 "쟤네 부부야" 가 늘 보이고,
      안 붙이면 화면이 깨끗하다
- [ ] **동물이 사물로 걸어가는 규칙** — 얼마나 자주, 어떤 순서로. 아이가 "쟤가 공을
      갖고 논다"를 알아볼 만큼은 자주 일어나야 한다 (§2.7)
- [ ] **게임 제목** — 폴더와 타이틀 화면은 `EVERDEN` 이고 이 문서 제목은 아직 "사파리(가제)"다.
      둘 중 하나로 정해야 하고, 한글 제목으로 갈 거라면 타이틀 로고를 한글 도트로 다시 찍어야 한다
- [ ] **타이틀 배경을 필드 맵과 같은 생성기로 합칠 것인가** — 지금 타이틀 배경은
      단일 지형을 타일로 채우는 간단한 생성기고(§6.7), 실제 필드는 별도다.
      필드 생성이 자리를 잡으면 하나로 합치는 편이 나을 수 있다
- [ ] 색 유전 규칙과 특수색 출현 확률의 구체값 (§4.3)
- [ ] **성별 뱃지가 7살에게 읽히는가** ★ v3.11 — ♂♀ 를 그려뒀지만(§4.9)
      규칙을 짊어지는 것은 하트다. 아이가 **반쪽 하트만 보고** "짝이 없구나"를
      말하는지 확인하고, 되면 뱃지는 그대로 두고 안 되면 **뱃지를 뺀다**
- [ ] **성별이 원정 이후 화면에 어떻게 보이는가** — 데려온 아이의 성별을
      돌아온 뒤 화면에서 언제 알려줄 것인가. 이형이 없는 종은 만날 때까지 모른다

**해소된 항목**
- v2 — 원정지에서의 플레이어 조작(→ §3.2), 사파리 뷰 각도(→ §4.5),
  seat 확장 방법(→ §2.4), 스프라이트 구성 상세(→ §4.6), 시작 지역과 첫 동물(→ §3.8)
- v3 — **밤낮·조명**(→ §6.3 팔레트 보간 + 틴트, Light2D 는 국소 광원만),
  **배경 타일 구성**(→ §6.2 타일 16×16, 지형 이름이 파일명),
  **능력치가 무엇을 결정하는가**(→ §3.3 `sense_range` 가 유도 반경에, `charm` 이 게이지 속도에)

---

## 8. 다음 단계

**동물 6종으로 전체 루프를 먼저 돌린다.**

```
동료 골라 떠나기 → 필드 돌아다니기 → 개가 킁킁 → 다가가니 너구리
    → 게이지 채워 교감 → 데려오기 → 자리가 없어 쉼터로
    → 밥 주고 → 다친 애 도와 번 재화로 자리 확장 → 가족 형성
```

이게 재밌으면 100종으로 늘리는 건 그냥 데이터 입력이다. 재미없으면 100종을 채워도 마찬가지다.

### 프로토타입에서 줄일 것

v2에서 늘어난 것이 많으므로 명시적으로 깎는다.

| 항목 | 프로토타입 범위 |
|---|---|
| 경제 | 숫자만. 상점 UI 없이 **seat 확장 하나에만** 소비 |
| 치료·도움 | 상성 판정 재사용, **1종류만** |
| 필드 맵 | **1개, 작게**. 지역 여러 개는 나중 |
| 동료 감각 | **후각·청각·시야 셋 다 + `sense_profile`** (§3.3). 개와 고양이의 대비가 검증 대상이므로 깎지 않는다 |
| 성장 단계 | **2단계**. 스키마는 배열로 열어둠 |
| 상성 축 | **2개만 켠다** (먹이 유형 + 활동 시간). 나머지는 데이터에만 |
| 표정 | 눈 1스타일 × 3표정 × 2각도 = 6장. **입은 생략**(§4.6 — 눈이 매력의 90%) |
| 표현 아이콘 | 5개 (발견·잠·배고픔·애정·냄새) |
| 이동 이펙트 | 먼지 1개 |
| 콘텐츠 팩 | 만들지 않는다. 폴더 구조만 팩 전제로 |

> 6종 × 16장 = **몸통 96장** + 공용 약 12장. 하루 2~3장이면 약 6~7주.
> 프로토타입 애셋은 Kenney(kenney.nl, CC0)나 itch.io 에셋팩으로 대체하고,
> **재미가 확인된 뒤에 그린다.**

### 밸런싱 지표는 하나로 고정한다

필드 상수가 다섯 개(§6.5)라 따로 만지면 수렴하지 않는다.

> **"입장 후 첫 유도(동료의 반응)까지 걸리는 시간 ≤ 30초"**

7살은 30초~1분 안에 아무 반응이 없으면 지루해한다.
이 하나를 고정하고 맵 크기·밀도·이동 속도를 역산한다(§6.5).

---

## 부록 A. 이 프로젝트에서 경계할 실패 패턴

- **데이터를 먼저 다 채운다** — 동물 100종 메타데이터를 다 채우고 정작 게임 로직이 없는 상태에 도달하기 쉽다
- **툴을 게임보다 먼저 만든다** — 아프기 전에 만들지 말 것. 툴 일주일 = 동물 한 종(§4.5)
- **보이지 않는 데이터 필드를 만든다** — 플레이어가 체감 못 하는 필드는 작업량만 늘린다
- **종별로 규칙을 쓴다** — 반드시 태그 조합으로. 단서와 유도도 마찬가지(§5.4)
- **미술을 먼저 완성한다** — 재미 검증 전 미술 투자는 매몰비용이 된다
- **7살에게 손실을 준다** — 죽음, 되돌릴 수 없는 실패, 파산, 끊기는 게이지.
  방치의 대가는 손실이 아니라 진행 정체로
- **수집을 벌로 만든다** ★ — 동물이 늘수록 유지비·관리 의무가 커지는 구조.
  경제(§2.6)와 여행 로테이션(§3.7)에서 반복해서 확인할 것
- **필드를 크게 그린다** — 큰 맵은 재미가 아니라 이동 시간이다(§6.5)
- **아이가 전략을 세울 거라고 가정한다** ★ v3.13 — 최적해를 상상하고, 그 최적해를
  UI 로 알려주고, 미묘한 배율로 보상한다. 셋 다 틀렸다. 아이는 애착으로 고르고
  **경험으로 느끼고 말로 공유한다** (§3.3)
- **문서를 코드보다 낡게 둔다** ★ — 설계 대화에서 정한 것과 구현에서 발명한 것이
  둘 다 이 문서에 안 들어오면, 문서가 폴더에서 가장 못 믿을 파일이 된다.
  **v3 를 만들 때 실제로 다섯 건이 밀려 있었다.** 결정이 나면 그 자리에서 절을 고칠 것

---

## 부록 B. 변경 이력

### v3.19 → v3.20 (2026-08-27)

- **§2.11 커플 카드 신설** — §2.4 의 결혼이 화면에서 일어나는 순간.
  ★ **거절이 없다.** 7살에게 두 동물이 친해지는 걸 거절하게 하는 건 이상하고,
  거절이 있으면 "잘못 눌렀다" 가 생긴다(원칙 2). 버튼은 **「잘됐다!」 하나뿐**
- **두 단계로 화면을 막지 않는다** — 마당에서 두 아이 사이에 하트가 뜨고(판 없이, §3.3 규약),
  다가가면 카드가 열린다. **안 가도 진행은 된다**
- **초대 카드(§3.12)와 같은 모양** — 아이가 이미 배운 화면이라 새로 가르칠 게 없다.
  두 마리는 **한 쪽을 뒤집어 마주 보게** 한다 — 도트 0장
- ★ **다음에 무슨 일이 생기는지 카드에서 말한다.** §2.4 의 "여분 seat 가 있어야 아기" 를
  여기서 안 보여주면 아이는 "왜 아기가 안 나와?" 를 영영 모른다(원칙 3).
  자리 표시는 **꽉 찼을 때만** 강조하고, **빠져나갈 길**("쉼터로 보내면 자리가 나요")을 같이 적는다
- **새로 그리는 도트 0장.** §7 에 미결 하나 추가(커플이 된 뒤 관계를 계속 보여줄지)

### v3.18 → v3.19 (2026-08-27)

- **§3.3 재개정 — 시야도 "거기에 뭔가 있다" 까지만 말한다.** v3.14 의
  "시야 = 보이는 거리를 늘린다" 를 철회한다. **시야로 보이는 것도 플레이어가 직접 보는 게 아니다** —
  동료 덕에 시야가 늘어나는 것도 결국 게임이 거들어주는 것이다.
  v3.14 의 핵심("보이는 것은 언제나 내 시야 안")이 오히려 **더 강해진다** — 이제 예외가 없다
- **그런데도 범주는 안 무너진다. 이미 갈려 있었다** (구현 세션이 찾아냈다):
  **코와 귀는 공중 표시라 방향을 말하고, 눈은 땅에 남은 자국이라 자리를 말한다.**
  한 마디로도 갈린다 — **"코로는 어느 쪽인지 알고, 눈으로는 어디였는지 안다."**
- **`reveal_radius` 는 이제 아무도 못 건드린다** — 오직 플레이어의 것이다.
  동료가 늘려주는 것은 **단서가 보이는 거리**지 몸이 보이는 거리가 아니다
- **딸려 나온 것: 승격 반경 26 → 8 타일** (구현 세션 측정, 13마리 중 노드 최대 1개)
- **§6.9 `screens.json` 에 `field` 항목을 넣었다** — 빠져 있었다.
  상시 HUD(지명·자리) · 지면 단서 · 감각 표시 · **게이지 규격** · 상황 한 줄
- **방향 표시 공용 4장** (`shared/dir_*.png`). ⚠️ **발자국을 쓰지 않는다** —
  지면 발자국은 "여기 지나갔다"(시야 단서)로 이미 쓰이고 있어서 한 화면에 두 뜻이 겹친다.
  **감각 아이콘(무엇으로) + 화살표(어느 쪽)** 로 나누면 2×4=8 이 아니라 **2+4=6** 이다
- **머리 위 표시에 판을 두르지 않는다.** 검은 판을 깔았더니 필드 위에 얹힌 **UI 위젯**으로 보였다.
  **1배**로 쓰고(동물이 32px 인데 아이콘이 32px 이면 아이콘이 동물만 해진다),
  **1px 그림자**로 띄우고 **꼬리 3px** 로 주인을 가리킨다
- **필드의 상황 안내 바는 평소에 없다.** 머리 위 표시가 이미 말하고 있다 —
  **안 보이는 것을 알려야 할 때만** 뜬다(게이지가 멈췄을 때). HUD 판도 옅게(알파 130)

### v3.17 → v3.18 (2026-08-27)

- **§2.10 — 패드를 들면 패드가 보인다.** 첫 플레이어는 엑스박스 패드로 할 가능성이 높다.
  **마지막으로 쓴 입력 장치**를 따라 **그림만** 갈아 끼운다 (설정 항목을 만들지 않는다 —
  패드를 집어 드는 것 자체가 이미 선택이다)
- ★ **`[스페이스]` 를 문장에 안 넣기로 한 규칙이 여기서 값을 했다** — 바뀌는 것이
  그림 하나뿐이다. 넣었다면 장치마다 문장을 다시 써야 했다
- `ui/pad_{stick,dpad,a,b,menu}.png` **5장**. 자리와 크기를 키캡과 맞춰서 배치가 안 흔들린다.
  대응표는 `ui/screens.json` 의 `glyphs.map`
- 7살에게는 **A·B 글자보다 색과 자리가 먼저 읽힌다** — 초록 Ⓐ · 빨강 Ⓑ.
  스틱은 화살표를 원 **밖에** 둘렀다(안에 넣으면 도넛으로 보인다). 메뉴는 ☰ 세 줄
- ⚠️ 엑스박스 배치 기준. 닌텐도는 A/B 가 반대다. `project.godot` 에 **패드 바인딩이 없다**

### v3.16 → v3.17 (2026-08-27) — 구현 점검

- **§6.9 화면 인계 규약 신설.** 세계 지도가 설계와 갈라진 **진짜 원인은 인계 형식**이었다 —
  설계가 시안을 **PNG 로만** 넘겼고, 그림을 보고 좌표를 재는 것보다 `Label.text` 한 줄이 빠르다.
  로고·타이틀은 `logo.json`·`title.json` 으로 넘겨서 **안 갈라졌다.** 그 방식을 나머지 화면에 쓴다.
  요소마다 `art` / `text` / `count` / `keycap` 을 표시한다 —
  **`art` 자리에 라벨이 들어가 있으면 구현 실수가 아니라 인계 실패다**
- **§2.9 첫 만남 신설.** 구현 세션이 먼저 만들었고, **브리프에 설계가 없던 것이 원인**이다.
  절대 규칙: **본편과 같은 조작·같은 게이지·같은 카드.** 별도 코드로 게이지를 다시 짜면
  반드시 갈라진다 — `Gauge` 를 그대로 쓴다.
  ⚠️ **누르고 있게 만들지 않는다** — 초대는 누르는 게 아니라 **곁에 있어 주는** 것이다(§3.4)
- 첫 만남은 **한 줄 + 키캡**. 마지막 칸에는 **키캡이 없다** — 누를 게 없다는 걸 부재로 말한다
- **§2.10 키캡 신설** — `ui/key_{wasd,space,start,esc,arrows}.png` **5장, 화면·종 수와 무관.**
  문장에 `[스페이스]` 를 넣지 않는다. 문장은 무엇을 하는지만, 어떻게 누르는지는 그림이
- **§3.13 필드에서 집으로 신설.** ⚠️ **지금 구현에는 나오는 길이 아예 없다** — 원정을 나가면
  못 돌아온다. START 로 여는 확인 창 하나: 집 아이콘 + **같이 가는 아이 얼굴** + 갈래요/더 놀래요.
  쏟다 만 게이지는 개체에 남으니 **나가는 것이 손해가 아니다**
- **§3.9 — 동료는 지도에서 고르지 않는다.** 지도는 **어디로**만. 동료는 **팀 편성 화면**에서
  얼굴로. **한 화면에 결정 하나** — 지도에 동료 줄을 붙이면 커서가 2차원이 되고 7살에게 무겁다

### v3.15 → v3.16 (2026-08-27)

- **§4.5 전면 개정 — 동물은 측면 1방향. 예산을 방향이 아니라 모션에 쓴다.**
  v2 는 종당 16장 중 **12장을 방향에** 썼다. 세 가지가 그 근거를 무너뜨렸다:
  **방향은 곱셈이고 모션은 덧셈**(3방향이면 새 모션 하나가 종당 6장, 측면이면 2장) ·
  **§4.2 와 모순**(*"무엇을 하고 있는가가 매력을 만든다"* 라고 적어놓고 예산은 방향에) ·
  **실제로 보니 이질적이지 않다**(여덟 종이 측면 둘로 서 있는데 어색하지 않다 — 눈으로 본 것)
- **유도의 방향은 몸이 아니라 (a) 동료가 그쪽으로 걸어가는 것과 (b) 머리 위 표시가 말한다.**
  11종 × 6장 = 66장을 **공용 4장**으로 바꾼다. 종이 늘어도 안 늘어난다
- **같은 16장으로 먹기 · 자기 · 놀기 · 기뻐하기를 산다** — 넷 다 설계가 이미 약속해놓고
  그림이 없던 것이다
- **앞으로가 싸진다** — 새 모션 하나가 11종 기준 66장에서 **22장**으로
- **함정**: 측면 스프라이트가 수직으로 움직이면 미끄러져 보인다.
  도트가 아니라 **AI 로 푼다** — 배회 목표에 **수평 성분이 항상 있게**. 도트 0장
- **§4.9 이형 파츠가 종당 2장 → 1장**이 됐다 (정면이 안 쓰인다)
- 플레이어는 **4방향 그대로**다. 개·고양이·다람쥐의 4방향 도트도 버리지 않는다 —
  본체는 능력을 갖고 데이터가 얼마나 쓸지 정한다

### v3.14 → v3.15 (2026-08-27)

- **야생 개체의 나이는 게임적 허용으로 닫는다** (§3.12 · §7). 근거를 만들지 않는다 —
  **아이가 분명히 궁금해하기 때문에** 답을 주는 것이다: *"얘는 엄마 사슴인가? 아기인가?"*
- 그래서 카드가 답하는 순서가 정해졌다 — **주인공은 성별 + 단계**(♀ + 어른),
  숫자 나이는 곁들이다. 아이가 묻는 건 숫자가 아니라 **관계**다
- **아기도 혼자 초대할 수 있다** — **애초에 포획이 아니라 초대**다.
  아이가 데려가는 게 아니라 얘가 따라오기로 한 것이라, 떼어온다는 서사가 들어설 자리가 없다.
  ⚠️ 그러니 화면에도 문서에도 **"잡다"를 쓰지 않는다** — 만나다 · 친해지다 · 초대하다. 대신 `부모근처` 태그를
  **행동으로 답하는 자리**로 쓴다: 부모가 없으면 **아무 어른 곁에나 붙는다.**
  아이가 "얘 엄마는?" 하고 물으면 글이 아니라 화면이 답한다. 도트 0장
- **게임이 "엄마"라고 부르지는 않는다.** 아직 아기가 없는데 엄마라 부르면 틀린 말이고,
  그렇게 부르는 것은 **아이의 몫**이다 (§4.8 과 같은 결)

### v3.13 → v3.14 (2026-08-27)

- **§3.12 초대 카드 신설** — 교감 게이지가 끝나면 누구를 초대했는지 띄운다.
  몸 · 종 이름 · **성별 뱃지** · 나이 · 감각 · **개략적인 설명 두 줄** · 도감 갱신.
  **축하지 평가가 아니다** — 능력치 숫자는 안 넣는다. 넣으면 "다시 뽑을까"가 생긴다(원칙 6).
  새로 그리는 도트 **0장**
- **§3.3 `reveals` 를 없앤다 — 보이는 것은 언제나 내 시야 안에 있는 것.**
  "시야로 잡으면 그 자리에서 몸이 보인다"는 **원격 표시**였다. 고양이가 감지했다고
  저 멀리 있는 동물을 화면에 그리는 건 세계의 사건이 아니라 **게임이 알려주는 것**이다
- **플레이어 시야는 좁게 유지한다.** 그래야 동료에게 의지하는 것이 직관적이 되고,
  **발견의 순간이 아이에게 남는다** — 동료는 데려다주고, 보는 것은 아이가 한다
- 감각의 범주는 유지된다: **후각·청각 = 방향** / **시야 = 보이는 거리를 늘린다.**
  시야의 한 마디가 "눈으로 찾으면 바로 보여"에서
  **"눈 좋은 애랑 가면 멀리서도 보여"**로 바뀌었고, 후자가 세계 안의 사건이라 더 잘 읽힌다
- **구현이 같이 쉬워진다** — `activation_radius`가 감각 반경 전체가 아니라
  **보이는 반경**만 덮으면 된다
- `reveal_radius` 기본값을 넉넉하게 잡지 말 것 — 넓으면 §3.3 이 통째로 죽는다

### v3.12 → v3.13 (2026-08-27)

- **§3.3 — 아이는 전략을 세우지 않는다.** "누굴 데려갈까가 진짜 **선택**" 이라는
  프레이밍을 버린다. 아이는 **애착으로 고르고**, 상성은 여러 번 갔다 온 뒤
  **경험적으로 느끼고 서로 공유하는 것**이다
- 여기서 조건 셋이 나온다: **UI 가 상성을 알려주지 않는다** · **효과가 커야 느껴진다** ·
  **한 마디로 옮겨져야 공유된다**. 그리고 **반복 가능해야 한다** —
  친구에게 말해줬는데 안 되면 배신이다
- **"위키에 적힐 만한 것을 만들되, 게임 안에는 적지 않는다."**
- **`sense_profile` 의 곱셈 다섯 개에 경고를 달았다** — 1.5×1.1×0.7×0.6×0.35 에는
  이름이 안 붙는다. 감각 차이는 **배율이 아니라 범주로**. `reveals` 는 그대로 두고,
  `range_scale` 은 차이를 벌리고, 시간대·지형은 **감각마다 무력화 하나만**
- **§3.9 — 지도의 "어디로·누구랑"을 손잡이로 설계하지 않는다.**
  지도가 상성을 안내하면 안 된다
- **§3.11 — "다음엔 코가 좋은 애를 데려가자"는 아빠가 하는 말이다.**
  게임이 하면 UI 로 가르치는 것이 된다
- 부록 A 에 실패 패턴 추가: **아이가 전략을 세울 거라고 가정한다**

### v3.11 → v3.12 (2026-08-27)

- **§3.11 정정 — 날씨는 고르는 것이 아니다.** "아이가 날씨를 보고 나갈 날을 고른다"를
  **철회한다.** 날씨를 보고 나갈 이유는 없고, **나갔는데 날씨에 따라 찾게 되는 동물이
  다를 뿐**이다. v3.10(날씨는 원정 중에 바뀐다)과 애초에 앞뒤가 안 맞았고,
  7살에게 예보 읽기와 "잘못 고름"을 만든다
- **지도에 날씨 위젯을 달지 않는다** (§3.9). 지도가 고르게 하는 것은
  **어디로**와 **누구랑** 둘뿐 — 동료 감각(§3.3)이 유일한 조절 손잡이가 된다
- **"예보는 없다. 창밖은 있다."** 집 마당에 비가 오는 걸 보고 나가는 것은 자연스럽다.
  그건 예보를 읽는 게 아니다. 그 이상을 화면에 만들지 않는다
- 도감의 "언제" 칸은 **할 일 목록이 아니라 추억**이다

### v3.10 → v3.11 (2026-08-27)

- **§4.9 암수 이형 신설 — 몸통 시트를 성별로 나누지 않는다.** 눈·입과 같은
  **파츠 레이어**로 얹는다(북향에서 숨기는 규칙까지 재사용). 이형이 있는 종 하나당
  **2장**(정면·측면), 아기는 0장. 몸통을 나눴다면 16장이었다 — **여덟 배 차이**
- **11종 중 이형이 있는 종은 고라니 하나**다. 수컷의 **엄니**(실존 그대로).
  나머지 아홉은 실제로 암수가 같게 생겼고, **없는 종에 리본이나 색을 붙이지 않는다**
- **§2.4 — 암수 한 쌍이어야 아기가 생긴다.** 대신 막힘 방지 장치 셋이 **같이** 들어간다:
  혼자예요 반쪽 하트 · 지도 하트 핀 · **확정 배치**(그 필드에 반대 성별이 반드시 정의된다).
  **확률로 풀지 않는다** — 짝이 없다는 게 보이고, 어디 가면 있는지가 보이고, 가면 있다
- **§3.11 1단계에 성별이 붙었다** — 개체 정의는 능력치·색·성별을 같이 정한다.
  그래서 이형이 있는 종은 **잡기 전에 멀리서도 암수가 보인다**
- **성별 표시는 종 수와 무관한 5장** (♂♀ 뱃지 · 하트 · 반쪽 하트 · 하트 핀).
  ★ 규칙을 짊어지는 것은 ♂♀ 가 아니라 **하트**다 — 뱃지 둘은 같은 회색이고 모양만 다르다
- **도감은 종 단위로 둔다** — 암수를 칸으로 나누지 않는다.
  이형이 없는 열 종은 칸이 둘인데 그림이 같아져서 미완성으로 읽힌다

### v3.9 → v3.10 (2026-08-27)

- **§3.11 재개정 — 정의 · 출현 · 발견은 세 단계다.** v3.9 는 배치와 발견 둘로 갈랐는데
  가운데가 빠져 있었다.
  **1) 개체 정의**는 필드에 들어가는 순간 한 번 — 이번 원정의 개체 수 분포가 여기서 끝나고
  **개체수 0 인 종이 있는 것이 정상**이다.
  **2) 출현**은 **날씨가 바뀔 때마다** 다시 정해진다 — 놓인 개체 중 지금 누가 나와 있는가.
  **3) 발견**은 플레이의 몫(감각·단서·관찰)
- **"한 원정 = 한 날씨" 철회.** 날씨는 원정 중에 바뀐다.
  원칙 2 는 깨지지 않는다 — 날씨 변화가 **개체를 빼앗지 않기** 때문이다.
  안 보이는 것은 사라진 게 아니라 지금 안 나와 있는 것이고,
  **날씨가 돌아오기를 기다리는 것 자체가 플레이**다
- **확률은 배치가 아니라 출현에 붙는다** — 여전히 수치로 노출하지 않는다.
  화면에 나오는 것은 관찰 가능한 사건뿐: "비 오니까 나왔네"
- **§7 의 "날씨가 바뀌는 리듬"을 다시 열었다** — 남은 것은 빈도와 전환 속도,
  그리고 전환을 반드시 예고할 것

### v3.8 → v3.9 (2026-08-27)

- **§3.11 개정 — 조건이 정하는 것은 배치지 발견이 아니다.**
  v3.8 은 "조건을 채우면 반드시 나온다"고 적었는데 한 단계를 빠뜨렸다.
  배치는 결정적, **발견은 플레이의 몫**이다. 철이 맞아도 산에서 사슴벌레는 보기 어렵다
- ~~**한 원정 = 한 날씨**~~ — **v3.10 에서 철회.** 날씨는 원정 중에 바뀐다
- **§6.8 에 빛줄기(박명광선) 도트 추가** (`light_shaft.png` 128²).
  띠가 촘촘하면 창살이 된다 — 주기를 크게, 심지를 좁게
- **햇살 얼룩** — 구현 세션 발명. 구름 그림자와 **같은 타일**을 위상만 어긋나게 해
  밝게 더한다. 새 도트 0장
- **구현에서 나온 함정 셋**을 §6.8 에 박았다 (디더 마스크≠반투명 판 ·
  region 은 빼야 한다 · 이름 바뀔 때만 다시 굴린다)

### v3.7 → v3.8 (2026-08-27)

- **§3.10 앰비언트 생물 신설** — 나비·잠자리·물고기·새그림자·개미·벌.
  도감에 없고 자리를 안 먹는다. 여섯 종 합쳐 **14장**
- **§3.11 희귀는 조건이다** — 낮은 확률이 아니라 날씨·시간·지형의 조합.
  **조건을 채우면 반드시 나온다.** 새 종을 안 그리고 "가끔 발견"이 생긴다
- **필드 1차 크기 96×54 타일** — 지형 넷이 한 필드에. 한 화면 필드에서는 유도할 거리가 없다
- §7 에 **수집종 추가는 빚을 갚고 결정한다**를 올렸다 (남은 빚 120장)

### v3.6 → v3.7 (2026-08-27)

- **날씨를 이름에서 축으로 개정**(§6.8) — `cloud`·`fog`·`rain`·`snow`·`wind` 다섯.
  폭우는 별개 날씨가 아니라 `rain` 이 센 것이다. 틴트도 표가 아니라 축에서 뽑는다
- **구름 그림자를 맑은 날에도 흘린다** — 날씨가 "없는" 상태를 만들지 않는 장치
- **강도에 상한** — 짙은 안개에서도 길과 실루엣은 읽혀야 한다
- **디더 마스크는 정수 픽셀로만 스크롤** — 반픽셀이면 자글거린다
- `weather_scale` 은 이름이 아니라 **축**에 건다

### v3.5 → v3.6 (2026-08-27)

- **먹이 아홉 장 완성** — 나뭇잎·뿌리·곤충·고기 추가. `likes` 의 모든 값에 그림이 생겼다
- **11종 전부 측면 대기 도트** — 도감의 못 만난 칸이 `?` 에서 **실루엣**이 됐다.
  다만 여덟 종은 §4.5 의 16장 중 **한 장뿐**이다
- **§6.8 날씨 레이어 신설** — 팔레트를 건드리지 않고 **틴트를 한 겹 더 곱한다**.
  시간대 × 날씨가 곱셈이 아니라 덧셈이 된다.
  `sense_profile` 에 `weather_scale` 열 하나를 더해 **팀 편성에 축을 하나 만든다**

### v3.4 → v3.5 (2026-08-26)

- **쉼터 화면**(§2.4) — 목록이 아니라 장소. 방출 버튼이 없고, "원정 갈 수 있어요"
  배지가 붙고, 자리가 꽉 차면 막는 대신 교체를 묻는다
- **§3.9 세계 지도 신설** — 길은 발자국, 배경은 지형 타일. 새 도트는 핀·집 아이콘 둘.
  안 가 본 곳의 조건을 수치로 쓰지 않는다
- **축척이 다르면 표현도 달라진다** — 지도의 "물가"는 가운데가 물이어야 한다

### v3.3 → v3.4 (2026-08-26)

- **§2.8 도감 신설** — 못 만난 종은 이름은 보여주고 모습은 감춘다.
  빈 칸이 아니라 **점선으로 두른 채워질 자리**
- **크기를 아이 키와 나란한 막대로** — 실측이 처음으로 화면에 나온다
- 도감의 모든 칸이 `animals.json` 의 기존 필드에서 나온다. 새 필드 없음
- 비어 있는 것을 명시: **도트 3/11**, **먹이 스프라이트 5종(4종 부족)**

### v3.2 → v3.3 (2026-08-26)

- **상점 · 배치 모드**(§2.7) — 값 옆에 붙는 것은 태그 이름이 아니라 우리 아이 얼굴.
  격자 스냅, 격자는 점으로만. **판 값은 그대로 돌려준다**
- **필드 HUD**(§3.3 · §3.4) 시안 — 동료의 감각 아이콘 + 방향을 가리키는 발자국,
  점유 시간 게이지. 상시 HUD 는 지명과 자리 둘뿐
- **팀 편성 · 출발**(§3.1) 시안 — 목적지 미리보기는 도감에 있는 종만,
  "여기 없는 아이도 있어요" 를 함께 적어 모름을 남긴다
- **`시야` 아이콘 신설** — 감각 셋 중 시야만 아이콘이 없어서 `발견`(!) 을 대신
  쓰고 있었다. 코·귀 옆에 눈을 놓았다 (`shared/emote_시야.png`)

### v3.1 → v3.2 (2026-08-26)

- **§2.7 집 신설** — 게임을 켜면 필드가 아니라 집으로 들어온다.
  집은 진행이 누적으로 보이는 화면이고, **보는 곳이지 해야 하는 곳이 아니다**
- **사물(시설)** — §2.6 이 비워둔 "시설" 칸을 채웠다. `for_tags` 는 전부
  `tags.json` 에 이미 있는 값이라 새 열거형이 없다
- **사물이 새 동작을 요구하지 않는다** — 동물의 기존 특징 동작이 사물 옆에서 재생된다.
  종×사물로 만들면 49벌이 된다
- HUD 는 재화와 자리 둘뿐. 배고픔·청결·기분 게이지를 두지 않는다

### v3 → v3.1 (2026-08-26)

- **§6.7 진입 화면 신설** — 제작사 로고(`RUN II`), 타이틀, 메뉴 4줄, 확인 창, 설정.
  세이브는 하나 + 자동 저장이라 세이브 선택 화면이 없다
- §7 의 "UI·화면 레이아웃"에서 진입 화면 몫을 덜어냈고, **게임 제목**을
  미결 항목으로 새로 올렸다
- **갈무리 11 반입 완료** (`fonts/`, OFL 1.1). 11px + 정수배 규칙과 Godot 임포트 설정은
  `fonts/README.md`
- **타이틀 동무를 무작위로** — 풀은 `animals.json` 의 모든 종 × 성장 단계.
  종을 코드에 적지 않는다. 못 만난 종도 앉는다
- **타이틀 배경을 무작위 지형으로** — 지형은 `habitat`, 시간대는 `activity` 에서 나온다.
  **타이틀 화면 자체가 단서다** — 글로 알려주는 대신 보여준다
- **이름표는 이름만** — 사는 곳은 배경이 말한다. 그리고
  **마우스 전용 정보를 만들지 않는다**(호버 즉시 / 무입력 2초)는 규칙을 §6.7 에 못박았다
- **로고·타이틀을 조각 + JSON 으로 내보낸다** — 합성 스틸로는 엔진이 발자국을 찍거나
  동무를 갈아끼울 수 없다. 구현 세션 요청(`sprites/구현세션-요청.txt`)을 반영한 것

### v2 → v3 (2026-08-25)

설계 대화에서 나온 것과 **구현 세션이 코드에서 발명한 것**을 함께 반영했다.

| 절 | 변경 | 출처 |
|---|---|---|
| §2.6 | **밥 주기는 배고픔을 막는 행위가 아니라 행복도를 올리는 행위** | UI 목업에서 모순 발견 |
| §3.1 | **팀 편성 미리보기는 도감에 기록된 종만** — 아니면 원정이 목표 지향으로 회귀 | UI 목업 |
| §3.3 | **`sense_profile` 신설** — `reveals`/`range_scale`/`daypart_scale`/`terrain_scale`, `reveal_radius` | 구현 세션 |
| §3.5 | **지형**이 감각 반경을 깎고 동물의 시작 위치를 정한다 | 구현 세션 |
| §4.5 | **E/W 미러 — 종당 22장 → 16장.** 11종 242 → 176장 | 스프라이트 제작 |
| §4.6 | 시무룩 트리거에서 배고픔 제거, 조름 추가 | §2.6 연쇄 |
| §5.4 | **지면 흔적(시야)과 공중 표시(후각·청각)의 구분.** 지면 흔적은 동료 없이도 보인다 | 단서 스프라이트 제작 |
| §6.2 | **타일 16×16 확정**, 지형 이름이 파일명, 변형은 좌표 해시로 | 배경 제작 |
| §6.3 | **밤낮 신설** — 팔레트 보간 + 오버레이 틴트 + 이펙트. Light2D 는 국소 광원만 | 배경 제작 |
| §7 | 밤낮·배경·능력치 항목 해소, 국소 광원만 남김 | |
| §4.6 | **눈 둘레 밝은 외곽 규칙 철회.** 대비는 동공 안 반짝임 1px 로. 표정은 같은 눈의 변형 | 눈이 개구리가 됨 |
| §4.7 | **플레이어 캐릭터 신설** — 여자아이 32×48, 레이어 6개, 사람 눈은 1px 폭 | 플레이어 제작 |
| 부록 A | **"문서를 코드보다 낡게 둔다"** 실패 패턴 추가 | 이 병합 자체 |
| 파일명 | `PROJECT-BRIEF-safari-v2.md` → **`PROJECT-BRIEF.md`** (버전은 문서 안에만) | 드리프트 방지 |

### v1 → v2

| 절 | 변경 |
|---|---|
| §2.2 | 방치를 "세션 내 원정 체류"로 재정의. 오프라인 시간 정지 확정. 행복도 상승 경로 2개로 |
| §2.3 | 2단계 고정 → **grow 배열**. 아기는 축소 재활용이 아니라 **별도 제작**으로 정정 |
| §2.4 | seat가 **원정 개체에도 적용**. **쉼터(대기)** 도입. seat 확장 = 도감 연동 |
| §2.5 | **여행 기록**(`visited_regions`) 신설 |
| §2.6 | **경제 신설** — 기본 먹이 무료, 성장비만 유료, 치료·도움으로 획득 |
| §3.1 | 원정이 **목표 지향이 아님**으로 재작성 |
| §3.2 | **필드는 실제 공간** — v1 §3.4 "미결: 조작" 해소 |
| §3.3 | **동료의 감각과 유도** 신설 |
| §3.4 | **교감·치료·도움 = 뭉뚱그린 모션 + 게이지** 신설. 대상 모션 0장 |
| §3.7 | **여행 프레이밍** 신설 — 동료에게 즐거움, 집 보는 당번, 로테이션 비강제 |
| §3.8 | **시작 지역과 첫 동물** 신설 — 집 근처 뒷산·냇가, 생태는 실존 지명은 무명. 프로토타입 6종 확정(개·고양이·청설모·너구리·수달·고라니) |
| §4.4 | 렌더 스케일 정수/반정수 양자화 추가 |
| §4.6 | **스프라이트 구성 신설** — 레이어 조합, 눈·입 공용 세트, 걷기 바운스는 노드로, 표현 아이콘 계층 |
| §4.5 | **뷰 각도·방향 확정** (3/4 뷰, 이동 4방향 + 대기 2방향 + 특징 1방향, 사파리·필드 동일). 종당 16~18장 → **22장**. 실루엣 원칙은 "알아보는 순간은 측면 고정"으로 양립 |
| §5.3 | `senses`·`traits`·`growth`·`visited_regions`·`location` 추가. `size_class`는 파생 계산으로 |
| §5.4 | **태그 → 단서·유도 자동 생성** 신설 |
| §5.5 | **툴 정책** 정리 — Godot 기본 기능 활용, 만들 것 3개, 안 만들 것 명시 |
| §6.3 | 입력 추상화 **부활** (필드 이동) |
| §6.4 | **필드 시뮬레이션** 신설 — 얕은/깊은 AI 경계, 튜닝 상수 외부화 |
| §6.5 | 지역 팩에 맵이 딸리는 문제 명시 |
| §8 | **프로토타입 축소 표**와 **밸런싱 지표** 신설 |
