# 사파리 (가제) — 프로젝트 기획서

> 이 문서는 프로젝트의 단일 기준 문서다. 대화 맥락 없이 이 문서만 읽어도
> 프로젝트를 이해할 수 있도록 작성되었다.
>
> **버전 v3.2** · 최종 수정: 2026-08-26 · 상태: Demo 1(필드 슬라이스) 구현 중
> 변경 이력은 **부록 B** 참조.
>
> ⚠️ **파일 이름에 버전을 넣지 않는다.** 버전은 이 줄에만 있다.
> 두 세션(설계·구현)이 같은 경로를 참조해야 문서가 갈라지지 않는다.

---

## 0. 이 프로젝트가 무엇인가

**7살 아이를 첫 플레이어로 상정한, 동물 수집·육성 게임을 1인 개발로 만든다.**

넓은 사파리를 소유하고, 그곳에서 동물들이 서로 어울려 살아간다.
플레이어는 먹이를 주고 돌보며, 동료들과 함께 세계 각지로 여행을 떠나
그곳에서 만난 동물들과 교감하고 우리 사파리로 **초대**한다.
사냥이나 포획이 아니라, 친구가 되어 초대하는 개념이다.

**만드는 사람**: 15년 경력 소프트웨어 엔지니어. 게임 개발과 도트 그래픽은 처음.
코드/설계 역량은 충분하나, 미술과 게임 특유의 감각은 경험이 없는 영역.

**진짜 목적**: 아이와 함께 만드는 것. 아이가 동물을 그리고 그게 게임에서 살아 움직이는
사이클이 이 프로젝트의 핵심 동력이다. 상업적 성공이 목표가 아니다.

---

## 1. 게임 구조

### 1.1 두 개의 층

```
┌─ 사파리 (집) ─────────────────────────┐
│  · 동물들이 서로 어울려 살아감          │
│  · 먹이 주기, 돌보기                    │
│  · 가족 형성 → 아기 탄생                │
│  · seat(자리) 확장이 진행도             │
│  · 정원을 넘은 동물은 쉼터에서 대기      │
└───────────────────────────────────────┘
        ↑ 새 친구를 초대해 옴   ↓ 동료를 데리고 떠남
┌─ 원정 (세계 지도 → 지역 필드) ────────┐
│  · 지역을 골라 동료와 함께 여행         │
│  · 필드를 돌아다니며 활동               │
│  · 동료의 감각이 동물의 위치를 알려줌    │
│  · 교감·치료·도움 → 초대 / 재화 획득    │
└───────────────────────────────────────┘
```

**두 층이 서로를 먹여준다.**
사파리에 동물이 늘수록 원정에 데려갈 동료가 늘고, 원정이 성공할수록 사파리가 풍성해진다.
사파리에 이미 있는 동물이 필드에서 힌트를 주는 구조가 이 순환을 강화한다.

### 1.2 핵심 정서

**"무슨 동물이건 매력이 중요하다."**
이 게임의 재미는 결국 "저 동물을 우리 사파리에 데려오고 싶다"에서 나온다.
모든 설계 판단은 이 감정을 해치지 않는지를 기준으로 한다.

> **이 정서를 가장 자주 위협하는 것은 "수집이 벌이 되는 구조"다.**
> 동물이 늘어날수록 유지비·관리 부담·의무가 커지면 데려오고 싶은 마음이 식는다.
> 경제(§2.6)와 여행 로테이션(§3.7)을 설계할 때 이 함정을 반복해서 확인할 것.

---

## 2. 사파리 층

### 2.1 동물의 삶

동물은 메타데이터에 따라 자율적으로 행동한다. 플레이어가 지시하지 않는다.
물가 동물은 물가로 가고, 야행성은 낮에 자고, 무리 성향은 같은 종 옆에 붙는다.

> **설계 원칙: 데이터는 화면에 드러나야 한다.**
> 메타데이터 필드를 만들 때마다 "이게 화면에서 어떻게 보이는가"를 같이 정한다.
> 보이지 않는 필드는 만들지 않는다. 스프레드시트만 화려하고 게임이 밋밋해지는 것을 막는다.

### 2.2 돌봄과 상태

- **행복도/건강 상태가 있다.** 아프거나 심심하면 표정이 시무룩해지고, 움직임이 느려지고,
  친구와 안 놀고, 아기를 낳지 않는다.
- **돌보면 회복된다.**
- ❌ **죽지 않는다.** 수명으로도, 방치로도 죽지 않는다.

> **왜 죽음을 뺐는가**: 다마고치식 사망은 7살에게 지나치게 무겁다. 게다가 이 게임은
> 동물이 수십 마리라 "죄책감 관리 게임"이 되기 쉽고, 그러면 아이가 게임을 안 켜게 된다.
> **방치의 대가는 손실이 아니라 진행 정체**로 표현한다.

#### 시간이 흐르는 조건 (v2 확정)

- **게임을 켠 동안에만 시간이 흐른다.** 오프라인 시간은 흐르지 않는다.
  이 게임은 인터넷 없이 완전히 동작하는 것이 전제다.
- 따라서 "방치"란 **세션 안에서 다른 활동(주로 원정)을 하는 동안** 사파리를 못 돌보는 것을 뜻한다.
- ⏳ **원정 중에 사파리 시간이 흐를지는 미정.** 프로토타입에서 만들어보고 결정한다(§7).

#### 행복도가 오르는 경로는 둘이다

| 경로 | 내용 |
|---|---|
| 돌봄 | 먹이·물·치료. 사파리에서 상시 가능 |
| **여행** | 원정에 동료로 데려가면 행복해진다 (§3.7) |

기본 먹이는 무료다(§2.6). 굶어서 나빠지는 일은 없다.

### 2.3 성장 — grow 배열

동물은 **성장 시퀀스**를 따라 자란다. 시퀀스는 데이터로 정의한다.

```
growth: [
  { stage: "baby",  from_day: 0, to_day: 5,    sprite_set: "...",
    size: "5-10cm",  weight: "1kg", behavior_tags: [부모근처, 원정불가] },
  { stage: "adult", from_day: 5, to_day: null, sprite_set: "...",
    size: "20-30cm", weight: "8kg", behavior_tags: [무리, 원정가능, 번식가능] }
]
```

**스키마는 배열로 열어두되, 초기 콘텐츠는 2단계(아기/성체)만 채운다.**
배열이면 나중에 대표 종 몇 개만 3단계로 만들 수 있고,
스프라이트 물량이 코드 문제가 아니라 **데이터 선택 문제**가 된다.
(§5.2의 "본체는 능력을 갖고 팩은 필요한 만큼만 참조한다"와 같은 모양)

**규칙:**

- `stage`는 **열거형으로 고정**하고 **필수 태그**로 분류한다 — 모르는 값이면 로드 거부(§5.2).
  자유 문자열로 두면 청소년/juvenile/teen이 섞인다.
- **단계별 차이는 숫자 스탯이 아니라 행동 태그로 표현한다.**
  숫자는 화면에 안 보이지만 행동은 보인다. 아기는 부모 옆에 붙어 다니고,
  청소년(넣는다면)은 혼자 돌아다니되 무리엔 안 낀다.
- ❌ **아기는 성체를 축소해서 재활용하지 않는다.** 비정수 배율 축소는 nearest 필터에서
  픽셀이 깨지고, 눈(§4.2에서 매력의 90%)이 제일 먼저 망가진다.
  아기는 **더 작은 캔버스에 새로 그린다** (성체 24×24면 아기 16×16).
  실제 아기 동물의 특징도 단순 축소가 아니라 머리가 크고 다리가 짧고 눈이 큰 것이다.
- 아기는 애니메이션을 줄인다 (대기·이동만, 특징 동작 없음).
- **아기는 원정에 못 간다.** 이것이 성장을 기다리는 이유가 된다 — "빨리 커라, 같이 가자".

`from_day` 값은 §7의 "시간 흐름의 단위" 미결에 물려 있다.
기준으로 삼을 것: **한 세션(20~30분) 안에 성장 이벤트를 최소 한 번은 봐야 한다.**
아이가 성장을 못 보면 그 시스템은 없는 것과 같다.

### 2.4 가족·번식·정원

- 동물이 **결혼**해서 가족을 이룬다. 짝짓기 시기 같은 생태적 요소는 넣지 않고 **의인화**한다.
  한 번 맺어지면 가족이 형성되는 형태.
- 행복하지 않은 동물은 아기를 낳지 않는다 (돌봄의 의미를 여기서 준다).

#### seat(자리)와 쉼터 — v2 확정

- 사파리에 정해진 수의 **seat**가 있다.
- **seat는 원정으로 데려온 동물에게도 적용된다.** 정원 제한이 있어야 "꾸린다"는 관리 감각이 생긴다.
- 여분의 seat가 있을 때만 아기가 태어난다.
- **정원을 넘은 동물은 쉼터에서 대기한다.**

**쉼터의 규칙:**

| 항목 | 내용 |
|---|---|
| 형태 | 목록 UI가 아니라 **장소**다. 별도 화면에서 동물들이 어슬렁거린다 |
| 왜 | 7살에게 "목록 화면의 동물"은 존재하지 않는 동물이다. 원정에서 데려온 애가 화면에 안 보이면 §1.2가 그 자리에서 깨진다 |
| 시간 | 대기 중인 동물은 **시간이 정지**한다. 배고픔·행복도 변화 없고 번식하지 않는다. 관리 부담이 늘지 않는다 |
| 이동 | 사파리 ↔ 쉼터는 언제든 자유롭게, 되돌릴 수 있게. **방출은 없다** |
| 원정 | **쉼터의 동물도 원정 동료로 쓸 수 있다.** 그래야 벤치가 죽은 공간이 안 된다 |

**이로써 "꾸린다"가 두 층으로 갈라진다** — 사파리에 누구를 둘까(전시·번식),
원정에 누구를 데려갈까(로스터).

#### seat 확장은 도감 진척에 연동한다

seat가 꽉 찬 뒤에도 원정이 보상을 줘야 한다.
새 동물이 다 쉼터로 가면 원정 동기가 약해지므로, **새 종을 만날수록 자리가 늘어나게** 한다.
재화 소비(§2.6)도 확장 경로로 함께 둔다.

### 2.5 개체의 개성

같은 종이어도 우리 사파리의 이 아이는 특별하다 — 이 감각을 만드는 장치가 셋이다.

**(1) 능력치** — 개체마다 바운더리 안에서 랜덤한 값을 갖고, 번식하면 부모 값을 물려받는다.

두 조건을 지킬 것:
1. **능력치가 무언가를 결정해야 한다** — 유도 반경, 교감 게이지 속도 등.
2. **7살이 알아볼 수 있게 표시한다** — 숫자가 아니라 별 개수, 크기 비교, 아이콘.
   "민첩 47"은 7살이 못 읽는다.

**(2) 색** — 인덱스 컬러 + 셰이더 팔레트 교체, 부모 색 유전, 특수색(§4.3).

**(3) 여행 기록** ★ v2 신설 — 개체에 `visited_regions: []`를 둔다.

> 필드 하나로 애착이 생긴다. **"민첩 47"은 7살이 못 읽지만
> "얘랑 호주 갔었지"는 즉시 읽힌다.**
> 화면 자리도 자명하다 — 도감 개체 페이지의 "함께 간 곳", 또는 사파리에서 누르면 나오는 말풍선.

### 2.6 경제 ★ v2 신설

원정에 나갈 이유를 만들고, 돕는다는 행위에 보상을 준다.

#### 원칙: 유지비가 아니라 성장비

> **기본 먹이는 무료다.** 사파리에 풀이 자라고 물이 있다. 굶어서 나빠지는 일은 없다.
> **재화는 "더 좋게 만드는 것"에만 쓴다.**

| 무료 | 유료 |
|---|---|
| 기본 먹이·물 | 특별한 먹이 (행복도, 번식 촉진, 특수색 확률) |
| 기본 돌봄 | seat 확장 |
| | 시설 |
| | 원정 준비물 |

#### ★ 밥 주기는 배고픔을 막는 행위가 아니다 (v3)

사파리에 풀이 자라고 물이 있으므로 **동물은 굶지 않는다.**
그런데도 "밥 주기"가 있는 이유는 **행복도를 올리기 때문**이다.

이 구분을 놓치면 화면이 거짓말을 한다 — 굶지 않는데 머리 위에 배고픔 아이콘이 뜨면
아이는 "안 주면 어떻게 되지?"를 묻고, 답이 "아무 일도 안 일어남"이면 그 아이콘은 무의미해진다.

> 머리 위 아이콘은 **"밥 달라고 조르는" 애정 표현**이지 경고가 아니다.
> §4.6 의 시무룩 트리거에서도 배고픔을 뺀다.

**왜 이렇게 나누는가**: 먹이에 유지비를 붙이면 "돈 없음 → 밥 못 줌 → 시무룩 → 돈 벌러 나감 →
더 시무룩"이라는 하강 나선이 생기고, 7살에게 파산은 **되돌릴 수 없는 실패**다(원칙 2 위반).
더 나쁜 것은 동물이 많을수록 유지비가 커져서 **수집이 벌이 되는 것**이다(§1.2 직격).

이 구조에서는 재화가 0이어도 게임이 멈추지 않고 **진행만 느려진다** —
§2.2의 "방치의 대가는 진행 정체"가 경제에도 그대로 적용된다.

#### 획득: 치료와 도움

원정지에서 **다친 동물을 치료하거나 곤란한 동물을 도와주면** 재화를 얻는다.
돕고 보상받는 구조는 "사냥이 아니라 초대"라는 컨셉과 같은 결이다.
자원 채집(광물 캐기 등)은 넣지 않는다 — 정서가 다르다.

**부상·곤란의 원인은 사고 쪽으로 둔다.** 가시덤불, 폭풍에 다친 날개, 진흙에 빠짐 등.
포식·폭력은 넣지 않는다. 7살은 "누가 다치게 했어?"를 반드시 묻는다.
(육식/초식을 상성 축으로만 쓰고 실제 포식 장면은 없는 §3.5의 태도와 같다)

재화의 이름은 미정(§7).

---

### 2.7 집 — 게임을 켜면 처음 서는 곳 ★ v3.2 신설

타이틀에서 `CONTINUE` / `NEW GAME` 을 고르면 **필드가 아니라 집으로 들어온다.**

나간다 → 만난다 → 데려온다 → **집이 달라진다.** 이게 이 게임의 고리인데,
집이 없으면 "데려온다"가 어디로 가는지가 화면에 없다.
집은 §"진행은 누적으로 보이게"(원칙 3)를 **눈으로 보여주는 화면**이다.
동물이 늘고 사물이 늘어난 마당이 곧 진행도다 — 숫자로 보여줄 필요가 없다.

문서에서는 **사파리 층**(§2)이고 화면에서는 **집**이다. 같은 것이다.

#### 집은 보는 곳이지 해야 하는 곳이 아니다

> **사물은 고장나지 않고 유지비가 없다.** 물을 갈아줄 필요도, 청소할 필요도 없다.
> 동물이 늘고 사물이 늘수록 할 일이 늘어나면 §"수집이 벌이 되면 안 된다"(원칙 6) 위반이다.

같은 이유로 **HUD 는 둘뿐이다 — 재화와 자리.** 배고픔·청결·기분 게이지를 두지 않는다.
그것들이 생기는 순간 집이 할 일 목록이 되고, 아이가 게임을 켜기 싫어진다.
사물의 값어치는 수치가 아니라 **동물이 그걸 갖고 노는 걸 보는 것**이다.

#### 사물 — 사서 놓는다

§2.6 이 "돈은 더 좋게 만드는 것에만 쓴다 — 특별한 먹이, seat 확장, **시설**, 원정 준비물"
이라고 정해뒀다. **그 "시설"이 이 사물이다.** 새 시스템이 아니라 이미 있던 칸을 채운 것이다.

| 사물 | 누가 쓰나 (`for_tags`) | 값 |
|---|---|---|
| 공 | `호기심` · `대담함` | 40 |
| 밥그릇 | `잡식` · `육식` · `초식` | 20 |
| 물그릇 | 누구나 | 20 |
| 긁는기둥 | `나무선호` | 60 |
| 그늘막 | `겁많음` | 80 |
| 웅덩이 | `물가선호` | 50 |
| 방석 | 누구나 | 30 |

**`for_tags` 의 값은 전부 `tags.json` 에 이미 있다.** 사물을 위해 새 열거형을 만들지 않는다.
그 태그를 가진 동물이 그 사물을 쓰고, 비어 있으면 누구나 쓴다 —
**종 이름으로 분기하지 않으므로 종을 추가해도, 사물을 추가해도 코드를 안 고친다.**

#### 사물이 새 동작을 요구하지 않는다 ★ 비용이 갈리는 자리

동물이 사물을 갖고 노는 것이 이 기능의 심장인데, **여기서 스프라이트가 곱셈으로 터질 수 있다.**
종 × 사물로 놀이 동작을 만들면 7종 × 7사물 = 49벌이다. 절대 그렇게 하지 않는다.

> **사물 옆에서 그 동물의 기존 특징 동작(`idle_animation`)이 재생된다.**
> 공 옆에서는 개가 꼬리를 흔들고, 긁는기둥 옆에서는 고양이가 몸을 턴다.
> 사물은 **자리와 태그만 제공하고 동작은 동물이 들고 온다.**

이 규칙이 종당 장수를 사물 수와 **무관하게** 묶는다 (§4.5 종당 16장은 그대로다).
나중에 사물 전용 동작이 정말 필요해지면 그때 **한 종에 한 벌씩만** 추가한다.

#### 화면

640×360. 마당을 울타리가 두르고 **집이 위쪽 담장을 대신한다.**
집 문에서 대문까지 흙길이 곧게 나 있고, **대문은 열려 있다** —
닫힌 문은 "못 나간다"로 읽힌다. 나가는 곳이 어디인지가 화면에서 보여야 한다(원칙 3).

새로 그린 도트는 **12장**이다 — 집 1, 울타리 3(가로·세로·대문), 사물 7.
바닥 타일·나무·풀꽃은 전부 필드 것을 그대로 쓴다. 밤낮도 §6.3 팔레트 보간이 그대로 걸린다.

산출물: `sprites/build_home.py` → `sprites/extracted/home/` (`house` · `fence_*` ·
`obj_*` · `home.json`). `home.json` 이 사물의 `for_tags` 와 값을 들고 있다.

---

## 3. 원정 층

### 3.1 원정은 목표 지향이 아니다 (v2 핵심 변경)

**특정 동물을 노리고 나가는 것이 아니다.**
동료들과 어떤 지역으로 떠나서, 그곳에서 활동하다가 이런저런 동물을 만나고 교감한다.

```
세계 지도에서 지역 선택 (때때로 "저기서 누가 도와달래" 호출)
    ↓
동료 팀 편성 (3마리 내외) — 감각 × 적응의 절충
    ↓
필드 도착 — 돌아다니며 활동
    ↓
동료가 무언가를 감지 → 유도 → 그 위치로 이동 → 짜잔
    ↓
교감 / 치료 / 도움  (점유 시간 게이지)
    ↓
초대 성공 → 사파리 또는 쉼터로 / 재화 획득
```

**팀 편성의 의미가 바뀐다.** "누구를 잡을까"가 아니라
**"오늘은 어떤 종류의 발견을 가능하게 할까"**를 고르는 것이다.

#### ★ 팀 편성 화면이 스포일러가 되면 안 된다 (v3)

팀 편성 화면에서 "이 팀으로 만날 수 있는 동물"을 미리 보여주면,
**원정이 다시 목표 지향으로 돌아간다** — 화면이 이 절과 정반대로 말하게 된다.

> **미리보기는 도감에 이미 기록된 종만 보여준다.**
> 처음 가는 지역은 전부 물음표다.

그러면 미리보기가 스포일러가 아니라 **§3.6 "알아낸 건 도감에 자동 기록된다"의 화면 표현**이 된다.
아이는 "가봤더니 얘가 있더라 → 다음엔 개를 데려가면 되겠네"를 배우고,
처음 가는 곳은 여전히 뭐가 나올지 모른다.

#### 왜 지금 나가는가

목표가 없으면 출발 이유가 빈다. 세 가지가 함께 작동한다:

1. **재화** — 치료·도움 보상 (§2.6)
2. **도감** — 지역마다 나오는 동물군이 다르다
3. **여행** — 동료가 즐거워한다 (§3.7)

7살에게 가장 확실한 것은 **"오늘 저기서 누가 도와달래"** 같은 가벼운 호출이다.
목적지가 생기고, 가면 반드시 뭔가 있고, 가는 길에 다른 걸 만난다.
퀘스트 시스템으로 만들 필요 없이 **데이터 한 줄**(지역 + 요청 태그 + 보상)이면 된다.

### 3.2 필드는 실제 공간이다 ★ v2 핵심 변경

**동물은 화면에 안 보여도 필드를 실제로 돌아다니고 있다.**
"발견 확률"은 통계 수치가 아니라 시뮬레이션 위에서 일어나는 일이다.

이 결정으로 **v1 §3.4의 "미결: 플레이어의 조작"이 해소된다.**
플레이어는 필드를 직접 이동한다 — 이동 조작이 이 게임의 손맛이다.
(v1의 세 후보 중 "탐색 구간을 직접 돌아다니기"로 착지)

### 3.3 동료의 감각과 유도 ★ v2 신설

**동료가 무언가를 감지하고 플레이어를 그쪽으로 이끈다.**
개가 너구리 냄새를 맡고 킁킁대며 방향을 알려주면, 다가가서 만난다.

> **왜 이게 좋은가**: 규칙이 **대사가 아니라 행동으로** 보인다.
> 아이는 설명을 읽지 않아도 "쟤가 저기 보고 있네"를 안다. 원칙 3이 UI 없이 충족된다.

태그 조합으로 완전히 자동화된다. 종별 스크립트가 하나도 필요 없다:

```
동료 senses: [후각]  ×  대상 traits: [냄새강함]   → 유도 발생
동료 senses: [청각]  ×  대상 noise: 시끄러움      → 유도 발생
동료 senses: [시야]  ×  대상 habitat: 초원        → 넓은 반경 유도
```

**표현 비용이 거의 없다**: 동료가 그 방향을 보고 코를 킁킁대는 2프레임
(= §4.2의 "종마다 대표 동작 하나"가 장식이 아니라 기능이 되는 지점)
\+ 화면 가장자리 방향 화살표.

**동료마다 감각이 다르므로 "누굴 데려갈까"가 진짜 선택이 된다.**
코가 좋은 애를 데려가면 숨은 애들이 나오고, 눈이 좋은 애를 데려가면 멀리 있는 게 보인다.

#### ★ 감각마다 성격이 다르다 — `sense_profile` (v3)

감각이 이름만 다르고 하는 일이 같으면 선택이 성립하지 않는다.
`tags.json` 의 `sense_profile` 이 감각마다 네 가지를 다르게 준다.

| | `reveals` | `range_scale` | 시간대 | 지형 | 성격 |
|---|---|---|---|---|---|
| **후각** | false | 1.5 | 영향 없음 | 영향 없음 | **멀리서 쫓는다.** 대신 방향과 단서까지만 |
| **청각** | false | 1.1 | 영향 없음 | 숲 ×0.8 | 중간. 역시 단서까지 |
| **시야** | **true** | 0.7 | 여명 ×0.6, 밤 ×0.35 | 숲 ×0.35, 바위 ×0.6 | **가깝지만 몸이 보인다.** 밤·숲에서 무력해진다 |

```
반경 = guide_radius × 개체 sense_range × range_scale × daypart_scale × terrain_scale
                       (개체차 §2.5)     (감각)        (시간대)        (대상이 선 지형)
```

**`reveals` 가 이 표의 핵심이다.**
시야로 잡으면 그 자리에서 **몸이 보이고**, 후각·청각으로 잡으면 **단서만 뜬다** —
누구인지는 다가가야 안다. 그래서 "코로 쫓아가서 눈으로 확인한다"가 자연스러운 순서가 된다.

**지형이 감각을 깎는다.** 숲에 있는 동물은 눈으로 못 찾고 코로 찾아야 한다.
이것이 §3.7 의 "감각 × 적응" 절충에 세 번째 축을 준다 — **어디를 뒤질 것인가.**

#### 가까이 가면 그냥 보인다 — `reveal_radius`

감각과 무관하게, 일정 거리 안에 들어오면 동물의 몸이 보인다.
동료가 하나도 없어도 **돌아다니는 것만으로 만날 수 있어야** 하기 때문이다(§3.6 안전장치).
동료는 그 거리를 늘려줄 뿐이다.

> ⚠️ `activation_radius`(풀 AI 승격, §6.4)가 가장 먼 감각 반경보다 좁으면
> 코가 닿는 곳에 아직 시뮬이 안 깨어 있어 유도가 헛돈다.
> 본체가 실제 반경을 계산해 모자라면 올려 쓰고 경고를 남긴다.

### 3.4 교감·치료·도움 — 점유 시간 게이지 ★ v2 신설

동물을 만난 뒤의 상호작용은 **뭉뚱그린 모션 + 점유 시간 게이지** 하나로 통일한다.
교감(초대), 치료, 도움이 전부 같은 구조를 쓴다.

#### 게이지가 상성을 보여주는 UI가 된다

게이지 길이를 태그에서 뽑는다 — 겁 많은 동물은 길고, 맞는 동료가 있으면 짧아진다.
그러면 **별도 UI 없이 상성이 읽힌다.** "어? 이번엔 금방 됐네"
7살에게 진행 바는 완전히 읽히는 몇 안 되는 UI라 특히 값어치가 있다.

원정 중 사파리 시간이 흐르게 한다면(§7), **점유 시간이 원정의 유일한 자원**이 된다.

#### 모션은 플레이어·동료 쪽에만 둔다 (물량 결정적)

> 종별 치료 모션을 만들면 20종 × 2프레임 × 4방향 = **160장 추가**다.
> **대상 동물은 기존 대기 스프라이트를 그대로 쓰고, 그 위를 이펙트로 덮는다** —
> 반짝임, 하트, 먼지 구름. 종별 추가 **0장**.

이펙트는 §6.2에서 이미 별도 레이어(Linear 필터, 자유 좌표)로 분리해뒀으니 도트 규칙에 안 걸리고,
파티클이라 손으로 그릴 것도 거의 없다.
**이펙트 하나로 교감·치료·도움·유인을 다 커버**한다. 색만 바꿔도 다르게 읽힌다.

#### 게이지는 중간에 끊기지 않는다

방해 요소나 실패 조건을 넣지 않는다.
"거의 다 찼는데 놓침"은 7살에게 되돌릴 수 없는 실패로 느껴진다(원칙 2).
**시작하면 반드시 완료.** 취소는 플레이어가 명시적으로 누를 때만.

### 3.5 상성 시스템

**축을 여러 개 두되, 각 축은 단순하게.**
"육식↔초식" 하나만 있으면 금세 단순해지고, 복잡한 상성표는 7살이 감당 못 한다.

| 축 | 규칙 |
|---|---|
| 먹이 유형 | 초식/육식 — 육식동물이 가면 초식동물이 무서워함 |
| 크기 | 큰 동물이 오면 작은 동물이 위축됨 (실측 임계값에서 유도, §5.3) |
| 소리 | 시끄러운 동물이 있으면 겁 많은 동물이 안 나옴 |
| 활동 시간 | 야행성 동물은 야행성 동료가 있어야 찾을 수 있음 |
| 서식지 | 물가 동물은 수영 가능한 동료가 있어야 접근 |

각 축은 규칙 하나뿐이지만 조합하면 경우의 수가 충분하다.
그리고 **아이가 하나씩 발견하는 재미**가 생긴다 — "아, 얘는 밤에만 나오는구나"가 핵심 쾌감.

상성의 결과는 **게이지 길이와 유도 반경**으로 나타난다(§3.3, §3.4).

#### 지형 (v3)

필드는 **초원 바탕에 숲·물가·바위 덩어리가 뿌려진** 형태다.
지형은 두 가지 일을 한다:

1. **감각 반경을 깎는다** (§3.3 `terrain_scale`) — 숲에 있으면 눈에 안 띈다
2. **야생동물이 자기 `habitat` 위에서 시작한다** — 수달은 물가에, 청설모는 숲에

그래서 "어디를 뒤질까"와 "누굴 데려갈까"가 맞물린다.
숲을 뒤질 거면 코가 필요하고, 초원이면 눈이 낫다.

**부가가치**: 아이가 게임하면서 실제 동물 지식을 배운다. 초식/육식, 무리 생활, 야행성 등.

### 3.6 안전장치

몰라서 못 하면 7살은 답답해서 그만둔다. 반드시 다음을 갖춘다:

- **관찰하면 알 수 있게** — 동료의 행동, 필드의 흔적이 곧 힌트다. 추론이 아니라 관찰로 풀리게
- **도감에 자동 기록** — 한 번 알아낸 건 적히고, 다음엔 안 헤맨다
- **실패해도 손해 없게** — 상성이 안 맞으면 게이지가 길어지거나 유도가 덜 될 뿐이다.
  **실패가 아니라 느려짐으로 표현한다**
- **동료가 힌트를 준다** — 사파리가 풍성해질수록 원정이 쉬워지는 순환

> ⚠️ 확률을 7살에게 그대로 노출하지 말 것.
> "제대로 했는데 안 나왔다"가 반복되면 규칙을 못 읽고 그만둔다.
> 진행은 항상 **누적으로 보이게** 한다(게이지, 동료의 반응).

### 3.7 여행이라는 프레이밍 ★ v2 신설

**동료를 데리고 어디에 간다는 것은, 동료에게도 즐거운 놀이이자 여행이다.**
일하러 끌고 가는 것이 아니다.

이 프레이밍이 여러 문제를 동시에 푼다:

- **행복도의 두 번째 경로**가 생긴다(§2.2). 지금까지는 먹이뿐이었다.
- **원정 중 사파리 방치가 벌이 아니게 된다.** 데려간 애는 행복해지고 남은 애는 심심해진다.
  손실이 아니라 **순번의 문제** = "나도 데려가줘". 원칙 1과 정확히 맞는다.
- **팀 인원 제한에 세계관 안의 이유가 생긴다.** "차에 세 자리뿐"이 아니라
  **누군가는 사파리를 지켜야 한다.** 남은 애는 방치된 애가 아니라 **집 보는 당번**이 된다.
  당번에게 작은 역할(그동안 아기를 돌봤다, 뭔가를 발견했다)을 주면 남는 것도 선택이 된다.

#### 지역 적응 — 팀 편성 축이 하나 늘고 비용은 0이다

`habitat` 태그를 재사용한다. 물가 동물을 사막에 데려가면 시무룩하고, 숲 동물은 숲에서 신난다.

```
감각(무엇을 발견할 수 있나)  ×  적응(이 지역에서 즐거운가)
```

최적해가 지역마다 달라지므로 편성이 매번 같은 답이 안 된다.
그리고 아이에게는 진짜 지식이다 — "펭귄은 사막에 못 가".

⚠️ **시무룩한 동료가 실패를 만들면 안 된다.** 유도를 덜 해서 발견이 조금 느려지는 정도까지만.

#### ⚠️ 로테이션을 의무로 만들지 말 것

심심함에 페널티를 붙이면 "모든 애를 골고루 데려가야 하는 관리 업무"가 된다.
seat가 20~30으로 늘면 물리적으로 불가능해지고, 그 순간 **수집이 다시 벌이 된다**(§1.2).

> 심심함은 **표정과 말풍선으로만** 표현한다.
> 능력치·번식·발견율에 영향을 주지 않는다.
> 아이가 "쟤 삐졌네, 다음엔 데려가야지" 하고 느끼면 그게 동기고, 안 데려가도 잃는 건 없다.
> **채찍이 아니라 당근으로만.**

#### 미루는 것: 동료 간 관계

"같이 여행한 동물끼리 친해진다"는 관계 그래프가 N² 데이터에 화면 표현이 애매해서
부록 A의 "보이지 않는 필드" 함정에 정확히 걸린다.
굳이 넣는다면 가장 싼 형태 하나만 — **같이 다녀온 쌍은 사파리에서 서로 옆에 잘 붙어 있는다**
(기존 무리 행동 로직 재사용, 새 시스템 0). 그 이상은 지금 넣지 않는다.

### 3.8 시작 지역과 첫 동물 ★ v2 확정

**시작 지역은 "집 근처의 뒷산과 냇가"다.** 실제 지명을 박지 않고 **생태만 가져온다.**

만드는 사람이 서울에 살고 첫 플레이어가 그 집 아이이므로, 첫 지역은 한국 중부의
산·하천 생태(청설모, 너구리, 수달, 고라니, 삵, 까치)를 기준으로 한다.

**왜 지명을 박지 않는가**: §7의 "실존 동물 vs 가상 동물"이 아직 미결이다.
`북한산`·`한강` 같은 고유명사를 넣으면 그 미결을 실존 쪽으로 강제 확정하게 된다.
**생태는 실존에서 가져오되 이름은 무명("뒷산", "냇가")으로 두면 양쪽이 다 열려 있다.**

**왜 집 근처인가:**

- **"우리 동네에 진짜 수달이 산대"는 7살에게 강력하다.** §3.5의 부가가치(실제 동물 지식)가
  가장 잘 작동하는 지점이 자기가 아는 장소다.
- **사파리(집)와 첫 필드의 생태가 같아서 배경·타일·식생 애셋이 겹친다.**
  §6.5에서 지적한 "지역 팩에 맵이 딸린다"는 부담을 첫 지역에서는 거의 안 진다.
- **세계로 뻗어나가는 구조가 성립한다.** 시작이 한국이어야
  "호주의 친구들이 도착했어요!"(§6.5)라는 확장이 말이 된다.

#### 프로토타입 6종

| 종 | 역할 | diet / activity | size | 실루엣 특징 |
|---|---|---|---|---|
| 개 | **동료**(후각) | 잡식 / 주행성 | 중 | 집에서 같이 온 첫 친구 |
| 고양이 | **동료**(청각·시야) | 육식 / 박명성 | 중 | 뾰족 귀 · **짧은 꼬리** · 보송한 윤곽 |
| 청설모 | 대상 | 초식 / 주행성 | 소 | 큰 꼬리 |
| 너구리 | 대상 | 잡식 / 야행성 | 중 | 눈가 무늬, 둥근 몸 |
| 수달 | 대상 | 육식 / 박명성 | 중 | 긴 몸, 물가 |
| 고라니 | 대상 | 초식 / 박명성 | 대 | 긴 다리 |

**고양이가 프로토타입에 들어가는 이유는 장식이 아니라 기능이다.**
개(후각)와 감각이 달라서 **§3.3의 "누굴 데려갈까가 진짜 선택인가"를 데모에서 실제로 답할 수 있다.**
동료가 하나뿐이면 그 질문 자체가 성립하지 않는다.
`trait_to_sense` 매핑(§5.4)을 통과시키면 대비가 이렇게 나온다:

```
개(후각)        → 너구리, 수달        ✅   청설모·고라니는 못 찾는다
고양이(청각·시야) → 청설모, 너구리, 수달, 고라니
```

즉 **"개로는 못 찾는 애가 있다"**가 데이터에서 자동으로 나오고,
아이가 고양이를 데려가면 풀린다. 이 대비가 데모의 핵심 질문이다.
그래서 §8의 "감각은 후각 하나만"은 **후각 + 청각·시야**로 확장한다.

프로토타입에서 켜는 상성 축 2개(먹이 유형 + 활동 시간)를 6종이 모두 커버하고,
크기도 소·중·대가 다 나오며 실루엣이 서로 겹치지 않는다.

> **고라니는 전 세계 개체의 대부분이 한국에 있다.** 아이에게 "우리나라에만 이렇게 많대"는
> 그 자체로 재밌는 사실이고, 부가가치(§3.5)의 좋은 예다.

> **고양이는 개와 같은 32×32 캔버스다.** 실루엣이 갈리는 지점은 이 셋이다 —
> **뾰족 선 귀**(개는 처진 귀), **아주 짧은 꼬리**(한국 길고양이에 흔하다),
> **보송보송한 윤곽**(매끈한 타원이 아니라 털 뭉치가 삐져나온다).
> 꼬리가 짧으므로 구분을 귀와 털이 떠맡는다.
>
> 귀엽게 만드는 것은 비례다 — **머리를 크게, 몸을 둥글게, 다리를 짧게.**
> 날씬하게 그리면 고양이가 아니라 사슴이 된다. (실제로 한 번 그렇게 됐다)

#### 지역의 나머지 5종 (데이터에만)

| 종 | 넣는 이유 | 주의 |
|---|---|---|
| 까치 | 소리 축 + 흑백으로 색 다양성 | 이동은 나는 게 아니라 **통통 뛰기**(`biped_hop`)라 장수는 네발동물과 같다 |
| 참새 | 가장 흔한 동물. seat 정원 감각 시험용 | **태그가 까치와 거의 겹친다** — 규칙상 구분이 안 되고 차이는 크기·실루엣·색에서만 온다 |
| 뱀 | 실루엣이 압도적으로 명확. 후각 동료로 유용 | `serpentine` 리그. 장수는 같지만 좌우 flip이 안 먹어 **난이도가 높다**. 무섭지 않게 — 눈을 크고 둥글게 |
| 삵 | 육식 축 확장 | ⚠️ 고양이와 겹칠 뻔했으나, **고양이 꼬리를 짧게 하면서 갈렸다** — 삵은 길고 굵은 줄무늬 꼬리로 그린다 |
| 두꺼비 | `senses`가 빈 종 — 동료가 될 수 없는 경우의 검증 | |

> **뱀이 스키마의 한계를 하나 드러냈다.** 실측 85cm라 `size_class` 파생 계산은 `대`가 나오는데,
> 부피는 작아서 "큰 동물이 오면 작은 동물이 위축"(§3.5) 축에 걸리면 안 된다.
> **길이만으로는 부피를 못 잡는다** — `manual_override_allowed`(§5.3)의 첫 실제 사용 사례이고,
> 오버라이드한 종은 임포터가 경고를 남기되 로드는 통과시킨다.

⚠️ **약점: 포유류 다섯이 전부 갈색 계열이다.** 화면이 단조로울 수 있다.
까치(흑백)와 참새가 색을 살려주고, 개체별 팔레트 교체(§4.3)가 나머지를 보완한다.

#### 물량

11종 × 22장 = **242장**. 그중 프로토타입 6종은 **132장**이다.
하루 2~3장이면 프로토타입 분량이 약 9주.

> ⚠️ **여기서 종을 더 늘리면 부록 A의 "데이터를 먼저 다 채운다" 구간에 들어간다.**
> 11종은 태그 스키마를 검증하기에 충분하고 그 이상은 아직 필요 없다.

---

## 4. 아트 방향

### 4.1 기본 방침: 데이터는 치밀하게, 그래픽은 단순하게

**콘텐츠가 스프라이트가 아니라 데이터에 들어간다.** 1인 개발에서 100종을 감당할 수 있는 거의 유일한 길.

| 방향 | 판정 |
|---|---|
| 해상도를 낮춘다 (24×24 등) | ✅ 특징만 남아 캐릭터성이 오히려 강해짐 |
| 애니메이션 프레임을 줄인다 (2~4장) | ✅ 뚝뚝 끊기는 움직임이 귀엽게 읽힘 |
| 형태를 뭉갠다 | ❌ 모든 동물이 비슷한 덩어리가 되어 구분 불가 |

### 4.2 단순한데 매력 있게 만드는 요령

- **실루엣만으로 구분되게** — 색을 다 빼고 검은 덩어리로 만들었을 때 어떤 동물인지 알아볼 수 있어야 한다.
  픽셀 캐릭터 디자인의 제1원칙. 목이 길거나, 귀가 크거나, 몸이 둥글거나.
  이 원칙은 **측면**에서 가장 잘 성립하므로, "알아보는 순간"은 항상 측면으로 고정한다(§4.5)
- **눈이 매력의 90%** — 픽셀 2~3개짜리 눈인데 위치와 크기만으로 인상이 완전히 바뀐다
- **종마다 대표 동작 하나** — 껑충 뛰기, 목 늘이기, 킁킁대기. 프레임 2장이면 충분한데 확 살아난다.
  (§3.3에서 이 동작이 유도 표현으로 쓰인다 — 장식이 아니라 기능이다)
- **"무엇을 하고 있는가"가 매력을 만든다** — 그림이 단순해도 물을 마시고, 옆 동물과 붙어 있고,
  밤엔 자고 있으면 살아있어 보인다

### 4.3 색 바리에이션과 유전 (상세는 추후 논의)

**작업량을 거의 안 늘리면서 콘텐츠를 크게 늘리는 방향.**

- **스프라이트를 인덱스 컬러로 그린다** — 런타임에 셰이더로 팔레트 교체.
  그림 한 장으로 무한한 색 변형이 나온다
- **눈 모양은 별도 레이어** — 몇 종만 따로 그려 얹으면 조합이 곱해진다
- **색 유전** — 부모의 색을 물려받는다. "엄마 색이랑 아빠 색이 섞였네"는 7살에게 즉시 이해되는 인과
- **특수색(희귀 변이)** —
  ⚠️ 확률을 극단적으로 희박하게 두지 말 것. 평생 한 번 볼까 말까 한 확률은 7살에게 존재하지 않는 것과 같다

> **seat 시스템과의 시너지**: 자리가 한정돼 있으니 "어떤 아이를 남길까"가 선택이 되고,
> 색 유전이 있으면 그 선택에 목적이 생긴다.

### 4.4 도트 규칙

- 전체 팔레트 32~48색으로 제한
- 외곽선은 검정 대신 해당 색의 어두운 톤
- 바닥은 어둡고 차분하게 / 동물은 밝고 선명하게
- 발밑 타원 그림자 필수
- **렌더 스케일은 정수/반정수만** (1x, 1.5x, 2x, 3x). 비정수 배율은 nearest에서 픽셀이 깨진다.
  실측 크기(§5.3)는 이 몇 개의 스케일 버킷으로 양자화해서 화면에 반영한다

### 4.5 뷰 각도와 방향 ★ v2 확정

> **3/4 뷰 · 이동은 동서남북 4방향 · 사파리와 원정 필드는 동일한 뷰.**

**왜 4방향인가.**
필드 탐색이 이 게임의 조작이 됐고(§3.2), 무엇보다 **동료의 유도(§3.3)가 본질적으로 방향을 가리키는 연출**이다.
좌우로만 설 수 있는 개는 "저쪽이야"를 표현하지 못한다.
이동이 4방향이면 유도도 4방향이어야 하고, 그것이 이 게임의 핵심 동사다.
좌우 2방향으로는 **필드를 탐색한다는 감각 자체가 서지 않는다.**

**사파리와 필드를 같은 뷰로 통일**하므로 스프라이트 한 벌로 두 층을 다 쓴다.

#### 비용은 생각보다 작다 — 모든 동작을 4방향으로 그리지 않기 때문이다

| 동작 | 방향 수 | 이유 |
|---|---|---|
| 이동 | **4방향** | 조작과 유도의 핵심 |
| 대기 | 좌우 2방향 (남·북은 이동 프레임 1을 재활용) | 멈춰 선 동물의 방향은 덜 중요하다 |
| 특징 동작 | 1방향 (측면) | "나를 봐" 하는 순간이라 항상 제일 잘 보이는 각도로 |

**★ E/W 는 미러다 — 실제로 그리는 것은 S·N·E 뿐이다.** (v3 정정)

```
성체   이동 2프레임 × 3방향(S·N·E) = 6장   ← W 는 E 를 flip_h
       대기 2프레임 × 1방향(E)      = 2장   ← W 는 flip_h
       특징 동작 2프레임             = 2장
       ─────────────────────────────── 10장
아기   이동 2프레임 × 3방향          = 6장   ← 대기는 이동 프레임 재활용, 특징 없음
       ─────────────────────────────── 6장
                              종당  16장
```

| 규모 | 총 장수 |
|---|---|
| 6종 (프로토타입) | **96장** |
| 11종 (시작 지역 전체) | **176장** |
| 20종 | 320장 |
| 3단계 성장 적용 시 | 종당 22~24장 (초기엔 적용하지 않음, §2.3) |
| *(참고)* 미러 없이 4방향을 다 그리면 | *종당 22장, 11종 242장* |

⚠️ **좌우 비대칭 무늬가 있는 종은 미러가 안 된다.** 그 종만 E/W 를 따로 그린다.

**대상 동물의 상호작용 모션은 0장이다**(§3.4 — 이펙트로 처리).

도트 초보 기준 하루 2~3장이면 **종 하나가 약 일주일**이다.
이 환산은 툴 제작·기능 추가를 판단할 때의 기준 단위로 쓴다.

#### 실루엣 원칙과의 충돌을 어떻게 푸는가

§4.2의 "실루엣만으로 종이 구분되어야 한다"는 **측면에서 가장 잘 성립한다.**
고라니의 긴 다리, 수달의 긴 몸, 청설모의 큰 꼬리는 다 옆에서 보인다.
정면·후면 실루엣은 대개 기둥이나 덩어리가 된다.

해법은 **"알아보는 순간"과 "움직이는 순간"을 분리하는 것**이다.

| 순간 | 방향 |
|---|---|
| 첫 만남 연출, 초대, 도감, 팩 티저 실루엣(§6.5) | **항상 측면 고정** |
| 필드·사파리에서 돌아다닐 때 | 4방향 |

아이가 "이게 무슨 동물이지?"를 판단하는 순간은 전부 측면이다. 이동은 알아보는 순간이 아니다.

그리고 **3/4 뷰라서 남향·북향이 완전한 정면·후면이 아니라 비스듬하다.**
순수 탑다운보다 실루엣이 훨씬 덜 무너진다 — 3/4 뷰를 고르는 이유 중 하나다.

> ⚠️ **북향(뒷모습)에는 눈이 없다.** §4.2에서 매력의 90%라고 한 그것이 사라진다.
> 뒷모습은 **꼬리·귀·등무늬**로 특징을 살려야 한다.
> 종을 디자인할 때 "이 동물의 뒤통수에서 뭐가 보이는가"를 같이 정할 것.

#### 방향 수는 나중에 늘릴 수 있다

방향 구성은 `sprite_set` 데이터에 들어가므로, 대표 종 몇 개만 대기·특징 동작을 4방향으로 늘릴 수 있다.
성장 시퀀스(§2.3)와 같은 철학이다 — **본체는 능력을 갖고, 데이터가 얼마나 쓸지 정한다.**

### 4.6 스프라이트 구성 — 레이어 조합 ★ v2 확정

동물 스프라이트는 **한 장이 아니라 레이어의 조합**이다.

```
[그림자]       발밑 타원 — 공용 1장, 크기만 스케일
[몸통]         종별 22장, 4방향, 팔레트 교체 대상 (§4.3)
[눈]           공용 세트, 표정별 × 정면/측면.  북향에서 숨김
[입]           공용 세트, 표정별 × 정면/측면.  북향에서 숨김
[이동 이펙트]  공용 파티클 — 먼지·물방울 (§6.2 이펙트 레이어)
[표현 아이콘]  공용 도트 아이콘 — 머리 위 (감정·상태·감각)
```

> **이 구성의 핵심 이점: 종당 물량이 하나도 늘지 않는다.**
> 눈·입·아이콘·이펙트가 전부 공용이라 종별로 그리는 것은 몸통 22장 그대로다.
> 대신 **종 수와 무관한 고정비**가 한 번 생긴다.

#### 눈·입은 반드시 공용 세트로 만든다

종별로 그리면 즉시 터진다 — 눈 5표정 × 2각도 = 10장, 입도 10장이면 종당 20장 추가로
몸통(14장)보다 커진다.

```
눈  스타일 4종 × 표정 5 × 2각도(정면/측면) = 40장
입  스타일 3종 × 표정 5 × 2각도            = 30장
                                     ─────  70장  ← 20종이든 100종이든 고정
```

종별로 필요한 것은 그림이 아니라 데이터다:

```
sprite_set {
  eye_style: "round",  mouth_style: "small"
  eye_anchor:   { front: [12, 9],  side: [15, 9]  }
  mouth_anchor: { front: [12, 13], side: [16, 13] }
}
```

**북향은 공짜로 절약된다** — 뒷모습엔 눈·입이 없으니 레이어를 숨긴다.
그래서 각도가 4개가 아니라 정면/측면 **2개**면 된다.
§4.5의 "북향엔 눈이 없다"는 제약이 여기서는 이득이 된다.

#### ★ 걷기 바운스는 스프라이트가 아니라 노드로 처리한다

레이어를 나눌 때 **실질적인 난이도는 물량이 아니라 앵커다.**
몸이 위아래로 통통 튀는 걸 스프라이트 안에 그리면 프레임마다 얼굴 위치가 달라지고,
프레임별 앵커를 데이터로 관리하기 시작하면 종당 데이터가 8배가 되고 손으로 맞추다 지친다.

> **걷기의 상하 바운스를 스프라이트 안에 그리지 말고, 노드 Y좌표로 처리한다.**
> 얼굴 파츠를 몸통의 자식 노드로 두면 부모를 따라 자동으로 움직인다.
> 걷기 2프레임은 **다리 모양만** 바꾸고, 몸 전체의 오르내림은 노드가 한다.

그러면 프레임별 앵커가 필요 없고 종당 앵커는 정면·측면 두 쌍이면 끝난다.
⚠️ 픽셀 퍼펙트이므로 **노드 Y는 정수 픽셀로 스냅**해야 한다. 안 하면 얼굴이 미세하게 떨린다.

#### 표정 — 추가할 때 트리거를 같이 정한다

규칙 없는 표정은 화면에 안 나온다 = §2.1의 보이지 않는 필드다.

| 표정 | 트리거 |
|---|---|
| 기본 | 평상시 |
| 기쁨 | 밥 먹은 직후, 여행 중 적응 지역(§3.7), 가족 형성 |
| 조름 | 밥을 오래 안 줬을 때. **경고가 아니라 애정 표현이다**(§2.6) |
| 시무룩 | 아픔·심심함 (§2.2). **배고픔은 넣지 않는다** — 굶지 않으므로(§2.6) |
| 졸림 | 야행성이 낮에, 주행성이 밤에 |
| 놀람 | 발견되는 순간, 유도 성공 |

**새 표정을 추가할 때는 트리거 조건을 반드시 같이 정한다.**

#### 표현 아이콘 계층 — §3.3이 여기로 흡수된다

7살에게 텍스트 없이 상태를 전달하는 최선의 수단이고,
무엇보다 **동료의 유도 표현(§3.3)이 별도 시스템 없이 여기서 해결된다.**
개가 킁킁대는 것은 머리 위의 코 아이콘이고, 감각 종류(후각·청각·시야)는 아이콘 교체로 끝난다.

기본 세트 20~30개면 게임 전체를 커버한다:
`❗발견  💤잠  🍖배고픔  ❤️애정  💧아픔  🎵신남  ❓궁금  👃냄새  👂소리  👀시야`

> ⚠️ **유니코드 이모지를 그대로 쓰지 말 것.** 폰트 이모지는 해상도와 팔레트가 게임과 안 맞아
> 화면이 즉시 튄다 — §4.6의 "아이 그림을 스캔해 그대로 넣으면 스타일이 튄다"와 같은 문제다.
> **16×16 도트로 직접 그린다.**

#### 이동 이펙트 — 파티클로, 종별 애셋 0장

§6.2의 이펙트 레이어를 그대로 쓴다(§3.4의 교감 반짝임과 같은 레이어).
작은 점 텍스처 하나로 먼지가 되므로 실질 애셋은 1~2장이다.

**종별 차이는 태그에서 뽑는다** — `size_class` 대형이면 먼지 크게,
`habitat` 물가면 물방울, 무거운 종은 착지 시 화면 흔들림. 종별 애셋 0장으로 개성이 생긴다.

파티클을 픽셀 그리드에 스냅할지는 취향 문제다. 프로토타입에서 눈으로 보고 정한다.

#### 눈 그리기 (v3 정정 — v2 의 규칙을 철회한다)

⚠️ **눈 둘레에 밝은 외곽을 두르지 않는다.**
v2 는 "검은 몸통에 검은 눈"을 흰 테로 풀려 했는데, 그러면 **눈이 두 배로 커져
개구리가 된다.** 얼굴이 눈에 다 먹힌다.

**눈은 어떤 털색에서도 검다.** 검은 개도 눈은 검다 — 색을 뒤집을 일이 아니다.
어두운 털에서 눈이 읽히게 하는 것은 **동공 안의 반짝임 1픽셀**(catchlight)이다.
테두리가 아니라 눈 안쪽에 밝은 점 하나를 찍는다.

| | |
|---|---|
| 32px 동물 몸통 | 눈 **2~3px** + 반짝임 1px |
| 사람 얼굴 (9px 폭) | 눈 **1px 폭** — §4.7 |

**표정은 같은 눈의 변형이어야 한다.**
기본에 없던 눈썹 같은 것을 표정에서만 새로 그리면 다른 얼굴이 된다.

- 기본 — 점 + 반짝임
- 기쁨 — 눈이 위로 휜다
- 시무룩 — 눈꺼풀이 덮여 납작해지고 바깥이 처진다

눈 색은 종 팔레트의 인덱스 7·8 이므로, 눈 레이어도 몸통과 같은 팔레트 셰이더를 통과시킨다.
그래야 특수색 개체에서 눈만 따로 놀지 않는다.

#### 물량 정리

| 구분 | 장수 | 종 수에 비례? |
|---|---|---|
| 종별 몸통 | 22장/종 | ✅ |
| 눈·입 세트 | 70장 | ❌ 고정 |
| 표현 아이콘 | 20~30장 | ❌ 고정 |
| 그림자·파티클 | 3~4장 | ❌ 고정 |

고정비 약 100장 ≈ 동물 4.5종어치. 한 번 만들면 100종까지 안 늘어나는 투자다.

### 4.7 플레이어 캐릭터 ★ v3 신설

**여자아이. 32×48 — 2타일 폭, 3타일 높이.** 개(32×32)보다 1타일 높다.
사람은 종이 아니므로 `size_class` 버킷(§5.3)에 들어가지 않는다.

#### 실루엣은 양갈래와 배낭이 만든다

- **양갈래인 이유는 좌우 대칭이라 E/W 미러가 깨지지 않기 때문이다**(§4.5).
  포니테일 하나면 미러할 때 머리가 반대쪽으로 넘어간다.
- **북향에는 얼굴이 없으므로**(§4.6) **배낭이 뒷모습의 정체성**을 맡는다.
  뒤에서 보이는 것이 머리와 옷뿐이면 누구인지 알 수 없다.

#### `special` 은 교감 모션이다

§3.4 에서 "상호작용 모션은 플레이어 쪽에만 둔다"고 했으므로,
플레이어의 특징 동작 자리는 **쪼그려 앉아 손을 내미는 동작**이 차지한다.
대상 동물은 대기 스프라이트 그대로 + 이펙트다.

#### ★ 플레이어만 레이어를 더 나눈다

동물은 `몸통 + 눈` 2레이어지만(§4.6), 플레이어는 여섯이다.
근거는 **프레임을 따라가는가**다.

| 레이어 | 장수 | 프레임 따라감? |
|---|---|---|
| `body` 피부·팔·다리·신발 | 10 | **예** |
| `bottom` 하의 | 4 (남 북 측 특징) | 아니오 |
| `top` 상의 | 4 | 아니오 |
| `gear` 배낭 | 4 | 아니오 |
| `hair` 머리 | 4 | 아니오 |
| `eye` 눈 | 6 (표정3 × 각도2) | 아니오 |

걷기에서 실제로 움직이는 것은 다리와 팔뿐이다(몸의 상하 바운스는 노드가 낸다, §4.6).
그래서 **몸통 레이어가 프레임을 전담하고 나머지는 방향당 한 장**이면 된다.

> **새 의상 한 벌 = 상의 4 + 하의 4 = 8장.** 통짜였다면 10장을 다시 그려야 한다.
> 머리 한 종 = 4장. 머리 3 × 상의 3 × 하의 3 = **27 조합**이 팔레트 교체와 곱해진다.

**신발은 나누지 않는다.** 다리 끝에 붙어 있어 걷기 프레임을 따라가므로,
나누면 장수가 안 줄고 정렬만 어려워진다. 색은 팔레트로 바꾼다.

머리카락 레이어에는 **얼굴 자리가 뚫려 있고** 그 구멍으로 몸통의 피부가 비친다.
머리를 교체할 때 구멍 위치가 맞아야 한다 — 이것이 머리 레이어의 유일한 제약이다.

#### ⚠️ 사람 눈에는 밝은 외곽을 쓰지 않는다

§4.6 의 "눈 레이어에 1픽셀 밝은 외곽"은 **어두운 털 위에서** 눈을 살리는 규칙이다.
피부 위에 얹는 사람 눈에 쓰면 눈이 얼굴을 다 덮는다.
얼굴 폭이 9px 이므로 **눈은 1픽셀 폭**이어야 얼굴이 남는다.
표정은 굵기와 곡률로 낸다 — 기본 1×2 점 · 놀람 굵은 세로 · 기쁨 위로 휜 호.

### 4.8 아이의 참여

동물 캐릭터를 **아이가 그리고, 그것을 도트로 옮긴다.**
아이 그림은 특징을 과장하고 디테일을 버린다 — 좋은 도트 캐릭터의 조건과 정확히 일치한다.

- **가져올 것**: 실루엣, 주요 색 2~3개, 특징 하나
- **통일할 것**: 크기, 외곽선 톤, 눈 스타일, 그림자
- **"아이가 디자인하고 아빠가 통일한다"**

---

## 5. 데이터 설계

### 5.1 태그 기반 원칙

**상호작용·유도·교감 판정을 종 이름이 아니라 태그 조합으로 한다.**
종별로 규칙을 쓰기 시작하면 20종에서 이미 관리 불능이 된다(상호작용 조합 400개).
태그 방식이면 동물을 100종 추가해도 규칙 수는 늘지 않는다.

### 5.2 행동 태그와 스키마 버저닝

**행동 양식도 태그로 표현한다.** pck에 스크립트를 넣지 않기로 했으므로(§6.5),
"이 지역 동물만 특별한 행동을 한다" 같은 요구는 **본체에 행동 타입을 추가하고 팩은 태그로 참조**하는 식으로 푼다.

#### 게임 버전이 아니라 스키마 버전에 의존시킨다

```
manifest {
  requires_behavior_schema: 3     // 게임 버전(1.2, 1.7)과 별개
}
```

본체는 자기가 지원하는 스키마 버전을 알고 있고, 팩이 요구하는 값이 더 크면
"게임을 업데이트해주세요"라고 안내한다.

#### 미지의 태그 처리 — 태그를 필수/선택으로 나눈다

| 태그 종류 | 예시 | 모를 때 처리 |
|---|---|---|
| **필수** (게임 규칙에 관여) | `diet`, `habitat`, `activity`, `stage`, `senses` | **로드 거부** |
| **선택** (연출·표현) | `idle_animation: "목_늘이기"` | **기본값으로 대체하고 진행** |

#### 태그 값은 열거형으로 고정하고 검증한다

JSON에 자유 문자열로 두면 오타 하나가 조용히 넘어가서,
나중에 "얘는 왜 물가에 안 가지?" 하는 디버깅을 하게 된다.

> **임포터에서 알려진 값 목록과 대조하는 검증 스크립트를 초기에 만들 것.**
> 이것은 "툴을 미리 만들지 말라" 원칙의 **예외**다(§5.5).

### 5.3 스키마 초안 (v2 갱신)

```
Animal {
  id, name

  // 상성 축
  diet:        초식 | 육식 | 잡식
  activity:    주행성 | 야행성 | 박명성
  habitat:     [초원, 물가, 숲, 바위]        // 지역 적응(§3.7)에도 재사용
  social:      무리 | 단독 | 쌍
  temperament: 겁많음 | 호기심 | 대담함
  noise:       조용함 | 보통 | 시끄러움
  likes:       [먹이태그...]
  fears:       [태그...]

  // 원정 동료로서의 능력 (§3.3)
  senses:      [후각 | 청각 | 시야 ...]
  traits:      [냄새강함, ...]               // 대상으로서 감지되는 특성

  // 크기 — 실측이 유일한 진실
  size:   { birth: "5-10cm",  adult: "20-30cm" }
  weight: { birth: "1kg",     adult: "8kg" }
  size_class:  소 | 중 | 대                  // 세그먼트 메타. 임포터가 실측에서 파생 계산,
                                            // 수동 오버라이드만 허용

  // 성장 시퀀스 (§2.3)
  growth: [ { stage, from_day, to_day, sprite_set, size, weight, behavior_tags }, ... ]

  // 개체 능력치 바운더리 (개체 생성 시 범위 내 랜덤, 번식 시 부모 값 상속)
  stats_range: { ... }
}

AnimalInstance {
  species_id                  // 문자열로 저장 (§6.5 세이브 안전장치)
  location: safari | waiting  // 사파리 / 쉼터 (§2.4)
  stage, stats, palette
  visited_regions: []         // 여행 기록 (§2.5)
}
```

**크기 규칙:**
- **실측(cm/kg)이 스케일의 유일한 진실이다.** 화면에 숫자로 노출하는 것이 목적이 아니라,
  막연한 소/중/대 대신 실제 수치로 종 간 차이를 정의하기 위한 것이다.
- `size_class`는 삭제하지 않고 **세그먼트 메타데이터**로 유지하되,
  **임포터가 실측에서 파생 계산**한다. 시트에 손으로 적으면
  "실측 32cm인데 중형으로 박힌" 행이 반드시 나온다.
- 상성의 크기 축은 실측 임계값에서 유도하고, 임계값은 코드가 아니라 스키마 테이블에 둔다.
- **같은 종 안의 개체 크기 차이는 픽셀로 표현하지 않는다.**
  24px에서 5% 차이는 1픽셀도 안 된다. 개체 차는 도감 숫자로만.

### 5.4 태그 → 단서·유도 자동 생성 ★ v2 신설

필드의 흔적과 동료의 유도는 **종별로 손으로 쓰지 않는다.**
`tags.json`의 `trait_to_sense` 테이블 하나가 전부를 만들어낸다.

| 대상의 trait | 감지하는 감각 | 화면에 보이는 단서 |
|---|---|---|
| 냄새강함 | 후각 | 코를 킁킁대는 자리 |
| 털 | 후각 | 걸려 있는 털뭉치 |
| 울음소리 | 청각 | 어디선가 들리는 소리 |
| 발자국 / 큰발자국 | 시야 | 작은 / 큰 발자국 |
| 나무흔적 | 시야 | 갉아먹은 솔방울 |
| 물자국 | 시야 | 젖은 흙 |
| 허물 | 시야 | 벗어놓은 허물 |

**동료의 `senses`와 대상의 `traits`가 이 표를 통해 만나면 유도가 발생한다.**
새 종을 추가할 때 코드를 건드리지 않고, 새 단서를 추가할 때도 표에 한 줄이 는다.

> `if species == "dog"` 같은 코드가 나오면 설계가 틀린 것이다(§5.1).
> 종별로 단서를 쓰기 시작하면 §5.1 원칙이 원정 층에서 무너진다.

#### ★ 지면 흔적과 공중 표시는 다르다 (v3)

단서를 실제로 그려보니 **지면에 실물이 있는 것은 시야 계열뿐이었다.**

| 감각 | 지면 오브젝트 | |
|---|---|---|
| 시야 | 발자국 · 큰발자국 · 나무흔적 · 물자국 · 허물 | **있다** |
| 후각 | 냄새강함 · 털 | **없다** (털뭉치만 실물) |
| 청각 | 울음소리 | **없다** |

냄새와 소리는 눈에 안 보이는 것이라 지면 스프라이트가 성립하지 않는다.
그래서 후각·청각 단서는 **동료 머리 위나 대상 위치에 뜨는 공중 표시**로 그린다.

여기서 규칙 하나가 따라 나온다 — **§3.3 의 `reveals` 와 같은 이야기다:**

> **지면 흔적은 동료가 없어도 플레이어가 직접 볼 수 있다.**
> 발자국이 땅에 찍혀 있는데 눈 좋은 동료가 있어야만 보이는 것은 이상하다.
> 시야 동료는 "더 멀리서 알아본다"이지 "없으면 안 보인다"가 아니다.
>
> **후각·청각은 동료가 없으면 아예 알 수 없다.** 냄새와 소리에는 실물이 없기 때문이다.

이것이 §3.6 의 "관찰로 풀린다"를 화면에서 성립시킨다 —
아이는 동료가 없어도 땅을 보고 다닐 수 있고, 동료는 그 범위를 넓혀준다.

### 5.5 관리 방식과 툴 정책

| 용도 | 도구 |
|---|---|
| 동물 데이터 정의 | **구글 시트 → JSON 익스포트 → 임포터 스크립트** |
| 태그 정의·스키마 버전 이력 | 같은 시트의 별도 탭 |
| 런타임 데이터 | Godot `Resource` |
| 맵 편집 | **LDtk** |
| 나머지 | 엔진 에디터 |

밸런싱은 숫자 수십 개를 나란히 놓고 비교하는 작업이라 스프레드시트가 가장 낫다.

> ⚠️ **전용 툴은 미리 만들지 말 것.** 같은 데이터를 손으로 20번 넘게 만졌을 때, 그때 만든다.

#### 이미 Godot가 주는 것 — 만들지 않는다

- **스프라이트 확인** — `SpriteFrames` 리소스를 인스펙터에서 열면 프레임 목록과 재생 미리보기가 나온다
- **필드 단독 실행** — F6으로 씬 하나만 실행된다. 필드 씬을 따로 띄우면 그게 테스트 환경이다
- **리모트 씬 트리 + 인스펙터** ★ — 실행 중인 게임의 노드를 에디터에서 실시간으로 열어보고 값을 바꾼다.
  이동 속도·유도 반경·게이지 시간을 게임 돌아가는 중에 만질 수 있다.
  **"관리 툴이 필요하다"는 욕구의 대부분을 이것이 흡수한다** — 슬라이더 UI를 만들 이유가 없다

#### 만드는 것 — 셋뿐, 전부 반나절~하루

| | 무엇 | 왜 지금 |
|---|---|---|
| 1 | **임포터 검증 스크립트** (CLI, GUI 금지) | §5.2가 인정한 예외. 태그 열거형 대조, 필수 필드 누락, 스프라이트 파일 존재, 실측↔`size_class` 불일치 |
| 2 | **소환·치트 화면** | 성장 5일짜리를 실제로 5일 기다릴 수 없다. 툴이 아니라 **테스트 가능성**의 문제. 종·색·성장 단계를 골라 소환하므로 **팔레트 교체 결과 확인 화면을 겸한다**(§4.3) |
| 3 | **측정 라벨 하나** | "입장 후 첫 유도까지 걸린 시간"을 화면에 찍는 `Label`. 밸런싱 지표(§8)를 측정할 수단 |

**모두 게임 씬으로 만들고 릴리스 빌드에서 제외한다**(export preset의 exclude filter).
외부 앱이나 별도 프로젝트로 만들면 데이터 구조가 이중화돼서 게임과 어긋나기 시작한다.
게임 씬으로 만들면 반대로 **툴이 게임의 첫 테스트가 된다.**

#### 지금 만들지 않는 것

- 동물 데이터 편집 GUI → 구글 시트가 그 역할. GUI를 만들면 "수십 개를 나란히 비교"라는 장점을 잃는다
- 맵 에디터 → LDtk
- 도감·밸런스 대시보드 → 20종 넘어 시트로 감당 안 될 때
- 스프라이트 아틀라스 자동화 → Godot가 임포트 시 처리

> **판단 기준**: 툴에 쓰는 하루는 스프라이트 2~3장이고, 일주일은 **동물 한 종**이다(§4.5).
> 새 툴이 떠오르면 이 환산을 먼저 해볼 것.

---

## 6. 기술 스택

### 6.1 엔진: Godot 4 (확정)

| | Godot 4 | Unity |
|---|---|---|
| 2D 길찾기 | `AStarGrid2D` 내장 | NavMesh가 3D 전제 |
| 픽셀 퍼펙트 | 프로젝트 설정으로 해결 | Pixel Perfect Camera 패키지 |
| 에디터 무게 | 가벼움, 반복 테스트 빠름 | 무거움 |
| 툴링 성숙도 | 보통 | 우수 |
| 자료 양 | 보통 | 압도적 |
| 라이선스 | MIT | 로열티 조건 확인 필요 |
| **exe 배포** | **단일 exe, 50~80MB** | exe + Data 폴더, 200~400MB |
| **DLC 지원** | **`.pck` 런타임 로드 내장** | AssetBundle/Addressables (설정 복잡) |

**배포 요구사항(윈도우 exe + 지역 팩 확장)까지 넣으면 Godot가 명확히 앞선다.**

### 6.2 렌더링

- **네이티브 해상도 렌더링 + 스프라이트 확대 배치**
- 텍스처 필터 **Nearest**, 밉맵 **끄기**
- 줌 지원 시 배율은 기본 대비 **0.7 ~ 1.5배**로 제한
- 카메라는 부드럽게 (서브픽셀 이동 허용)

```
[월드 레이어]    스프라이트, 타일, 동물 — Nearest 필터
[이펙트 레이어]  파티클, 글로우 — Linear 필터, 자유 좌표   ← §3.4의 상호작용 이펙트가 여기
[UI 레이어]      네이티브 해상도                          ← 게이지가 여기
```

**타일 16×16** (v3 확정). 32px 캐릭터가 정확히 2타일 높이, 소형 동물(24px)은 1.5타일이다.
지형 타일은 `terrain/<지형 이름>.png` 로 두어 코드에서 지형별로 분기하지 않는다.
변형 선택은 **타일 좌표 해시**로 한다 — 규칙적으로 고르면 세로 줄무늬가 생긴다.

**Y-sort** — 4방향 탑다운이므로 아래쪽 동물이 위쪽 동물을 가려야 한다. Godot의 `YSort`로 해결된다.
⚠️ 레이어로 나뉜 캐릭터(§4.6)는 **몸통·눈·입·아이콘이 하나의 노드로 묶여** 통째로 정렬되어야 한다.
구조를 잡을 때 미리 알고 있어야 나중에 안 뜯는다.

### 6.3 밤낮 — 팔레트 보간 + 오버레이 틴트 ★ v3 신설

시간대는 **다섯 겹**으로 만든다. 이 순서가 §7 이 걱정하던 셰이더 충돌을 없앤다.

```
1) 인덱스 아트     한 벌뿐이다. 시간과 무관하게 변하지 않는다
2) 팔레트 보간     새벽 → 낮 → 저녁 → 밤 팔레트를 t 로 섞는다      ← 색조
3) 오버레이 틴트   CanvasModulate 전역 곱연산                      ← 밝기·채도
4) 이펙트 레이어   반딧불·안개·노을 (Linear 필터, 자유 좌표)
5) 국소 광원       모닥불 같은 것만 Light2D
```

**팔레트를 이산적으로 두지 않고 보간하므로 시간이 튀지 않는다.**
팔레트는 16×1 텍스처라 섞는 비용이 사실상 없다.

> ⚠️ **전역 조명에 Light2D 를 쓰지 않는다.**
> 인덱스 컬러 팔레트 교체 셰이더 위에 라이팅을 얹으면 색이 의도와 다르게 나오는데,
> 전역을 팔레트+틴트로 처리하면 그 문제가 아예 생기지 않는다.
> Light2D 는 모닥불 같은 **국소 광원 전용**으로 좁힌다.

부수 효과가 하나 있다 — 갈색 동물이 파란 밤 틴트 아래서도 대비가 남아,
§4.1 의 "바닥은 어둡고 차분하게 / 동물은 밝고 선명하게"가 자동으로 성립한다.

시간대는 §3.3 의 `daypart_scale` 을 통해 **감각 반경에도 영향을 준다.**
밤에는 눈이 거의 안 보이고 코는 그대로다.

### 6.4 조작

원정 필드에서 플레이어가 직접 이동하므로(§3.2) 입력 레이어가 필요하다.

- **기본**: WASD / 패드 왼쪽 스틱 (동일 입력으로 처리)
- **입력 추상화 레이어**:
  ```
  원시입력 → 추상입력(moveVector, interact, ...) → 게임로직
  ```
  게임 로직은 입력 장치를 몰라야 한다
- 사파리 층은 이동 + 커서 수준

### 6.5 필드 시뮬레이션 ★ v2 신설

**동물은 화면 밖에서도 필드를 돌아다닌다**(§3.2). 구현 비용은 낮지만 경계를 처음부터 나눠둔다.

```
화면 밖  →  얕은 시뮬: 위치·이동벡터만 갱신
근접 반경 →  풀 AI로 승격 (행동 태그, 애니메이션, 유도 판정)
```

수십 마리 수준은 Godot에서 성능 문제가 아니지만, 이 경계를 나중에 넣으려면 다 뜯어야 한다.
몇십 줄이니 처음부터 둔다.

#### 튜닝 상수는 리소스로 뺀다

필드 규모는 만들어보며 조정한다(§7). 그러려면 다음이 코드에 박히면 안 된다:

```
맵 크기 · 동물 밀도 · 이동 속도 · 유도 반경 · 게이지 시간
```

리소스로 빼두면 리모트 인스펙터(§5.5)로 실행 중에 만질 수 있다.
툴을 만드는 것이 아니라 상수를 빼두는 것이므로 §5.5 원칙에 걸리지 않는다.

#### 맵은 생각보다 작아도 된다

밸런싱 지표(§8)를 역산해보면 — 이동 속도 초당 3~4타일, 첫 유도까지 30초면 100타일 남짓이다.
유도 반경까지 감안하면 **화면 몇 개 분량이면 충분**하다.
"넓은 지역"을 상상하며 큰 맵을 그릴 필요가 없고, 이는 지역 팩의 미술 부담에도 좋은 소식이다.

### 6.6 배포와 콘텐츠 팩

**게임은 스탠드얼론으로 완전히 동작한다.** 인터넷 연결은 선택이며, 연결되면 추가 콘텐츠를 받는다.

#### 팩 단위는 "지역"

세계 지도가 이미 있으므로 지역 단위 확장이 자연스럽다.
태그 기반 데이터 설계 덕분에 **새 동물 20종을 추가해도 게임 로직은 손댈 필요가 없다.**

> ⚠️ v2에서 필드가 실제 공간이 되면서(§3.2) 지역 팩에 **맵이 딸린다.**
> 팩 제작 비용의 성격이 "데이터 입력"에서 "미술 작업"으로 바뀌는 지점이다.
> §6.5의 "맵은 작아도 된다"가 이 부담을 상당히 덜어준다.

#### 팩은 패키지(.pck) 단일 파일로

**사용자가 폴더를 만지지 않는다.** 실제 사용자가 7살이므로, 게임 안에서 버튼 하나로 완결되어야 한다.

```gdscript
ProjectSettings.load_resource_pack("user://packs/oceania.pck")
```

- 팩 하나 = 파일 하나
- 설치 위치는 **`user://packs/`** (%APPDATA% 아래). Program Files 아래는 권한 문제가 생긴다
- 개발 중에는 팩 폴더를 프로젝트 안에 두고 쓰다가, 릴리스할 때만 pck로 분리한다

| 항목 | 내용 |
|---|---|
| **경로 충돌** | pck 로드는 같은 경로의 파일을 덮어쓴다. 팩별로 `res://packs/<id>/...` 접두사를 분리할 것 |
| **스크립트 금지** | pck에는 데이터(JSON)와 이미지·맵만. 로직은 전부 본체에 |
| **무결성 검증** | 인덱스 JSON에 해시를 적어두고 대조. 받다 끊긴 pck를 로드하면 크래시 |

#### 매니페스트

```
pack_manifest {
  id: "oceania"
  region_name: "호주"
  requires_behavior_schema: 3
  welcome: {
    title: "호주의 친구들이 도착했어요!"
    body:  "지금 만나러 가볼까요?"
    teaser_sprites: [...]            // 실루엣으로만 보여준다
  }
}
```

#### 7살을 위한 UX

- ❌ "다운로드 하시겠습니까?" 같은 시스템 대화상자를 그대로 쓰지 않는다
- ✅ **부모가 설정에서 켜두면 조용히 받고**, 아이에게는 게임 내 사건으로 알린다
- ✅ 대표 동물은 **실루엣으로만** 보여준다 (§4.2의 실루엣 원칙과 맞물린다)
- ✅ **"지금 만나러 가볼까요?"를 누르면 바로 세계 지도의 그 지역으로 이동한다**

#### 세이브 안전장치 ★

- 동물 인스턴스에 **원본 종 ID를 문자열로 저장**
- 종 정의를 못 찾으면 "잠든 상태"로 표시하고, 팩을 다시 넣으면 살아난다
- 7살이 실수로 뭘 지웠는데 사파리가 통째로 날아가면 안 된다

#### 호스팅 / exe 빌드 / 자동 업데이트

- GitHub Releases나 정적 파일 서버에 pck와 인덱스 JSON 하나. **서버 코드가 필요 없다**
- 첫 export 전에 **Editor → Manage Export Templates**에서 템플릿을 받아야 한다
- exe 아이콘·제품명·버전은 **rcedit** 경로를 지정해야 붙는다
- 서명 없는 exe는 SmartScreen이 막는다. 본인·아이 PC용이면 무시
- 자동 업데이트는 직접 구현 (100줄 남짓):
  `인덱스 조회 → 새 팩 발견 → 다운로드 → 해시 검증 → user://packs/ 저장 → 로드`

---

### 6.7 진입 화면 — 부팅 · 타이틀 · 메뉴 ★ v3.1 신설

게임이 켜지고 필드에 들어가기 전까지. **해상도는 640×360 으로 고정**이고
(`project.godot` 의 뷰포트와 같다) 확대는 정수배만 쓴다.

```
제작사 로고 (RUN II, 2.5초 · 아무 키나 누르면 건너뛴다)
   ↓
타이틀 — 살아있는 필드 배경 + EVERDEN + 메뉴 4줄
   ↓
CONTINUE → 필드 / NEW GAME → 확인 창 → 필드 / SETTING / EXIT → 확인 창
```

**제작사 이름은 `RUN II`.** 뜻은 "2회차" — 부모가 된 두 번째 삶.
회차 = playthrough/run 이라 직역에 가장 가깝다. 로고 연출은 큰 발자국이 먼저
찍히다가 어느 지점부터 작은 발자국이 나란히 붙는 것이고, 새로 그린 도트는
글자와 발자국 둘뿐이다.

**배경은 켤 때마다 지형이 바뀌고, 거기 사는 동물이 선다.** 정지 일러스트를 그리지 않는다.

이게 이 화면의 핵심이다. **타이틀 화면 자체가 단서다.** 글로 "얘는 숲에 살아요" 라고
알려주는 대신 숲을 깔고 그 위에 청설모를 세운다. 아이는 읽는 게 아니라 **본다** —
§3.6 "관찰로 풀린다" 를 게임에 들어가기 전에 한 번 하는 셈이고, 글을 못 읽어도 통한다.

| 무엇이 | 어디서 나오나 |
|---|---|
| 지형 | 그 종의 `habitat[]` 중 하나 |
| 시간대 | 그 종의 `activity` — 주행성 낮 · 야행성 밤 · 박명성 저녁 |
| 타일·프롭 | `terrain/<지형>.png` 와 `palettes.json` 의 `prop_terrain` |

⚠️ **짝이 어긋나면 틀린 것을 가르친다.** 수달이 초원에 서 있으면 안 된다.
반드시 `habitat` 에서 뽑을 것 — 종 이름으로 분기하면 언젠가 어긋난다.

§6.3 의 팔레트 보간이 그대로 걸리므로 시간대는 색으로 표현된다.
필드는 무늬가 많아 아래로 갈수록 어두워지는 스크림을 한 겹 깐다 — 패널을 깔면
확실하지만 배경을 가려버린다. 설정 같은 하위 화면은 전체를 한 겹 더 누른다.

#### 메뉴

| | |
|---|---|
| 항목 | `CONTINUE` · `NEW GAME` · `SETTING` · `EXIT` |
| 세이브 | **하나뿐이고 자동 저장.** 세이브 선택 화면이 없다 |
| 첫 실행 | `CONTINUE` 가 **아예 없다.** 회색으로 두지 않는다 — 고를 수 없는 것을 보여줄 이유가 없다 |
| 커서 | **발자국.** 삼각형이 아니다. 제작사 로고와 같은 물건이고, 이 게임이 흔적을 따라가는 게임이라는 걸 메뉴에서부터 말한다 |
| 선택 표시 | 밝기 + 오른쪽으로 6px. 둘 다 걸어야 작은 화면에서 읽힌다 |

#### 문구는 영문, 확인 창은 한글

메뉴는 도트 게임 관례대로 영문이다. 대신 **되돌릴 수 없는 동작에는 한글 확인 창**이
붙는다. 첫 플레이어가 `EXIT` 도 `NEW GAME` 도 못 읽기 때문이다.

- 확인 창은 **되돌릴 수 없는 것에만** 뜬다. 그 외에는 묻지 않는다
- **기본 선택은 언제나 안전한 쪽**(아니오)이다
- 새 게임: "지금까지 만든 사파리가 사라져요. / 그래도 새로 시작할까요?"

세이브를 조용히 덮어쓰는 것은 §"절대 어기면 안 되는 것" 2번(되돌릴 수 없는 실패)
위반이다. 게임 안에만 적용되는 원칙이 아니다.

#### 설정은 셋

`SOUND` · `MUSIC` · `FULLSCREEN`. 값은 **숫자가 아니라 칸 수**로 보인다 —
칸이 몇 개 찼는지가 곧 값이다(§"규칙은 눈에 보여야 한다").

#### 글자

| | |
|---|---|
| 로고 (`RUN II` · `EVERDEN`) | **손으로 찍은 도트.** 14×18 격자, 획 3px, 정수배 확대만 |
| 메뉴 · 라벨 | 자체 5×7 비트맵 폰트 (`build_title.py` 의 `FONT5`) |
| 한글 | **갈무리 11 (SIL OFL 1.1).** `fonts/` 에 들어왔다 |

로고를 폰트로 찍으면 다른 게임과 같은 얼굴이 된다. 반대로 본문까지 손으로 찍는 것은
낭비다 — 경계는 **로고냐 아니냐**다.

그리고 로고 안에서도 한 번 더 갈린다. **제작사 로고는 절제하고 게임 타이틀은 정반대다.**
처음에 `EVERDEN` 을 `RUN II` 와 같은 14×18 격자로 찍었더니 타이틀이 제작사 로고처럼
보여서 심심했다. 타이틀은 16×20·획 4px 에 세로 그러데이션·윗변 하이라이트·오른쪽 아래
돌출·완만한 아치를 얹는다. 흔들림은 무작위가 아니라 **아치**여야 한다 — 무작위로 흔들면
실수처럼 보인다.

**갈무리는 11px 로 찍어 정수배로만 키운다.** 22px 로 바로 찍으면 힌팅이 개입해 획이
흔들린다. Godot 임포트에서 안티에일리어싱·힌팅·서브픽셀·밉맵을 전부 꺼야 한다 —
자세한 것은 `fonts/README.md`.

#### 타이틀 옆의 동무 — 켤 때마다 바뀐다

글자 발밑에 풀·꽃을 겹치고 마지막 글자 옆에 동물 하나를 앉힌다. 워드마크만 얹으면
공중에 뜬 스티커로 보인다.

**풀은 `data/animals.json` 의 모든 종 × 모든 `growth` 단계다.** 종을 코드에 적지 않는다 —
`growth[]` 가 이미 `baby`/`adult` 와 `sprite_set` 이름을 들고 있으므로 데이터에서 뽑는다.
종을 하나 추가하면 타이틀 풀이 저절로 늘어난다(§"종 이름으로 분기하지 않는다").
첫 실행·스프라이트 없음은 **개 성체**로 떨어진다.

> 지금 풀은 22칸(11종 × 2단계)인데 실제로 그려진 것은 **개·고양이·청설모의 성체 셋뿐**이다.
> 아기 스프라이트는 아직 한 장도 없다 — §4.5 가 종당 아기 6장을 잡아뒀고, 그게 채워지는
> 만큼 타이틀 풀도 채워진다. `title.json` 의 `candidates[].available` 이 그 상태를 들고 있다.

**못 만난 종도 앉는다.** 도감으로 막지 않는다 — 모르는 동물이 타이틀에 앉아 있는 것은
스포일러가 아니라 미끼다.

#### 이름표 — 이름만 적는다

동무에 마우스를 올리거나 아무것도 안 하고 2초가 지나면 옆에 작은 이름표가 뜬다.
**이름만 적는다.** 도감에 없어도 이름은 보여준다 — `"???"` 보다 `"너구리"` 가 훨씬 세게
당긴다. 아이가 이름을 부를 수 있으니까.

사는 곳은 **쓰지 않는다.** 배경이 이미 말하고 있다. 숲을 깔고 청설모를 세워놓고 그 위에
"숲에 살아요" 를 또 쓰면, 그림으로 읽는 법을 배울 기회를 뺏는 것이다.

한 번 만난 동물이 **한 군데서만 사는 것도 아니다.** 배경은 매번 `habitat` 중 하나를
고르므로, 여러 번 켜다 보면 고양이가 초원에도 숲에도 바위에도 있는 것을 아이가 스스로
알게 된다. 목록을 보여주는 것보다 이쪽이 이 게임의 문법이다.

> **마우스에서만 얻을 수 있는 정보를 만들지 않는다.** 기본 조작은 WASD/패드다(§6.4).
> 이름표는 마우스를 올리면 즉시, 아무 입력이 없으면 2초 뒤에 뜬다 — **마우스는 더 빠를 뿐이다.**
> 이 규칙은 타이틀만의 것이 아니라 앞으로 만들 모든 화면에 걸린다. 툴팁을 마우스에만
> 달기 시작하면 패드로 하는 아이가 게임의 절반을 못 본다.

#### 조각으로 내보낸다 — 합성 스틸이 아니다

로고도 타이틀도 **완성된 한 장이 아니라 조각 + 좌표**로 나간다. 합성본을 주면 엔진이
발자국을 하나씩 찍을 수도, 동무를 갈아끼울 수도 없다. 32프레임을 통째로 굽는 것은
640×360 × 32장이라 낭비다. 지형·프롭·단서를 `palettes.json` 으로 넘긴 것과 같은 방식이다.

- `ui/logo.json` — 워드마크 위치, 발자국 좌표 배열, 프레임 타이밍
- `ui/title.json` — 워드마크·풀밭 위치, 동무 앵커와 후보 목록, 메뉴 좌표, 확인 창 문구

**좌표와 타이밍이 데이터로 나오면 아트를 다시 그려도 엔진 코드를 안 고친다.**

#### 산출물

`sprites/build_logo.py` · `build_title.py` → `sprites/extracted/ui/`

| | |
|---|---|
| 로고 | `logo_wordmark` · `logo_paw_big` · `logo_paw_small` · `logo.json` (+ 검수용 `logo_screen`) |
| 타이틀 | `title_mark` · `title_grass` · `cursor_paw` · `title.json` (+ 검수용 `title_screen`) |
| 폰트 | `fonts/Galmuri11.ttf` 외 3종 + `LICENSE-Galmuri.txt` |

---

## 7. 아직 정하지 않은 것

- [ ] **원정 중 사파리 시간이 흐르는가** — 만들어보고 결정(§2.2).
      흐르게 하면 점유 시간이 원정의 자원이 되고, §3.7의 "남은 애는 심심하다"가 자연스러워진다
- [ ] **필드 규모 밸런싱** — 만들어보며 조정(§6.5). 지표는 §8
- [ ] **시간 흐름의 단위** — 게임 내 하루가 무엇인가. `growth.from_day`가 여기 물려 있다
- [ ] **재화의 이름과 표현** (§2.6)
- [ ] **치료·도움 상황의 종류** — 사고 유형을 몇 개나 둘 것인가
- [ ] **실존 동물 vs 가상 동물** —
      실존이면 위키 등에서 기초 정보를 반자동으로 채울 수 있고 아이가 실제 지식을 배운다.
      대신 사실과 게임 규칙이 충돌할 때 처리를 정해야 한다.
      가상이면 그런 제약이 없고 아이 그림을 활용하기 좋다.
      §3.8에서 **생태는 실존, 지명은 무명**으로 두어 양쪽을 다 열어놓았다
- [ ] 동물 종 수의 1차 목표
- [ ] **국소 광원** — 모닥불·등불을 언제 어디에 두는가.
      전역 밤낮은 §6.3 으로 닫혔고, 남은 것은 국소 광원의 용도다
- [ ] **UI·화면 레이아웃** — 쉼터, 세계 지도, 팀 편성, 도감, 팩 웰컴.
      글을 이제 배우는 7살이 쓰는 UI라 별도 설계가 필요하다.
      **진입 화면(§6.7)과 집(§2.7)은 닫혔다** — 나머지는 남아 있다
- [ ] **사물을 어디서 사고 어떻게 놓는가** — 상점 화면이 따로 필요한가, 아니면
      집 안에서 바로 놓는가. 배치는 격자인가 자유인가 (§2.7)
- [ ] **동물이 사물로 걸어가는 규칙** — 얼마나 자주, 어떤 순서로. 아이가 "쟤가 공을
      갖고 논다"를 알아볼 만큼은 자주 일어나야 한다 (§2.7)
- [ ] **게임 제목** — 폴더와 타이틀 화면은 `EVERDEN` 이고 이 문서 제목은 아직 "사파리(가제)"다.
      둘 중 하나로 정해야 하고, 한글 제목으로 갈 거라면 타이틀 로고를 한글 도트로 다시 찍어야 한다
- [ ] **타이틀 배경을 필드 맵과 같은 생성기로 합칠 것인가** — 지금 타이틀 배경은
      단일 지형을 타일로 채우는 간단한 생성기고(§6.7), 실제 필드는 별도다.
      필드 생성이 자리를 잡으면 하나로 합치는 편이 나을 수 있다
- [ ] 색 유전 규칙과 특수색 출현 확률의 구체값 (§4.3)

**해소된 항목**
- v2 — 원정지에서의 플레이어 조작(→ §3.2), 사파리 뷰 각도(→ §4.5),
  seat 확장 방법(→ §2.4), 스프라이트 구성 상세(→ §4.6), 시작 지역과 첫 동물(→ §3.8)
- v3 — **밤낮·조명**(→ §6.3 팔레트 보간 + 틴트, Light2D 는 국소 광원만),
  **배경 타일 구성**(→ §6.2 타일 16×16, 지형 이름이 파일명),
  **능력치가 무엇을 결정하는가**(→ §3.3 `sense_range` 가 유도 반경에, `charm` 이 게이지 속도에)

---

## 8. 다음 단계

**동물 6종으로 전체 루프를 먼저 돌린다.**

```
동료 골라 떠나기 → 필드 돌아다니기 → 개가 킁킁 → 다가가니 너구리
    → 게이지 채워 교감 → 데려오기 → 자리가 없어 쉼터로
    → 밥 주고 → 다친 애 도와 번 재화로 자리 확장 → 가족 형성
```

이게 재밌으면 100종으로 늘리는 건 그냥 데이터 입력이다. 재미없으면 100종을 채워도 마찬가지다.

### 프로토타입에서 줄일 것

v2에서 늘어난 것이 많으므로 명시적으로 깎는다.

| 항목 | 프로토타입 범위 |
|---|---|
| 경제 | 숫자만. 상점 UI 없이 **seat 확장 하나에만** 소비 |
| 치료·도움 | 상성 판정 재사용, **1종류만** |
| 필드 맵 | **1개, 작게**. 지역 여러 개는 나중 |
| 동료 감각 | **후각·청각·시야 셋 다 + `sense_profile`** (§3.3). 개와 고양이의 대비가 검증 대상이므로 깎지 않는다 |
| 성장 단계 | **2단계**. 스키마는 배열로 열어둠 |
| 상성 축 | **2개만 켠다** (먹이 유형 + 활동 시간). 나머지는 데이터에만 |
| 표정 | 눈 1스타일 × 3표정 × 2각도 = 6장. **입은 생략**(§4.6 — 눈이 매력의 90%) |
| 표현 아이콘 | 5개 (발견·잠·배고픔·애정·냄새) |
| 이동 이펙트 | 먼지 1개 |
| 콘텐츠 팩 | 만들지 않는다. 폴더 구조만 팩 전제로 |

> 6종 × 16장 = **몸통 96장** + 공용 약 12장. 하루 2~3장이면 약 6~7주.
> 프로토타입 애셋은 Kenney(kenney.nl, CC0)나 itch.io 에셋팩으로 대체하고,
> **재미가 확인된 뒤에 그린다.**

### 밸런싱 지표는 하나로 고정한다

필드 상수가 다섯 개(§6.5)라 따로 만지면 수렴하지 않는다.

> **"입장 후 첫 유도(동료의 반응)까지 걸리는 시간 ≤ 30초"**

7살은 30초~1분 안에 아무 반응이 없으면 지루해한다.
이 하나를 고정하고 맵 크기·밀도·이동 속도를 역산한다(§6.5).

---

## 부록 A. 이 프로젝트에서 경계할 실패 패턴

- **데이터를 먼저 다 채운다** — 동물 100종 메타데이터를 다 채우고 정작 게임 로직이 없는 상태에 도달하기 쉽다
- **툴을 게임보다 먼저 만든다** — 아프기 전에 만들지 말 것. 툴 일주일 = 동물 한 종(§4.5)
- **보이지 않는 데이터 필드를 만든다** — 플레이어가 체감 못 하는 필드는 작업량만 늘린다
- **종별로 규칙을 쓴다** — 반드시 태그 조합으로. 단서와 유도도 마찬가지(§5.4)
- **미술을 먼저 완성한다** — 재미 검증 전 미술 투자는 매몰비용이 된다
- **7살에게 손실을 준다** — 죽음, 되돌릴 수 없는 실패, 파산, 끊기는 게이지.
  방치의 대가는 손실이 아니라 진행 정체로
- **수집을 벌로 만든다** ★ — 동물이 늘수록 유지비·관리 의무가 커지는 구조.
  경제(§2.6)와 여행 로테이션(§3.7)에서 반복해서 확인할 것
- **필드를 크게 그린다** — 큰 맵은 재미가 아니라 이동 시간이다(§6.5)
- **문서를 코드보다 낡게 둔다** ★ — 설계 대화에서 정한 것과 구현에서 발명한 것이
  둘 다 이 문서에 안 들어오면, 문서가 폴더에서 가장 못 믿을 파일이 된다.
  **v3 를 만들 때 실제로 다섯 건이 밀려 있었다.** 결정이 나면 그 자리에서 절을 고칠 것

---

## 부록 B. 변경 이력

### v3.1 → v3.2 (2026-08-26)

- **§2.7 집 신설** — 게임을 켜면 필드가 아니라 집으로 들어온다.
  집은 진행이 누적으로 보이는 화면이고, **보는 곳이지 해야 하는 곳이 아니다**
- **사물(시설)** — §2.6 이 비워둔 "시설" 칸을 채웠다. `for_tags` 는 전부
  `tags.json` 에 이미 있는 값이라 새 열거형이 없다
- **사물이 새 동작을 요구하지 않는다** — 동물의 기존 특징 동작이 사물 옆에서 재생된다.
  종×사물로 만들면 49벌이 된다
- HUD 는 재화와 자리 둘뿐. 배고픔·청결·기분 게이지를 두지 않는다

### v3 → v3.1 (2026-08-26)

- **§6.7 진입 화면 신설** — 제작사 로고(`RUN II`), 타이틀, 메뉴 4줄, 확인 창, 설정.
  세이브는 하나 + 자동 저장이라 세이브 선택 화면이 없다
- §7 의 "UI·화면 레이아웃"에서 진입 화면 몫을 덜어냈고, **게임 제목**을
  미결 항목으로 새로 올렸다
- **갈무리 11 반입 완료** (`fonts/`, OFL 1.1). 11px + 정수배 규칙과 Godot 임포트 설정은
  `fonts/README.md`
- **타이틀 동무를 무작위로** — 풀은 `animals.json` 의 모든 종 × 성장 단계.
  종을 코드에 적지 않는다. 못 만난 종도 앉는다
- **타이틀 배경을 무작위 지형으로** — 지형은 `habitat`, 시간대는 `activity` 에서 나온다.
  **타이틀 화면 자체가 단서다** — 글로 알려주는 대신 보여준다
- **이름표는 이름만** — 사는 곳은 배경이 말한다. 그리고
  **마우스 전용 정보를 만들지 않는다**(호버 즉시 / 무입력 2초)는 규칙을 §6.7 에 못박았다
- **로고·타이틀을 조각 + JSON 으로 내보낸다** — 합성 스틸로는 엔진이 발자국을 찍거나
  동무를 갈아끼울 수 없다. 구현 세션 요청(`sprites/구현세션-요청.txt`)을 반영한 것

### v2 → v3 (2026-08-25)

설계 대화에서 나온 것과 **구현 세션이 코드에서 발명한 것**을 함께 반영했다.

| 절 | 변경 | 출처 |
|---|---|---|
| §2.6 | **밥 주기는 배고픔을 막는 행위가 아니라 행복도를 올리는 행위** | UI 목업에서 모순 발견 |
| §3.1 | **팀 편성 미리보기는 도감에 기록된 종만** — 아니면 원정이 목표 지향으로 회귀 | UI 목업 |
| §3.3 | **`sense_profile` 신설** — `reveals`/`range_scale`/`daypart_scale`/`terrain_scale`, `reveal_radius` | 구현 세션 |
| §3.5 | **지형**이 감각 반경을 깎고 동물의 시작 위치를 정한다 | 구현 세션 |
| §4.5 | **E/W 미러 — 종당 22장 → 16장.** 11종 242 → 176장 | 스프라이트 제작 |
| §4.6 | 시무룩 트리거에서 배고픔 제거, 조름 추가 | §2.6 연쇄 |
| §5.4 | **지면 흔적(시야)과 공중 표시(후각·청각)의 구분.** 지면 흔적은 동료 없이도 보인다 | 단서 스프라이트 제작 |
| §6.2 | **타일 16×16 확정**, 지형 이름이 파일명, 변형은 좌표 해시로 | 배경 제작 |
| §6.3 | **밤낮 신설** — 팔레트 보간 + 오버레이 틴트 + 이펙트. Light2D 는 국소 광원만 | 배경 제작 |
| §7 | 밤낮·배경·능력치 항목 해소, 국소 광원만 남김 | |
| §4.6 | **눈 둘레 밝은 외곽 규칙 철회.** 대비는 동공 안 반짝임 1px 로. 표정은 같은 눈의 변형 | 눈이 개구리가 됨 |
| §4.7 | **플레이어 캐릭터 신설** — 여자아이 32×48, 레이어 6개, 사람 눈은 1px 폭 | 플레이어 제작 |
| 부록 A | **"문서를 코드보다 낡게 둔다"** 실패 패턴 추가 | 이 병합 자체 |
| 파일명 | `PROJECT-BRIEF-safari-v2.md` → **`PROJECT-BRIEF.md`** (버전은 문서 안에만) | 드리프트 방지 |

### v1 → v2

| 절 | 변경 |
|---|---|
| §2.2 | 방치를 "세션 내 원정 체류"로 재정의. 오프라인 시간 정지 확정. 행복도 상승 경로 2개로 |
| §2.3 | 2단계 고정 → **grow 배열**. 아기는 축소 재활용이 아니라 **별도 제작**으로 정정 |
| §2.4 | seat가 **원정 개체에도 적용**. **쉼터(대기)** 도입. seat 확장 = 도감 연동 |
| §2.5 | **여행 기록**(`visited_regions`) 신설 |
| §2.6 | **경제 신설** — 기본 먹이 무료, 성장비만 유료, 치료·도움으로 획득 |
| §3.1 | 원정이 **목표 지향이 아님**으로 재작성 |
| §3.2 | **필드는 실제 공간** — v1 §3.4 "미결: 조작" 해소 |
| §3.3 | **동료의 감각과 유도** 신설 |
| §3.4 | **교감·치료·도움 = 뭉뚱그린 모션 + 게이지** 신설. 대상 모션 0장 |
| §3.7 | **여행 프레이밍** 신설 — 동료에게 즐거움, 집 보는 당번, 로테이션 비강제 |
| §3.8 | **시작 지역과 첫 동물** 신설 — 집 근처 뒷산·냇가, 생태는 실존 지명은 무명. 프로토타입 6종 확정(개·고양이·청설모·너구리·수달·고라니) |
| §4.4 | 렌더 스케일 정수/반정수 양자화 추가 |
| §4.6 | **스프라이트 구성 신설** — 레이어 조합, 눈·입 공용 세트, 걷기 바운스는 노드로, 표현 아이콘 계층 |
| §4.5 | **뷰 각도·방향 확정** (3/4 뷰, 이동 4방향 + 대기 2방향 + 특징 1방향, 사파리·필드 동일). 종당 16~18장 → **22장**. 실루엣 원칙은 "알아보는 순간은 측면 고정"으로 양립 |
| §5.3 | `senses`·`traits`·`growth`·`visited_regions`·`location` 추가. `size_class`는 파생 계산으로 |
| §5.4 | **태그 → 단서·유도 자동 생성** 신설 |
| §5.5 | **툴 정책** 정리 — Godot 기본 기능 활용, 만들 것 3개, 안 만들 것 명시 |
| §6.3 | 입력 추상화 **부활** (필드 이동) |
| §6.4 | **필드 시뮬레이션** 신설 — 얕은/깊은 AI 경계, 튜닝 상수 외부화 |
| §6.5 | 지역 팩에 맵이 딸리는 문제 명시 |
| §8 | **프로토타입 축소 표**와 **밸런싱 지표** 신설 |
