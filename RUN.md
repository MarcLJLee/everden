# Demo 1 실행·검증

Godot 4.7 (`brew install --cask godot`) 기준. 프로젝트 루트가 이 폴더다.

## 실행

```bash
/Applications/Godot.app/Contents/MacOS/Godot --path . 
```

에디터로 열려면 위 명령에 `--editor`, 바로 플레이하려면 그대로 두면 된다.

```
제작사 로고 (RUN II) — 발자국이 하나씩 찍히고 이름이 뜬다. 아무 키나 누르면 건너뜀
    ↓
타이틀 (EVERDEN) — 켤 때마다 배경 지형과 동무가 바뀐다
    ↓
NEW GAME → **집** / SETTING → 설정 / EXIT → 확인 창
    ↓
집 — 대문으로 걸어 나가면 필드
```

`Title.gd` 의 `demo_build` 를 켜면 첫 항목이 `DEMO` 가 되고 집을 건너뛰고 필드로 간다.

### 집 (BRIEF §2.7)

마당을 울타리가 두르고 **집이 위쪽 담장을 대신한다.** 문에서 대문까지 흙길이 나 있고
**대문은 열려 있다** — 닫힌 문은 "못 나간다"로 읽힌다.

- **HUD 는 재화와 자리 둘뿐이다.** 배고픔·청결·기분 게이지를 두지 않는다 —
  그게 생기는 순간 집이 할 일 목록이 되고 "수집이 벌이 되면 안 된다"를 어긴다
- 사물은 `home.json` 의 `for_tags` 로 임자를 정한다. 비어 있으면 누구나 쓴다
- ★ **사물이 새 동작을 요구하지 않는다.** 사물 옆에서 그 동물의 **기존 특징 동작**이
  재생된다. 종 × 사물로 동작을 만들면 7×7 = 49벌이 된다
- **울타리는 막는다.** 대문 구간에서만 아래로 나갈 수 있고 동물은 아예 못 나간다.
  사각형으로 자르면 아래쪽 울타리를 아무 데서나 통과한다 — `Actor.confine` 이 그 장치다

### 갈 수 있는 지형 — habitat 이 정한다

`tags.json` 의 `terrain_walkable` 이 기본을 정하고, 막힌 지형은 **그 지형이 habitat 인
동물만** 들어간다.

| | 초원 · 숲 | 물가 · 바위 |
|---|---|---|
| 플레이어 | 지나간다 | **못 지나간다** (habitat 이 없다) |
| 개 (초원·숲) | 지나간다 | 못 지나간다 — 수영 안 하는 개가 호수를 건너면 이상하다 |
| 수달 (물가) | 지나간다 | **물가는 지나간다** — 자기 집이다 |
| 청설모 (숲) | 지나간다 | 못 지나간다 |

기본 지형은 habitat 과 무관하게 누구나 지나간다. habitat 은 **사는 곳**이지 갈 수 있는
곳의 전부가 아니다 — 청설모가 숲에만 갇히면 한 조각에서 못 나온다.

막힌 곳에 닿으면 **미끄러진다.** 그냥 되돌리면 비스듬히 붙었을 때 갇힌 것처럼 느껴진다.

> ⚠️ **막힌 지형은 얇게 찍는다.** 두껍게 찍으면 한가운데 선 동물에게 교감 반경(1.5타일)
> 안으로 다가갈 수 없어 **초대가 아예 불가능해진다.** 두꺼운 덩어리로 재봤더니 물가
> 타일의 60%, 최대 6타일이 그랬다. 지금은 최대 2타일이고 회귀 테스트가 3타일을 넘지
> 않는지 본다. `물가` 는 물가(가장자리)이지 호수가 아니다.

**아무 키나 누르면 로고를 건너뛴다** — 아이는 이 화면을 수백 번 본다.
개발 중에 진입 화면을 통째로 건너뛰려면 에디터에서 `scenes/field/Field.tscn` 을 열고 F6.

로고 연출(발자국 좌표·타이밍)은 `ui/logo.json`, 타이틀 배치는 `ui/title.json` 에 있다.
**둘 다 다시 그려도 엔진 코드는 안 고친다.**

### 타이틀 배경은 단서다

켤 때마다 종을 하나 고르고, **그 종의 `habitat`** 에서 지형을 뽑고,
**그 종의 `activity`** 로 시간대를 정한다. 숲을 깔고 그 위에 청설모를 세우는 식이다 —
글로 알려주지 않는다. 짝이 어긋나면 틀린 것을 가르치므로 지형은 반드시 habitat 에서 뽑는다.

### ⚠️ Control 의 `_draw` 안에서 텍스처·글자를 그리지 말 것

`draw_texture` · `draw_texture_rect` 는 물론 **`draw_string` 까지** 통짜 사각형으로 나온다
(글리프도 텍스처다). 세 번 겪었다 — 단서 마름모 · 메뉴 커서 · 집 HUD.

**텍스처는 `Sprite2D`, 글자는 `Label` 노드로 둔다.** `_draw` 에는 `draw_rect` 같은
도형만 남긴다. 5×7 비트맵 폰트(`BitmapFont5`)는 도형으로 찍으므로 `_draw` 에서 써도 된다.

| 키 | 동작 |
|---|---|
| WASD / 방향키 / 패드 스틱 | 이동 |
| 스페이스 | 교감 시작 |
| ESC | 교감 취소 (게이지를 끊는 유일한 방법) |
| 1 / 2 | 동료 켜고 끄기 — **개만 켜고 청설모를 찾아보는 것이 이 데모의 핵심 장면** |
| T | 시간대 전환 (낮 → 여명 → 밤) |
| R | 필드를 다시 깔고 "첫 유도까지 시간" 다시 측정 |

## 무엇이 보이는가

멀리 있는 동물은 **그냥 보이지 않는다.** 보이게 만드는 길은 둘뿐이다.

| | 조건 | 화면 |
|---|---|---|
| 가까이 간다 | `reveal_radius`(기본 3타일) 안 | 몸이 보인다 |
| 몸을 드러내는 감각으로 잡는다 | `sense_profile.reveals` 가 true 인 감각 (지금은 시야) | 그 자리에서 몸이 보인다 |
| 그 밖의 감각으로 잡는다 | 후각·청각 | **마름모 단서만** 뜬다. 누구인지는 다가가야 안다 |

보이던 동물이 사라지거나 나타나면 **먼지 이펙트**가 뜬다. 그냥 없어지면
아이가 사라진 줄도 모르기 때문이다.

## 시간대는 **누가 나와 있는가**를 바꾼다

감각 반경만 바뀌는 게 아니다. **밤에만 나오는 동물은 낮에 필드에 아예 없다** —
아무리 코가 좋아도 못 찾는다. 규칙은 `tags.json` 의 `activity_presence` 에 있고,
종이 다르게 굴면 `animals.json` 의 그 종에 `presence` 를 적어 덮는다
(같은 야행성이라도 "주로 밤"과 "낮에는 절대 없음"이 갈린다).

| activity | 낮 | 여명 | 밤 |
|---|---|---|---|
| 주행성 | 1.0 | 0.5 | **0** |
| 야행성 | **0** | 0.5 | 1.0 |
| 박명성 | 0.3 | 1.0 | 0.3 |

박명성을 0 으로 두지 않은 것은 게이지의 시간대 불일치 계수(×1.5)가 살아 있어야 해서다 —
아예 못 만나면 그 규칙이 죽는다.

`T` 로 시간대를 바꾸면 사라지는 동물에 **먼지가 남는다.** 그냥 없어지면 사라진 줄도 모른다.

## 교감은 **점유 시간** 게이지다

가까이 가서 한 번 누르고 딴 데로 가버리면 차오르지 않는다. 자리를 비우면 **멈춘다.**

> ⚠️ **멈출 뿐 줄지 않고 취소되지도 않는다.** 절대 규칙 2 — 되돌릴 수 없는 실패를
> 만들지 않는다, 중간에 끊기는 게이지도 포함된다 — 를 지키기 위해서다.
> 원칙 1 과 같은 구조다: **방치의 대가는 손실이 아니라 진행 정체다.**

거리는 `hold_radius`(2.6타일)고 시작 반경(`interact_radius` 1.5타일)보다 넉넉하다 —
발을 조금 떼는 것만으로 멈추면 그게 더 답답하다.

멈춘 것은 화면에 보인다: 게이지가 흐려지고 테두리가 깜빡이며 **대상까지 점선**이 그어진다.
왜 안 차는지 모르면 고장으로 읽힌다.

## 개체마다 다르다 (BRIEF §2.5)

**능력치가 무언가를 결정해야 하고, 숫자가 아니라 보이는 것으로 표시한다.**
"민첩 47"은 7살이 못 읽지만 "쟤가 더 빨라"는 즉시 읽힌다.

| | 어디에 |
|---|---|
| 이동속도 | `animals.json` 의 `stats_range.move_speed` — 종마다 범위가 다르고 개체마다 그 안에서 다르다 |
| 개성 | `tags.json` 의 `quirks` 가 어휘, **선택지는 종의 `quirk_pool`** 이 갖는다 |
| 몇 개 갖는가 | 종의 `quirk_count` `[최소, 최대]`. `[0, n]` 이면 아무것도 없는 개체가 나온다 |

```
청설모  1.94(수줍음,재빠름) · 1.32(수줍음) · 2.02(수줍음,재빠름)
너구리  0.99 · 0.82 · 0.98(수줍음)
```

| 개성 | 무엇이 바뀌나 | 화면에서 |
|---|---|---|
| 느긋함 · 재빠름 | 이동속도 ×0.75 / ×1.35 | 또래보다 느리게·빠르게 걷는다 |
| 붙임성 | 교감 게이지 ×0.7 | 게이지가 눈에 띄게 짧다 |
| 수줍음 | 3타일 안에 들어가면 물러선다 | 가까이 가면 물러선다 |

청설모가 `느긋함` 을 갖지 않는 것은 코드가 막아서가 아니라 **풀에 없어서**다.
개성을 늘리려면 `tags.json` 에 어휘를 넣고 그 종의 풀에 적으면 된다 — 코드는 안 고친다.
검증기가 풀에 없는 개성과 `quirk_count` 의 이상을 잡는다.

## 날씨 — 감각에 걸리는 세 번째 축

`tags.json` 의 `weather`. `Y` 로 바꾼다.

| | 개 후각 | 고양이 청각 | 고양이 시야 |
|---|---|---|---|
| 맑음 | 21.5타일 | 13.6 | 8.6 |
| **비** | **9.7** | 9.5 | 7.3 |
| **안개** | 21.5 | 15.6 | **3.0** |

비는 냄새를 씻고 안개는 시야를 먹는다. **비가 개의 우위를 절반 이하로 좁히고,
안개는 고양이의 눈을 3타일까지 줄인다** — 날씨가 "누굴 데려갈까"를 흔든다.

날씨는 화면에서도 보여야 한다(빗줄기·안개). 각 날씨가 무엇으로 보이는지는
`weather.<이름>.shows` 에 적혀 있다 — 보이지 않는 규칙을 만들지 않는다.

## 감각마다 닿는 거리가 다르다

전부 `data/tags.json` 의 `sense_profile` 에 있다. 코드에는 감각 이름이 없다.

```
반경 = guide_radius × 개체 sense_range × range_scale × daypart_scale × terrain_scale
                                          (감각 고유)   (시간대)      (대상이 선 지형)
```

| 감각 | 배율 | 시간대 | 지형 | 성격 |
|---|---|---|---|---|
| 후각 | 1.5 | 영향 없음 | 영향 없음 | **멀리서 추적한다.** 대신 방향과 단서까지만 |
| 청각 | 1.1 | 영향 없음 | 숲 ×0.8 | 중간. 역시 단서까지 |
| 시야 | 0.7 | 여명 ×0.6, 밤 ×0.35 | 숲 ×0.35, 바위 ×0.6 | **가깝지만 몸이 보인다.** 밤·숲에서 무력해진다 |

디버그 오버레이가 지금 각 동료의 실제 반경을 타일 단위로 찍는다
(`감각 반경: 개 후각 21타일   고양이 시야 3타일`).

지형은 `TerrainMap` 이 초원 바탕에 숲·물가·바위 덩어리를 뿌려 만든다
(개수는 튜닝 리소스). 야생동물은 **자기 `habitat` 위에서 시작한다.**

⚠️ `activation_radius`(풀 AI 승격)가 가장 먼 감각보다 좁으면 코가 헛돈다.
Field 가 실제 반경을 계산해 모자라면 올려 쓰고 경고를 남긴다.

## 스프라이트

`SpriteLibrary` 가 `sprites/extracted/<종 id>/<애니메이션>.png` 를 찾는다.
없으면 색 사각형으로 떨어지므로, 그린 종과 안 그린 종이 섞여 있어도 게임은 돈다.

- 스트립은 **가로로 이어붙인 프레임**이고 프레임 폭 = 캔버스 폭(`canvas_by_size_class`)
- 애니메이션 이름: `idle` / `move_side` / `move_north` / `move_south` / `special`
- 서향은 파일을 만들지 않는다 — `move_side` 를 `flip_h` 로 뒤집는다 (§4.5)
- 눈은 공용이 원칙이다. `shared/` 에 **round 스타일**이 구워져 있고, 스타일이 다른 종만
  종 폴더에 따로 구워진다. **어느 쪽을 볼지는 `animals.json` 의 `eye_style` 이 정한다** —
  폴더가 있느냐로 정하면 종 폴더가 다시 생성되는 순간 조용히 그쪽으로 끌려간다.
  **둘 다 없으면 눈 레이어를 얹지 않는다** — 몸통에 얼굴을 그려 넣은 종을 위한 규칙이다
- 없는 애니메이션은 대역으로 채우고 `push_warning` 으로 무엇을 때웠는지 남긴다

지금 그려진 것: **개**(5종 애니메이션), **청설모**(대기·남·측면).
나머지 4종과 플레이어는 아직 색 사각형이다.
눈은 `shared/eye_{front,side}_{기본,기쁨,시무룩}.png` 로 공용이다.

### ⚠️ 내보내는 순서

`export_godot.py` 는 `extracted/` 를 **비우고** 다시 쓴다. 플레이어는 `present_player.py` 가
내보내므로 순서가 뒤바뀌면 `player/` 38개가 통째로 사라진다. 실제로 한 번 사라졌다.

```bash
python export_godot.py      # 동물 · 배경 · 단서 · 먹이
python present_player.py    # 플레이어 (반드시 뒤에)
```

### 그림이 스스로 말하는 것

| 파일 | 무엇을 말하는가 |
|---|---|
| `<종 id>/<종 id>.json` | 캔버스 크기와 앵커. **있으면 이쪽이 이긴다** — 그림에서 나온 값이다 |
| 그림의 아래쪽 빈 줄 | **접지선.** 캔버스 맨 아래가 아니라 발끝이 Actor 원점에 온다 |
| 맨 아래 줄의 가로 폭 | 발밑 그림자 폭 |

`build.py` 는 발끝을 y=27 에 둔다(32px 캔버스). 캔버스 바닥을 발밑으로 삼으면
**동물이 그림자 위에 3px 떠 보인다** — 그래서 접지선을 그림에서 직접 읽는다.
플레이어는 캔버스가 32×48 이라 `size_class` 버킷으로는 안 나온다. `player.json` 이 그것을 말한다.

### 배경 · 표현

| | 어디서 오는가 |
|---|---|
| 지형 타일 | `terrain/<지형 이름>.png` — 파일명이 지형 이름이라 분기가 없다. 변형은 **타일 좌표 해시**로 고른다 |
| 프롭 | `palettes.json` 의 `prop_terrain`. 앵커는 바닥 중앙, 타일보다 높은 것만 발밑 그림자 |
| 공중 단서 | `palettes.json` 의 `clue_airborne` — 후각·청각은 지면에 실물이 없다 |
| 머리 위 아이콘 | `tags.json` 의 `sense_profile.<감각>.icon` → `shared/emote_<이름>.png` |
| 밤낮 | `palettes.json` 의 `background` 4벌을 보간 + `overlay_tint` 를 `CanvasModulate` 로 |
| 눈 되비침 | `tags.json` 의 `eyeshine.by_activity` — 야행성·박명성이 여명·밤에 |

**어두울 때 눈이 되비친다.** 빛을 내는 게 아니라 되비추는 것이라 어두운 시간대에만 걸리고,
대상은 종이 아니라 `activity` 로 정해진다 — 야행성·박명성이면 어느 종이든 자동이다.
눈 텍스처를 되비추는 판으로 갈아끼우고, `self_modulate` 로 화면 전체 틴트를 되돌린다
(되비추는 빛이 어둠에 같이 묻히면 안 된다). 색은 `FieldTuning.eyeshine_color`.

> 밤에는 시야 감각이 3타일까지 줄어든다. 그래서 **어두운 데서는 눈이 먼저 보인다** —
> 두 규칙이 따로 만들어졌는데 같은 그림을 만든다.

밤낮은 **전역 조명을 Light2D 로 하지 않는다** — 배경 팔레트를 갈아끼우고 화면 전체에
틴트를 곱한다. 표현(단서·아이콘)은 팔레트를 타지 않는다. 밤이라고 색이 바뀌면 안 읽힌다.

⚠️ **단서·아이콘 같은 월드 표시는 `CanvasLayer` 가 아니라 월드 노드로 둔다.**
화면 레이어에서 텍스처를 그리면 배율·틴트를 따로 맞춰야 하고, 실제로 통짜 사각형으로
렌더되는 문제도 겪었다.

## 튜닝

`tuning/field_tuning.tres` 를 인스펙터에서 연다.
실행 중이면 **디버그 → 리모트 씬 트리**에서 `Field` 를 골라 `tuning` 을 열면
게임을 돌린 채로 반경·속도·게이지 시간을 바꿀 수 있다. 슬라이더 UI는 만들지 않았다.

## 검증

```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tools/validate_data.gd
```

태그 검증기. 종료 코드 0 = 통과, 1 = 로드 거부. `-- --all` 로 prototype:false 종까지,
`-- --animals=<경로>` 로 다른 데이터 파일을 검사한다.

```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tools/smoke_test.gd
```

DEMO-SPEC §4 완료 조건 중 코드로 확인 가능한 것들(북향 숨김, 정수 스냅 바운스,
감각 대비, 승격/강등, 상성 계수, 게이지 비중단, 초대 처리)을 점검한다.
**재미있는지는 여기서 안 나온다.** 그건 직접 돌려봐야 한다.
