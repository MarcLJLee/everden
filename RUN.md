# Demo 1 실행·검증

Godot 4.7 (`brew install --cask godot`) 기준. 프로젝트 루트가 이 폴더다.

## 실행

```bash
/Applications/Godot.app/Contents/MacOS/Godot --path . 
```

에디터로 열려면 위 명령에 `--editor`, 바로 플레이하려면 그대로 두면 된다.

```
제작사 로고 (RUN II) — 발자국이 하나씩 찍히고 이름이 뜬다. 아무 키나 누르면 건너뜀
    ↓
타이틀 (EVERDEN) — 켤 때마다 배경 지형과 동무가 바뀐다
    ↓
NEW GAME → **집** / SETTING → 설정 / EXIT → 확인 창
    ↓
집 — 대문으로 걸어 나가면 필드
```

`Title.gd` 의 `demo_build` 를 켜면 첫 항목이 `DEMO` 가 되고 집을 건너뛰고 필드로 간다.

### 집 (BRIEF §2.7)

마당을 울타리가 두르고 **집이 위쪽 담장을 대신한다.** 문에서 대문까지 흙길이 나 있고
**대문은 열려 있다** — 닫힌 문은 "못 나간다"로 읽힌다.

- **HUD 는 재화와 자리 둘뿐이다.** 배고픔·청결·기분 게이지를 두지 않는다 —
  그게 생기는 순간 집이 할 일 목록이 되고 "수집이 벌이 되면 안 된다"를 어긴다
- 사물은 `home.json` 의 `for_tags` 로 임자를 정한다. 비어 있으면 누구나 쓴다
- ★ **사물이 새 동작을 요구하지 않는다.** 사물 옆에서 그 동물의 **기존 특징 동작**이
  재생된다. 종 × 사물로 동작을 만들면 7×7 = 49벌이 된다
- **울타리는 막는다.** 대문 구간에서만 아래로 나갈 수 있고 동물은 아예 못 나간다.
  사각형으로 자르면 아래쪽 울타리를 아무 데서나 통과한다 — `Actor.confine` 이 그 장치다

### 갈 수 있는 지형 — habitat 이 정한다

`tags.json` 의 `terrain_walkable` 이 기본을 정하고, 막힌 지형은 **그 지형이 habitat 인
동물만** 들어간다.

| | 초원 · 숲 | 물가 · 바위 |
|---|---|---|
| 플레이어 | 지나간다 | **못 지나간다** (habitat 이 없다) |
| 개 (초원·숲) | 지나간다 | 못 지나간다 — 수영 안 하는 개가 호수를 건너면 이상하다 |
| 수달 (물가) | 지나간다 | **물가는 지나간다** — 자기 집이다 |
| 청설모 (숲) | 지나간다 | 못 지나간다 |

기본 지형은 habitat 과 무관하게 누구나 지나간다. habitat 은 **사는 곳**이지 갈 수 있는
곳의 전부가 아니다 — 청설모가 숲에만 갇히면 한 조각에서 못 나온다.

막힌 곳에 닿으면 **미끄러진다.** 그냥 되돌리면 비스듬히 붙었을 때 갇힌 것처럼 느껴진다.

> ⚠️ **막힌 지형은 얇게 찍는다.** 두껍게 찍으면 한가운데 선 동물에게 교감 반경(1.5타일)
> 안으로 다가갈 수 없어 **초대가 아예 불가능해진다.** 두꺼운 덩어리로 재봤더니 물가
> 타일의 60%, 최대 6타일이 그랬다. 지금은 최대 2타일이고 회귀 테스트가 3타일을 넘지
> 않는지 본다. `물가` 는 물가(가장자리)이지 호수가 아니다.

**아무 키나 누르면 로고를 건너뛴다** — 아이는 이 화면을 수백 번 본다.
개발 중에 진입 화면을 통째로 건너뛰려면 에디터에서 `scenes/field/Field.tscn` 을 열고 F6.

로고 연출(발자국 좌표·타이밍)은 `ui/logo.json`, 타이틀 배치는 `ui/title.json` 에 있다.
**둘 다 다시 그려도 엔진 코드는 안 고친다.**

### 타이틀 배경은 단서다

켤 때마다 종을 하나 고르고, **그 종의 `habitat`** 에서 지형을 뽑고,
**그 종의 `activity`** 로 시간대를 정한다. 숲을 깔고 그 위에 청설모를 세우는 식이다 —
글로 알려주지 않는다. 짝이 어긋나면 틀린 것을 가르치므로 지형은 반드시 habitat 에서 뽑는다.

### ⚠️ Control 의 `_draw` 안에서 텍스처·글자를 그리지 말 것

`draw_texture` · `draw_texture_rect` 는 물론 **`draw_string` 까지** 통짜 사각형으로 나온다
(글리프도 텍스처다). 세 번 겪었다 — 단서 마름모 · 메뉴 커서 · 집 HUD.

**텍스처는 `Sprite2D`, 글자는 `Label` 노드로 둔다.** `_draw` 에는 `draw_rect` 같은
도형만 남긴다. 5×7 비트맵 폰트(`BitmapFont5`)는 도형으로 찍으므로 `_draw` 에서 써도 된다.

| 키 | 동작 |
|---|---|
| WASD / 방향키 / 패드 스틱 | 이동 |
| 스페이스 | 교감 시작 |
| ESC | 교감 취소 (게이지를 끊는 유일한 방법) |
| 1 / 2 | 동료 켜고 끄기 — **개만 켜고 청설모를 찾아보는 것이 이 데모의 핵심 장면** |
| T | 시간대 전환 (낮 → 여명 → 밤) |
| R | 필드를 다시 깔고 "첫 유도까지 시간" 다시 측정 |

## 무엇이 보이는가

멀리 있는 동물은 **그냥 보이지 않는다.** 보이게 만드는 길은 둘뿐이다.

| | 조건 | 화면 |
|---|---|---|
| 가까이 간다 | `reveal_radius`(기본 3타일) 안 | 몸이 보인다 |
| 몸을 드러내는 감각으로 잡는다 | `sense_profile.reveals` 가 true 인 감각 (지금은 시야) | 그 자리에서 몸이 보인다 |
| 그 밖의 감각으로 잡는다 | 후각·청각 | **마름모 단서만** 뜬다. 누구인지는 다가가야 안다 |

보이던 동물이 사라지거나 나타나면 **먼지 이펙트**가 뜬다. 그냥 없어지면
아이가 사라진 줄도 모르기 때문이다.

## 시간대는 **누가 나와 있는가**를 바꾼다

감각 반경만 바뀌는 게 아니다. **밤에만 나오는 동물은 낮에 필드에 아예 없다** —
아무리 코가 좋아도 못 찾는다. 규칙은 `tags.json` 의 `activity_presence` 에 있고,
종이 다르게 굴면 `animals.json` 의 그 종에 `presence` 를 적어 덮는다
(같은 야행성이라도 "주로 밤"과 "낮에는 절대 없음"이 갈린다).

| activity | 낮 | 여명 | 밤 |
|---|---|---|---|
| 주행성 | 1.0 | 0.5 | **0** |
| 야행성 | **0** | 0.5 | 1.0 |
| 박명성 | 0.3 | 1.0 | 0.3 |

박명성을 0 으로 두지 않은 것은 게이지의 시간대 불일치 계수(×1.5)가 살아 있어야 해서다 —
아예 못 만나면 그 규칙이 죽는다.

`T` 로 시간대를 바꾸면 사라지는 동물에 **먼지가 남는다.** 그냥 없어지면 사라진 줄도 모른다.

## 교감은 **점유 시간** 게이지다

가까이 가서 한 번 누르고 딴 데로 가버리면 차오르지 않는다. 자리를 비우면 **멈춘다.**

> ⚠️ **멈출 뿐 줄지 않고 취소되지도 않는다.** 절대 규칙 2 — 되돌릴 수 없는 실패를
> 만들지 않는다, 중간에 끊기는 게이지도 포함된다 — 를 지키기 위해서다.
> 원칙 1 과 같은 구조다: **방치의 대가는 손실이 아니라 진행 정체다.**

거리는 `hold_radius`(2.6타일)고 시작 반경(`interact_radius` 1.5타일)보다 넉넉하다 —
발을 조금 떼는 것만으로 멈추면 그게 더 답답하다.

멈춘 것은 화면에 보인다: 게이지가 흐려지고 테두리가 깜빡이며 **대상까지 점선**이 그어진다.
왜 안 차는지 모르면 고장으로 읽힌다.

### 쏟은 시간은 그 아이에게 남는다

**진행도는 게이지가 아니라 개체가 들고 있다.** 멈춘 동안 다른 아이에게 가도 되고,
돌아오면 쏟다 만 자리에서 이어 찬다. 취소(ESC)해도 시간은 안 사라진다 —
버리면 그게 되돌릴 수 없는 실패다.

- **점유 중일 때는 대상을 못 바꾼다.** 하고 있는 일이 있는데 딴 데를 누르면 그게 더 이상하다
- **멈춰 있을 때 다른 아이 옆에서 누르면 그쪽으로 옮겨간다**
- 쏟다 만 아이 머리 위에 **작은 막대**가 남는다. 어디까지 했는지 안 보이면
  다시 찾아갈 이유가 안 생긴다 (원칙 3 — 진행은 누적으로 보이게)

## 한 판의 흐름 — 첫 만남 → 집 → 지도 → 필드

```
타이틀 ─ NEW GAME ─▶ 첫 만남(강아지 초대) ─▶ 집 ─ 대문 ─▶ 세계 지도 ─▶ 필드
       └ CONTINUE ──────────────────────────┘                        │
                                       (초대할 때마다 저장) ◀────────┘
```

**새 판에는 아무도 없다.** 처음부터 개와 고양이를 들려주면 "친구가 생긴다" 는 이 게임의
첫 사건이 사라진다 — 첫 강아지는 **초대해서 얻는 것**이고, 그 자리가 곧 초대 규칙을
배우는 자리다. 첫 만남이 가르치는 것은 셋뿐이다:
**① 움직인다 ② 가까이 가서 붙어 있으면 친구가 된다 ③ 강아지는 코로 찾아 준다.**

### 이어하기

메뉴는 **이어할 것이 있느냐**에 따라 갈린다. 목록은 설계가 넘긴
`ui/title.json` 의 `items_with_save` · `items_first_run` 이다.

| | 메뉴 |
|---|---|
| 세이브 있음 | **CONTINUE** · NEW GAME · SETTING · EXIT |
| 첫 판 | NEW GAME · SETTING · EXIT |

**이어하기가 맨 위**다 — 흔한 쪽이 위에 있어야 아이가 매번 고르지 않는다.
커서 기본값도 거기다.

이어하면 **첫 만남을 안 지났으면 거기서**, 지났으면 **집으로** 간다.

> ⚠️ **원정 중에 껐어도 잃는 것은 없다.** 초대한 아이는 그 자리에서 저장된다(원칙 2).
> 다만 다시 나가면 개체는 새로 정해진다 — §3.11 의 "개체 정의는 진입 시 한 번" 그대로다.
> ⚠️ NEW GAME 은 세이브가 있을 때만 묻는다. 지울 것이 없으면 되돌릴 것도 없다.

### 상태는 `Game` 오토로드 하나에 있다 (`scripts/game/GameState.gd`)

| | |
|---|---|
| `collection` | 모아온 **개체들**. 종이 아니다 — 같은 청설모라도 `uid` 로 센다 |
| `seen` | 한 번이라도 만난 종. 지도의 "만난 적 있는 아이" 가 여기서 나온다 |
| `region_id` · `party` | 지도에서 고른 **어디로**와 **누구랑** |
| `tutorial_done` | 첫 만남을 지났는가 |

**종 id 는 문자열로 저장한다** (BRIEF §6.5) — 콘텐츠 팩이 바뀌어도 저장이 안 깨진다.
**빼는 길이 없다** — 동물은 죽지 않는다(원칙 1). 회귀가 그런 메서드가 생기는지 지킨다.

> ⚠️ **동료가 느는 순간마다 저장한다.** 원정 중에 창을 닫아도 만난 아이가 사라지면
> 그건 되돌릴 수 없는 손실이다(원칙 2). `add()` 안에서 바로 쓴다.
> ⚠️ **회귀는 진짜 저장을 건드리면 안 된다** — 사람이 모아온 아이들이 거기 있다.
> `Game.autosave = false` 로 잠그고 판을 직접 짠다.
> ⚠️ 오토로드의 `_ready` 는 **첫 프레임에** 돌면서 저장을 읽어온다. 그 전에 판을 짜면
> 조용히 덮어써진다 — 실제로 그렇게 돌다가 동료가 하나로 줄어 세 판정이 깨졌다.
> 테스트는 `await process_frame` **뒤에** 상태를 만든다.

### 집이 보여주는 것은 **모아온 아이들**이다

예전엔 "그림이 있는 모든 종" 을 뿌렸다. 아트가 들어올 때마다 집 식구가 늘어난 것이
그 증상이었다. 지금은 `Game.collection` 그대로다.

### 화면은 **인계 규약**을 따른다 (BRIEF §6.9)

설계가 시안을 PNG 로만 넘겼더니 세계 지도가 갈라졌다. 로고·타이틀은 `logo.json`·
`title.json` 으로 넘겨서 안 갈라졌고, 그 방식을 나머지 화면에 쓴다 —
`sprites/extracted/ui/screens.json` 이 화면마다 요소를 `art` / `text` / `count` /
`keycap` 으로 표시한다.

> ⚠️ **`art` 자리에 라벨이 들어가 있으면 구현 실수가 아니라 인계 실패다.**
> "지형: 물가 · 숲" 도, "만난 아이: 청설모 · 너구리" 도, "동료 2 / 초대 1" 도
> 전부 그림이어야 할 자리다. 7살은 이름을 읽는 것보다 **얼굴을 알아보는 게 빠르다.**

`scripts/ui/Faces.gd` 가 그 얼굴을 만든다. **새로 그리는 도트 0장** — 몸 그림의
머리 언저리를 오린다.

> ⚠️ **`head_anchor` 를 쓰지 않는다.** 그건 머리가 아니라 **머리 위 아이콘이 뜨는 자리**라
> (개는 `[16, 2]`) 그걸 기준으로 오리면 그림 위쪽 **빈 줄**을 오려 온다.
> 캔버스가 아니라 **잉크**를 재고, 그 **오른쪽 위**를 오린다 — 측면 그림은 오른쪽을 본다.
> 풀 흔들림에서 배운 것과 같은 실수다(캔버스 ≠ 그림).
> ⚠️ 크게 오리면 얼굴이 아니라 **작은 전신 그림**이 된다. 32px 몸에서 머리는 14px 다.

### 키캡 — 어떻게 누르는지는 **그림이 말한다** (BRIEF §2.10)

**문장에 `[스페이스]` 를 넣지 않는다.** 넣으면 장치마다 문장을 다시 써야 한다 —
패드를 들었을 때 바뀌는 것이 **그림 하나뿐**이어야 값을 한다.
마지막으로 쓴 입력 장치를 따라 갈아 끼우고, **설정 항목을 만들지 않는다**:
패드를 집어 드는 것 자체가 이미 선택이다.

`Keycap.texture_for("이동" | "고르기" | "인사·정하기" | "그만두기" | "메뉴")`.
자리와 크기가 키캡과 패드에서 같아서 배치가 안 흔들린다.

> ⚠️ 엑스박스 배치 기준이다. 닌텐도는 A/B 가 반대라 글리프를 갈아야 한다.
> ⚠️ `Keycap` 은 `class_name` 스크립트라 **오토로드를 이름으로 못 쓴다** —
> 등록 전에 컴파일될 수 있다. 트리에서 찾아 쓴다.

### 한 화면에 결정 하나 — 지도 → 팀 편성 → 출발

| 화면 | 정하는 것 |
|---|---|
| 세계 지도 | **어디로**만 |
| 팀 편성 | **누구랑**만 |

지도에 동료 줄을 붙였더니 커서가 2차원이 됐고, 7살에게 그건 무겁다(§3.9 v3.17).

### 세계 지도 (BRIEF §3.9)

- 배경을 **새로 그리지 않는다.** 지형 타일을 덩어리로 깔고 길은 **발자국**으로 잇는다 —
  로고·메뉴 커서와 같은 그림이다. 새로 그린 도트는 핀 하나와 집 아이콘 하나뿐
- **가장 많은 지형이 그 지역의 얼굴이다.** 적은 지형을 가운데 두었더니 뒷산 한가운데가
  바위가 돼서 "돌산" 으로 읽혔다
- **축척이 다르면 표현도 달라진다.** 필드에서 "물가" 는 젖은 흙 타일이지만 지도에서
  그것만 깔면 갈색 땅이다 — 가운데를 **물**로 채워야 물가로 읽힌다 (`MAP_CORE`)
- 안 가 본 곳은 `???` 이고 길이 흐리다. 여는 조건을 확률이나 수치로 쓰지 않는다 —
  `promise` 는 조건이 아니라 **약속**이다
- 짝 없이 혼자인 종이 사는 곳에는 **하트 핀**이 뜬다 (§2.4)
- 지형은 **타일 그림 + 이름**, 만난 아이는 **얼굴 줄**. 못 만난 게 있으면 **실루엣 하나** —
  몇 종인지 세지 않는다. 그건 스포일러다
- 동료가 좋아할지는 **얼굴 + 기쁨 눈**으로. 유불리가 아니라 그 아이의 기분이다

> ⚠️ **날씨는 여기 안 들어온다.** 예보도 지역별 날씨 표시도 없다 — 날씨는 나가서
> 겪는 것이지 고르는 재료가 아니다(§3.12).
> ⚠️ **손잡이로 설계하지 않는다.** "코가 좋은 애를 데려가면 유리해요" 같은 말을 하지
> 않는다. 안내는 **"좋아할 거예요"** 까지 — 유불리가 아니라 그 아이의 기분이다.
> ⚠️ 동료가 될 수 있는 것은 `senses` 가 빈 종이 아닌 개체다. 종 이름으로 거르지 않는다
> (두꺼비가 그 경우다).

### 조사는 받침을 따라간다 (`scripts/ui/Josa.gd`)

"개 가 좋아할 거예요" 처럼 띄거나 "청설모이" 처럼 틀리면 아이가 읽다가 걸린다.
한글 음절은 `(코드 − 0xAC00) % 28` 이 0 이면 받침이 없다.

## 동물은 측면 1방향이다 (BRIEF §4.5 ★ v3.16)

**방향은 곱셈이고 모션은 덧셈이다.** 3방향이면 새 모션 하나가 종당 6장, 측면이면 2장이다.
그래서 예산을 방향이 아니라 모션에 쓴다 — 같은 16장으로 먹기·자기·놀기·기뻐하기를 산다.

| | 방향 | 왜 |
|---|---|---|
| 플레이어 | **4방향** | 조작하는 몸이라 어디를 보는지가 곧 입력의 되먹임이다 |
| 동료(개·고양이) | **4방향** | 플레이어를 따라다니느라 **세로로 걷는 시간이 길다** |
| 나머지 동물 | **측면 1방향** | E/W 는 미러. 북·남 그림을 안 쓴다 |

몇 방향을 쓸지는 `animals.json` 의 `sprite_set.facing` 이 정한다. 기본은 `side` 다.
**4방향 도트를 버리지는 않는다** — 본체는 능력을 갖고 데이터가 얼마나 쓸지 정한다.

> ⚠️ **측면 스프라이트가 수직으로 움직이면 미끄러져 보인다. 도트가 아니라 AI 로 푼다** —
> `FieldSim.sideways()` 가 배회 방향에 수평 성분을 항상 남긴다(`SIDEWAYS = 0.55`).
> **방향만 눕히고 속도는 그대로 둔다.** 도트 0장이고, 덤으로 동물이 곧게 안 걸어서
> 더 동물처럼 보인다.

> ⚠️ **"잡다" 를 쓰지 않는다** (§3.12). 이건 포획이 아니라 **초대**다 — 아이가
> 데려가는 게 아니라 얘가 따라오기로 한 것이다. 만나다 · 친해지다 · 초대하다 · 찾다까지다.

## 성별 — 정의 때 정해지고 화면에서 보인다 (BRIEF §4.9 · §2.4)

개체를 정의할 때 성별이 같이 정해진다(§3.11 1단계). 원정 중에 바뀌지 않는다.

**몸통 시트를 성별로 나누지 않는다.** 이형은 눈·입과 같은 **파츠 레이어**고,
북향에서 숨기는 규칙도 그대로 쓴다. 나눴다면 종당 16장이 32장이 됐다 — 파츠면 2장이다.

> ⚠️ `if species == "water_deer"` 를 쓰지 않는다. `palettes.json` 의
> `dimorphism.species` 에 **그 종이 있는지만** 본다. 새 이형 종이 늘어도 코드는 그대로다.
> 지금 표에 있는 종은 고라니 하나 — 수컷의 엄니다.

| 층 | 무엇 | 어디에 |
|---|---|---|
| 이형 | 도트 파츠. 친해지기 전에 멀리서도 안다 | 필드 · 집 (표에 있는 종만) |
| 성별 뱃지 ♂♀ | 같은 회색이고 모양만 다르다. **색으로 나누지 않는다** | 집 마당 |
| 짝 표시 | 하트 · 반쪽 하트 | 집 마당 |

★ **규칙을 짊어지는 것은 ♂♀ 가 아니라 하트다.** 7살은 기호를 읽어서 아기가 왜 안
생기는지 아는 게 아니라, **반쪽 하트**를 보고 "얘는 혼자예요" 를 안다.

**확정 배치** — 둘 이상 놓이는 종은 암수가 반드시 섞인다(`Actor.roll_sexes`).
집에 혼자인 개체가 있으면 그 종의 반대 성별이 필드에 반드시 정의된다
(`FieldSim.pair_needed`, `Home.lonely_species()`). 확률을 손보는 게 아니라
**개체가 반드시 있다** — 성별 때문에 생기는 영구 벽을 구조적으로 막는다.

> ⚠️ 아직 집과 필드가 상태를 주고받지 않아 `pair_needed` 는 실제로는 비어 있다.
> 수집 목록이 생기면 Field 가 `Home.lonely_species()` 를 그대로 넘기면 된다.
> ⚠️ 필드에는 이름표가 없어서 **이형이 없는 아홉 종은 필드에서 성별이 안 보인다.**
> 그때까지는 디버그 오버레이의 `이번 원정:` 줄에 `(♂n♀n)` 로 적힌다.
> 이름표(§2.8 도감)가 생기면 그 옆에 뱃지를 붙인다.

> ⚠️ 짝 표시는 그림 **윗변**(`canvas_offset.y`)에 붙인다. 캔버스 높이만큼 올리면
> 그림에서 한참 떠서 누구 것인지 안 보인다 — 캔버스 바닥은 접지선이 아니다.

## 개체마다 다르다 (BRIEF §2.5)

**능력치가 무언가를 결정해야 하고, 숫자가 아니라 보이는 것으로 표시한다.**
"민첩 47"은 7살이 못 읽지만 "쟤가 더 빨라"는 즉시 읽힌다.

| | 어디에 |
|---|---|
| 이동속도 | `animals.json` 의 `stats_range.move_speed` — 종마다 범위가 다르고 개체마다 그 안에서 다르다 |
| 개성 | `tags.json` 의 `quirks` 가 어휘, **선택지는 종의 `quirk_pool`** 이 갖는다 |
| 몇 개 갖는가 | 종의 `quirk_count` `[최소, 최대]`. `[0, n]` 이면 아무것도 없는 개체가 나온다 |

```
청설모  1.94(수줍음,재빠름) · 1.32(수줍음) · 2.02(수줍음,재빠름)
너구리  0.99 · 0.82 · 0.98(수줍음)
```

| 개성 | 무엇이 바뀌나 | 화면에서 |
|---|---|---|
| 느긋함 · 재빠름 | 이동속도 ×0.75 / ×1.35 | 또래보다 느리게·빠르게 걷는다 |
| 붙임성 | 교감 게이지 ×0.7 | 게이지가 눈에 띄게 짧다 |
| 수줍음 | 3타일 안에 들어가면 물러선다 | 가까이 가면 물러선다 |

청설모가 `느긋함` 을 갖지 않는 것은 코드가 막아서가 아니라 **풀에 없어서**다.
개성을 늘리려면 `tags.json` 에 어휘를 넣고 그 종의 풀에 적으면 된다 — 코드는 안 고친다.
검증기가 풀에 없는 개성과 `quirk_count` 의 이상을 잡는다.

## 날씨는 이름이 아니라 축이다 (BRIEF §6.8)

`cloud · fog · rain · snow · wind` 각각 0~1. **이름(맑음·폭우…)은 한 점에 붙인 별명일 뿐**이라
새 날씨를 추가해도 코드를 안 고친다. 축 값 자체가 세기라 "조금 오는 비"가 표현된다.

- 축·상한·프리셋은 `weather/weather.json`(설계 세션 소유)
- **감각 배율은 이름이 아니라 축에 건다** — `sense_profile.<감각>.weather_axis_scale`
  (`rain` → 후각 · `fog` → 시야 · `wind` → 청각)
- **틴트는 표가 아니라 축에서 뽑는다.** 가랑비와 폭우가 같은 색일 수 없다
- **맑은 날의 출발점은 흰색이 아니라 살짝 밝고 따뜻한 색**이다(`TINT_CLEAR`).
  흰색에서 시작하면 맑은 날이 "아무 일도 없는 날"로 보인다 — 맑음도 하나의 날씨다
- 틴트만으로는 밝기만 오르고 밋밋하다. **햇살 얼룩**이 그 위를 지나가며 결을 만든다.
  `daypart_daylight` 에 묶여 있어 **밤에는 비치지 않는다**
- **팔레트는 시간대만 건드린다.** 날씨는 틴트와 오버레이로 만난다 —
  그래야 시간대 × 날씨가 곱셈(4×6)이 아니라 덧셈이 된다

### 지형이 날씨를 정하고, 날씨는 흘러간다

`tags.json` 의 `weather_bias` 가 지형별로 어느 축이 잘 서는지 갖는다.
필드는 지형이 섞여 있으므로 **타일 수로 가중평균**한다.

| 지형 | 안개 | 비 | 가장 잦은 날씨 |
|---|---|---|---|
| 초원 | 15% | 30% | 맑음 22% |
| 숲 | 40% | 25% | 짙은안개 21% |
| 물가 | **41%** | **31%** | 짙은안개 25% |
| 바위 | 18% | 24% | 맑음 23% |

날씨는 목표를 하나 잡고 **천천히 옮겨간다.** 도착하면 24~60초 머물다 다음을 잡는다.

- **다음 목표는 지금과 가까운 것 중에서 고른다.** 맑음에서 폭우로 바로 가면
  그건 날씨가 아니라 스위치다. 맑음 → 구름조금 → 흐림 → 가랑비 → 비 로 **걸어간다**
- 속도가 일정하므로 **크게 바뀔수록 오래 걸린다.** `weather_drift_seconds`(22초)는
  축을 0 에서 1 까지 옮기는 데 걸리는 시간이고, 0.2 만 바뀌면 4.4초다

```
40분 관찰: 가랑비 → 흐림 → 구름조금 → 흐림 → 구름조금 → 맑음 → 구름조금
        → 옅은안개 → 흐림 → 옅은안개 → 짙은안개 → 맑음 → 흐림 → …
```

> ⚠️ 추첨을 "축 합이 큰 프리셋이 이긴다"로 짰더니 **늘 폭우만 나왔다.**
> 축 합은 *얼마나 사나운가*이지 *얼마나 잦은가*가 아니다. 지금은 셋을 곱한다 —
> 사나울수록 드물게 · 지형이 받쳐주면 자주 · 같은 날씨 연달아는 덜.

`Y` 로 다음 날씨를 강제로 뽑는다(치트). 어느 지형도 축을 0 으로 막지 않는다 —
"이 지형에서는 절대 비가 안 온다"는 기다림을 강요하는 규칙이 된다.

## 풀과 나무는 바람에 흔들린다

**새 도트가 필요 없다.** `PlaceholderArt.swayed_texture()` 가 가로줄을 **정수 픽셀로
밀어서** 기운 판을 굽고, `PropScatter` 가 그걸 갈아끼운다. 도트 게임에서 바람은
원래 이렇게 만든다 — 스프라이트를 실제로 회전·전단하면 Nearest 에서 도트가 뭉개진다.

- **밑동은 안 움직인다.** 위로 갈수록 많이 밀린다(`pow(높이비, 1.7)`) —
  뿌리가 흔들리면 그건 바람이 아니다
- **색을 새로 만들지 않는다.** 미는 것이지 그리는 게 아니다
- 어느 프롭이 얼마나 흔들리는지는 `data/prop_sway.json`. 여기 없으면 안 흔들린다 —
  바위와 통나무가 흔들리면 그게 더 이상하다

> ⚠️ 기준은 캔버스 위가 아니라 **그림의 우듬지**다. 프롭은 캔버스 아래쪽에 붙어 있어서
> (풀 뭉치는 16 캔버스에 아래 10줄만 그려져 있다) 캔버스 위를 꼭대기로 잡으면
> **가장 많이 밀리는 줄이 빈 줄**이 된다. 실제로 풀이 거의 안 흔들려 보였다.

### 눈금은 **실제로 부는 바람**에 맞춘다

0~1 을 눈금으로 쓰면 기울어지는 모습을 평생 못 본다. 100시간치를 재봤다:

```
0.1~0.2  16.0%   0.3~0.4  54.4%   0.5~0.6   0.0%
0.2~0.3  16.4%   0.4~0.5  12.3%   0.6~0.7   0.8%   ← 폭우 하나뿐
```

그래서 **0.55 를 "센 바람"** 으로 놓는다. 흐림 0.46 이 눈에 띄고 폭우 0.66 이 다 눕는다.

| 바람 | 언제 | 기울기(px, lean 2) | 모양 |
|---|---|---|---|
| 0.10 | 짙은안개 | −1 ~ +1 | 거의 안 움직인다 |
| 0.24 | 함박눈 | −1 ~ +1 | 제자리에서 살랑 |
| 0.38 | 구름조금 · 비 | −1 ~ +2 | 바람 쪽으로 조금 |
| 0.46 | 흐림 | −1 ~ +2 (중심 +0.8) | 눈에 띄게 치우친다 |
| 0.66 | 폭우 | +1 ~ +3 (중심 +2.0) | **기운 채로 잔떨림** |

산들바람에는 좌우로 흔들리다가, 세지면 흔들림의 **한가운데가 바람 쪽으로 밀려간다.**
다 누운 뒤에는 크게 흔들릴 자리가 없어 잔떨림만 남는다.
기우는 쪽은 구름·비가 흐르는 쪽과 같다(+x) — 따로 놀면 화면이 어긋나 보인다.

### 한 박자로 흔들리면 바람이 아니다

어긋나게 하는 장치가 셋이다. **셋째가 제일 크다.**

1. **자리에서 뽑는 위상** — 돌풍이 들판을 가로질러 지나간다
2. **포기마다의 위상 흔들기** — 옆 포기와 딱 붙어 놀지 않는다
3. **포기마다의 빈도 흔들기**(±30%) — 시간이 갈수록 서로 벌어진다

> ⚠️ 빈도를 포기마다 달리해도 **한 포기 안에서는 상수**라 위상이 튀지 않는다.
> ⚠️ 위상은 **더해서 쌓는다.** `sin(경과시간 × 속도)` 로 쓰면 바람이 변하는 동안
> 위상이 `경과시간 × 속도변화` 만큼 건너뛰어서 **날씨가 바뀌는 순간 나무가 휘청인다.**
> 날씨 겹에서 똑같은 실수를 했다 — 회귀가 "바람이 변할 때와 안 변할 때의
> 한 프레임 변화폭"을 견주어 잡는다.
> ⚠️ 화면 안에 있는 것만 갈아끼운다. 안 보이는 데서 흔들 이유가 없다.
> ⚠️ 팔레트가 바뀌면 **기울인 판도 같이 버려야 한다.** 안 그러면 밤이 됐는데
> 흔들리는 풀만 낮 색으로 남는다.
> ⚠️ 투명 픽셀도 그대로 옮긴다. 건너뛰면 한 칸도 안 밀린 줄까지 원본과 달라진다.

**커다란 나무는 그림만 오면 된다.** 프롭은 바닥 중앙 앵커에 Y-sort 대상이라 크기
제한이 없다 — `sprites/HANDOVER-flora.md` 로 요청해 두었다.
다만 프롭은 아직 길을 막지 않는다.

## 화면에 늘 보이는 것은 **지명과 자리 둘뿐**이다 (BRIEF §3.4)

집이 재화·자리 둘만 두는 것과 같은 규칙이다(§2.7). 배고픔·청결·기분 게이지를 두지
않는 이유와 같다 — **늘 보이는 것이 늘어나면 화면이 할 일 목록이 된다.**
나머지(동료 감각 아이콘 · 방향 · 점유 시간 게이지)는 **그때만** 뜬다.

> ⚠️ 개발용 숫자는 **평소에 꺼져 있다** — `F3` 로 켠다. 늘 떠 있으면 그게 화면의
> 첫인상이 되고, 다른 화면 위로도 겹친다(실제로 나가는 문 위에 겹쳐 나왔다).
> 데이터 로드 거부는 예외다 — 꺼져 있어도 뜬다.

## 갇히면 안 된다

스폰 자리가 물이나 바위면 어느 쪽으로도 못 움직여 **그대로 갇혔다**(사용자 지적).
되돌릴 수 없는 벽이라(원칙 2) 세 겹으로 막았다.

1. 플레이어는 `nearest_standing()` 이 찾은 **설 수 있는 가장 가까운 자리**에 선다
2. 동물도 스폰 자리를 못 찾으면 마찬가지로 옮긴다
3. **`slide` 가 나가는 길을 언제나 연다** — 못 서는 자리에 있으면 어느 쪽으로든 나갈 수 있다

> ★ 셋째가 본체다. **들어가는 것만 막을 일이지 나가는 것까지 막을 이유가 없다** —
> 이 한 줄이 "어쩌다 갇혔는가" 를 전부 덮는다. 스폰이든, 지형이 바뀌었든.
> ⚠️ 그래도 **들어가는 것은 여전히 막힌다.** 나가는 길을 연다고 통행 규칙이 풀리면 안 된다 —
> 회귀가 둘 다 잰다.

## 초대 카드 (BRIEF §3.12)

게이지가 끝나면 누구를 초대했는지 띄운다. **축하지 평가가 아니다** —
능력치 숫자를 넣지 않는다. 넣는 순간 "다시 뽑을까" 가 생기고, 그건 수집이 벌이 되는 길이다.

주인공은 **성별 + 단계**다. 아이가 묻는 것은 숫자가 아니라 관계다 —
*"얘는 엄마 사슴인가, 아기인가?"* 숫자 나이는 곁들이고, 아기는 숫자를 안 붙인다.
설명 두 줄은 **실존에서 가져온 사실**이다(`animals.json` 의 `blurb`).

새로 그리는 도트 **0장** — 몸도 눈도 뱃지도 이미 있는 것을 크게 얹을 뿐이다.
몸은 **정수배로만** 키운다.

> ⚠️ 회귀가 "매력 · 감각 반경 · 속도 · × · %" 가 새어 나오는지 지킨다.
> ⚠️ **캔버스 크기를 못 박지 않는다.** 32 로 박아뒀다가 24폭으로 그려진 청설모가
> 한 프레임 반으로 잘려 **몸이 두 번** 나왔다. `SpriteLibrary.canvas_of()` 로 구한다.
> ⚠️ 같은 프레임에 두 번 띄우면 `queue_free()` 만으로는 **옛 글자가 아직 붙어 있다.**
> 떼고 나서 버린다.

## 나가는 길 (BRIEF §3.13)

**START(키보드는 Enter, 패드는 Menu)로 연다.** 이게 없어서 원정을 나가면 못 돌아왔다.
쏟다 만 게이지는 개체에 남으니 **나가는 것이 손해가 아니다**(원칙 2) —
그래서 묻는 말도 경고가 아니라 물음이다: *"집에 갈까요?"*

고를 것은 둘뿐이고 기본은 언제나 안전한 쪽(**더 놀래요**)이다.
같이 가는 아이는 **얼굴 줄**로 보여준다 — "동료 2 / 초대 1" 처럼 적으면 그게 성적표가 된다.

> ⚠️ **여는 키도 이 화면이 갖는다.** 필드가 따로 폴링하면 같은 한 번의 누름을 양쪽이
> 보게 되어 **열자마자 닫힌다.** 키 하나는 한 곳이 갖는다.

## 잡을 수 없는 것들 (BRIEF §3.10)

> 보이는 것이 전부 수집 대상이면 숲이 아니라 **쇼핑 목록**이다.

앰비언트 생물은 **도감에 없고, 자리를 안 먹고, 데이터도 없고, 상호작용도 없다.**
지형 태그 하나만 들고 있다. 종당 도트 한두 장이라 수집종(종당 16장) 대비
10분의 1 값으로 생태를 열 배로 만든다.

| | 지형 | 층 |
|---|---|---|
| 나비 · 벌 | 초원 · 숲 | 공중 — Y-sort 밖, 지면보다 10px 위 |
| 잠자리 | 물가 | 공중 |
| 물고기 | 물가 | 수면 — Y-sort 안 |
| 새 그림자 | 초원 · 숲 · 바위 | 지면 — Y-sort 안 |
| 개미 | 초원 · 숲 | 지면 |

**새는 그리지 않고 그림자만 그린다** — 하늘은 화면 밖이다. 그림자만으로 위에
무언가 날고 있다는 게 성립하고 도트가 절반으로 준다. 반투명으로 얹는다.

표가 둘이다. 무엇이 어디 사는지는 **설계 세션**(`ambient/ambient.json`),
어떻게 움직이는지는 **구현**(`data/ambient_motion.json`) — 넷뿐이다:
`flutter`(갈지자로 떠다닌다) · `dart`(멈춰 있다 가끔 튄다) ·
`crawl`(느리게 한 방향) · `glide`(가로질러 지나가고 사라진다).

### 얼마나 사는지는 지역이 정한다

`regions.json` 의 `ambient` 가 수집종 `ecology` 와 **같은 규칙**을 쓴다 —
**적히지 않은 생물은 그 지역에 없다.** 값은 기본 마릿수에 곱하는 배율이고,
소수점은 굴린다(0.4 마리 = 열에 넷은 있다). 어디에 나타날지는 지형 태그가 따로 정한다.
뒷산에 나비가 많고 도시에 없는 것은 여기서 갈린다.

> ⚠️ 지역이 `ambient` 를 아예 안 적으면 **기본값으로 산다.** 새 지역을 추가했을 때
> 조용히 죽지 않게 하기 위한 것이다 — 검증기가 이름 오타는 따로 잡는다.
> ⚠️ 공중은 이 노드에, 지면·수면은 **Actors 안**에 붙인다. Y-sort 는 노드를 넘어
> 동작하지 않으므로, 캐릭터에 가려야 하는 것은 같은 컨테이너에 있어야 한다.

## 정의 · 출현 · 발견은 다른 시점이다 (BRIEF §3.11)

| 단계 | 언제 | 무엇이 정하나 |
|---|---|---|
| **개체 정의** | 필드에 들어가는 순간 **한 번** | 지역 생태 × 그 필드의 지형 비율 |
| **출현** | **날씨가 바뀔 때마다** | 놓인 개체가 지금 나와 있는가 |
| **발견** | 플레이 내내 | 동료의 감각 · 단서 · 관찰 |

**개체수 0 은 정상이다.** 어떤 종은 이번 원정에 아예 없다 — 그것이
"조건을 다 맞췄는데 왜 없지"의 정직한 답이고, 확률 파라미터가 아니라 **개체 수 분포**다.
디버그 오버레이의 `이번 원정:` 줄이 그 목록이다.

⚠️ **날씨가 바뀌어도 개체는 늘지도 줄지도 않는다.** 바뀌는 것은 지금 나와 있는가뿐이다.

### 냇가는 덩어리가 아니라 **줄기**다

물을 웅덩이 아홉 개로 찍었더니 물이 **3.2%** 뿐이고 흩어져서 **"냇가에 물이 없다"** 가
됐다(사용자 지적). `patches` 옆에 `streams` 를 뒀다 — 한쪽 끝에서 반대쪽 끝까지
**이어져 흐른다.** 이어져 있어야 찾아가면 반드시 만난다.

| | 물줄기 | 물 |
|---|---|---|
| 뒷산 | 1 갈래 · 폭 2 | 3.5% |
| 냇가 | 2 갈래 · 폭 4 | 11.4% |

**폭도 흔들린다** — 좁아졌다 넓어지는 게 실제 냇가고, 넓기만 하면 한가운데 선 동물에게
다가갈 수 없다. 폭 4로 곧게 뻗었더니 물 타일의 절반이 뭍에서 2타일 넘게 떨어졌다.

> ⚠️ 그래도 **물 한가운데 갇히면 영영 다가갈 수 없다** — 되돌릴 수 없는 벽이다(원칙 2).
> 재보니 수달 하나가 **60초 내내 0%** 였다. 물줄기를 좁히면 도랑이 되므로 **AI 로 푼다**:
> 밟을 수 있는 땅이 초대 반경 안에 없으면 배회 목표를 **뭍 쪽으로** 돌린다
> (`FieldSim._bankward`). 도트 0장이고, 수달은 원래 물가로 나온다.
> 물 밖으로 **내보내는 게 아니다** — 서식지는 `slide` 가 그대로 지킨다.
> 고친 뒤 18마리 전부 100%, 갇힌 개체 0.

### 물가는 **자리에 따라 다르게 그린다**

`물가` 는 **젖은 흙** 타일이라 얇은 가장자리일 때는 물가로 읽히지만, 물줄기 폭이
넷이 되니 그냥 **흙길**로 보였다(사용자 지적). 지도에서 배운 것("축척이 바뀌면 표현도
바뀐다")이 필드 안에서도 그대로다.

| 자리 | 그림 |
|---|---|
| 사방이 물 | `extra/water_0` · `water_1` |
| 뭍과 만남 | 그쪽 `extra/shore_*` |
| 맞는 조각 없음 | 원래 젖은 흙 |

> ⚠️ **지형 이름을 늘리지 않는다.** 늘리면 통행·서식지·생태 표가 전부 따라 늘어난다 —
> 이건 **그리는 문제**지 세계의 문제가 아니다. 회귀가 지형이 넷 그대로인지 지킨다.

### 지역이 생태와 지형을 함께 정한다

`data/regions.json`. **같은 종이라도 지역마다 마릿수가 다르다** —
청설모가 뒷산에 흔해도 다른 곳에선 드물 수 있다. 적히지 않은 종은 그 지역에 살지 않는다.

```
                 뒷산과 냇가   (가상) 냇가 하류
청설모              5.0마리        0.8마리
수달                0.8마리        3.7마리
삵                  0.7마리        0마리 (안 산다)
```

지역은 **지형도 정한다.** 뒷산에 늘 개울이 있으면 "뒷산에 수달이 산다"가 되어버리므로,
`home_hills` 는 숲 14 · 바위 5 · **물가 1** 이다. 개울 한 줄기뿐이라 수달은
원정 서너 번에 한 번 보인다(평균 0.3마리) — **가끔 보여야지 늘 있으면 안 된다.**

> ⚠️ 처음엔 "지나가는 개체"를 위해 기대값에 하한을 뒀는데, 그러면 물가가 한 타일도
> 없는 필드에도 수달이 두 마리씩 나왔다. 서식지가 없으면 기대값도 0 이어야 한다.

### 날씨가 누가 나오는지를 바꾼다

`animals.json` 의 `weather_likes` 가 종마다 어느 축을 좋아하는지 갖는다.
축이 1 일 때의 배율이고, **축이 0 이면 아무 영향이 없다** — 맑은 날은 모두에게 평등하다.

| 종 | 맑음 | 비 | 안개 |
|---|---|---|---|
| 두꺼비 | 50% | **89%** | 58% |
| 너구리 | 50% | 58% | 50% |
| 청설모 | 46% | **29%** | 48% |
| 참새 | 42% | **24%** | 46% |

- **날씨가 성공·실패를 바꾸지 않는다.** 바꾸는 것은 누가 나오는지뿐이다
- **0 으로 막지 않는다.** "이 날씨엔 절대 안 나온다"는 기다림을 강요한다.
  검증기가 0 이하를 거부한다
- 축이 계속 흐르므로 매 프레임 다시 굴리면 동물이 깜빡인다 —
  **날씨의 이름이 바뀔 때만** 다시 정한다

### 화면에 얹는 겹 (WeatherLayers)

`weather/*.png` 는 전부 **이어 붙는 타일**이고 엔진은 스크롤만 한다 —
화면 크기 텍스처를 굽지 않으므로 해상도가 바뀌어도 그대로 쓴다.
액터 **다음에** 그리므로 구름 그림자가 캐릭터 위에도 떨어진다.

| 겹 | 언제 | |
|---|---|---|
| **빛줄기** | 구름 0.26 부근 · 낮에만 · **비·안개가 끈다** | 구름 사이로 쏟아지는 선형 빛. 구름이 아주 없으면 새어 나올 틈이 없고, 꽉 차면 샐 곳이 없다 |
| **햇살 얼룩** | 맑을수록 · 낮에만 · **비·안개가 끈다** | **구름 그림자와 같은 타일**을 위상만 어긋나게 해 밝게 더한다. 구름 사이로 새는 빛이라 그림자와 짝이고, 새로 그리는 도트가 0장이다 |
| 구름 그림자 | **항상** | 맑은 날에도 흐른다. 날씨가 "없는" 상태를 만들지 않는 장치 |
| 안개 | `fog` | |
| 비 | `rain` | |
| 폭우 | `rain > 0.55` | 별개 날씨가 아니라 비가 센 것이다 |
| 눈 | `snow` | **여기 없다.** 낱개가 눈에 보이는 유일한 날씨라 `SnowField` 가 하나씩 그린다 (아래) |

바람 하나가 **구름 방향과 빗줄기 기울기를 동시에** 정한다. 따로 놀면 화면이 어긋나 보인다.

#### 해가 드는 연출은 구름만 보고 정하지 않는다

`damp` 가 다른 축으로 겹을 끈다 — 비가 오면 직사광이 없으므로 빛줄기도 햇살 얼룩도
사라져야 한다. **제곱으로 떨어뜨린다**: 선형으로 깎으면 비가 꽤 와도 빛이 남는다.
이슬비에는 해가 남고 소나기에는 없다 — 껐다 켜지는 스위치가 아니다.
안개는 `0.6` 으로 덜 깎는다. **빛이 물방울에 걸려야 줄기가 보이기 때문**이다.
축을 곱해서 끄므로 "비 오는 날씨" 라는 이름을 새로 만들 필요가 없다 (원칙 4).

#### 빛줄기는 점심 무렵에만 선다

`daypart_sun_height` 가 빛의 **양**(`daypart_daylight`)과 따로 있다. 여명에도 빛은
있지만 해가 낮게 걸린다 — 기둥이 수직에서 27° 라 **해가 높이 떠야 나오는 각도**고,
해가 낮은 시간에 그대로 쏟으면 각도가 거짓말이 된다. 제곱해서 정오 쪽으로 몰아준다.
낮 1.0 · 여명 0.12 · 밤 0.

#### 눈만 타일이 아니다 (SnowField)

다른 날씨는 전부 이어 붙는 타일을 스크롤한다 — 싸고, 구름·비·안개는 낱개가 눈에 안
띄므로 충분하다. **눈은 다르다. 눈송이는 하나씩 보인다.** 타일로 만들었다가 셋이
한꺼번에 어긋났다(전부 사용자 지적):

- 96 타일이 화면에 세 번 들어가 **같은 배열이 반복돼 패턴이 읽혔다**
- 2×2 눈을 2배로 키워 **4×4 덩어리**가 됐다 — 눈은 커지는 게 아니라 큰 게 섞이는 것이다
- 타일이 통째로 움직이니 **모든 눈이 한 몸처럼 흔들렸다**

셋 다 "낱개를 낱개로 다루지 않아서" 생긴 것이라, 낱개로 그리니 한꺼번에 없어졌다.
640 송이가 512×288 월드 격자에 뿌려져 있고, 각자 크기·낙하·바람·흔들림 진폭·주기·위상이
다르다. 큰 눈일수록 가깝다 — 빨리 떨어지고 크게 흔들리고 진하다.

**텍스처가 아니라 네모를 그린다.** 눈송이가 원래 1~3px 흰 네모라 `draw_rect` 가 곧
도트다 — Control 의 `_draw` 에서 텍스처를 그리면 안 된다는 그 규칙에 걸리지 않는다.
자리는 **월드에 고정**한다: 화면 기준으로 감으면 카메라를 따라 눈이 끌려다닌다.

> `weather/snow_far.png` · `snow_near.png` 는 이제 **쓰이지 않는다.** 지우지는 않았다 —
> 설계 세션 소유이고, 쌓인 눈이나 정지 연출에 다시 쓸 수 있다.

#### 눈은 [U] 로 켠다

눈은 계절이 생긴 뒤에만 저절로 온다(`weather.json` 의 `season_note`) — `_pick_target`
이 `snow > 0` 인 프리셋을 후보에서 뺀다. 그 전에도 눈으로 확인할 수 있게 `[U]` 가
진눈깨비 → 눈 → 함박눈 → 평소로 돌린다. 치트지 계절 구현이 아니다.

#### 빛은 한 겹이면 판때기다

같은 타일을 **2배로 키운 먼 겹**(`rays_far`)을 뒤에 깔고 더 느리게 흘린다.
굵기가 섞이고 두 겹이 서로 스쳐 지나가면서 산란처럼 읽힌다 —
새로 그리는 도트는 0장이고, 배율은 **정수배** 라 픽셀 규칙도 그대로다.

거기에 겹마다 **아주 느린 숨쉬기**(`pulse`, 주기 8·13·17초)를 얹는다. 주기가 서로
어긋나 있어 맞물렸다 풀린다. 세기만 건드리고 무늬는 건드리지 않는다 —
무늬를 흔들면 디더가 자글거린다. 숨쉬기는 그리는 쪽에만 있고 `alpha_for` 에는 없다:
시간이 섞이면 회귀가 잴 수 없는 값이 된다.

#### 무늬는 카메라가 아니라 **땅**에 붙는다

도트 게임에서 비가 화면에 고정된 채 내리면 유리창에 그린 것처럼 보인다.
카메라는 오려내는 창일 뿐이다 — 그래서 텍스처를 샘플링하는 원점에 화면 위치를 넣는다
(`region_rect.position = view.position - travel`). 월드 한 점은 카메라가 어디에 있든
같은 텍스처 좌표를 본다. 화면 고정으로 만들려면 오히려 `view.position` 을 **빼야** 하니,
비용은 뺄셈 하나 차이다. 회귀가 이 성질을 직접 잰다.

> ⚠️ **흐른 양은 더해서 쌓는다.** `속도 × 경과시간` 으로 매번 다시 세면 바람이 변하는
> 동안 속도가 **경과 시간만큼 뻥튀기**돼서 날씨가 바뀔 때마다 화면이 확 쓸려 간다
> (사용자 지적 — 300초 지난 판에서 한 프레임에 163px, 정상의 200배였다).
> 보이지 않는 겹도 같이 쌓아야 다시 나타날 때 튀지 않는다.
> ⚠️ **정수 픽셀로만 스크롤한다.** 반픽셀이면 디더 무늬가 자글거려 도트가 아니라 노이즈가 된다.
> ⚠️ **region 을 빼야 무늬가 그 방향으로 흐른다.** 더하면 텍스처 아래쪽을 화면 위에서
> 샘플링하게 되어 **비가 아래에서 위로 올라간다** — 실제로 그렇게 만들었다.
> `drift` 는 이제 화면에서 보이는 방향이다.
> ⚠️ **타일은 디더 마스크지 반투명 판이 아니다.** 구름 그림자는 53%가 거의 검정이라
> 곱연산으로 통째로 깔면 **맑은 날에도 화면이 까매진다** — 실제로 그렇게 만들었다가 못 볼
> 화면이 나왔다. 세기는 알파로 준다.
> ⚠️ **겹은 화면보다 넓게 깐다** (`Field.WEATHER_MARGIN`, 1.6배). 화면 크기를 그대로 쓰면
> 창 종횡비가 기준과 어긋날 때 뷰포트가 기준 해상도보다 커져서 **오른쪽 절반이 맨땅으로
> 남는다** — 실제로 그렇게 보였다. 타일 반복이라 넓게 까는 값은 공짜다.
> ⚠️ **카메라가 실제로 보는 곳에 깐다** — `get_screen_center_position()`.
> `global_position` 은 카메라가 *가려는* 곳이지 보고 있는 곳이 아니다. 맵 끝에서는
> `limit_*` 이 잡아 세우고, 달리는 동안은 `position_smoothing` 이 뒤처진다.
> 플레이어 위치로 깔았더니 **구석에서 화면 한쪽이 맨땅으로 남았다** (사용자 지적).
> ⚠️ 회귀에서 판을 둘 세울 때 **나중에 들어온 카메라는 current 가 되지 않는다.**
> 그대로 두면 `get_screen_center_position()` 이 (0,0) 을 돌려주고 겹과 화면이 둘 다
> 원점을 봐서 판정이 늘 통과한다 — `make_current()` 를 부르고, 카메라가 실제로
> 움직였는지 따로 단언한다. 조용히 넘어가는 테스트가 이렇게 생긴다.

회귀가 "가장 사나운 날씨에도 화면이 60% 넘게 덮이지 않는다"를 지킨다 —
날씨는 연출이지 장애물이 아니다.

**집과 타이틀에도 날씨가 걸린다.** 다만 집은 **맑고 구름이 흐르는 정도**만 한다 —
마당은 보는 곳이지 견디는 곳이 아니다. 타이틀은 그 배경 지형의 날씨를 한 점 골라 머문다.

#### 빛줄기는 **끝이 있는 기둥**이다

처음 넘겨받은 도트는 화면 끝까지 이어지는 사선 격자였고, 빛이 아니라 **줄무늬 필터**로
보였다. 굵기를 흐트러뜨려도 소용없었다 — 문제는 굵기가 아니라 **길이**였다.

박명광선은 구름 틈에서 시작해 아래로 옅어지며 사라진다. 그래서 좌표를 둘 쓴다:
`u = (2x + y) % n` 이 굵기를, `v = (x - 2y) % n` 이 **길이**를 정한다.
`x` 에 곱해야 기둥이 서고(수직에서 27°), `y` 에 곱하면 눕는다.
아래로 1px 갈 때 `v` 가 2 줄므로 세로 230px 기둥은 `len=460` 이고 타일이 **512** 다.

규칙은 `PlaceholderArt.light_shaft_texture()` 한 곳에만 있다.
`tools/export_light_shaft.gd` 가 그걸 구워 `weather/light_shaft.png` 에 넣는다 —
**설계 세션이 `sprites/HANDOVER-light_shaft.md` 를 반영하면 이 스크립트는 지운다.**

회귀가 "빛이 4.8%뿐이다(격자가 아니다)"와 "빛이 닿지 않는 열이 있다(끝이 있다)"를
파일에서 직접 잰다 — 도트가 다시 들어와도 이 성질은 지켜져야 한다.

## 보이는 것은 언제나 **내 시야 안**에 있는 것이다 (BRIEF §3.14)

예전에는 고양이가 18타일 밖의 동물을 감지하면 **그 몸이 화면에 그려졌다.**
아이 눈에는 안 보이는 거리인데 말이다 — 그건 세계의 사건이 아니라
**게임이 알려주는 것**이었다(미니맵에 적이 뜨는 것과 같은 종류다).

**몸을 원격으로 드러내는 감각은 없다.** `reveals` 를 지웠다.
감각은 전부 "거기에 뭔가 있다" 까지만 말하고, 몸은 `reveal_radius` 안에서만 보인다.
그래서 **발견의 순간이 아이에게 남는다** — 동료는 데려다주고, 보는 것은 아이가 한다.

### 그래도 범주는 갈린다

표를 새로 두지 않았다. `trait_to_sense` 가 이미 그렇게 짜여 있다.

| 감각 | 흔적 | 무엇을 말하나 |
|---|---|---|
| 후각 | 냄새강함 · 털 | **공중 표시** — "이쪽에서 난다" (방향) |
| 청각 | 울음소리 | **공중 표시** — "이쪽에서 들린다" (방향) |
| 시야 | 발자국 · 큰발자국 · 나무흔적 · 물자국 · 허물 | **땅에 남은 자국** — "여기 지나갔다" (자리) |

검증기의 대조표가 이걸 `◆`(자리) / `△`(방향) / `X`(못 찾음) 로 찍는다.

### 승격 반경이 26 → 8 타일로 줄었다

감각 반경 전체를 덮어야 했던 이유가 **저 멀리서 감지된 몸을 그려야 해서**였다.
이제 몸은 시야 안에서만 보이고 **단서는 노드 없이도 나오므로**(자리에서 나오는 것이지
몸에서 나오는 게 아니다) 승격은 보이는 반경 언저리면 된다.
재보니 13마리 중 노드가 붙는 것은 **가장 많을 때 1개**다.

> ⚠️ `reveal_radius` 를 넉넉하게 잡지 말 것. 넓으면 동료에게 의지할 이유가 사라져서
> §3.3 이 통째로 죽는다.
> ⚠️ 승격 반경은 `reveal_radius` 보다 넉넉해야 한다 — **화면에 들어오기 전에 이미
> 걷고 있어야** 툭 나타나지 않는다.

## 감각마다 닿는 거리가 다르다

전부 `data/tags.json` 의 `sense_profile` 에 있다. 코드에는 감각 이름이 없다.

```
반경 = guide_radius × 개체 sense_range × range_scale × daypart_scale × terrain_scale
                                          (감각 고유)   (시간대)      (대상이 선 지형)
```

| 감각 | 배율 | 시간대 | 지형 | 성격 |
|---|---|---|---|---|
| 후각 | 1.5 | 영향 없음 | 영향 없음 | **멀리서 추적한다.** 대신 방향과 단서까지만 |
| 청각 | 1.1 | 영향 없음 | 숲 ×0.8 | 중간. 역시 단서까지 |
| 시야 | 0.7 | 여명 ×0.6, 밤 ×0.35 | 숲 ×0.35, 바위 ×0.6 | **가깝지만 몸이 보인다.** 밤·숲에서 무력해진다 |

디버그 오버레이가 지금 각 동료의 실제 반경을 타일 단위로 찍는다
(`감각 반경: 개 후각 21타일   고양이 시야 3타일`).

지형은 `TerrainMap` 이 초원 바탕에 숲·물가·바위 덩어리를 뿌려 만든다
(개수는 튜닝 리소스). 야생동물은 **자기 `habitat` 위에서 시작한다.**

⚠️ `activation_radius`(풀 AI 승격)가 가장 먼 감각보다 좁으면 코가 헛돈다.
Field 가 실제 반경을 계산해 모자라면 올려 쓰고 경고를 남긴다.

## 스프라이트

`SpriteLibrary` 가 `sprites/extracted/<종 id>/<애니메이션>.png` 를 찾는다.
없으면 색 사각형으로 떨어지므로, 그린 종과 안 그린 종이 섞여 있어도 게임은 돈다.

- 스트립은 **가로로 이어붙인 프레임**이고 프레임 폭 = 캔버스 폭(`canvas_by_size_class`)
- 애니메이션 이름: `idle` / `move_side` / `move_north` / `move_south` / `special`
- 서향은 파일을 만들지 않는다 — `move_side` 를 `flip_h` 로 뒤집는다 (§4.5)
- 눈은 공용이 원칙이다. `shared/` 에 **round 스타일**이 구워져 있고, 스타일이 다른 종만
  종 폴더에 따로 구워진다. **어느 쪽을 볼지는 `animals.json` 의 `eye_style` 이 정한다** —
  폴더가 있느냐로 정하면 종 폴더가 다시 생성되는 순간 조용히 그쪽으로 끌려간다.
  **둘 다 없으면 눈 레이어를 얹지 않는다** — 몸통에 얼굴을 그려 넣은 종을 위한 규칙이다
- 없는 애니메이션은 대역으로 채우고 `push_warning` 으로 무엇을 때웠는지 남긴다

지금 그려진 것: **개**(5종 애니메이션), **청설모**(대기·남·측면).
나머지 4종과 플레이어는 아직 색 사각형이다.
눈은 `shared/eye_{front,side}_{기본,기쁨,시무룩}.png` 로 공용이다.

### ⚠️ 내보내는 순서

`export_godot.py` 는 `extracted/` 를 **비우고** 다시 쓴다. 플레이어는 `present_player.py` 가
내보내므로 순서가 뒤바뀌면 `player/` 38개가 통째로 사라진다. 실제로 한 번 사라졌다.

```bash
python export_godot.py      # 동물 · 배경 · 단서 · 먹이
python present_player.py    # 플레이어 (반드시 뒤에)
```

### 그림이 스스로 말하는 것

| 파일 | 무엇을 말하는가 |
|---|---|
| `<종 id>/<종 id>.json` | 캔버스 크기와 앵커. **있으면 이쪽이 이긴다** — 그림에서 나온 값이다 |
| 그림의 아래쪽 빈 줄 | **접지선.** 캔버스 맨 아래가 아니라 발끝이 Actor 원점에 온다 |
| 맨 아래 줄의 가로 폭 | 발밑 그림자 폭 |

`build.py` 는 발끝을 y=27 에 둔다(32px 캔버스). 캔버스 바닥을 발밑으로 삼으면
**동물이 그림자 위에 3px 떠 보인다** — 그래서 접지선을 그림에서 직접 읽는다.
플레이어는 캔버스가 32×48 이라 `size_class` 버킷으로는 안 나온다. `player.json` 이 그것을 말한다.

### 배경 · 표현

| | 어디서 오는가 |
|---|---|
| 지형 타일 | `terrain/<지형 이름>.png` — 파일명이 지형 이름이라 분기가 없다. 변형은 **타일 좌표 해시**로 고른다 |
| 프롭 | `palettes.json` 의 `prop_terrain`. 앵커는 바닥 중앙, 타일보다 높은 것만 발밑 그림자 |
| 공중 단서 | `palettes.json` 의 `clue_airborne` — 후각·청각은 지면에 실물이 없다 |
| 머리 위 아이콘 | `tags.json` 의 `sense_profile.<감각>.icon` → `shared/emote_<이름>.png` |
| 밤낮 | `palettes.json` 의 `background` 4벌을 보간 + `overlay_tint` 를 `CanvasModulate` 로 |
| 눈 되비침 | `tags.json` 의 `eyeshine.by_activity` — 야행성·박명성이 여명·밤에 |

**어두울 때 눈이 되비친다.** 빛을 내는 게 아니라 되비추는 것이라 어두운 시간대에만 걸리고,
대상은 종이 아니라 `activity` 로 정해진다 — 야행성·박명성이면 어느 종이든 자동이다.
눈 텍스처를 되비추는 판으로 갈아끼우고, `self_modulate` 로 화면 전체 틴트를 되돌린다
(되비추는 빛이 어둠에 같이 묻히면 안 된다). 색은 `FieldTuning.eyeshine_color`.

> 밤에는 시야 감각이 3타일까지 줄어든다. 그래서 **어두운 데서는 눈이 먼저 보인다** —
> 두 규칙이 따로 만들어졌는데 같은 그림을 만든다.

밤낮은 **전역 조명을 Light2D 로 하지 않는다** — 배경 팔레트를 갈아끼우고 화면 전체에
틴트를 곱한다. 표현(단서·아이콘)은 팔레트를 타지 않는다. 밤이라고 색이 바뀌면 안 읽힌다.

⚠️ **단서·아이콘 같은 월드 표시는 `CanvasLayer` 가 아니라 월드 노드로 둔다.**
화면 레이어에서 텍스처를 그리면 배율·틴트를 따로 맞춰야 하고, 실제로 통짜 사각형으로
렌더되는 문제도 겪었다.

## 튜닝

`tuning/field_tuning.tres` 를 인스펙터에서 연다.
실행 중이면 **디버그 → 리모트 씬 트리**에서 `Field` 를 골라 `tuning` 을 열면
게임을 돌린 채로 반경·속도·게이지 시간을 바꿀 수 있다. 슬라이더 UI는 만들지 않았다.

## ⚠️ 판정이 조용히 사라지는 것

판정 하나가 터지면 **그 함수의 나머지가 통째로 안 돈다.** 실패가 아니라 "없어짐" 이라
눈치채기 어렵다 — 이 프로젝트에서 **다섯 번** 겪었다(고양이가 그림을 얻었을 때,
모든 종이 그림을 얻었을 때, 수달이 지역 생태로 0마리가 됐을 때, 물가가 드물어졌을 때,
그리고 얕은 시뮬 개체의 `.actor` 가 null 이었을 때).

그래서 `smoke_test.gd` 에 **총 개수의 바닥**(`FLOOR`)이 있다. 이 아래로 떨어지면
어딘가에서 끊긴 것이다. **판정을 지우려면 이 숫자도 같이 내린다.**

## 검증

```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tools/validate_data.gd
```

태그 검증기. 종료 코드 0 = 통과, 1 = 로드 거부. `-- --all` 로 prototype:false 종까지,
`-- --animals=<경로>` 로 다른 데이터 파일을 검사한다.

```bash
/Applications/Godot.app/Contents/MacOS/Godot --headless --script tools/smoke_test.gd
```

DEMO-SPEC §4 완료 조건 중 코드로 확인 가능한 것들(북향 숨김, 정수 스냅 바운스,
감각 대비, 승격/강등, 상성 계수, 게이지 비중단, 초대 처리)을 점검한다.
**재미있는지는 여기서 안 나온다.** 그건 직접 돌려봐야 한다.
