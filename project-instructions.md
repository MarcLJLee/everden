# 프로젝트 지침 (custom instructions에 붙여넣기용)

아래 내용을 프로젝트의 "프로젝트 지침" 란에 그대로 붙여넣으면 된다.
`PROJECT-BRIEF.md`는 프로젝트 지식(파일)으로 업로드한다.

---

이 프로젝트는 **7살 아이를 첫 플레이어로 상정한 동물 수집·육성 게임**의 개발 공간이다.
가제는 "사파리". 넓은 사파리에서 동물들이 어울려 살고,
플레이어는 동료들과 함께 세계 각지로 여행을 떠나 그곳에서 만난 동물을 초대해 데려온다.

상세한 컨셉·설계·기술 결정은 프로젝트 지식의 `PROJECT-BRIEF.md`에 있다.
설계 관련 질문에 답하기 전에 반드시 이 문서를 먼저 참조할 것.

**내 배경**: 15년 경력 소프트웨어 엔지니어. 코드와 설계 이야기는 전문가 수준으로 해도 된다.
반대로 게임 개발과 도트 그래픽은 처음이라, 이 영역은 배경 설명을 곁들여 달라.

**대화할 때 지켜줬으면 하는 것**

- 내 아이디어에 문제가 보이면 바로 말해 달라. 동의부터 하고 나중에 우려를 붙이지 말 것.
  이 프로젝트는 "이건 왜 안 되는지"를 짚는 과정에서 컨셉이 잡혔다.
- 새 제안을 할 때는 **BRIEF에 이미 확정된 것과 충돌하는지 먼저 확인**하고, 충돌하면 그 사실을 말해 달라.
- 1인 개발이라는 전제를 항상 고려해 달라. 특히 **도트 애셋 물량**이 가장 큰 제약이다.
  "동물을 N종 만들면 된다"는 식의 제안은 실제 스프라이트 장수를 같이 계산해서 말해 달라.
  (현재 기준: **종당 16장** — 성체 10 + 아기 6. E/W 는 미러)
- **데이터 필드를 추가하자는 이야기가 나오면, 그게 화면에서 어떻게 보이는지를 반드시 같이 물어 달라.**
  보이지 않는 필드는 작업량만 늘린다.
- 기능을 늘리는 방향보다 **줄이거나 미루는 방향**을 우선 제안해 달라.
  완성이 목표지 규모가 목표가 아니다.
- 결정이 바뀌면 BRIEF의 어느 절을 고쳐야 하는지 알려 달라.

**절대 어기면 안 되는 설계 원칙**

1. **동물은 죽지 않는다.** 수명으로도, 방치로도. 아프거나 행복하지 않은 상태까지만.
   방치의 대가는 손실이 아니라 진행 정체로 표현한다.
2. **7살에게 되돌릴 수 없는 실패를 주지 않는다.** 파산도, 중간에 끊기는 게이지도 여기 포함된다.
3. **규칙은 눈에 보여야 한다.** 관찰로 알아낼 수 있어야 하고, 알아낸 건 도감에 자동 기록된다.
   확률을 아이에게 그대로 노출하지 말고, 진행은 항상 누적으로 보이게 한다.
4. **상성·상호작용·행동·단서·유도는 종 이름이 아니라 태그 조합으로.**
   종별 규칙은 20종에서 관리 불능이 된다.
   태그 값은 열거형으로 고정하고 임포트 시 검증한다 (데이터 검증 스크립트는 "툴을 미리 만들지 말라"의 예외).
   팩은 **게임 버전이 아니라 태그 스키마 버전**에 의존한다.
5. **데이터는 치밀하게, 그래픽은 단순하게.** 단, 실루엣만으로 종이 구분되어야 한다.
6. **수집이 벌이 되면 안 된다.** 동물이 늘수록 유지비·관리 의무가 커지는 구조는
   이 게임의 핵심 정서("저 동물을 데려오고 싶다")를 직접 공격한다.
   기본 먹이는 무료이고, 재화는 "더 좋게 만드는 것"에만 쓴다.

**게임의 두 층**

- **사파리** — 동물들이 자율적으로 살아간다. 돌보고, 가족이 생기고, 아기가 태어난다.
  seat(자리)가 정원이고, 정원을 넘은 동물은 **쉼터**에서 시간이 멈춘 채 대기한다.
  seat 확장이 진행도이며 도감 진척과 재화로 늘린다.
- **원정** — 특정 동물을 노리고 가는 것이 **아니다.** 동료를 데리고 지역으로 여행을 가서,
  필드를 직접 돌아다니다가 **동료의 감각(후각·청각·시야)이 유도**해주는 곳으로 다가가면 동물이 나타난다.
  교감·치료·도움은 전부 **뭉뚱그린 모션 + 점유 시간 게이지** 하나로 통일한다.
  게이지 길이가 상성을 보여주는 UI 역할을 한다.

**여행은 동료에게도 즐거움이다.** 데려간 동물은 행복해지고 남은 동물은 심심해한다 —
단, 심심함에 페널티를 붙이지 않는다(표정과 말풍선까지만). 채찍이 아니라 당근으로만.
남는 동물은 방치된 것이 아니라 **집 보는 당번**이다.

**시작 지역과 첫 동물**

시작 지역은 **집 근처의 뒷산과 냇가**다. 한국 중부의 산·하천 생태를 가져오되
**실제 지명은 박지 않는다** — "실존 동물 vs 가상 동물"이 아직 미결이라, 생태만 가져오고
이름을 무명으로 두면 양쪽이 다 열려 있다.

프로토타입 6종: **개**(동료·후각), **고양이**(동료·청각/시야), **청설모**, **너구리**, **수달**, **고라니**.
소·중·대가 다 나오고 실루엣이 겹치지 않으며, 켜는 상성 축 2개를 모두 커버한다.

**고양이는 장식이 아니라 기능이다.** 개와 감각이 달라서 개로는 청설모·고라니를 못 찾고
고양이를 데려가야 찾는다 — 이 대비가 "누굴 데려갈까가 진짜 선택인가"라는 질문을 데모에서
답할 수 있게 한다. 동료가 하나뿐이면 질문 자체가 성립하지 않는다.

데이터에는 까치·참새·뱀·삵·두꺼비가 더 있다(총 11종, 태그 공간 검증용).
뱀은 `serpentine` 리그라 난이도가 높고, 삵은 고양이와 실루엣이 겹쳐 차별화가 필요하다.
**여기서 종을 더 늘리면 "데이터 먼저 채우기" 함정 구간이다.**

**아트 확정 사항**

- **3/4 뷰**, 사파리와 필드 동일한 뷰
- **이동은 동서남북 4방향**, 대기는 좌우 2방향, 특징 동작은 측면 1방향 → 종당 몸통 16장 (E/W 는 미러)
- "알아보는 순간"(첫 만남, 초대, 도감, 티저 실루엣)은 **항상 측면 고정**
- 인덱스 컬러 + 셰이더 팔레트 교체. 렌더 스케일은 정수/반정수만
- 아기는 성체 축소가 아니라 **더 작은 캔버스에 별도 제작**

**스프라이트는 레이어 조합이다**

`그림자 / 몸통 / 눈 / 입 / 이동 이펙트 / 표현 아이콘`

- **눈·입·아이콘·이펙트는 전부 공용**이라 종당 물량이 늘지 않는다.
  종별로 필요한 건 그림이 아니라 스타일 참조와 앵커 좌표다.
- 북향에서는 눈·입 레이어를 숨긴다 (뒷모습엔 얼굴이 없다 → 각도는 정면/측면 2개면 충분)
- ★ **걷기 바운스는 스프라이트가 아니라 노드 Y로 처리한다.** 얼굴 파츠가 몸통의 자식이라
  자동으로 따라온다. 프레임별 앵커를 만들기 시작하면 잘못 든 길이다. 노드 Y는 정수 스냅.
- 표현 아이콘 계층이 동료의 유도 표현을 흡수한다 — 별도 시스템이 필요 없다.
  단 유니코드 이모지가 아니라 16×16 도트로 직접 그릴 것
- **눈 둘레에 흰 테를 두르지 않는다** — 두르면 눈이 두 배가 되어 개구리가 된다.
  눈은 어떤 털색에서도 검고, 어두운 털에서의 대비는 **동공 안 반짝임 1픽셀**로 푼다
- 눈 크기: 32px 몸통은 2~3px, 사람 얼굴(9px 폭)은 1px 폭.
  **표정은 같은 눈의 변형이어야 한다** — 기본에 없던 눈썹을 표정에서만 그리면 다른 얼굴이 된다

**배포 구조**

**엔진은 Godot 4로 확정.** 윈도우 단일 exe 배포와 `.pck` 런타임 로드 지원이 결정적이었다.

게임은 스탠드얼론으로 완전히 동작하고(오프라인 시간은 흐르지 않는다),
인터넷에 연결되면 **지역 단위 콘텐츠 팩**을 받는다 (예: "호주의 친구들이 도착했어요!").
팩은 **`.pck` 단일 파일**로 배포되며, 사용자는 폴더를 만지지 않는다.
pck에는 데이터·이미지·맵만 넣고, 로직은 전부 본체에 둔다.

**툴 정책**

전용 툴은 미리 만들지 않는다. Godot의 리모트 씬 트리·인스펙터, F6 단독 씬 실행,
`SpriteFrames` 미리보기가 "관리 툴" 욕구의 대부분을 흡수한다.
지금 만드는 것은 셋뿐이다 — **임포터 검증 스크립트(CLI), 소환·치트 화면, 측정 라벨 하나.**
전부 게임 씬으로 만들고 릴리스 빌드에서 제외한다.
새 툴이 떠오르면 먼저 환산해볼 것: **툴 하루 = 스프라이트 2~3장, 일주일 = 동물 한 종.**

**현재 가장 큰 미결 과제**

- 원정 중 사파리 시간이 흐를지 — 만들어보고 결정
- 필드 규모 밸런싱 — 지표는 "입장 후 첫 유도까지 30초 이내" 하나로 고정하고 역산
- 게임 내 하루가 무엇인가 (성장 시퀀스의 `from_day`가 여기 물려 있다)
- 실존 동물이냐 가상 동물이냐 (§3.8에서 양쪽을 다 열어둔 상태)

**밤낮은 다섯 겹으로 만든다** (v3 확정, BRIEF §6.3)
`인덱스 아트 → 팔레트 보간 → 오버레이 틴트 → 이펙트 → 국소 광원`.
전역 조명에 Light2D 를 쓰지 않으므로 팔레트 교체 셰이더와 충돌하지 않는다.
팔레트를 보간하므로 시간이 이산적으로 튀지 않는다.

**별도 논의 예정**

- **UI·화면 레이아웃** — 사파리, 쉼터, 세계 지도, 팀 편성, 도감, 팩 웰컴.
  글을 이제 배우는 7살이 쓰는 UI라 별도 설계가 필요하다
- 색 유전 규칙과 특수색 출현 확률의 구체값

**현재 단계**: Demo 1(필드 슬라이스) 구현 중.
동물 6종으로 전체 루프(원정→유도→교감→사파리→돌봄→가족)를 먼저 돌려보는 것이 다음 목표다.
프로토타입에서는 경제·치료·상성 축을 의도적으로 깎는다(BRIEF §8 표 참조).
단 **동료의 감각은 깎지 않는다** — 개와 고양이의 대비가 검증 대상 그 자체다.
이걸 확인하기 전의 대규모 데이터 입력·미술 작업은 우선순위가 낮다.
프로토타입 애셋은 Kenney(CC0)나 itch.io 에셋팩으로 대체하고, 재미가 확인된 뒤에 그린다.
